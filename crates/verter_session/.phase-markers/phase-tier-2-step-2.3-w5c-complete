phase-tier-2-step-2.3-w5c-complete

Step: 2.3 (Tier 2 W5c — split crates/verter_session/src/host_resolve.rs)
Plan: D:/tmp/verter-debt-and-deferred-fixes-plan.md §4
Authority audit: docs/arch/debt-closure/13-god-module-split-audit/host_resolve.md
Branch base: refactor/legacy-to-graph-dispatch-migration HEAD f5a1d10e
prior_known_passed_count: 10552

Pre-split:
- crates/verter_session/src/host_resolve.rs — 4207 LOC (single file).

Post-split (10 sub-modules under crates/verter_session/src/host_resolve/):
- mod.rs                            —   94 LOC
- frontier_helpers.rs               —  385 LOC
- test_guards.rs                    —   86 LOC
- external_macro_collector.rs       —   89 LOC
- dependency_resolution.rs          —  471 LOC
- external_type_resolution.rs       —  599 LOC
- frontier_engine.rs                —  791 LOC
- route_owned_shallow.rs            —  515 LOC
- virtual_file_pipeline.rs          — 1112 LOC
- vue_script_extract.rs             —  240 LOC
- frontier_adapter.rs               —  101 LOC
Total: 4483 LOC across 11 files (each < 4000 LOC).

Public surface unchanged:
- HostFrontierAdapter, RouteOwnedShallowStateSnapshot,
  template_converter_inputs, extract_vue_script_content,
  forbid_route_frontier_for_tests, forbid_import_route_shadow_for_tests,
  route_frontier_forbidden_for_current_thread,
  import_route_shadow_forbidden_for_current_thread,
  assert_import_route_shadow_allowed,
  ImportRouteShadowGuard, RouteFrontierGuard
  all reachable through `crate::host_resolve::*` via re-exports in
  the new `host_resolve/mod.rs`.

Tier 1C-α invariant preserved:
- lookup_resolved_external_type_cache /
  store_resolved_external_type_cache (in external_type_resolution.rs)
  still delegate to `self.resolved_type_cache()` =
  `project_type_store.resolved_type_cache()`.
- Bounded clear-all-at-4096 cap on ResolvedTypeCacheDb stays inside
  project_type_store.rs (untouched by this split).

Verification:
- cargo test -p verter_session                              passed=2556 failed=0
- cargo test --workspace --tests -j 4                       passed=10552 failed=0
- cargo clippy --workspace --tests -- -D warnings           clean
- cargo fmt --all --check                                   clean
