//! Consolidated integration-test group `binder`: the family-A
//! `BinderIdentityFacts` substrate discriminating tests, merged to cut
//! test-link count.
#[path = "g_binder/binder_identity_facts.rs"]
pub(crate) mod binder_identity_facts;
