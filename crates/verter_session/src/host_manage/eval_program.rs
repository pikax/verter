//! `host_manage::eval_program` — eval-source / parsed-program / external-type-analysis bridge.
//!
//! Phase 11c sub-module scaffold. Domain D contents (per
//! `D:/tmp/verter-architecture-cutover-phase-11c.md` §11c.2) are
//! moved into this file in a follow-up commit; this scaffold exists
//! so the parent `host_manage.rs` can declare `pub(crate) mod
//! eval_program;` ahead of the move.
