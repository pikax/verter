// @ai-generated - Synthetic ambient module for the `declare module
// "string-literal"` augmentation test. Mirrors the canonical Vue/Vite
// `vite/client` pattern where a virtual module is declared in one
// place and augmented from another.

declare module "external-spec" {
  export interface Config {
    base: string;
  }
}
