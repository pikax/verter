//! Private sorted manifest of the consolidated integration-test entries.
//! One `mod <entry>;` per logical entry; per-entry helpers stay in
//! disjoint scopes — do NOT centralise shared helpers here, and keep this
//! list sorted.

mod http_serving;
