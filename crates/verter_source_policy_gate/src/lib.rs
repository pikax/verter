//! No production surface. This crate exists solely to host the guards
//! relocated out of `verter_session`'s consolidated test binary
//! (gate-performance step 2) as one lightweight `cargo nextest` target, run
//! exactly once by `cargo nextest run --workspace` — see the crate-level
//! description in `Cargo.toml`.
