//! TypeProvider trait and priority tiers — re-exported from `verter_type_runtime`.
//!
//! All trait definitions now live in `verter_type_runtime::traits`.
//! This module re-exports them for backward compatibility within `verter_lsp`.

pub use verter_type_runtime::traits::*;
