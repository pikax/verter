//! Protocol types — re-exported from `verter_type_runtime`.
//!
//! All protocol DTOs now live in `verter_type_runtime::protocol`.
//! This module re-exports them for backward compatibility within `verter_lsp`.

pub use verter_type_runtime::protocol::*;
