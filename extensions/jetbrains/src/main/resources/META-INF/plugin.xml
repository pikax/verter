<idea-plugin>
    <id>com.verter.jetbrains</id>
    <name>Verter</name>
    <vendor>Verter</vendor>
    <description><![CDATA[
        Verter for JetBrains IDEs — build skeleton (JBT1).
        Loads against the pinned WebStorm build and contributes a health action.
        No semantic features yet: the sole semantic entry for editor clients
        remains the native verter-lsp server until JBT2 owns the lifecycle.
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <notificationGroup id="VerterHealth" displayType="BALLOON"/>
    </extensions>

    <actions>
        <action id="verter.Health"
                class="dev.verter.jetbrains.VerterHealthAction"
                text="Verter Health"
                description="Report whether the Verter plugin loaded on the running platform build">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
