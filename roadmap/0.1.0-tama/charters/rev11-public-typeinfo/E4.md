<!-- unified-charter-v2
id=E4
name=Reclaimable semantic storage and scoped interning
phase=rev11
train=rev11.public-typeinfo
product=rev11
kind=implementation
semantic_role=delivery
class=foundational
predecessors=E2,E3,MEM0
owner=rev11.public-typeinfo:reclaimable physical semantic storage and allocation charge ownership
conflict_domains=semantic_cache_store,semantic_authority
resource_class=rust-mixed
review_profile=semantic-3
gate_profile=targeted-domain
implementation_effort_min=medium
implementation_effort_default=medium
review_effort_min=medium
review_effort_default=medium
verification_effort_min=medium
verification_effort_default=medium
confirmation_effort_min=medium
confirmation_effort_default=medium
size=M
dispatchable=true
optional=false
release_gating=none
external_requirements=
charter=charters/rev11-public-typeinfo/E4.md
max_production_loc=800
max_production_files=8
max_related_packages=3
rescope_loc=1500
rescope_files=12
rescope_unrelated_packages=3
-->

# E4 — Reclaimable semantic storage and scoped interning

Readiness comes only from trusted implementation-ledger rows. A READY node may start; tooling does not validate commit locators, Git identity, receipts, leases, external state, or runtime admission.

## Independently acceptable outcome

Reclaimable semantic storage and scoped interning. The current owner is **retained parse workers, memo bodies, graph arenas and interners with independent physical lifetimes**. The final and sole owner is **reclaimable physical semantic storage with one allocation charge owner and safe retained-reader handles**. This charter accepts one authority/migration/cutover boundary; it contains no independently dispatchable subblocks.

## Concrete surfaces and APIs

- Production surfaces: `crates/verter_session/src`, `crates/verter_semantic/src`, `crates/verter_workspace/src/fact_cache.rs`. Public DTO semantics and TypeExpr route deletion remain E2/E3 obligations.
- Named API/data boundaries: `DeclBodyMemo`, `IdentityInterner`, semantic memo arenas and `DepSignatureInterner`, `DeclLoweringService`/`SnapshotShard` retained parse-worker handles, allocation charge transfer and reader lifetimes. Bind their exact current files and symbols from MEM0's inventory before mutation.
- Mutation boundary: only the production surfaces and named API/data boundaries above; every changed path must be inside both that charter surface and the acquired conflict domain, and sibling ownership is excluded.

## Exact predecessor contracts

- **E2:** implemented ledger row for “Eliminate internal general TypeExpr transit”; ledger presence alone satisfies the predecessor. Its commit message, approximate timezone-bearing date, and optional PR are locator hints only.
- **E3:** implemented ledger row for “Public operation DTOs and optional graph export”; ledger presence alone satisfies the predecessor. Its commit message, approximate timezone-bearing date, and optional PR are locator hints only.
- **External requirements:** agents check any listed requirement; tooling does not validate external state.

## Source-specific scope

- Deliver exactly “Reclaimable semantic storage and scoped interning” as the independently acceptable boundary; no neighboring authority is included.
- Reclamation, generational handles, and scoped interning are PHYSICAL STORAGE policies only (`decisions/2026-08-31-canonical-type-algebra-predecessor.md`). They must preserve TA1A's semantic composite equality, the `Global` scope of a derived multi-arm composite, singleton-normalization scope retention (a singleton returns its retained member unchanged, including that member's scope), and the origin-edge / observed-self-root freshness evidence. Interner scope, arena ordinal, and export serialization order must never become a second semantic-identity or normalization authority.

## Acceptance IDs and discriminating proof

Preflight evidence selection: preserve all four acceptance outcomes below, then select the smallest evidence set that actually discriminates the touched contract. Existing behavioral coverage, compiler/type/capability enforcement, static validation, canonical gates, bounded inspection, and benchmarks are valid when accompanied by a terse rationale.

- **E4-AC1 — sole-owner outcome:** every MEM0 allocation class has one physical charge/lifetime owner; shared references never double-charge and ownership transfer never resets accounting. Prefer existing type, capability, dependency, compiler, or static enforcement. Add a negative or mutation test only for a plausible critical fail-closed/correctness boundary or a reproduced defect that existing evidence does not discriminate.
- **E4-AC2 — positive contract:** eviction and project close preserve live reader handles, and final reader release makes attributable allocations reclaimable without changing semantic identity, provenance or completeness. Reuse existing coverage or extend/table-drive one test before creating a new test.
- **E4-AC3 — incremental equivalence:** when the changed scope owns or affects incremental, cache, cancellation, stale-publication, or partial-result authority, prove incremental equals fresh and degraded outcomes cannot warm. Otherwise record a terse not-applicable rationale tied to the untouched authority.
- **E4-AC4 — bounded work:** when the changed scope owns or affects a hot path, prove no hidden duplicate parse, resolve, plan, emit, copy, allocation, or retained candidate using applicable existing counters, inspection, or benchmarks. Otherwise record a terse not-applicable rationale; do not add counters or a soak by default.
- Every proposed new test must name a plausible regression or contract boundary not already discriminated; prose/format assertions are allowed only when those bytes are the public contract. Do not add implementation mirrors, duplicate permutations, or universal negative/mutation tests.
- Test homes: `crates/verter_session/tests/cases`, the existing declaration-memo and semantic-query-memo tests, and retained-worker lifetime tests in `crates/verter_session/src/decl_lowering.rs`.

## Deletions and forbidden designs

- Delete or structurally reject: **unaccounted physical ownership and process-lifetime storage that survives its final reader**.
- Delete or structurally reject: **charge duplication/reset across shared references or ownership transfer, and reclaimed storage reachable by live handles**.
- MEM0 supplies the complete physical population; this node owns charge/lifetime mechanics only. MEM1 supplies aggregate reservation policy later. E2/E3 remain the public protocol and TypeExpr migration owners.
- Never add a dual-running authority, compatibility fallback, string/regex semantic recovery, test-only production bypass, resource-capacity predecessor, sleep/poll readiness, or unqualified cache/public identity.
- Do not implement successors or silently enlarge this charter. Discovery of a second independently acceptable outcome requires an amendment and a new DAG node before mutation.

## Budgets and mandatory rescope

- Target ceiling: 800 production LOC, 8 production files, 3 related crates/packages. The three storage-owning crates form one related physical-lifetime boundary; investigate material divergence before implementation.
- Mandatory rescope above 1,500 production LOC, 12 files, 3 unrelated crates/packages, or when public/wire, unsafe, concurrency, or lifetime work is combined with another major concern.
- Correctness budget: zero stale publication, silent fallback, wrong-complete result, map/provenance loss, or identity aliasing.
- Performance acceptance: use the exact applicable metric rows and methodology from performance-gates.toml or the owning ratified product catalog, under contracts/resource-and-finalization.md (L2). Exact work invariants, statistical latency/RSS limits and bounded new-capability budgets are distinct. Missing required coverage needs an owning-contract amendment before measurement; no implicit 0.0% threshold or post-hoc rebaseline applies.

## Abort conditions

- Stop before mutation if current source disproves the named owner/API boundary, an ancestor lacks an implemented ledger row, or the complete diff will not fit one review context.
- Abort the candidate on unexplained output, source-map, diagnostic, cancellation, allocation, latency, or RSS divergence; do not convert it into residue locally.

## Targeted verification

1. `cargo nextest run -p verter_session -p verter_semantic -p verter_workspace`
2. Run every final command in the bound `targeted-domain` profile on the squashed review candidate; targeted success alone is iteration evidence, not acceptance.
3. Bind the preflight evidence selection and terse rationale in the review report. Behavioral code changes require TDD with a failing discriminating regression before production changes; do not invent a test or mutation solely to populate evidence.

## Review and lower-severity findings

Apply `semantic-3`: 2 fresh distinct harness tasks covering exactly `adversarial`, `conformance`. P0/P1 block final acceptance. A P2 follows the owning review policy and must have a named owner when deferred; otherwise it blocks. P3 follows the currently binding owning policy and must be recorded when that policy requires it. Any post-review content change invalidates every verdict. Final acceptance requires the complete 2/2 current-round profile to contain independent clean PASS reports on the squashed review candidate, plus `targeted` confirmation when required. A failed review/fix cycle is complete only after all assigned lenses and a FIX_REQUIRED disposition.

## Trusted implementation ledger

Before squashing or review, the implementation patch transitions this node's predeclared row in `authority/state/implemented.toml` from `status = "pending"` to `status = "implemented"` with the planned squash commit message, approximate date with timezone, and optional pull-request number. The transitioned row is the implementation fact. Commit metadata is a loose locator only and is never resolved or validated against Git or GitHub. Reviewers inspect the squashed candidate patch without SHA-, tree-, ancestry-, receipt-, lease-, or digest-bound orchestration manifests.

## Required contract contribution

- **MEM0:** The ratified allocation population, charge ownership, pinned-reader policy and finite retention/request ceilings bind physical reclamation. Its implemented ledger row supplies this predecessor; locator hints remain non-authoritative.
