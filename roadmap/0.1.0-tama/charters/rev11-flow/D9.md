<!-- unified-charter-v2
id=D9
name=Return-position subtype reunion and intersection reduction in value inference
phase=rev11
train=rev11.flow
product=rev11
kind=implementation
semantic_role=delivery
class=foundational
predecessors=D8
owner=rev11.flow:sole shared flow authority
conflict_domains=flowslice
resource_class=rust-mixed
review_profile=semantic-3
gate_profile=targeted-domain
implementation_effort_min=high
implementation_effort_default=high
review_effort_min=high
review_effort_default=high
verification_effort_min=high
verification_effort_default=high
confirmation_effort_min=high
confirmation_effort_default=high
size=M
dispatchable=true
optional=false
release_gating=none
external_requirements=
charter=charters/rev11-flow/D9.md
max_production_loc=800
max_production_files=8
max_related_packages=2
rescope_loc=1500
rescope_files=12
rescope_unrelated_packages=3
-->

# D9 — Return-position subtype reunion and intersection reduction in value inference

Readiness comes only from trusted implementation-ledger rows. A READY node may start; tooling does not validate commit locators, Git identity, receipts, leases, external state, or runtime admission.

## Independently acceptable outcome

The value-inference layer of the sole shared flow authority (`ProjectSemanticDispatch` / `execute_function_return_source` over `crates/verter_semantic/src/analysis/flow`) applies TypeScript's return-position subtype reduction when it joins a function's return arms — a subtype arm is absorbed into its supertype peer — and reduces a literal intersected with the empty object type to the bare literal, so the eleven corpus rows parked as extensionally-equal residue publish the checker's exact node. The canonical algebra layer is untouched: TA1A forbids subtype absorption there (“a supertype arm never swallows a subtype arm”) and the standing ruling forbids adding intersection reduction to the canonical layer; both reductions live in the flow-return evaluator's inference layer (the authority the `U6.VALUE_INFERENCE` label names). One independently acceptable outcome; no subblocks.

## Concrete surfaces and APIs

- Production surfaces: `crates/verter_session/src` (the return-arm join and value-inference sites in `project_semantic_dispatch/flow_return.rs` and `flow_return_products.rs`); `crates/verter_semantic/src/analysis/flow` only if the join needs a typed carrier.
- Named boundaries: the relation authority (`SemanticQueryKey::Relate`) as the sole subtype oracle; `NormalizeUnion` / `NormalizeIntersection` consumed, never re-implemented; `FlowReturnResult`; the corpus pins in `crates/verter_session/src/u6_flow_shape_corpus_rows_tests.rs` and the `OPEN_DEBTS` / `CONFORMANCE` ledgers in `u6_flow_shape_corpus_tests.rs`.
- Mutation boundary: only those surfaces and their test homes; sibling ownership is excluded.

## Exact predecessor contracts

- **D8:** implemented ledger row for “U6 convergence and complete-result admission proof”; ledger presence alone satisfies the predecessor. D8 fixes the carrier-retention and demand-forcing contract this node's reductions must preserve.
- **External requirements:** none.

## Source-specific scope

- Reunion reduction at the flow return join: `X26`, `X36`, `X39`, `X45`, `X46`, `X47`, `X48`, `X49`, `X55` publish the checker's node (e.g. `{ v: number }` absorbed into `{ v: string | number }`), each decided through the relation authority per arm pair.
- Intersection residue: `N106`, `N115` publish `"x"` for `{} & "x"` in the inference layer.
- Each row's `Verdict::KnownOwed` becomes `MatchesChecker` with its full-depth value pin and audit pin re-pinned, its `OPEN_DEBTS` entry removed, and the `CONFORMANCE` ledger re-pinned, in the same change.
- An undecidable relation keeps every arm and records the typed gap (`ReturnOnly`); a reduction is never guessed.

## Acceptance IDs and discriminating proof

- **D9-AC1 — sole-owner outcome:** the reduction lives in the inference layer and decides through the relation authority; no second relation, algebra, or per-row special case; the canonical-layer diff is empty.
- **D9-AC2 — positive contract:** each named row's published node equals the checker's; surviving arms keep source order; the corpus pins and both ledgers move in the same change.
- **D9-AC3 — incremental equivalence:** reduced results admit warm under the existing fact rail and replay equal to fresh; an undecidable relation stays `ReturnOnly` (fail-closed leg on one row).
- **D9-AC4 — bounded work:** at most one relation read per arm pair per cold join; an identical warm demand adds zero relation reads (existing `relation_check_count`).
- Test homes: `crates/verter_session/src/u6_flow_shape_corpus_rows_tests.rs`, `crates/verter_session/src/project_semantic_dispatch/flow_return_tests.rs`, `crates/verter_session/tests/cases`.

## Deletions and forbidden designs

- No reduction in the canonical layer; no comparison of printed type text; no per-row special casing; no second flow engine or resolver; never route around the sole dispatch.
- Never add a dual-running authority, compatibility fallback, string/regex semantic recovery, test-only production bypass, or unqualified cache/public identity.
- Do not implement successors or silently enlarge this charter.

## Budgets and mandatory rescope

- Target ceiling: 800 production LOC, 8 production files, 2 related crates/packages. Mandatory rescope above 1,500 production LOC, 12 files, 3 unrelated crates/packages, or when public/wire, unsafe, concurrency, or lifetime work is combined with another major concern.
- Correctness budget: zero stale publication, silent fallback, wrong-complete result, map/provenance loss, or identity aliasing.
- Performance acceptance: exact applicable metric rows from performance-gates.toml under contracts/resource-and-finalization.md (L2).

## Abort conditions

- Stop before mutation if current source disproves the named owner/API boundary, D8 lacks an implemented ledger row, or the complete diff will not fit one review context.
- Abort on unexplained output, cancellation, allocation, latency, or RSS divergence.

## Targeted verification

1. `cargo nextest run -p verter_semantic -p verter_session`
2. Run every final command in the bound `targeted-domain` profile on the squashed review candidate.
3. TDD: each row's re-pin fails before the reduction lands and passes after.

## Review and lower-severity findings

Apply `semantic-3`: 2 fresh distinct harness tasks covering exactly `adversarial`, `conformance`. P0/P1 block final acceptance; P2 needs a named owner when deferred; any post-review content change invalidates every verdict.

## Trusted implementation ledger

Before squashing or review, the implementation patch transitions this node's predeclared row in `authority/state/implemented.toml` from `status = "pending"` to `status = "implemented"` with the planned squash commit message, approximate date with timezone, and optional pull-request number.
