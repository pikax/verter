<!-- unified-charter-v2
id=D14
name=Checker-syntax comparer coverage and deep pins
phase=rev11
train=rev11.flow
product=rev11
kind=implementation
semantic_role=delivery
class=foundational
predecessors=D8
owner=rev11.flow:sole shared flow authority
conflict_domains=flowslice
resource_class=rust-mixed
review_profile=semantic-3
gate_profile=targeted-domain
implementation_effort_min=medium
implementation_effort_default=medium
review_effort_min=medium
review_effort_default=medium
verification_effort_min=medium
verification_effort_default=medium
confirmation_effort_min=medium
confirmation_effort_default=medium
size=S
dispatchable=true
optional=false
release_gating=none
external_requirements=
charter=charters/rev11-flow/D14.md
max_production_loc=0
max_production_files=0
max_related_packages=1
rescope_loc=300
rescope_files=4
rescope_unrelated_packages=1
-->

# D14 — Checker-syntax comparer coverage and deep pins

Readiness comes only from trusted implementation-ledger rows. A READY node may start; tooling does not validate commit locators, Git identity, receipts, leases, external state, or runtime admission.

## Independently acceptable outcome

The test-only checker-syntax comparer (`checker_syntax::parse` in `crates/verter_session/src/u6_flow_expect_tests.rs`) parses the checker prints it rejects today — method and accessor members, `readonly` modifiers, optional members, generic arguments, and the `... N more ...` print elision as an explicit elided marker — so the 14 rows `A15`, `X14`, `B03`, `B04`, `B10`, `CC03`, `E03`, `E04`, `H02`, `X10`, `X27`, `N88`, `X18`, `X19` are deep-pinned and compared against the live semantic surface by `deep_pinned_rows_semantic_equality_follows_their_verdict`, each with the verdict the live measurement gives. A row whose live surface diverges from its `MatchesChecker` label is re-labelled `KnownOwed` with a note and placed on the successor that owns its authority in the same change. Zero production code. One independently acceptable outcome; no subblocks.

## Concrete surfaces and APIs

- Surfaces: `crates/verter_session/src/u6_flow_expect_tests.rs`, `crates/verter_session/src/u6_flow_shape_corpus_rows_tests.rs`, `crates/verter_session/src/u6_flow_shape_corpus_tests.rs` (`SHALLOW_PINNED_ROWS`, `SHALLOW_PINNED_ROWS_CEILING`, `OPEN_DEBTS`, `CONFORMANCE`).
- Mutation boundary: test code only; no production surface.

## Exact predecessor contracts

- **D8:** implemented ledger row; ledger presence alone satisfies the predecessor.
- **External requirements:** none.

## Source-specific scope

- Extend the grammar deliberately (the parser's own rule: never exempt silently); retire the `SHALLOW_PINNED_ROWS` entries of `X14`, `CC03`, `X10`, `X27`, `N88` and lower `SHALLOW_PINNED_ROWS_CEILING` accordingly; deep-pin all 14 rows.

## Acceptance IDs and discriminating proof

- **D14-AC1 — sole-owner outcome:** not applicable (test instrumentation only); recorded rationale.
- **D14-AC2 — positive contract:** every one of the 14 rows carries `Expect::Node` and passes the semantic-equality gate under its recorded verdict; the comparer's strict trailing-input rule is preserved (one negative leg per new construct).
- **D14-AC3 — incremental equivalence:** not applicable; recorded rationale.
- **D14-AC4 — bounded work:** not applicable; recorded rationale.
- Test homes: the surfaces above.

## Deletions and forbidden designs

- No silent exemption of an unparseable print; no production change; no re-measurement of a checker column without the pinned oracle.
- Do not implement successors or silently enlarge this charter.

## Budgets and mandatory rescope

- Target ceiling: 0 production LOC; test-only. Mandatory rescope if any production file must change.

## Abort conditions

- Stop before mutation if D8 lacks an implemented ledger row or the complete diff will not fit one review context.

## Targeted verification

1. `cargo nextest run -p verter_session`
2. Run every final command in the bound `targeted-domain` profile on the squashed review candidate.

## Review and lower-severity findings

Apply `semantic-3`: 2 fresh distinct harness tasks covering exactly `adversarial`, `conformance`. P0/P1 block; P2 needs a named owner when deferred; any post-review content change invalidates every verdict.

## Trusted implementation ledger

Before squashing or review, the implementation patch transitions this node's predeclared row in `authority/state/implemented.toml` from `status = "pending"` to `status = "implemented"` with the planned squash commit message, approximate date with timezone, and optional pull-request number.
