<!-- unified-charter-v2
id=D13
name=Evaluator over-degradation residue: write-effect application, hoisted var branch join, type-space owner probe
phase=rev11
train=rev11.flow
product=rev11
kind=implementation
semantic_role=delivery
class=foundational
predecessors=D8
owner=rev11.flow:sole shared flow authority
conflict_domains=flowslice
resource_class=rust-mixed
review_profile=semantic-3
gate_profile=targeted-domain
implementation_effort_min=high
implementation_effort_default=high
review_effort_min=high
review_effort_default=high
verification_effort_min=high
verification_effort_default=high
confirmation_effort_min=high
confirmation_effort_default=high
size=M
dispatchable=true
optional=false
release_gating=none
external_requirements=
charter=charters/rev11-flow/D13.md
max_production_loc=800
max_production_files=8
max_related_packages=2
rescope_loc=1500
rescope_files=12
rescope_unrelated_packages=3
-->

# D13 — Evaluator over-degradation residue: write-effect application, hoisted var branch join, type-space owner probe

Readiness comes only from trusted implementation-ledger rows. A READY node may start; tooling does not validate commit locators, Git identity, receipts, leases, external state, or runtime admission.

## Independently acceptable outcome

The three evaluator debt records in `crates/verter_session/tests/cases/manifest_data/typeinfo_guard_registry.rs` gated on the U6 lane close are closed inside the sole shared flow authority, each exactly as its record rules: **R2-UNAPPLIED-WRITE-EFFECT-SOURCE-ORDER** — whole-slot write effects into a parameter or value-selected slot are applied with evaluation-order reasoning, so `{ b: x, a: (x = "s") }` publishes `{ b: string | number; a: string }` clean and warm while the deferred closure read `{ b: () => x, a: (x = "s") }` still observes the write (`b: () => string`); the span-comparison suppression stays rejected as unsound. **CONDITIONAL-VAR-BRANCH-JOIN** — a join algebra over the function-scoped `var` layer built on the shared `FlowFrame` lattice over the ONE `FunctionFlowGraph`'s narrowing-predicate edge class (never a merge over the evaluator's `locals` / `var_locals` maps), so `branchVar` publishes `any` and `branchVarUndef` publishes `number | undefined`, both clean and warm, while `if (flag) { var w = 1 } return w` stays degraded (tsc rejects it). **TYPE-SPACE-OWNER-PROBE-CARRIER-SETTLEMENT** — `owner_scope_answers_name` settles through the shared head-resolution authority distinguishing resolved / genuine miss / degraded uncertainty, so an owner-absent class or enum name preserves the unchanged unresolved carrier and a real owner-scope collision still fails closed. One independently acceptable outcome; no subblocks.

## Concrete surfaces and APIs

- Production surfaces: `crates/verter_session/src` (`project_semantic_dispatch/flow_return.rs`, `flow_slice_content.rs`); `crates/verter_semantic/src/analysis/flow` for the lattice join if the frame does not carry it.
- Named boundaries: `FlowFrame`, `FunctionFlowGraph`, `FlowReturnDegradation::{UnappliedWriteEffect, ConditionalVarDefinition}`, the head-resolution authority, `FlowReturnResult`.
- Mutation boundary: only those surfaces and their test homes.

## Exact predecessor contracts

- **D8:** implemented ledger row; ledger presence alone satisfies the predecessor.
- **External requirements:** none.

## Source-specific scope

- Land the acceptance tests the debt records name: `flow_return_source_order_object_read_before_write_publishes_clean` (with the deferred-read negative), `branchVar` and `branchVarUndef`, and `flow_return_unanswered_type_space_owner_probe_preserves_unresolved_carrier`; close the three debt records in the same change.

## Acceptance IDs and discriminating proof

- **D13-AC1 — sole-owner outcome:** the join rides the shared lattice on the one graph; the probe settles through the shared head resolution; no second flow structure or resolver.
- **D13-AC2 — positive contract:** the named tests publish the checker's answers; the deferred-read and one-armed-`var` negatives stay as ruled.
- **D13-AC3 — incremental equivalence:** clean results admit warm and replay equal to fresh; the retained degradations never warm.
- **D13-AC4 — bounded work:** no re-evaluation of a slot beyond its write-effect application; existing counters.
- Test homes: `crates/verter_session/src/project_semantic_dispatch/flow_return_tests.rs`, `crates/verter_session/tests/cases`.

## Deletions and forbidden designs

- No merge algebra over the evaluator's locals maps; no span-comparison suppression; no second flow engine; never route around the sole dispatch.
- Do not implement successors or silently enlarge this charter.

## Budgets and mandatory rescope

- Target ceiling: 800 production LOC, 8 production files, 2 related crates/packages. Mandatory rescope above 1,500 production LOC, 12 files, 3 unrelated crates/packages, or when public/wire, unsafe, concurrency, or lifetime work is combined with another major concern.
- Correctness budget: zero stale publication, silent fallback, wrong-complete result, or identity aliasing.

## Abort conditions

- Stop before mutation if current source disproves the named owner/API boundary, D8 lacks an implemented ledger row, or the complete diff will not fit one review context.
- Abort on unexplained output, cancellation, allocation, latency, or RSS divergence.

## Targeted verification

1. `cargo nextest run -p verter_semantic -p verter_session`
2. Run every final command in the bound `targeted-domain` profile on the squashed review candidate.
3. TDD: each named acceptance test fails before its repair lands.

## Review and lower-severity findings

Apply `semantic-3`: 2 fresh distinct harness tasks covering exactly `adversarial`, `conformance`. P0/P1 block; P2 needs a named owner when deferred; any post-review content change invalidates every verdict.

## Trusted implementation ledger

Before squashing or review, the implementation patch transitions this node's predeclared row in `authority/state/implemented.toml` from `status = "pending"` to `status = "implemented"` with the planned squash commit message, approximate date with timezone, and optional pull-request number.
