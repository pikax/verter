# Contract dependency and closure correction

The maintainer requested implementation of the roadmap review's dependency, resource, acceptance and orchestration corrections. This ordinary amendment changes pending work and active policy; it implements no pending runtime node and preserves every existing implementation-ledger status and historical charter.

1. MEM0 ratifies a measured aggregate semantic-memory budget and executable workload. E4 consumes that contract for physical storage accounting; MEM1 adds shared retention reservations and pressure policy; G4 certifies convergence. L1 and L2 must execute the bound churn and performance evidence. MEM0 and MEM1 are newly authorized nodes, unrelated to the historical collapsed G4 subblock labels.
2. SG0 owns restoration and behavioral verification of the temporarily disabled shipped-configuration lane. L4 requires SG0 and a complete amended outcome/owner/evidence inventory, with no open deferrals or unexecuted required verification. This amendment does not claim the lane is restored.
3. Native module/project checker families require PM3's coherent project snapshot and inherited PM2 resolution proof. NCK8, LSO10 and CLI2 require PM4 consumer convergence; LSO2 consumes PM3. The new contract-dependency catalog guards these transitive obligations.
4. CVO0's observation harness starts after CCA0 and CCA1O1. Compiler promotion and performance certification retain their later compiler gates. Pending charters use the controlling ratified metric rows, and zero-production contracts inventory later deletion owners rather than promising future production deletions. Stale numbered-freeze prose is removed from pending work.
5. PM0–PM4 are explicitly included in 0.0.4. Source-module, bundler-host and paired Nuxt/SvelteKit profile work is explicitly included in 0.1.0. Pending flow/type-evaluation foundations needed for Rev11 close are included in 0.0.2. Independent product terminals remain independent. `programctl products` reports their ledger state without claiming reviewed or executed evidence.
6. Autonomous repository-roadmap orchestration defaults to cumulative architecture checkpoints after three new blocks and a hard six-block unchecked bound. It persists coordination progress, handles unknown prior coverage conservatively, and reviews the actual final candidate in addition to node and checkpoint reviews. These are manager execution obligations, not a second implementation ledger or a new DAG readiness input.

The active details live in `contracts/resource-and-finalization.md`, `contracts/successor-seams.md`, `APPLICATION.md`, and the amended pending charters. Future implementations must produce their own evidence before transitioning their own ledger rows.

The existing TypeScript-mapper closure instrument is refreshed from executed evidence: strict validation now covers 411 nodes, the ledger suite executes 30 tests, and its reapplied invalid-ledger-status control reports 22 passes and eight refusals. Update that control's statement pin and regenerate the human view with the observed transcript; no claim, subject or acceptance obligation is weakened.

Review clarified that zero-production contracts must enumerate their full population and bind concrete later production owners, while convergence verifiers require completed predecessor ownership and cannot defer required deletion. EPR1 remains a schema/fixture contract; existing EPR4 owns its shared runtime validator, types, cache and source-adapter migration. Optional EPR2/EPR3 now consume EPR4 without becoming prerequisites of the required EPR4–EPR6 path. The static dependency inventory also records EPR4 consumers, bringing the graph to 1,044 edges with the same 411 nodes.

Further review separates E1's component-meta graph/protocol consumer migration, E2's internal general TypeExpr transit elimination, and E3's public DTO/graph-export contract, including their deletion populations. CAT0's transferred source-plan clauses now respect its zero-production contract boundary. EPR1 and L4 explicitly own their future inventory schemas, executable validators and discriminating negative fixtures; L4 must inspect the trusted implementation rows for every required owner and transitive ancestor and reject omitted outcome categories. This amendment does not implement those pending deliverables or change the current ledger's completion semantics or TypeScript-mapper-specific closure validator.

The ownership checks explicitly reject a known contract-only or documentation-only node assigned production work, with a negative fixture that keeps the node, dependency path and receiving acceptance ID valid. E2's mutation surfaces are restricted to residual internal TypeExpr transit; public DTO and graph boundaries are verification-only there, preserving E3's public/wire ownership and legitimate authored-input/output conversion boundaries.
