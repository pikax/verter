# LSP Workstream Ledger

Ordered backlog for the `fix/lsp-provider-parity` LSP-excellence workstream. This is the active-issue sequence (distinct from [`lsp-deferred-overlap-items.md`](./lsp-deferred-overlap-items.md), which tracks items deferred on overlap with the semantic-db-overhaul plan). Top of list = next.

| # | Issue | Status | Design of record | Notes |
|---|---|---|---|---|
| **I0** | **Editor-client local DX** — local build→package→install + tests + docs + "make it work" for the four added editors (Lapce, Zed, Helix, Neovim) | **IN PROGRESS** | per-editor design docs: `lapce-extension-design.md`, `zed-extension-design.md`, `helix-support-design.md`, `neovim-support-design.md` | Trigger: Lapce volt could not be packaged/installed for local testing on Windows. Acceptance bar: scripted install + docs + packaging/contract tests + Neovim CI **and** an attempt at automated Lapce/Zed smokes (accept infeasible if proven). |
| I1 | **IDE error recovery** — broken `.vue`/`.svelte` script/template behaves like a broken `.ts`: native syntax-diagnostic rail ∪ surviving type diagnostics; reference-preserving recovery | QUEUED | [`ide-error-recovery-design.md`](./ide-error-recovery-design.md) | Codex-architect verdict reproduced in the design (§6). `verter_session` edits **authorized as-specced**. Stages S1→S2→S3. |
| I2 | **tsserver carrier membership** — shadow configured-project (or plugin probe) so carriers join the real tsconfig project: Case-V auto-import + cross-file rename | QUEUED | [`tsserver-carrier-membership-design.md`](./tsserver-carrier-membership-design.md) | Codex-architect verdict referenced in the design. Structured to avoid `verter_session` (escalate if unavoidable). Plugin-vs-shadow decided by a stage-1 feasibility probe, not up front. tsserver-only; tsgo unchanged. |
| I3 | **macOS/BSD parent-death watchdog** — a SURVIVING watchdog process — or the tsgo child watching the parent via `kqueue(NOTE_EXIT)` (BSD `EVFILT_PROC`/`NOTE_EXIT`) — that OUTLIVES the relay-shim, to close the best-effort hard-kill orphan window on macOS/BSD (the platforms with no `PR_SET_PDEATHSIG` / Job-Object primitive); an in-shim thread cannot act after the shim's own `SIGKILL`, so only a watcher that survives its death can reap the orphan | QUEUED | — | Transferred from the SB6c-4 relay-shim containment work (deferral D-13). Today the macOS/BSD hard-kill no-orphan guarantee is best-effort (cooperative RAII + signal backstop only; it cannot run on a `SIGKILL` / hard-crash), documented on `OwnedChild` in `crates/verter_relay_shim/src/main.rs`; Linux (`PR_SET_PDEATHSIG`) + Windows (kill-on-close Job Object) already close it intrinsically at spawn. |
| I4 | **Signed/hashed packaging manifest rail** — replace the relay-shim/tsgo staged-bytes provenance marker (an accidental-mixup guard, NOT forgery-proof) with a signed or content-hashed manifest rail as the path to true identity proof | QUEUED | — | Transferred from SB6c-4 packaging (deferral P3). The code already discloses the marker is not forgery-proof — the `VERTER_RELAY_SHIM_IDENTITY:v1:` prefix proves a candidate file's bytes ARE the shim, not that an adversary could not embed the same prefix. |

Serial-implementation rule: one landing-bound block active at a time (avoid rebase churn). Issues execute top→bottom. Each issue lands via worktree forked from `fix/lsp-provider-parity` → 3/3 review → §1a verify → land → independent confirm.

_Maintained by the LSP workstream CTO/MoM orchestration. Update status as issues land; add design pointers as designs are adopted._

## Received close-out transfers (external-ts-engine plan, 842f95a2e)

Formal cross-plan transfer rows received at the external-ts-engine plan close-out; the source rows are marked `TRANSFERRED`/`MOVED` in [`external-ts-engine-architecture.md`](./external-ts-engine-architecture.md). Each carries source id, owner, trigger, acceptance bar, and the codex-sol architect ruling (grand-plan-aware, forward-compat verified). Correctness rows close before I1 closes; robustness/benign rows close by the LSP-workstream final integration-confirm.

| Source row | Class | Owner | Trigger | Acceptance bar | Close-by | Ruling |
|---|---|---|---|---|---|---|
| external-ts `ROW UNIT-B` — OWNED `--lsp` served-diagnostics program-parity | membership-parity | tsgo LSP program-membership owner | I1-S1 external-provider hardening, before real-provider diagnostics acceptance | OWNED `--lsp` program roots match the configured-project `--api` oracle under include/exclude + membership changes; in-project diagnostics/features served, out-of-project carriers not; runtime editor traffic stays exclusively `--lsp` (never editor diagnostics on `--api`) | before I1 real-provider diagnostics acceptance | codex-sol |
| external-ts §2.7.1 imported-module-edit invalidation locality | correctness | verter-lsp cross-file invalidation | I1-S1 native diagnostic rail / cross-file re-diagnose | repeated overlay edits to an imported module re-invalidate + re-diagnose EVERY open dependent (not only the first edit); the perf workload can then edit the imported module directly | before I1 closes | codex (roadmap CODEX-C: MOVE→I1) |
| external-ts overlay `R1` — gate-deadline ownership-loss (shadow-class) | correctness | I1 external-LSP transport-lifecycle owner | I1 transport-lifecycle hardening | forced gate-expiry-before-retract test proves ownership survives until deterministic reconciliation and a subsequently real file is never shadowed | before I1 closes | codex-sol |
| external-ts overlay `R2` — missing-transport (shadow-class) | correctness | I1 external-LSP transport-lifecycle owner | I1 transport-lifecycle hardening | transport-loss/epoch-replacement test proves pending physical overlays remain durably tracked and are swept/retracted on recovery before they can shadow a real file | before I1 closes | codex-sol |
| external-ts overlay `R3` — retract unknown-timeout/cancellation outcome | correctness | I1 external-LSP transport-lifecycle owner | I1 transport-lifecycle hardening | timeout/cancellation tests cover applied + unapplied retract outcomes, retaining a tombstone/ownership record until idempotent reconciliation proves removal; no real-file shadow possible | before I1 closes | codex-sol |
| external-ts overlay `R4` — epoch-advance retirement sweep (shadow-class) | correctness | I1 external-LSP transport-lifecycle owner | I1 transport-lifecycle hardening | epoch-advance test proves every retired-epoch overlay is enumerated + reconciled while committed reopens survive and real files stay unshadowed | before I1 closes | codex-sol |
| external-ts overlay `R5` — transport-wire lease/incarnation-token ABA (non-shadow) | robustness | tsgo transport-protocol owner | LSP-workstream transport hardening | incarnation tokens are non-reusable or generation-fenced; an ABA test proves stale wire messages cannot act on a later transport incarnation | LSP-workstream final integration-confirm | codex-sol |
| external-ts overlay `R6` — retract-idempotency / veto-interleaving double-retract (benign) | robustness (benign, dual-model-adjudicated) | tsgo overlay-cleanup owner | LSP-workstream transport hardening | reopen-inject-commit-VETO test codifies retract idempotency + bounded duplicate cleanup, proving no committed-reopen deletion, shadow, orphan, or unbounded retry | LSP-workstream final integration-confirm | codex-terra audit + codex-sol adjudication (benign) |
| external-ts `ROW D1-HOVER-TYPE` — carrier-hover TYPE surfacing | correctness (Carrier IDE TS Surface) | crates IDE Vue-augmented-hover composition (`verter_lsp`/`verter_session`) | I1 Carrier IDE TS Surface hover-completeness hardening | the `{{ label }}` interpolation hover surfaces the resolved prop type; `CRATES_HOVER_TYPE_GAP_OPEN=false` re-enables the hard D1 assertion and it passes | before LSP-workstream close | codex-sol |
| external-ts `ROW D1-HOST-STABILITY` — @tsgo automated extension-host stability | robustness | `packages/vue-vscode` e2e harness / automated host | before any D1 FULL-SUITE-stable claim | the D1 suite deterministically completes across N consecutive automated runs with CORE assertions reached + green | ≤ LSP-workstream final integration-confirm | codex-sol |
| external-ts `ROW D1-EDITOR-SESSION-REUSE` — real editor tsgo reuse (retire duplicate-tsgo interim relay mode) | productization | `packages/vue-vscode` SHARED wiring | before SHARED is claimed PRODUCTIZED | Native-Preview routes through the relay shim to share ONE warm editor tsgo Program; the duplicate-tsgo mode is removed or demoted | ≤ LSP-workstream final integration-confirm | codex-sol |

The SUPERVISED D1 native-preview editor pass is a reserved manual acceptance touch-point, NOT a debt row (codex-sol), and must not underwrite a FULL-SUITE-stable or PRODUCTIZED claim.

## Deferred B0 carrier-ownership rows (best-implementation feed-forward, ac10607d8)

Feed-forward rows tracking the deferred better design for **B0** (canonical carrier ownership + readiness transaction), landed as a correct, non-regressing increment at `ac10607d8`. The B0 confirm rated BAR 1/2/3 PASS and BAR 4 **not met** (codex-sol `BEST-LONG-TERM-DESIGN: no`, REOPEN); the independent landability consult ruled **CORE-LANDABLE-WITH-DEFERRAL** (RESOLVED: yes). Each row carries source id, class, owner, trigger, acceptance bar, and the codex ruling, and points to the best-implementation design reference [`../better-implementation/b0-carrier-ownership.md`](../better-implementation/b0-carrier-ownership.md). These rows are feed-forward tracking only — they do **not** alter the active issue sequence above.

| Source row | Class | Owner | Trigger | Acceptance bar | Close-by | Ruling |
|---|---|---|---|---|---|---|
| B0 `R-HARDEN` — carrier-sync-concurrency hardening block | robustness (async concurrency hardening) | carrier-sync coordinator owner (`verter_lsp` `carrier_sync`/`background_drain`/`provider_sync`) | the codex-DEFER-ruled dedicated carrier-sync-concurrency block (one block, not B0 round-6 point-fixes) | one per-source async transaction actor (monotonic sequence, revalidate-after-await, live-retry/requeue, structural admission-class enforcement) replaces the split reconciler/barrier/admission machinery; round-5 findings 1–4 + F2 + the F3 divergent selector (`nearest_config_for_path` at `background_drain.rs:294-305` + `provider_sync.rs:355-367`, re-resolve canonically on the reconciliation-skip path) are eliminated; adversarial race tests at block level. Design: `b0-carrier-ownership.md` §2.4, §2.6, §2.7 | LSP-workstream final integration-confirm | codex-DEFER (landability RESOLVED: yes → land-with-deferral; confirm BAR-4) — codex-sol + landability consult |
| B0 `R-OWN` — ownership-model better-design | correctness / better-design (non-regressing corner case) | workspace ownership evaluator (`verter_workspace` `membership`/`workspace_snapshot`/`resolver`) | ownership-model rework (candidate-preserving resolution; maximal-SCC pruning over a formal domination relation; materialized-set cache-only; separate `CarrierServeBlocker` from `Ambiguous`; remove `nearest_config_for_path` owner inference) | an overlap never collapses to `None` when the candidate set is nonempty — closes the verified P2 mixed-cycle collapse at `workspace_snapshot.rs:160-213` (≥3 co-claiming configs, acyclic reference graph); §2.1–§2.5 land with a discriminating test for the mixed-cycle topology. Design: `b0-carrier-ownership.md` §2.1–§2.5 | LSP-workstream final integration-confirm | codex-DEFER (confirm BAR-4 REOPEN best-design; landability RESOLVED: yes) — codex-sol REOPEN + triple-confirmed mixed-cycle |
| B0 `R-F2` — `PresentSnapshotAuthoritative` cold-snapshot OWNED-baseline serve | robustness (transient/degraded; by-design-noted) | tsgo project-binding owner (`verter_lsp` `tsgo/project_binding`) | cold-snapshot readiness hardening (the narrow F2 item within the R-HARDEN envelope; rides the `VerifiedProjectLease` work) | `PresentSnapshotAuthoritative => ownership_ready=true` (`project_binding.rs:169-172`) no longer serves the OWNED-native baseline from a present-but-cold snapshot during bootstrap; readiness is minted only from a verified lease (a cold snapshot cannot mint readiness). External-TS result path already fail-closed; this closes the transient/degraded OWNED-baseline window. Design: `b0-carrier-ownership.md` §2.7 | LSP-workstream final integration-confirm | codex-DEFER (codex-terra WITHDREW as fail-closed after grounding; noted residual, not a blocker) |
