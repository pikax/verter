# B3 — Canonical typed compiler request and prerequisite planner

**Status:** PROPOSED amendment / LOCKED. **Predecessor:** BF3.

B3 owns the sole canonical typed compiler request: framework; exact compatibility
domain; requested products; client/server profile; development/production; Vue
VDOM/Vapor; Svelte runes/legacy where applicable; normalized framework-local options;
map requests; capability lookup; early unsupported-combination rejection; and the
minimum prerequisite plan.

Each semantics-affecting option maps exactly once from the BF1 inventory. Unknown
semantic options and unsupported combinations fail request construction. No public
option is silently ignored. A universal framework options bag or generic fact bag
cannot remain as a second semantic authority.

K2 later owns transport carriers and mechanical conversion into this request. K2
cannot reinterpret defaults, frameworks, capabilities, options, or products. B3 does
not own parser recovery, framework lowering/codegen, publication, or route exposure.

Exit requires exhaustive constructor tests for capability/default reachability,
unknown-option refusal, `SSR x Vapor` topology, product minimality, and zero
unrequested prerequisites. B2/B3 overlap requires proven disjoint ownership.
