# Receiving-owner coverage — derived, not asserted

Generated by `probes/receiving-coverage-derivation.mjs`. **Do not edit by hand**; re-run it.
`node probes/receiving-coverage-derivation.mjs --check` re-derives and exits 1 on drift.

Every HIT quotes the criterion's own opening line. Read it: the script proves somebody looked
at every criterion, not that the quoted one binds the part.

| id | obligation | part | covered by | criterion's own words |
|---|---|---|---|---|
| A1 | A — string-encoded surface | the DIRECT CodeTransform producer chain | TCM1 criterion 1 | **No production caller reads `source_projection_map()` (or any other string-encoded field carrying |
| A2 | A — string-encoded surface | the FFI/NAPI/WASM wire boundary | TCM1 criterion 3 | **The `verter_protocol` DTO fields carry the typed shape**, not a raw `String`; the actual |
| A3 | A — string-encoded surface | externally-supplied INBOUND map fields | **EXCLUDED** by TCM1 criterion 1; TCM1 criterion 3 | **No production caller reads `source_projection_map()` (or any other string-encoded field carrying — **The `verter_protocol` DTO fields carry the typed shape**, not a raw `String`; the actual |
| A4 | A — string-encoded surface | the `pub use oxc_sourcemap;` re-export | TCM1 criterion 11 | **`oxc_sourcemap` re-export disposition** (relocated by AMD-023, derived part `A4`): the re-export is |
| B1 | B — deletion items 17-18 | RECORDING types introduced or orphaned inside the deleted set | TCM1 criterion 12; TCM2 criterion 14; TCM3 criterion 10 | **Introduced-or-orphaned type recording** (relocated by AMD-023, derived part `B1`): every DTO or — **Introduced-or-orphaned type recording** (relocated by AMD-023, derived part `B1`): every DTO or — **Introduced-or-orphaned type recording** (relocated by AMD-023, derived part `B1`): every DTO or |
| B2 | B — deletion items 17-18 | item 18's negative check: exactly one codec ships | TCM2 criterion 1 | **Exactly one codec ships.** Evidence: a negative test asserting no second codec path, versioned/ |
| B3 | B — deletion items 17-18 | VERIFYING the accumulated list | TCM4 criterion 5 | **Deletion-closure completion.** Evidence: for each of items 1-16 and 19 in `deletion-closure.md`'s |

Criteria read: TCM1=12, TCM2=14, TCM3=10, TCM4=11.
Parts: 6 covered, 1 excluded by the receiving charter, 0 uncovered, of 7.

**Uncovered parts, which are what a binding act must address:**


