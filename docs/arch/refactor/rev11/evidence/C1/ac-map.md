# C1 acceptance map — every material change → `C1-AC-1..9`, Stage-2 outcome, or explicit exclusion

Reviewed candidate: `c46c60c52f33784356a9f1d7fade31627486e874`, tree
`031c84419aaa1bc851c24e31add987c9ad678ba8`. Its production subject is
`2820cf2eb790caffdb69f59bc20402d7d0a6647b`, tree
`ef8efbec06c8e87d1d6d72d9ea8e69fa624f515b`; the intervening commits add only exact performance
evidence and its registered quantitative disposition. The three required exact-candidate mandates
PASS with no findings. Independent Step-8 verification, the canonical gate, landing equivalence,
landing, and maintainer acceptance remain pending.

Historical registered-authority rebase:
`23f60303f28a0a53b4fe6aa750887bb5f2a46b14`; the implementation delta remains rooted at
`d1f3d50a948597f036868543b9bb21acacd730ff`.
Authority order applied: `charters/C1.md` as amended by `ARCH-ADDENDUM-C1-THREE-GAPS.md` (governs on
conflict) and scoped by `ARCHITECT-RULING-2026-08-25-C1-STAGE2-CUTOVER.md` (§13.2: the live scope
carrier is Stage 2, measured against the charter entire the plan "omits large parts"); execution
sequence per `ARCHITECT-RULING-2026-08-26-C1-LANDING-PATH.md`.

Status vocabulary: **DISCHARGED** (evidence in the candidate tree and executed), **DISCHARGED (amended
form)** (per a registered amendment, not the charter's earlier literal text), and **CODEX-DEFER**
(a registered successor obligation with a durable owner and closure gate; never called complete here).

| ID | Charter row | Status | Evidence in the candidate |
|---|---|---|---|
| C1-AC-1 | Same query, same content, different lifecycle ⇒ same resolved answer | **DISCHARGED** (this landing) | `crates/verter_session/src/lifecycle_answer_equivalence_tests.rs` — one fixed 4-file corpus (cross-file interfaces, generic component, barrel re-export, `Pick`, `withDefaults`), two components, three lifecycles: host-bound (`HostResolverContext` via `VerterHost::{get_component_meta_output, get_component_meta_surface, evaluate_types}`), overlay-less `MetaSession`, and a session republishing byte-identical content through its overlay (`SessionResolverContext`, overlay-rooted view). Structural equality of the published analysis + materialized type lanes + selective surface + expanded types, with the snapshot-registration identity (`ordered_sfc_structure` / `ordered_sfc_structure_bytes` — `artifact_token`, `file_incarnation`, `generation`, block tokens, which an overlay legitimately re-mints) excluded and documented. Negative control: a dependency member rename through the overlay changes the importer's published output and never leaks into the host answer. 3/3 green (`suite-results.md`). Charter's "SemanticNodeId surfaces" wording: the comparison is over the projector's published typed-IR surfaces that the node graph produces; the I/O-free observation lifecycle named in the FOUR-FORKS row is compared at the kernel level by the 24 converted resolution cases (production driver vs `ModuleResolverCore::resolve_attempt` equality on every case). |
| C1-AC-2 | `verter_semantic` production closure excludes `verter_workspace`/`verter_session`/`verter_scheduler`/`verter_tsgo_api`; A5-DD1 exception row deleted, not widened | **DISCHARGED** | `crates/verter_semantic/Cargo.toml` `[dependencies]`: `verter_debug_assert`, `verter_span`, `verter_audit` (layer 1, added this landing for the restored counters), `verter_parser`, `verter_language`, `verter_css_syntax`, `verter_no_typeexpr`, `verter_type_expr`, `verter_type_expr_oxc` — no upward edge on any target. `crates/verter_identity/tests/cases/workspace_dependency_layers.rs::ratified_upward_exceptions` lists only `verter_diagnostics` (the `verter_semantic` row is gone; `RATIFIED_ROOT_CRATES` still names `verter_semantic` as a root). `verter_workspace/Cargo.toml` depends on `verter_semantic` (edge reversed). Guard executed: `verter_identity` suite in `suite-results.md`. |
| C1-AC-3 | Authority-uniqueness contract holds after the crate move | **DISCHARGED (Stage-2 form)** | `crates/verter_session/src/project_semantic_dispatch_invariants_tests.rs` green and unmodified in substance; it stays with `project_semantic_dispatch` in `verter_session` because the dispatcher did not relocate under the Stage-2 scope (ruling §7.2 anticipated its relocation "until the transition moves it"; the transition moved the module resolver, not the dispatcher). Authority uniqueness for the moved resolver: exactly one production module resolver (`ModuleResolverCore`), no forwarding wrapper/alias — `verter_workspace/tests/compile-fail/legacy_resolver_surface_is_absent.rs` (S2-F1) and `verter_semantic/tests/cases/resolver_core_ownership.rs` (per-definition destination), both executed. |
| C1-AC-4 | A resolver-tier call site cannot reach resolution without a request-bound context | **DISCHARGED (amended form)** | The lifecycle `ResolverContext` seal stays in `verter_session`. `RequestBoundResolverContext` is implemented only for `HostResolverContext` and `SessionResolverContext`; `SessionQueryHostPort::new` accepts only that sealed request-bound marker. `VerterHost` has no production `ResolverContext` implementation: its direct implementation and dispatcher access exist only under `cfg(any(test, feature = "test-support"))`. The query adapter's explicit-overlay route now fails closed when view-authoritative materialization declines, and cannot fall through to a base artifact or base publication state. Exact conformance and architecture receipts bind the reviewed candidate. |
| C1-AC-5A-MODULE-RESOLVER | The registered C1 portion of C1-AC-5: `AttemptOutcome::{Complete, NeedInputs(LoadSet), Terminal}` covers both production module-resolution attempts | **DISCHARGED (registered split)** | `ARCHITECT-RULING-2026-08-26-C1-AC5-GAP3-AUTHORITY.md` accepts the two `ModuleResolverCore` production entries, immutable `ResolverAttemptView` observations, exhaustive `drive_attempt` retry, and C1-AC-9 path-probe/realpath/package-manifest I/O conversion. `ResolverObservation` is sealed and every method returns `AttemptOutcome<T>`; the exhaustive test double must implement every method; `ResolverAttemptView` is the only production implementation. The 24 converted cases prove driver/kernel result, ordered load-set, wave, observation, replay, and witness correspondence. |
| C2-AC-C1-GAP3-TYPEINFO-GATEWAY-001 | The absent C2-facing `TypeInfoCore::attempt(NonFlowOperation)` portion formerly assigned to C1 GAP 3 | **CODEX-DEFER to C2 — registered exclusion, not C1 evidence** | The distinct AC5 authority ruling binds this as C2's first semantic slice before any `CompileTypeInfo` projection path may land or enter review, due before C2 acceptance and Revision 11 close. The gateway remains absent and is not called complete. It reuses no authority, evidence, or tests from the independent `C2-AC-C1-A6-CONTINUATION-001` performance-continuation obligation. Trunk registry/ledger bytes are inherited exactly; `rebase-proof.md` records the zero-collision rebase and passing live-validator overlay. |
| C2-AC-C1-A6-CONTINUATION-001 | Typed request-local continuation needed to restore the original A6 relative wall gate | **CODEX-DEFER to C2 — registered exclusion, not C1 evidence** | This distinct successor obligation remains unchanged. The corrective rounds add no continuation, cross-request cache, public DTO, or alternate state machine. All historical A6 receipts are exact-subject records only and provide no performance disposition for the current corrective candidate. |
| C1-AC-6 | Duplicated lifecycle-adapter boilerplate collapses to one shared implementation | **DISCHARGED** | `RequestBoundAdapter<L>` owns the single generic `ResolverContext` implementation; `HostRequestLifecycle` and `SessionRequestLifecycle` supply only the genuinely different request-view hooks. `request_bound_lifecycles_share_one_resolver_context_implementation` structurally rejects a lifecycle-local duplicate. The request-view reuse tests and lifecycle-equivalence cases retain the sealed request-bound rail. The exact architecture receipt confirms distinct host/session adapters with no duplicate semantic core. |
| C1-AC-7 | The observation interface does not extend `ResolverContext` and cannot be built holding a host/scheduler reference | **DISCHARGED** | `ResolverObservation` is defined in `verter_semantic`, where no `VerterHost` or scheduler type is nameable under the dependency closure. The sealed-trait compile-fail rail rejects an external host-shaped implementor. Workspace fact and policy ownership is enforced through cross-crate nominal identity and dependency direction; the prohibited name/path/AST source scanner is deleted. Exact conformance and architecture receipts bind the reviewed candidate. |
| C1-AC-8 | Route extraction takes an owned immutable snapshot, not a live `&dyn WorkspaceRead` | **DISCHARGED** | `crates/verter_semantic/src/analysis/routes.rs` extractors take `&RouteAnalysisInputs` (`routes.rs:45`); `grep WorkspaceRead crates/verter_semantic/src` → only two doc-comment mentions in `resolution_world_identity.rs:8` and `observation.rs:176-179` (prose, no type reference; the crate cannot name the trait). Snapshot producer: `crates/verter_session/src/route_analysis_inputs.rs` + `_tests.rs`. |
| C1-AC-9 | `ModuleResolverCore` has no direct scheduler/tsgo I/O left unconverted | **DISCHARGED** | `verter_semantic` cannot name scheduler, tsgo, or workspace. Every probe/realpath/manifest read remains a `ResolverAttemptView` observation. The sole semantic-owned `InputResolutionBudgets` carrier has seven fields: complete `InputKey` identity remains exclusively workspace-ledger accounting, while alias-geometry and tagged completed-witness live retention have distinct operation-wide meters and terminal identities. Alias geometry is priority ordered before normalized-target deduplication and generated one live bundle at a time; every completed-output insertion and merge shares the exact tagged witness counter. Workspace loads and resolver publications remain operation-local until integrity and final operation commitment. Exact conformance and architecture receipts bind the reviewed candidate. |

## Stage-2 outcomes (S2-R1…, S2-F1…F6) as they stand on the candidate

| Outcome | Status | Evidence |
|---|---|---|
| S2-R1 dependency direction | met | `verter_workspace → verter_semantic`; semantic production closure has no upward edge and `verter_semantic/Cargo.toml` has no normal or dev `verter_workspace` dependency. The exact dependency-closure guard passed on the final lineage. |
| S2-R2 A5-DD1 exception deletion | met | `ratified_upward_exceptions` no longer contains `verter_semantic`; the equality-pinned dependency guard passed. |
| S2-R3 every production caller uses `ModuleResolverCore` | met | Destination ownership guard, legacy-surface compile-fail, and the zero production occurrence inventory for `ProjectResolver` / `NativeProjectResolver`. |
| S2-R4 all five F24 driver contracts | met | `s2f4/correspondence.md` maps all 24 cases; seven driver tests cover ordered replay, basis restart, transient retry, no-progress, limits, fingerprint change, and consumed-member exactness. |
| S2-R5 converted dual-runner coverage | met | Final-lineage workspace suite: 38/38, including all 24 converted cases, ledger totality/uniqueness, registration, seven driver cases, and two canonical-replay controls. |
| S2-R6 locked A6 | **met only through exact registered dispositions** | `a6/final-round6-performance.md` binds production `2820cf2e…`: control `+3.096636412%`, relative wall `+16.910458677%`, absolute wall `111.895 ms`, allocation count `+26.675814105%`, and bytes `+32.375733608%` remain literal FAIL. `ARCHITECT-RULING-2026-08-27-C1-FINAL-ROUND6-PERFORMANCE-DISPOSITION.md` supplies only the exact non-transferable C1 dispositions and end-of-C-train closure; RSS, counters, admissions, digest, and resource correctness PASS. |
| S2-F1 no second resolver / no forwarding alias | met | `legacy_resolver_surface_is_absent` compile-fail; `resolver_core_ownership`; grep `ProjectResolver`/`NativeProjectResolver` in production → none. |
| S2-F2 both-edges Cargo state | met on the cumulative tree | `verter_semantic/Cargo.toml` has no `verter_workspace` normal or dev dependency. |
| S2-F3 code and guard flip in one commit | landing-equivalence property of the squash (step 9) | the cumulative delta is reviewed as one tree. |
| S2-F4 no test deleted rather than converted | **met after this landing's repairs** | `test-relocation.md`: 9 `preferred_specifier_*` cases and `macro_kind_round_trips_with_template_kind` had been deleted with no destination — converted/restored (commit `3a1533cd3`); `native_project_resolver_alias_works` is the one explicit exclusion (it asserts the alias S2-F1 deletes). 24-case historical correspondence: `s2f4/correspondence.md`. |
| S2-F5 evidence on the final tree | **review evidence complete; verifier/gate pending** | Round-5 targeted and affected-closure verification is recorded in `final-review-round5-kernel-retention-fix.md`; final retention-lifetime work is production commit `2820cf2e…`; exact production performance evidence is `a6/final-round6-performance.md`; all three exact-candidate receipts are under `reviews/c46c60c52f33784356a9f1d7fade31627486e874/`. Step-8 verification and the canonical gate are not claimed. |
| S2-F6 no fuse-table / A6 reweighting | met for this change | No threshold, lock, validator, performance configuration, or authority record changed. Historical performance results remain literal and exact-subject only. |

## Material changes with no acceptance row (explicit)

| Change | Disposition |
|---|---|
| `performance-gates.toml` A6 corpus sha256 correction | deviation recorded in `a6/receipt.md` (ruling step 2 is maintainer-owned). |
| Restored `attribute_n!` counters + `verter_semantic → verter_audit` edge | severed-governance repair (`governance-join.md`, class 2 defect). |
| Retired allowlist rows (`gate.mjs` oversize exemption; `guard2_allowlist`) | governance join, `retired`. |
| Skill/doc pointer retargets | governance join, class 3. |
| Historical-outcome ledger totality (`resolution_conversion_tests.rs`) | landing-path ruling step 5 (S2-F4 instrument replacement). |
| Carrier-extension probe widening (`6d2741f9d`, pre-existing on the branch) | reviewed behaviour change, pinned by `every_historical_delta_is_exactly_the_registered_carrier_probe_set`. |
| Driver `AttemptFailure::InputResolutionDepthLimit` reporting (`341b2c3f3`, pre-existing) | the first confirmed severed-governance repair (`docs/arch/relocation-severs-reachability.md`); cache-fence assertion executed (`suite-results.md`). |

## Final disposition

The reviewed candidate closes the C1 rows without expanding C1 or transferring C2/C4 work into this
block. This map makes no landing or acceptance claim for its containing evidence commit. Independent
verification, canonical gate, landing equivalence, landing, and trunk-side accepted identity remain
pending.
