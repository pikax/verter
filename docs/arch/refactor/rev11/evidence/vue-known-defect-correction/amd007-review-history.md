# AMD-007 review history

Six review rounds against the interim assembled-module source-map amendment
(`docs/arch/refactor/rev11/amendments/AMD-007-assembled-module-source-map-interim.md`)
before ratification. Consolidated here per this program's commit/evidence conciseness
rule — full raw transcripts are not committed (they were ephemeral Codex CLI session
logs); this is the authoritative disposition record.

| Round | Scope | Verdict | Real findings, fixed |
|---|---|---|---|
| 1 | Full 3-mandate (conformance/architecture/governance) on the first draft | BLOCKING, all 3 | Draft depended on the (then-just-repaired-away) candidate-vs-official mapping design throughout — killed before completion once the underlying oracle defect was found; draft rewritten to depend on the new authored-source oracle. |
| 2 | Full 3-mandate on the corrected draft, post BF2 reopen #4 acceptance | BLOCKING, all 3 | Scattered leftover "candidate-versus-official" language across multiple sections; an abort/rescope loophole letting BV0A change BF2's oracle "if its own composition work requires it"; §3 overstated the oracle's actual guarantee (3 of 9 relations are authored-side-loose, not position-exact); §8 didn't explicitly require all 3 reviews PASS; stale "BF2 reopen #4 must land" language. All fixed with a full rewrite verified against the real `mapping-oracle.mjs` source. |
| 3 | Full 3-mandate combined re-verification | **PASS** | No new defects; confirmed all round-2 fixes genuine, DAG edit acyclic, EM-038 preserved, oracle-accuracy claims match the real `RELATIONS` array. |
| 4 | Focused, after a maintainer-directed KISS simplification (drop mandated `CodeTransform`/chunk-IR, use a write-order running-cursor instead) | BLOCKING | The charter's export-default removal was described as suffix-only; the real assembler (`compile.rs`) does a GLOBAL replace of every occurrence, which could hit authored content ahead of the compiler-emitted trailing line. Fixed to require modeling every occurrence. |
| 5 | Focused, after applying a further maintainer-suggested simplification (make the `__sfc__`→`_sfc_main` rename byte-length-identical by renaming Verter's internal placeholder, eliminating column-delta tracking entirely) | BLOCKING (3) | (1) The export-removal fix from round 4 still under-modeled mid-line matches (line-count-only shift, missing the column splice). (2) The equal-byte-length placeholder rename is unsafe: `__sfc__` is not purely internal to the session assembler — it's independently produced by the IDE Options API path (never passing through the assembler) and recognized by name in the playground's own `compiler.ts`. (3) The claimed file-ownership inventory for that rename was incomplete/inaccurate. All three fixed: the placeholder-rename idea was reverted entirely (restoring the original, already-round-3-passed 7-to-9-byte per-occurrence column-delta handling), and the export-removal geometry was corrected to full line-and-column splice per occurrence. |
| 6 | Focused re-verification of the round-5 fixes | BLOCKING (1), **dismissed as a false positive** | The sole finding: a one-sentence historical note ("An equal-byte-length placeholder rename was investigated and rejected: ...") was read as a "dangling reference" to the reverted idea, despite the review's own summary confirming every substantive claim correct — the restored column-delta math re-derived and confirmed correct, the export-removal splice geometry independently re-derived and confirmed correct, no other section touched. This is a documented REJECT disposition with rationale (required by this project's "Explicit finding disposition" rule), not an active requirement or ambiguity — item 3 of the owned-scope list picks up cleanly with no dependency on it. Program orchestrator judgment: not a substantive defect; not worth a 7th round. If the maintainer disagrees, the sentence can be trimmed to a shorter form or moved to a footnote without touching any binding text. |

**Disposition:** ready for ratification as of round 6, with the round-6 finding
explicitly dismissed (see above) rather than silently dropped.
