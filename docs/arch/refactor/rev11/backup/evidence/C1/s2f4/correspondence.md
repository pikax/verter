# S2-F4 — 24-row historical correspondence

Candidate: <FINAL_REVIEW_SHA to be stamped at freeze>

Pre-deletion engine executed at `c204cfc23a836000b3ace3c3d4473d88eacd69c8` (parent of `d31410791`, the commit that deleted `resolution_dual_runner_tests.rs` and the `ProjectResolver` legacy engine) in a throwaway worktree (`git worktree add --detach … c204cfc23`, removed afterwards). Instrumentation: `historical-engine-instrumentation.patch` (adds `eprintln!` capture only; no assertion changed). Run: `cargo nextest run -p verter_semantic -E "test(resolution_dual_runner_tests)" --no-capture --no-fail-fast` → 24 passed (`historical-engine-run.txt`). Raw captured outcomes: `historical-outcomes-raw.tsv` (60 lines: legacy result/witness/probe order, kernel result/witness, normalized pair per case).

Current suite: `crates/verter_workspace/src/resolution_conversion_tests.rs` (24 converted cases + 2 ledger-integrity tests), `HISTORICAL_OUTCOMES` ledger: one row per (case, outcome kind), 25 rows over 24 cases (the carrier case carries a `dto` row and a `witness` row). Every case asserts against the ledger through a total lookup that panics on a missing row (`assert_historical_outcome`); nothing declines to fire.

| # | Case (old name = new name) | Historical assertion shape | Historical outcome (captured) | Current assertion | Ledger rows | Historical vs current |
|---|---|---|---|---|---|---|
| 1 | `kernel_runner_positive_case_matches_the_witness_contract_result` | kernel-only narrow probe-slice witness (no legacy counterpart in the old file either) | kernel narrow resolved=Some("/store/pkg/mod.tsx"), restarts=0 | production driver vs kernel attempt + ledger witness (narrow cases now run the full driver over the same fixture) | `witness` | `witness`: identical (`=`) |
| 2 | `kernel_runner_restarts_cleanly_on_a_basis_change` | kernel-only narrow probe-slice witness (no legacy counterpart in the old file either) | kernel narrow resolved=Some("/store/pkg/mod.tsx"), restarts=0 | production driver vs kernel attempt + ledger witness (narrow cases now run the full driver over the same fixture) | `witness` | `witness`: identical (`=`) |
| 3 | `kernel_runner_miss_case_matches_the_witness_contract_result` | kernel-only narrow probe-slice witness (no legacy counterpart in the old file either) | kernel narrow resolved=None, restarts=0 | production driver vs kernel attempt + ledger witness (narrow cases now run the full driver over the same fixture) | `witness` | `witness`: widened by the registered-carrier probe set (`.svelte` absent probes; pinned by `every_historical_delta_is_exactly_the_registered_carrier_probe_set`) |
| 4 | `resolution_witness_positive_case_kernel_matches_legacy` | legacy vs kernel narrow probe slice: `resolved`, raw witness set, probe order/count | legacy `source_id`=Some("/store/pkg/mod.tsx"), kernel_core `resolved`=— | production driver vs kernel attempt + ledger witness (narrow cases now run the full driver over the same fixture) | `witness` | `witness`: identical (`=`) |
| 5 | `resolution_witness_miss_case_kernel_matches_legacy` | legacy vs kernel narrow probe slice: `resolved`, raw witness set, probe order/count | legacy `source_id`=None, kernel_core `resolved`=— | production driver vs kernel attempt + ledger witness (narrow cases now run the full driver over the same fixture) | `witness` | `witness`: widened by the registered-carrier probe set (`.svelte` absent probes; pinned by `every_historical_delta_is_exactly_the_registered_carrier_probe_set`) |
| 6 | `full_driver_resolves_a_relative_specifier_for_an_owned_importer` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/src/sibling.ts"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/src/sibling.ts", prov | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: identical (`=`) |
| 7 | `full_driver_resolves_via_a_workspace_alias` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/src/util.ts"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/src/util.ts", provide | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: identical (`=`) |
| 8 | `full_driver_resolves_via_tsconfig_paths` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/src/app/thing.ts"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/src/app/thing.ts", pr | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: identical (`=`) |
| 9 | `full_driver_resolves_via_the_base_url_fallback` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/src2/thing.ts"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/src2/thing.ts", provi | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: identical (`=`) |
| 10 | `full_driver_resolves_via_a_project_reference` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/b/src/thing.ts"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/b/src/thing.ts", prov | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: identical (`=`) |
| 11 | `full_driver_a_project_reference_cycle_terminates_on_both_engines` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/c/src/thing.ts"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/c/src/thing.ts", prov | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: identical (`=`) |
| 12 | `full_driver_resolves_via_hash_imports` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/src/utils/format.ts"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/src/utils/format.ts", | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: identical (`=`) |
| 13 | `full_driver_resolves_via_node_modules_exports_with_conditions` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/node_modules/lodash/esm/index.js"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/node_modules/lodash/e | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: widened by the registered-carrier probe set (`.svelte` absent probes; pinned by `every_historical_delta_is_exactly_the_registered_carrier_probe_set`) |
| 14 | `full_driver_resolves_a_scoped_package_via_legacy_main_field` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/node_modules/@scope/pkg/index.js"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/node_modules/@scope/p | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: widened by the registered-carrier probe set (`.svelte` absent probes; pinned by `every_historical_delta_is_exactly_the_registered_carrier_probe_set`) |
| 15 | `full_driver_resolves_via_explicit_project_ownership` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/src/thing.ts"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/src/thing.ts", provid | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: identical (`=`) |
| 16 | `full_driver_owner_overlap_selects_the_nearest_root` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/pkg/INNER/thing.ts"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/pkg/INNER/thing.ts",  | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: identical (`=`) |
| 17 | `full_driver_a_full_chain_miss_agrees_on_both_engines` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=None, kernel_core `resolved`=None | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: widened by the registered-carrier probe set (`.svelte` absent probes; pinned by `every_historical_delta_is_exactly_the_registered_carrier_probe_set`) |
| 18 | `full_driver_resolves_an_absolute_specifier_for_an_owned_importer` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/abs/target.ts"), kernel_core `resolved`=Some(ResolveResult { source_id: "/abs/target.ts", provider_i | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: identical (`=`) |
| 19 | `full_driver_workspace_alias_wins_over_tsconfig_paths_and_base_url` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/alias/thing.ts"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/alias/thing.ts", prov | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: identical (`=`) |
| 20 | `full_driver_a_dangling_project_reference_falls_through_without_panicking` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=None, kernel_core `resolved`=None | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: widened by the registered-carrier probe set (`.svelte` absent probes; pinned by `every_historical_delta_is_exactly_the_registered_carrier_probe_set`) |
| 21 | `full_driver_resolves_via_node_modules_exports_array_form` | legacy vs kernel: `resolved` + `resolution_kind` + `primitive_witness_pair` equality (`assert_engines_agree`) | legacy `source_id`=Some("/proj/node_modules/pkg/dist/second.js"), kernel_core `resolved`=Some(ResolveResult { source_id: "/proj/node_modules/pkg/dist | production driver (`resolve_tracked`/`resolve_for_project_tracked`) vs kernel attempt equality + ledger witness | `witness` | `witness`: widened by the registered-carrier probe set (`.svelte` absent probes; pinned by `every_historical_delta_is_exactly_the_registered_carrier_probe_set`) |
| 22 | `full_driver_carrier_import_provider_projection_matches_legacy_end_to_end` | complete `ResolveResult` DTO equality, legacy vs kernel | legacy DTO = ResolveResult { source_id: "/proj/src/Comp.vue", provider_id: "/proj/src/Comp.vue.verter.ts", provider_specifi… | production driver + complete DTO vs ledger `dto` row + ledger witness | `dto`, `witness` | `dto`: identical (`=`); `witness`: identical (`=`) |
| 23 | `full_driver_preferred_specifier_candidates_agrees_with_legacy` | `preferred_specifier_candidates` list equality, legacy vs kernel | legacy candidates = Some(["@app/thing.ts", "@/app/thing.ts"]) | `ModuleResolverCore::preferred_specifier_candidates` vs ledger `candidates` row | `candidates` | `candidates`: identical (`=`) |
| 24 | `full_driver_project_exact_result_agrees_with_legacy` | `project_exact_result` DTO equality, legacy vs kernel | legacy DTO = ResolveResult { source_id: "/proj/src/exact.ts", provider_id: "/proj/src/exact.ts", provider_specifier: "whate… | `ModuleResolverCore::project_exact_result` vs ledger `dto` row | `dto` | `dto`: identical (`=`) |

## Verification of the captured values against the pre-existing rows

The 18 `HISTORICAL_WITNESSES` rows that existed at `6d2741f9d^` (before the carrier-extension widening commit) are byte-identical to the historical engine's captured normalized legacy witness for those 18 cases (18/18 MATCH). The 6 cases whose rows were updated by `6d2741f9d` differ from history by exactly the added `.svelte` absent probes — no fact removed, no other fact added — enumerated per case in the ledger and enforced by the `every_historical_delta_is_exactly_the_registered_carrier_probe_set` test (7 deviating rows: the 6 plus `kernel_runner_miss…`, which shares the miss fixture).

Cases that previously asserted nothing against history (the six the ruling names): `kernel_runner_positive`, `kernel_runner_restarts`, `kernel_runner_miss` (witness rows captured from the kernel narrow slice, which the old file itself compared to legacy for the identical fixtures), `full_driver_carrier_import…` (legacy DTO captured verbatim plus the legacy witness captured by re-instrumenting the case), `full_driver_preferred_specifier_candidates…` (legacy candidates verbatim), `full_driver_project_exact_result…` (legacy DTO verbatim). All six now carry rows and assert.

## Registration and execution proof

See `registration-and-execution.txt`: `cargo nextest list -p verter_workspace -E "test(resolution_conversion_tests)"` (26 tests) and the green run.

## RED→GREEN discrimination plants

See `plants.md`.
