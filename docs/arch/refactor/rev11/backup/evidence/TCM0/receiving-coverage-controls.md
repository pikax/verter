# Negative controls for the receiving-coverage derivation

Generated by `probes/receiving-coverage-controls.sh`. **Do not edit by hand**; re-run it.
Each block is a real run: a defect is planted in a charter, the derivation is re-run, the verdict
is read, and the charter is restored. A claim that a control fired is worth nothing unless the
observation is in the tree.

Baseline verdicts, no plant:
```
 A1   TCM1 criterion 1 
 A2   TCM1 criterion 3 
 A3   **EXCLUDED** by TCM1 criterion 1; TCM1 criterion 3 
 A4   TCM1 criterion 11 
 B1   TCM1 criterion 12; TCM2 criterion 14; TCM3 criterion 10 
 B2   TCM2 criterion 1 
 B3   TCM4 criterion 5 
```

**False HIT — an EXEMPTING paragraph must not become a covering hit; the planted criterion 11 must be ABSENT from A1's row.**
```
planted into TCM1: 11. **Probe.** `source_projection_map()` is exempt from COMPILER-ENFORCED DELETION.
 A1   TCM1 criterion 1 
```

**False EXCLUDED — a criterion imposing the obligation while calling a DIFFERENT field out of scope in another sentence must NOT read as excluding; A3 stops being EXCLUDED.**
```
planted into TCM1: 11. **Probe.** `FfiBlockOverrideEntry.source_map` is migrated here. A different field is out-of-scope.
 A3   TCM1 criterion 11 
```

**Exclusion is load-bearing — a plain non-excluding mention flips the excluded part to covered.**
```
planted into TCM1: 11. **Probe.** `FfiBlockOverrideEntry.source_map` is migrated here.
 A3   TCM1 criterion 11 
```

**Drift — the committed derivation must not silently disagree with the charters.**
```
DRIFT — the committed derivation no longer matches the charters. Re-run without --check.
exit=1
```
