# BV0 — Immediate Vue known-defect correction

**Status:** PROPOSED / LOCKED. **Class:** Framework subsystem.
**Predecessor:** BV0A.

## Objective

Correct the genuine Vue VDOM, Vapor, SSR, assembly, and mapping defects exposed by
BF2's exact `vuejs/core v3.6.0-rc.3` seed manifest while preserving every public
route's existing successful result contract. No temporary safety or tracking
mechanism is produced.

## Owned scope

BV0 owns source-root-cause corrections necessary for the exact 36-cell BF2 Vue seed
matrix and independently authored controls. It expressly owns:

1. non-inline `<script setup>` expose binding and implicit `__expose();` emission;
2. VDOM `<slot>` fallback static caching and the `CACHED` patch flag;
3. the remaining genuine seed-matrix defects, including invalid Vapor module
   references/imports, fragment and patch topology, dynamic-props membership,
   setup-return behavior, and source-map correctness defects against the exact
   authored Vue SFC after harness artifacts are removed (incorrect source
   identity/coordinates, missing required authored anchors, and fabricated
   provenance over synthetic output — judged by BF2's accepted authored-source
   mapping oracle, never by official-compiler map equality). BV0A owns the
   existence and composition of the final assembled-module map itself; BV0 owns
   any residual script-emitter, template-emitter, or Vue-semantic mapping defect
   the accepted map then exposes; and
4. shared lower-owner corrections when the same root cause serves multiple Vue
   backends.

BV0 does not introduce B3's canonical request, B4's publication architecture, a new
universal IR, fixture-identity branches, generated-output scanning,
official-compiler production fallback, a known-divergence allowlist, a temporary
typed refusal, or the complete BV1 official-case pack.

## Required procedure

For each defect: add a failing independently authored regression first; reproduce
against the exact rc.3 oracle; identify the existing typed compiler-owner root cause;
implement the minimum reusable correction through the existing structured
emitter/CodeTransform path; rerun every affected axis and a negative control; and
prove unaffected routes retain their prior successful result contract.

A candidate-generator test that merely produces output is not acceptance evidence.
It must be connected to the BF2 comparator, parse/link/runtime/map oracles, and
mutation discrimination. The mapping axis specifically must consume BV0A's genuine
production code-plus-map result through BF2's accepted authored-source mapping
oracle; BV0 may not synthesize a candidate map in the harness.

## Required exits

All 36 exact BF2 Vue seed cells pass their applicable assembled-parse,
exact-package-link, normalized structural/helper-topology, deterministic
runtime/server, diagnostic, and mapping checks — the mapping check validated
against the exact authored SFC content under BF2's accepted oracle's
source-identity, per-segment relation, required-anchor, and generated-only-range
rules, genuinely executing after harness artifacts are removed. An absent map,
decode/bounds-only check without authored-source provenance, skipped required
mapping check, future-B4 promise, or BV0A debt row cannot satisfy this exit. The
isolated oracle install is present so link checks genuinely execute. Every planted
control is detected. No generated Vue route was changed to typed non-success. No
Vue tracking, backlog, waiver, or retraction artifact remains.

The `__expose` and slot-fallback-cache regressions pass for development and
production, inline and non-inline applicability, and VDOM/Vapor/SSR where applicable.
Locked performance cells remain within their existing thresholds.

This exit is not satisfied by BV0A's narrower composition-equality proof, which
deliberately does not gate on the oracle's verdict — BV0 owns bringing every
residual violation BV0A's composition faithfully carried forward to zero, across
script, VDOM, Vapor, and SSR paths, before this exit is met.

## Abort/rescope

Stop with `RESCOPE_REQUIRED` if a genuine seed-matrix defect cannot be fixed without
introducing B3/B4 authority or changing a ratified public product contract. Do not
substitute a guard, tracker, waiver, fixture-specific branch, or silent deferral.
