<!-- unified-charter-v2
id=G4
name=Cache store convergence and bounded retention
phase=rev11
train=rev11.cache-runtime
product=rev11
kind=convergence
semantic_role=convergence
class=foundational
predecessors=E4,G1,G2,G3
conditional_predecessors=
owner=rev11.cache-runtime:fact-validated ProjectTypeStore with bounded per-family retention
conflict_domains=semantic_cache_store
resource_class=rust-mixed
review_profile=architecture-3
gate_profile=targeted-domain
size=S
dispatchable=true
optional=false
release_gating=none
source_refs=live:docs/arch/refactor/rev11/program-dag.toml
external_requirements=
activation_gate=ORC0
charter=charters/rev11-cache-runtime/G4.md
max_production_loc=300
max_production_files=3
max_related_packages=1
rescope_loc=1500
rescope_files=12
rescope_unrelated_packages=3
initial_state=
-->

# G4 — Cache store convergence and bounded retention

Authority state is derived at dispatch. The canonical CLI must validate the current phase, exact predecessor receipts, activation/release gates, external authorizations, and atomic admission before mutation.

## Independently acceptable outcome

Cache store convergence and bounded retention. The current owner is **split cache/store authorities and unbounded candidate retention**. The final and sole owner is **fact-validated ProjectTypeStore with bounded per-family retention**. This charter accepts one authority/migration/cutover boundary; it contains no independently dispatchable subblocks.

## Concrete surfaces and APIs

- Production surfaces: `crates/verter_session/src`, `crates/verter_workspace/src/fact_cache.rs`, `crates/verter_semantic/src`.
- Named API/data boundaries: `ProjectTypeStore`, `StoreViewValidationToken`, `ReadSetSignature`, `candidate_cap`.
- Mutation boundary: bounded validation, named residual deletion, and/or one atomic route switch only; no new authority may be introduced.

## Exact predecessor contracts

- **E4:** exact current receipt ID and digest for “Reclaimable semantic storage and scoped interning”; Git ancestry, touched-blob integration equivalence, and receipt currency must validate at dispatch and acceptance.
- **G1:** exact current receipt ID and digest for “Hermetic query facts and result contracts”; Git ancestry, touched-blob integration equivalence, and receipt currency must validate at dispatch and acceptance.
- **G2:** exact current receipt ID and digest for “FlightCell-owned same-key production”; Git ancestry, touched-blob integration equivalence, and receipt currency must validate at dispatch and acceptance.
- **G3:** exact current receipt ID and digest for “Bounded CPU execution and owner-affine commands”; Git ancestry, touched-blob integration equivalence, and receipt currency must validate at dispatch and acceptance.
- **External custody:** no node-specific external authorization beyond the package activation boundary.

## Source-specific scope

- Deliver exactly “Cache store convergence and bounded retention” as the independently acceptable boundary; no neighboring authority is included.

## Acceptance IDs and discriminating proof

- **G4-AC1 — sole-owner proof:** add `g4_rejects_displaced_authority`; planting any deleted route must make the targeted gate fail.
- **G4-AC2 — positive contract:** add `g4_publishes_exact_projecttypestore`; assert exact identities, provenance, completeness, and deterministic ordering.
- **G4-AC3 — incremental equivalence:** add `g4_incremental_equals_fresh`; cancellation/stale/partial outcomes must be refused from warm publication.
- **G4-AC4 — bounded work:** capture equivalent-work counters for the named surfaces; no extra parse, resolve, plan, emit, copy, allocation, or retained candidate is hidden by wall time.
- Test homes: `crates/verter_session/tests/cases`, `crates/verter_workspace/tests`.

## Deletions and forbidden designs

- Delete or structurally reject: **duplicate cache authority**.
- Delete or structurally reject: **generation-only validity**.
- Delete or structurally reject: **unbounded family slot**.
- Never add a dual-running authority, compatibility fallback, string/regex semantic recovery, test-only production bypass, resource-capacity predecessor, sleep/poll readiness, or unqualified cache/public identity.
- Do not implement successors or silently enlarge this charter. Discovery of a second independently acceptable outcome requires an amendment and a new DAG node before mutation.

## Budgets and mandatory rescope

- Target ceiling: 300 production LOC, 3 production files, 1 related crates/packages.
- Mandatory rescope above 1,500 production LOC, 12 files, 3 unrelated crates/packages, or when public/wire, unsafe, concurrency, or lifetime work is combined with another major concern.
- Correctness budget: zero stale publication, silent fallback, wrong-complete result, map/provenance loss, or identity aliasing.
- Performance budget: equivalent-work counters may increase by 0 and wall/allocation/RSS regression allowance is 0.0% unless an owning-authority amendment supplies exact replacement thresholds; after warmup, retained bytes may not increase across 100 identical requests.

## Abort conditions

- Stop before mutation if current source disproves the named owner/API boundary, a predecessor receipt is stale, or the complete diff will not fit one review context.
- Abort the candidate on unexplained output, source-map, diagnostic, cancellation, allocation, latency, or RSS divergence; do not convert it into residue locally.

## Targeted verification

1. `cargo nextest run -p verter_session -p verter_workspace`
2. Run every final command in the bound `targeted-domain` profile on the landing-frozen candidate; targeted success alone is iteration evidence, not acceptance.
3. Re-run the planted RED mutation, restore, then GREEN, and bind both outputs to the candidate SHA/tree.

## Review and lower-severity findings

Apply `architecture-3`. P0/P1 block final acceptance. A P2 may be accepted only when the currently binding owning policy explicitly authorizes that disposition and the evidence binds its stable fingerprint, named owner, class-wide bounded sweep, and next-cycle receipt; otherwise it blocks. P3 follows the currently binding owning policy and must be recorded when that policy requires it. Any post-review content change invalidates the gate and all three review verdicts. Final acceptance requires three independent PASS verdicts on the exact candidate tree; clean 3/3 means no P0/P1 and no unauthorized or undispositioned P2.

## Dispatch-time immutable bindings

The packet is incomplete and dispatch must fail unless it embeds: exact candidate base SHA/tree; canonical whole-authority digest; current charter digest; each source/corpus revision and excerpt digest; exact worktree and `codex/<node>` branch; the static conflict-domain path/symbol sets and acquired lease receipt; the complete gate command list; three named independent reviewers with assigned lenses, model `gpt-5.6-sol`, effort `ultra`, and report destinations; and the required report-back schema (candidate SHA/tree, changed paths, migration/deletion counts, RED/GREEN commands and outputs, gate receipt digests, review report digests, residual findings, and abort/rescope decisions). These values are derived by the canonical CLI from current authority and evidence; implementers never invent or restamp them.

## Citations

- `live:docs/arch/refactor/rev11/program-dag.toml`

## Collapsed non-authoritative subblock disposition

The recovery candidate mechanically split this source-owned atomic node into the following labels: `G4A`, `G4B`, `G4C`, `G4D`, `G4Z`. They have no separate dispatch, lease, receipt, migration manifest, deletion ownership, or review standing. Their useful source-described concerns are internal RED/GREEN checklist items of **G4**; G4 alone owns the complete migration population, exactly one final deletion/cutover, and atomic acceptance. Any quoted “suggested subblock” wording in transferred source text is non-authoritative planning context.

## Transferred source requirement atoms

These clauses are operative only for the exact applicability set shown. Cold packets include the exact applicable subset and its source digest.

### SRC-ORCH-L440-8A83D361D0F4

- Kind: `context`
- Source: `orchestration-findings.md:440-440`
- Applicability: `G4`
- Exact text SHA-256: `8a83d361d0f4582a9c8f03957ef0b829a78817eb1b8550cf85ed73688dd728ac`

~~~~markdown
## G4 / G5
~~~~

### SRC-ORCH-L442-0F2DCF46685D

- Kind: `context`
- Source: `orchestration-findings.md:442-442`
- Applicability: `G4`
- Exact text SHA-256: `0f2dcf46685d6186fca153e2538cf278add932d403fe2847d81d652df3e67ade`

~~~~markdown
Risk: **\~7/10 each**
~~~~

### SRC-ORCH-L444-6053EDEAE10D

- Kind: `context`
- Source: `orchestration-findings.md:444-444`
- Applicability: `G4`
- Exact text SHA-256: `6053edeae10d88b1bcd09e9c957c06b5460a6f7e32f1585487020146552f09fc`

~~~~markdown
Both have the pattern:
~~~~

### SRC-ORCH-L446-46E20CA3FECE

- Kind: `deletion`
- Source: `orchestration-findings.md:446-452`
- Applicability: `G4`
- Exact text SHA-256: `46e20ca3fece40b2a4bf1c1bb93aba79547906b99d2b67d2c03af1c6291f5747`

~~~~markdown
```text
inventory a broad population
→ classify
→ migrate/converge
→ delete old mechanisms
→ prove terminal
```
~~~~

### SRC-ORCH-L454-356FD03AE077

- Kind: `context`
- Source: `orchestration-findings.md:454-454`
- Applicability: `G4`
- Exact text SHA-256: `356fd03ae077bf7bdf32c8c2b2bed5360fcdb86a2a24cc2df3da82619bd5cfc6`

~~~~markdown
They should receive explicit pre-dispatch decomposition review.
~~~~

### SRC-ORCH-L456-F52D711103D5

- Kind: `context`
- Source: `orchestration-findings.md:456-456`
- Applicability: `G4`
- Exact text SHA-256: `f52d711103d50a437830c6fbcd04fb4bab49a0f82f6d26d1c791c6e8488dd090`

~~~~markdown
---
~~~~

## Live authority inputs

- `live:docs/arch/refactor/rev11/program-dag.toml` — 9506 bytes, SHA-256 `5fbd1dca2126c38514a12fc52149bd2d53b3ba7cb461d9f6d3595b54bae380f1`
