<!-- unified-charter-v2
id=C2
name=TypeInfo gateway, staged compile transaction, and sealed facade
phase=rev11
train=rev11.semantic-compile
product=rev11
kind=cutover
semantic_role=delivery
class=foundational
predecessors=CCA2,B4R0
conditional_predecessors=
owner=rev11.semantic-compile:sealed staged compile transaction over one semantic facade
conflict_domains=diagnostic_action_service,semantic_authority
resource_class=rust-mixed
review_profile=public-3
gate_profile=targeted-domain
size=M
dispatchable=true
optional=false
release_gating=none
source_refs=live:docs/arch/refactor/rev11/program-dag.toml,live:docs/arch/architecture-lock/ledger/program-state.toml,live:docs/arch/refactor/rev11/rulings/ARCHITECT-RULING-2026-08-26-C1-AC5-GAP3-AUTHORITY.md,live:docs/arch/refactor/rev11/rulings/ARCHITECT-RULING-2026-08-26-C1-PERFORMANCE-AUTHORITY.md
external_requirements=
activation_gate=ORC0
charter=charters/rev11-semantic-compile/C2.md
max_production_loc=800
max_production_files=8
max_related_packages=2
rescope_loc=1500
rescope_files=12
rescope_unrelated_packages=3
initial_state=
-->

# C2 — TypeInfo gateway, staged compile transaction, and sealed facade

Authority state is derived at dispatch. The canonical CLI must validate the current phase, exact predecessor receipts, activation/release gates, external authorizations, and atomic admission before mutation.

## Independently acceptable outcome

TypeInfo gateway, staged compile transaction, and sealed facade. The current owner is **direct/managed compile routes and TypeInfo entry points**. The final and sole owner is **sealed staged compile transaction over one semantic facade**. This charter accepts one authority/migration/cutover boundary; it contains no independently dispatchable subblocks.

## Concrete surfaces and APIs

- Production surfaces: `crates/verter_session/src`, `crates/verter_compiler/src`, `crates/verter_protocol/src`.
- Named API/data boundaries: `CompileAttempt`, `NeedInputs`, `InputBasisId`, `TypeInfoRequest`, `CompileArtifactSet`.
- Mutation boundary: only the production surfaces and named API/data boundaries above; every changed path must be inside both that charter surface and the acquired conflict domain, and sibling ownership is excluded.

## Exact predecessor contracts

- **CCA2:** exact current receipt ID and digest for “Compiler artifact, assembly, style-stage, and host boundary”; Git ancestry, touched-blob integration equivalence, and receipt currency must validate at dispatch and acceptance.
- **B4R0:** exact current receipt ID and digest for “Stable SourceUnitId lineage repair”; Git ancestry, touched-blob integration equivalence, and receipt currency must validate at dispatch and acceptance.
- **External custody:** no node-specific external authorization beyond the package activation boundary.

## Source-specific scope

- Deliver exactly “TypeInfo gateway, staged compile transaction, and sealed facade” as the independently acceptable boundary; no neighboring authority is included.

## Acceptance IDs and discriminating proof

- **C2-AC1 — sole-owner proof:** add `c2_rejects_displaced_authority`; planting any deleted route must make the targeted gate fail.
- **C2-AC2 — positive contract:** add `c2_publishes_exact_compileattempt`; assert exact identities, provenance, completeness, and deterministic ordering.
- **C2-AC3 — incremental equivalence:** add `c2_incremental_equals_fresh`; cancellation/stale/partial outcomes must be refused from warm publication.
- **C2-AC4 — bounded work:** capture equivalent-work counters for the named surfaces; no extra parse, resolve, plan, emit, copy, allocation, or retained candidate is hidden by wall time.
- Test homes: `crates/verter_session/tests/cases`, `crates/verter_compiler/tests`.

## Deletions and forbidden designs

- Delete or structurally reject: **alternate public compile facade**.
- Delete or structurally reject: **public semantic trait injection**.
- Delete or structurally reject: **managed construction on direct path**.
- Never add a dual-running authority, compatibility fallback, string/regex semantic recovery, test-only production bypass, resource-capacity predecessor, sleep/poll readiness, or unqualified cache/public identity.
- Do not implement successors or silently enlarge this charter. Discovery of a second independently acceptable outcome requires an amendment and a new DAG node before mutation.

## Budgets and mandatory rescope

- Target ceiling: 800 production LOC, 8 production files, 2 related crates/packages.
- Mandatory rescope above 1,500 production LOC, 12 files, 3 unrelated crates/packages, or when public/wire, unsafe, concurrency, or lifetime work is combined with another major concern.
- Correctness budget: zero stale publication, silent fallback, wrong-complete result, map/provenance loss, or identity aliasing.
- Performance budget: equivalent-work counters may increase by 0 and wall/allocation/RSS regression allowance is 0.0% unless an owning-authority amendment supplies exact replacement thresholds; after warmup, retained bytes may not increase across 100 identical requests.

## Abort conditions

- Stop before mutation if current source disproves the named owner/API boundary, a predecessor receipt is stale, or the complete diff will not fit one review context.
- Abort the candidate on unexplained output, source-map, diagnostic, cancellation, allocation, latency, or RSS divergence; do not convert it into residue locally.

## Targeted verification

1. `cargo nextest run -p verter_session -p verter_compiler`
2. Run every final command in the bound `targeted-domain` profile on the landing-frozen candidate; targeted success alone is iteration evidence, not acceptance.
3. Re-run the planted RED mutation, restore, then GREEN, and bind both outputs to the candidate SHA/tree.

## Review and lower-severity findings

Apply `public-3`. P0/P1 block final acceptance. A P2 may be accepted only when the currently binding owning policy explicitly authorizes that disposition and the evidence binds its stable fingerprint, named owner, class-wide bounded sweep, and next-cycle receipt; otherwise it blocks. P3 follows the currently binding owning policy and must be recorded when that policy requires it. Any post-review content change invalidates the gate and all three review verdicts. Final acceptance requires three independent PASS verdicts on the exact candidate tree; clean 3/3 means no P0/P1 and no unauthorized or undispositioned P2.

## Dispatch-time immutable bindings

The packet is incomplete and dispatch must fail unless it embeds: exact candidate base SHA/tree; canonical whole-authority digest; current charter digest; each source/corpus revision and excerpt digest; exact worktree and `codex/<node>` branch; the static conflict-domain path/symbol sets and acquired lease receipt; the complete gate command list; three named independent reviewers with assigned lenses, model `gpt-5.6-sol`, effort `ultra`, and report destinations; and the required report-back schema (candidate SHA/tree, changed paths, migration/deletion counts, RED/GREEN commands and outputs, gate receipt digests, review report digests, residual findings, and abort/rescope decisions). These values are derived by the canonical CLI from current authority and evidence; implementers never invent or restamp them.

## Citations

- `live:docs/arch/refactor/rev11/program-dag.toml`
- `live:docs/arch/architecture-lock/ledger/program-state.toml`
- `live:docs/arch/refactor/rev11/rulings/ARCHITECT-RULING-2026-08-26-C1-AC5-GAP3-AUTHORITY.md`
- `live:docs/arch/refactor/rev11/rulings/ARCHITECT-RULING-2026-08-26-C1-PERFORMANCE-AUTHORITY.md`

## Mandatory convergence closure

The convergence receipt is invalid unless both `C2-AC-C1-GAP3-TYPEINFO-GATEWAY-001` and `C2-AC-C1-A6-CONTINUATION-001` appear as fully closed claim IDs in candidate-bound gate evidence. It must prove the C2A closed gateway landed first, the C2B continuation is request-local and sealed, the exhaustive operation table matches the real `CompileTypeInfo` surface, every privacy/alternate-entry compile-fail control ran, every observation-specific resume mutation failed, and fresh A6 evidence satisfies the ratified gate. No reusable or cross-request continuation state may survive.

## Ratified C2 entry contract

This atomic C2 node replaces the unrepresentable C2A–C2D mechanical split. Its first implementation slice, before any projection route may enter review, creates these future-declared exact owners:

- `crates/verter_semantic/src/type_info/core.rs::TypeInfoCore::attempt` is the only C2-accessible non-flow semantic gateway.
- `crates/verter_semantic/src/type_info/non_flow.rs::NonFlowOperation` is closed and matched exhaustively without `_`.
- `crates/verter_compiler/src/compile_transaction/type_info.rs::CompileTypeInfo` is concrete, sealed, and exposes exactly the six projection methods below.
- `crates/verter_session/src/typeinfo/vue_macro_codegen.rs::{produce_vue_macro_codegen_with_ctx,produce_vue_macro_codegen_inner,project_tsc_macro,project_runtime_props,project_runtime_emits,project_runtime_model,project_expose_runtime_object}` are the migration population. They may call the new transaction driver but may not remain semantic authorities.

| `CompileTypeInfo` method | `NonFlowOperation` variant | `AttemptOutcome` complete payload | Missing-input proof ID |
|---|---|---|---|
| `project_vue_macro_semantics` | `ProjectVueMacroSemantics` | `VueMacroSemanticInput` | `C2-GAP3-MISSING-VUE-MACRO` |
| `resolve_imported_component_surface` | `ResolveImportedComponentSurface` | `ImportedComponentSurface` | `C2-GAP3-MISSING-IMPORTED-COMPONENT` |
| `project_runtime_props` | `ProjectRuntimeProps` | `RuntimePropsProjection` | `C2-GAP3-MISSING-PROPS` |
| `project_runtime_emits` | `ProjectRuntimeEmits` | `RuntimeEmitsProjection` | `C2-GAP3-MISSING-EMITS` |
| `project_runtime_model` | `ProjectRuntimeModel` | `RuntimeModelProjection` | `C2-GAP3-MISSING-MODEL` |
| `project_expose_surface` | `ProjectExposeSurface` | `ExposeSurfaceProjection` | `C2-GAP3-MISSING-EXPOSE` |

The minimum real `TypeInfoCore` extraction owns imported-component lookup, macro type-query traversal, and props/emits/model/expose projection over immutable observation snapshots. A facade that forwards these operations to `VerterHost`, the scheduler, `produce_vue_macro_codegen_inner`, or a session-owned callback fails `C2-GAP3-NO-FORWARDING-FACADE`. Tests live at `crates/verter_semantic/tests/type_info_non_flow.rs`, `crates/verter_semantic/tests/ui/type_info_privacy.rs`, and `crates/verter_compiler/tests/compile_type_info_routes.rs`. Each table row must pass its all-missing `NeedInputs|Terminal`, complete/preloaded equivalence, and route-removal mutation. `C2-GAP3-FOREIGN-IMPL`, `C2-GAP3-PRIVATE-FIELDS`, `C2-GAP3-ALTERNATE-ENTRY`, and `C2-GAP3-WILDCARD-DISPATCH` are distinct blocking privacy rails.

`C2-AC-C1-A6-CONTINUATION-001` remains separate. Its private request-local continuation identity binds operation/request, originating/current snapshots, `ResolutionBasis`, canonical frontier order, and every consumed observation version. Changed, appeared, disappeared, reordered, or reconfigured input forces a whole-operation restart; invalidation, cancellation, terminal failure, or no-progress discards sealed unpublished output. No cross-request cache, public continuation DTO, reusable warm state, or new retention authority is permitted. The exact tests are `crates/verter_session/tests/c2_continuation_revalidation.rs` and `crates/verter_session/tests/c2_continuation_mutations.rs`; acceptance also requires fresh A6 evidence restoring the original relative gate with every absolute/RSS/counter/digest/correctness lock green.

The locked `program-state.toml` C2 row must contain both complete `CODEX-DEFER` obligations, their distinct owners/rulings/resolution gates, the full continuation contract, and the full GAP-3 contract above before dispatch. The packet must include that exact C2 row and the operative sections of both rulings; a heading-only citation is insufficient.

## Collapsed non-authoritative subblock disposition

The recovery candidate mechanically split this source-owned atomic node into the following labels: `C2A`, `C2B`, `C2C`, `C2D`. They have no separate dispatch, lease, receipt, migration manifest, deletion ownership, or review standing. Their useful source-described concerns are internal RED/GREEN checklist items of **C2**; C2 alone owns the complete migration population, exactly one final deletion/cutover, and atomic acceptance. Any quoted “suggested subblock” wording in transferred source text is non-authoritative planning context.

## Transferred source requirement atoms

These clauses are operative only for the exact applicability set shown. Cold packets include the exact applicable subset and its source digest.

No clause targets this file directly. Applicable contract clauses are selected by the validated `applicable_nodes` ledger and embedded verbatim in cold packets.

## Live authority inputs

- `live:docs/arch/architecture-lock/ledger/program-state.toml` — 184386 bytes, SHA-256 `2b176c4c15730ff9698c73f677eb239800c1adff3891e4e77493a768ea630dee`
- `live:docs/arch/refactor/rev11/program-dag.toml` — 9506 bytes, SHA-256 `5fbd1dca2126c38514a12fc52149bd2d53b3ba7cb461d9f6d3595b54bae380f1`
- `live:docs/arch/refactor/rev11/rulings/ARCHITECT-RULING-2026-08-26-C1-AC5-GAP3-AUTHORITY.md` — 11042 bytes, SHA-256 `7b26645f49a62a7c5de06c6630a87f0d934ae06898c98aedf6a7e46c600ee2de`
- `live:docs/arch/refactor/rev11/rulings/ARCHITECT-RULING-2026-08-26-C1-PERFORMANCE-AUTHORITY.md` — 17854 bytes, SHA-256 `c969bc5962d2ec6ee1a8cd3d5cad7a37b2acf5a0a2d8e4b37549fb097dcd64f6`
