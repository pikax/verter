{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Unified authority DAG module",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "module", "pinned_commit", "pinned_tree", "node"],
  "properties": {
    "schema": { "const": 4 },
    "module": { "type": "string", "pattern": "^[a-z0-9-]+$" },
    "pinned_commit": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "pinned_tree": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "node": { "type": "array", "minItems": 1, "items": { "type": "object" } }
  }
}
