# Plan review round 1 — consolidated findings (3 reviewers)

Reviewers: R1=Claude grounding (ACCEPT_WITH_NITS), R2=codex compiler-hazards (REJECT), R3=codex arch-conformance (REJECT). Gate = 3/3 ACCEPT or nits-only → NOT met. Fix cycle required.
Plan file: D:/tmp/goto-def-plan.md. Architecture (binding): D:/tmp/goto-def-architecture-decision.md.

## P0 — blockers (must fix)
- **P0-1 OQ-1 one-engine (R3 P0; R2/R1 disagree → tie-break pending).** R3: direct tsgo `get_definition` from nav = fork; semantic lookup must enter typed-IR dispatch. R2+R1: tsgo-as-backend + DefinitionEngine-as-navigation-orchestrator is rule-compliant (one-engine rule governs TYPE resolution; go-to-def is navigation; go-to-def already calls type_provider.get_definition, never SemanticQueryKey). TALLY 2-1 for tsgo-backend. → BINDING decision from codex tie-breaker (bt2yavpf5). Whatever wins, ADD a guard (P1-M) enforcing no type-resolution leaks into nav.
- **P0-2 SnapshotId derivation (R3 P0 + R2 P1-5 + R1 P2-6).** Must equal the EXACT compile warm-hit identity: `semantic_hash` + both override hashes (style/content) + profile + parser/compiler version + **cross-file fact signature** (ensure_compiled also validates `compile_slot_facts_validate` — semantic_hash covers own-content only, so SnapshotId is stale-stable under cross-file dep edits unless the fact signature is folded in OR the engine calls ensure_compiled to re-validate facts immediately before render). ALSO: `.ts/.js/.d.ts` real-source targets have NO IDE TSX → need a separate source snapshot/revision for all host files; TSX SnapshotId is only for generated→Vue mapper validation. Mismatch ⇒ DROP (not recompute).
- **P0-3 Phase 6 not independently landable (R2 P0-1 + R3 P1-6).** Phase 6 deletes `resolve_vue_tsx_range`/`merge_definitions_with_barrel_resolver` whose last callers disappear only in Phase 7; retired-symbol test can't pass mid-sequence. → Merge Phases 6+7 (route all nav surfaces through engine, THEN delete in the same landable change), or split into dead-only deletion + final retirement after rewire.

## P1 — major
- **P1-A EmitOp not typed (R1 P2-8 + R2 P1-2 + R3 P1-3).** Use typed coordinate wrappers (SourceByteOffset/GeneratedByteLen/SourceByteRange), not raw u32. Include `MoveOriginal` + the `anchor` field, OR add an explicit justified-deviation note (4 sites need no move; anchors stay unmapped) so the "verbatim architecture" claim isn't silently broken.
- **P1-B Prefix lowering internally contradictory (R2 P1-3).** Plan says emit prefix as `InsertUnmapped` AND `content_offset = prefix.len()` — mutually exclusive. Separate prefix ⇒ mapped content_offset=0; content_offset=prefix.len ⇒ mapped text IS `prefix+original` and prefix NOT separately inserted. `PreserveOriginal` lowers using variables not in its variant. → Define exact per-op lowering + data; tests that fail on prefix double-shift OR unmapped identifier interior.
- **P1-C OverwriteSyntheticBoundary still maps at boundary start (R2 P1-4).** `Chunk::Overwritten` carries source at start, so `innerHTML=`/`{`/`=>`/`=` would map instead of None — contradicts the None-on-synthetic tests. → Lower synthetic visible text as unmapped insertion + deletion, OR explicit anchor-only semantics (only named anchors map; synthetic starts/interiors map to None).
- **P1-D §1B replace-not-delete within-run precision (R1 P1-2).** Deleting position_map.rs:50-53 + :154-158 kills the WITHIN-RUN interior column-delta the architecture's round-trip invariant requires (breaks retained interior tests). Only delete the CROSS-TOKEN extrapolation (:56-109 backward-scan + any unconditional delta). Replace with a guarded within-run delta (applied only when enclosing token mapped AND next token starts after query).
- **P1-E Phase 1 caller list incomplete (R1 P1-1).** Missing `merge_semantic_tokens` (merge.rs:1183,1198) + `merge_inlay_hints` (merge.rs:1283) — direct mapper callers; tree won't compile. Also ~20 `integration_tests.rs` call sites use the old `(line,column)` signature. Replace "etc." with the verified complete set. NOTE (R1): methods are ALREADY Option-returning — the real change is typed-input signatures + deleting Some-extrapolation; fix the "flip to Option" framing.
- **P1-F defineOptions name-span field missing (R1 P1-3).** `AnalyzedMacro`/`AnalyzedOptionsApi` have no `name:`-prop value span (only whole-macro `span`, `object_span`). Priority-1 anchor + `anchor_prefers_define_options_name` test unimplementable without a NEW producer-side field captured in shallow analysis. Add it (Phase 3 producer change) or priority-1 falls back to whole-macro span.
- **P1-G SfcComponentAnchor production site PINNED + OQ-3 resolved in-phase (R3 P1-4 + R2 P1-6 + R1 OQ-3).** `FileAnalysisSnapshot.template: Option` "present after compilation when scope flags active" → priority-4 (template-only) silently degrades to FileStart. Do NOT punt to implementer. Compute template-root anchor from SFC block SYNTAX (independent of optional semantic analysis), OR force the template scope flag for `.vue` records. Guard must DISCRIMINATE: template-only → TemplateRootStart, not FileStart (R1 P2-7). FileStart only for truly-empty SFC.
- **P1-H ban_range_default guard unsatisfiable as scoped (R1 P1-4).** `Range::default()` lives in nav-adjacent files the plan never migrates: features/workspace_symbol.rs, call_hierarchy.rs, action_utils.rs, extract_component.rs, and merge.rs hover/semantic-tokens/inlay/highlight/code-lens sites (1136,1142,1647,1705,1785,1820,2023,3060). → Narrow the guard to the engine-routed handler/merge fns (allowlist the rest w/ justification), OR expand Phase 7 to eliminate them. Pick explicitly.
- **P1-I stale snapshot = DROP not recompute (R3 P1-5).** Phase 5 render() says "re-fetch/recompute else drop" — violates binding "stale mapper ⇒ drop". Mismatch drops the target; recompute only by restarting resolution against the current snapshot BEFORE target construction.
- **P1-J one-engine guard missing (R3 P1-7).** Add static guard + critical-rule registry entry banning direct tsgo/type-provider SEMANTIC calls outside the dispatch adapter (exact form depends on tie-break P0-1).

## P2 — minor
- **P2-A v-model reverse selection (R2 P2-7).** 1 source span → 2-3 generated occurrences; `vue_to_tsx` returns one. Specify deterministic reverse selection (value/read occurrence before assignment LHS) + Rust source→generated v-model test + hover/go-to-def through `($event) => (X = $event)`.
- **P2-B CRLF/tabs/multiline tests missing (R2 P2-8 + R3).** Add mapper/codegen tests for CRLF, tabs, multiline mapped expressions (architecture invariant).
- **P2-C retired-symbol guards + full caller enumeration (R2 P2-9 + R1 P2-5 + R3 OQ-2).** Producer guard regex too narrow. Enumerate FULL caller set of resolve_prefixed_expr (process_element_props:279, process_v_bind 422/436/465/505, process_v_on 594/621/647/693, process_v_model 842, process_v_html 1032, process_v_text 1048) + resolve_prefixed_dynamic_arg (process_v_bind:475, process_v_on:616, process_v_model:861). Migrate ALL before delete; add RETIRED_SYMBOLS-style name guard.
- **P2-D e2e non-skippable (R1 OQ-6).** definition.test.ts has 34 `this.skip()`; new D-series/anchor/desync assertions would silently skip. Extend vendored fixtures so assertions can't skip; fail CI on skip. Rust engine/source-map tests are the HARD gate; e2e is integration.
- **P2-E Phase 7 code-action/rename deletions vague (R3 P2-8).** Name exact functions/files for external .ts/.js + code-action Range::default fallbacks; add to retired-symbol/source-scan guard.
- **NITs:** critical_rules_have_guards.rs real path is tests/g_misc0/... (plan + CLAUDE.md drift); several cited line ranges off by ≤2 (find_export_span 1944-1953, static-split 504-564, dedup 800-806, contract-prop 493-498, ide_context_by_path 1380-1389, resolved_import_definition 413-424).

## OQ RESOLUTIONS — BINDING (codex OQ-resolver + tie-breaker, CONVERGED). These are final; the plan must adopt them.
- **OQ-1:** `DefinitionEngine` = navigation orchestrator; NO typed-IR declaration-site query. tsgo via a LOCATION-ONLY `TsNavigationBackend::definition_at_position(tsx_file, GeneratedByteOffset, SnapshotId) -> Vec<TsDefinitionSpan{canonical_id, span, coordinate_space, snapshot}>`, implemented by `verter_lsp::tsgo::TsgoNavigationBackend` (getDefinitionAtPosition). BOUNDARY: type/member-valued answers (TypeExpr, SemanticNodeId, member sets, component-meta, projections, expansions, skeletons, generic instantiations) route through SemanticQueryKey→ProjectSemanticDispatch→SemanticGraphStore; declaration-site lookup returns an OPAQUE location only. GUARDS: (1) TsNavigationBackend exposes no TypeExpr/SemanticNodeId/checker types/member lists/quickinfo; (2) static: nav engine modules may not import/call SemanticQueryKey/ProjectSemanticDispatch/SemanticGraphStore/type materializers/tsgo type APIs; (3) static: direct tsgo nav calls ONLY in the TsgoNavigationBackend adapter, all LSP handlers route through DefinitionEngine; (4) runtime/audit: go-to-def tests assert ZERO typed-IR semantic dispatches + no type expansion.
- **OQ-2:** new `crates/verter_compiler/src/ide/template/emit.rs`: `EmitText{Static/Borrowed/Owned}` + typed `EmitOp` (incl `MoveOriginal` + `anchor`) + `emit_jsx_binding_value(out, at, JsxBindingValue)` + `JsxBindingValue` (source spans, prefix/suffix, mapped occurrences). Migrate ALL IDE callers of resolve_prefixed_expr/resolve_prefixed_dynamic_arg (v-html, v-text, :[key], native v-model, .foo, v-bind spread, every static-key caller, process_v_on family, process_element_props), THEN delete both producers in the SAME Phase-2 commit + retired-symbol guard.
- **OQ-3:** compute `SfcComponentAnchor` at Vue `IndexedReady` publication (script+template guaranteed ready). Store NON-OPTIONAL via `TemplateAnalysisState{NoTemplate, Ready(Arc<TemplateAnalysisSnapshot>)}` on the Vue analysis record (TS-facing `sfcComponentAnchor?` only because non-SFC files have none). A `.vue` nav artifact must NOT publish if template analysis is deferred. `FileStart` only for truly-empty SFC. Add a producer-side `defineOptions({name})` name-span field for priority-1. Guard DISCRIMINATES priority-4 (template-only → TemplateRootStart, not FileStart).
- **OQ-4:** `SnapshotId(u128) = Hash128("verter.ide.tsx.v1" || generated_tsx_bytes)`, computed once at compile-cache publish (stream while materializing TSX). Store on `IdeResponse.snapshot_id`, `TargetIdeContext.snapshot_id`, tsgo synced-file registry. Every TsDefinitionSpan/CompilerLocation carries the SnapshotId it answered against. O(1) u128 compare; warm hit returns stored id (no recompute, no per-request hash). Non-IDE targets (.ts/.js/.d.ts) have no TSX → validate via host `TargetSourceContext{source, line_index, source_hash}`. Mismatch ⇒ DROP (never recompute-in-place-and-remap).
- **OQ-5:** canonical nav core in `verter_session::navigation::definition` (LSP-FREE): DefinitionRequest, DefinitionTarget{RealSource/SfcComponent/ExternalDeclaration} (CanonicalFileId + Span/SourceByteRange + SnapshotId), SnapshotId, TargetIdeContext, SfcComponentAnchor, SfcAnchorKind, DefinitionSymbolKind, dedup identity, target normalization, barrel terminalization, host-sourced mapper lookup, TargetSourceContext. `verter_lsp`: convert tower_lsp::Position→typed offsets, implement TsgoNavigationBackend, call session DefinitionEngine, render DefinitionTarget→tower_lsp::Location via target LineIndex/source-map/snapshot-check. NO tower_lsp/lsp_types in verter_session. Guard: ban LSP types in session nav targets.
- **OQ-6:** Rust engine tests = AUTHORITATIVE gate (hermetic inline/committed fixtures for desync constructs, anchors, barrels, .ts, .d.ts). e2e = mandatory NON-SKIPPABLE: committed fixture workspace `packages/vue-vscode/e2e/fixtures/goto-definition/` + replace `this.skip()` with marker assertions that THROW on missing marker + a source guard banning `this.skip()` in the definition suite.
- **OQ-7:** split nav surfaces by semantic need: definition/references/rename may use the compiler navigation backend; **type-definition MUST route type computation through ProjectSemanticDispatch**, only final rendering shared. `CompilerLocation{..., snapshot_id}` mandatory; guard that no compiler-derived location is normalized without snapshot provenance.
- KNOCK-ONS: Phase 5 moves DefinitionTarget into verter_session; introduce TsNavigationBackend; rename handlers away from direct type_provider.get_definition; Phase 5/7 guards (ban direct tsgo outside adapter, ban typed-IR dispatch from def nav, ban LSP types in session targets); docs define "type resolution" vs "declaration-site navigation"; NO new SemanticQueryKey mode.
