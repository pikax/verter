# External TypeScript Engine Architecture (LSP / TSC)

**Status:** Proposed — awaiting user sign-off. Not yet implemented. **Hardened after a four-reviewer adversarial pass** (see §7 Review-hardening log); the *direction* survived unanimously (including an independent cold-brief reviewer that reconstructed the same shape), but several load-bearing claims were over-stated and are now corrected/demoted to proof obligations.
**Provenance:** Synthesised from a source-verified evidence investigation (vize/corsa-bind, Volar, golar, typescript-go issue #2824 + PRs #711/#2620) and three independent un-primed architect legs that converged on the architecture below, then subjected to an adversarial review panel. Convergence is recorded as process context, **not** as technical proof — the architecture stands or falls on the falsifiable invariants and Block-0 conformance fixtures in §3/§6, not on reviewer agreement.
**Scope:** This document governs ONLY how Verter drives the **external** TypeScript engine (tsserver / the TS JS language service for TS<7; tsgo/"Corsa" for TS≥7) to type-check generated **carrier** files and answer IDE queries on them. Verter's own semantic type-resolution engine (`SemanticGraphStore`/`ProjectSemanticDispatch`, the 5 query modes) and the `typeinfo`/component-meta feature are **explicitly out of scope** — they never call tsserver/tsgo and the LSP/TSC path never routes through them.

**FRAMEWORK-NEUTRAL by construction (read first).** This architecture keys off the **carrier** (a generated, valid TSX/TS surface) and its **source carrier file**, NOT off `.vue` specifically. Every part of the design — the §2.1 contract, the carrier store, the FS/LSP overlay, ownership/discovery, `.x`→companion redirection, both engines, the TSC path, the WASM path — is framework-agnostic. The **only** framework-specific pieces are: (i) the **carrier source extension** and its redirection specifier (`.vue`, `.svelte`, future `.astro`), and (ii) the **SFC→carrier codegen** itself (the per-framework adapter). Those live behind Verter's **Framework Adapter Substrate** (`/framework-adapters`): each adapter produces the carrier for its component format, and the **companion naming** is owned by the per-adapter `VirtualFileNaming` descriptor. The **gate-proven** naming (§2.2, GATE 5) splits by how the engine reaches the surface: the bare-import-probed **component IDE carrier** is `{name}.vue.tsx` / `{name}.svelte.tsx` (it must be a path tsgo's basename-extension probe actually reaches, and its `.tsx` extension is path-collision-free against a *different* `.svelte.ts`/`.svelte.js` rune-module extension — though the carrier path sits in the USER namespace, so a real same-path file or a same-stem rune is a resolution conflict Verter detects and FAILS CLOSED, §2.2 Carrier-path conflict model); the **redirect-reached `.ts` public-API carrier** — never bare-probed — uses the Verter-reserved `.verter.` infix `{name}.vue.verter.ts` / `{name}.svelte.verter.ts` so it cannot alias a real rune module. **Vue and Svelte are the first two adapters** (a Svelte compiler is being built in Verter now); this doc must read framework-neutral with Vue/Svelte as concrete instances. Where a token like `.vue` appears below it is an **example of the carrier source extension**, read it as "`.vue`/`.svelte`/…". This aligns with the Carrier IDE TS Surface Principle (explicitly Vue AND Svelte): the script block(s) + supported template/markup expressions of every carrier-bearing format lower into ONE generated TS/JSX surface.

**Two distinct surfaces under "LSP / TSC" (do not conflate — see §2.4):** the interactive **LSP** surface (an editor session driving tsserver's language service or tsgo's `--api`/LSP, or the in-context TS JS language service in WASM — §2.11) and the batch **TSC** surface (a `vue-tsc`/`svelte-check`-equivalent whole-project typecheck). They do **not** share a backend on TS<7: **a tsserver language-service plugin does NOT load for the `tsc` command line** (it affects editing only). The TSC path therefore has its own design (§2.4) and is a first-class deliverable, not an afterthought of the plugin.

---

## 1. Context & problem

Verter compiles each component SFC (`.vue`, `.svelte`, … — via the per-framework adapter's `CompileTarget::IDE` codegen) into a real, valid TSX **carrier** file that mirrors the script block(s) + supported template/markup expressions, with a token-precise `CodeTransform` source-map back to the source carrier file. An external TypeScript engine type-checks that carrier; results are mapped back through `ProviderPositionMapper`. Everything below is framework-neutral (per the header); `.vue` tokens are example carrier-source extensions.

Two problems, **one root cause:**

- **Correctness:** the engine places the carrier in a **config-less inferred project** (tsserver gets hardcoded `compilerOptionsForInferredProjects`, `tsserver/ipc.rs:738-761,934-947`; tsgo gets only `rootUri` in `initialize`, no project association, `tsgo/ipc.rs:1466-1481`). So the real `tsconfig.json` — `paths`, `baseUrl`, `lib`, `types`, `jsx`, `moduleResolution`, project references — never applies to the carrier → false `TS2307` and a class of wrong diagnostics. An identical *on-disk* file resolves fine.
- **Performance:** the fastest reference (vize) is ~20–84× faster than `vue-tsc`. vize is **not** in-process magic — it drives a stock user-provided tsgo as a subprocess over msgpack stdio (`ubugeeei-prod/corsa-bind`, no fork/patch). Its speed comes from (a) **rooting its virtual docs in the real workspace tsconfig** so they are real project members, (b) **one persistent project-Program reused** across the whole graph (keyed by tsconfig), (c) lean transport, (d) MT + targeted queries. Verter already runs one persistent engine per session, so spawn cost is *not* the gap.

**The fix has two parts, not one.** (1) The carrier must be made a member of the real configured project — but membership is **injected, not inherited**: a `.vue` (or its carrier) is *not* a configured-project member merely because it sits under the tsconfig's directory. TypeScript's default `include` is `**/*` **filtered to known extensions** (`.ts`/`.tsx`/`.d.ts`, `.js`/`.jsx` iff `allowJs`, plus declared `extraFileExtensions`); an explicit `include: ["src/**/*.ts"]` or a `files` array excludes `.vue` entirely. So each engine must be *told* the carrier is a member, by a **different mechanism**: tsserver via `extraFileExtensions` + `getExternalFiles` on the language-service plugin; tsgo via the `--api` **FS overlay + `open_project`** (the carrier is served through the overlay's `read_file`/`get_accessible_entries` and made a member of the `open_project` configured Program — see §2.3; the shipped API has no inline `default_project` field). (2) Once a member, single-Program reuse becomes available — and is now **proven** for tsgo: the Block-0 gate (GO, 17/17 against the user-installed tsgo) confirmed off-disk-carrier membership via the FS overlay, project-scoped diagnostics/hover/definition from the configured Program, and content-addressed incremental Program reuse across snapshot edits (§2.3, §2.5). The earlier open question is resolved; the residual discipline is version-pinning the `unstable` `--api` and re-gating on bumps (§2.8). The gate harness + report are committed and reproducible at `tools/tsgo-api-gate/` (run `node tools/tsgo-api-gate/run-gate.mjs`).

Verter's own ownership resolver must match this: today's `verter_workspace` ownership is glob-membership-based and would return **no owner** for a `.vue` under an explicit-`include` tsconfig (and `extraFileExtensions` is not modelled in `verter_workspace` at all), which under the §2.4 "no owner ⇒ no external TS result" rule would silently disable TS for that `.vue`. The corrected ownership definition is in §2.6.

Raw type-answer latency is at parity (same tsgo checker); **the lever to be *faster* than vize is the systems layer — doing less non-checker work** (vize regenerates virtual TS per request; Verter can skip *re-codegen* on untouched carriers). Crucially, skipping re-codegen is **not** the same as skipping an engine re-check: cross-file type invalidation stays the engine's job (see §2.7, the corrected cache contract).

---

## 2. The architecture

### 2.1 One project-bound semantic contract; two engine backends
Unify the **semantics**, not the transport. A single provider-neutral contract in which a **config-less / inferred-project operation for a production `.vue` is not representable**. Every operation carries its owning project; every query carries the expected carrier identity (content-hash + source-map id) and fails closed on mismatch (retry once, then no result) — **never** an inferred fallback.

The contract is **thin and three-layered** so the two genuinely different engines (a synchronous in-process tsserver plugin vs an asynchronous out-of-process tsgo `--api` client) do not get forced under one fat abstraction that leaks. The shared layer owns parsing, carrier generation, source-maps, VFS generations, ownership resolution, result mapping, caching, batching, and fail-closed policy. Each backend owns *only* its native project/host mechanics and lifecycle:

```text
ProjectResolver:  source_uri  ->  ProjectBinding | NoProject | Ambiguous | SyntheticScratch
CarrierRegistry:  source_uri  ->  CarrierArtifact { provider_uri, role, content, content_hash, map_hash, version }
EngineBackend  (one per engine kind):
  ensure_project({ workspace_root, tsconfig_uri, ts_version, project_identity, env_dims })
  publish_snapshot({
    project: tsconfig_uri,
    files: [{ source_uri, provider_uri, role, script_kind,
              content, content_hash, map_hash, version, open_state }],
    resolution_map_version,
    fs_generation,
  })
  query({ project, provider_uri, carrier_offset, feature, required_version })
  diagnostics({ project, files?, required_snapshot })
  capabilities()  ->  EngineCapabilities          // negotiated, not assumed — see §2.3/§2.8
```

**Project-resolution states (explicit — config-less is not a silent fallthrough):**
- `ProjectBinding` — a resolved configured project; the only state in which external-TS results are produced for a `.vue`.
- `NoProject` / `Ambiguous` — no owning tsconfig, or two configs claim the file with no deterministic leaf; **fail closed** (no external-TS result; Verter-native non-TS features may still answer per §2.6).
- `SyntheticScratch` — untitled buffers / files outside any tsconfig (e.g. a standalone `.vue` opened with no project). These are **not** production `.vue` project semantics; they may be served by an explicit, clearly-labelled scratch project **only for non-cross-file features**, and never warm any project cache. This is why the invariant is "config-less is illegal *for production `.vue` semantics*," not "config-less is unrepresentable everywhere."

**Roles** (`role`): `CarrierIde` = the `{name}.vue.tsx` / `{name}.svelte.tsx` IDE surface (script + template TSX, full interactive projection) — the **bare-import-probed** component identity (§2.2, GATE 5); `CarrierApi` = the `{name}.vue.verter.ts` / `{name}.svelte.verter.ts` macro-derived public-API surface a cross-file rename resolves against — **redirect-reached**, so it carries the reserved `.verter.` infix; `CarrierBatch` = the **minimal-diagnostic** carrier for the cold TSC surface (§2.7: script + template-expression type-checkable surface + import/redirection shape, WITHOUT the interactive-only IDE embellishments — inlay/semantic-token scaffolding, hover-only helper regions); `Shadow` = a self-file shadow / rune-module surface; `Real` = a genuine non-carrier `.ts`/`.tsx` synced verbatim for context. `CarrierIde`/`CarrierApi`/`Shadow`/`Real` are already modelled by the existing `ProviderSurfaceStore`. **`CarrierBatch` is PROVISIONAL** (the one new role this design contemplates, a distinct `compile_profile`): it is kept ONLY if **Block 2 measures a material cold-perf gain** for a separate minimal-diagnostic surface over simply sharing the one `CarrierIde` surface for the batch run; if the measurement shows no material gain, `CarrierBatch` is **merged into the single `CarrierIde` carrier surface** and the cold path reads `CarrierIde`. Either way the strong equivalence gate `carrier_batch_typechecks_same_as_ide` holds — as a distinct-role equivalence if `CarrierBatch` survives, or trivially (one surface) if it is merged — so the cold path can never silently drop a type-observable template-expression construct (§2.7). Each role's snapshot/ownership/cache semantics are defined where it is produced.

**Env dimensions (`env_dims`), not one bundled hash:** per the project's fact-based-cache R21 rule, `env_dims` carries the *orthogonal* dimensions the value actually depends on — at minimum `parse_env_hash`, `resolve_env_hash`, `lib_env_hash`, and `project_identity` — never a single bundled `compiler_env_hash`. Each cache layer (§2.7) keys only on the subset it depends on.

Queries are illegal without a resolved `ProjectBinding`; `NoProject`/`Ambiguous` fail closed. All LSP features depend on this contract — never on a raw `open_tsx`/`update_file` path-only primitive.

### 2.2 The carrier-serving host-proxy (TS<7 / JS language service) — shared by two instantiations
Drive the TypeScript **JS language service** with a Verter **carrier-serving host-proxy**. This host-proxy has **two instantiations that reuse one shared carrier-serving CORE** — the carrier store/overlay, the §2.9 `.x`→companion redirection + specifier-rewrite, and the fail-closed `ProviderPositionMapper` — but they are **NOT identical host hooks** (the §2.1 contract is unchanged across both): **(a)** the Node **tsserver plugin** on native (this section), which feeds tsserver's plugin-host APIs (`getExternalFiles`/`getScriptSnapshot`/`resolveModuleNameLiterals`); and **(b)** the **in-context `ts.LanguageService`** in WASM/playground (§2.11), which implements `LanguageServiceHost` directly. Both bind to the same shared CORE; the tsserver-specific negative-cache mechanics below (two-phase publish / sticky-resolution eviction) are instantiation-(a) concerns and do not all carry to (b). **Instantiation (a) extends the existing `@verter/typescript-plugin`, which already implements the load-bearing hooks** (`getExternalFiles` returning the descriptor-owned virtual companion, `resolveModuleNameLiterals`, `getCompilationSettings`, `getScriptSnapshot` overrides) — Block 4 is a hardening of that plugin, not a greenfield build; factor the shared CORE so instantiation (b) reuses it.

**File identity = the descriptor-owned virtual companion, NOT the source path. The companion suffix is gate-decided by how the engine reaches it (GATE 5).** A source carrier file projects to a virtual companion whose suffix is owned by the `VirtualFileNaming` descriptor (CRITICAL: the byte-pinned `packages/typescript-plugin/src/generated/virtual-file-naming.ts`, guard `virtual_file_naming_ts_freshness`). The shipped engine has **no module-resolution-map endpoint** (§2.3, GATE 5), so the path Verter serves a carrier at IS the engine identity and a **bare-import-probed** carrier MUST live at a path the engine's appended-extension probe actually reaches. Two distinct identities follow:
- **Component IDE carrier (`CarrierIde`) = `{name}.vue.tsx` / `{name}.svelte.tsx`** (or `.jsx` per source lang). This is the surface a bare `import "./Comp.vue"` / `import "./Widget.svelte"` resolves to, and tsgo reaches it by appending `.tsx`/`.ts` to the FULL basename (`Comp.vue` → `Comp.vue.tsx`). It is **path-collision-free** against a *different* real source extension in the normal case: it ends in `.tsx`, whereas real Svelte **rune modules** are user source at exactly `*.svelte.ts`/`*.svelte.js` (never `.tsx`). **GATE 5 proves this empirically:** `Comp.vue.tsx` resolves the bare `.vue` import (types flow), and a `Widget.svelte.tsx` component carrier coexists in one directory with a real `state.svelte.ts` rune module, correct types both ways, no clash. A `.verter.`-infixed *component* identity is **REJECTED** — serving `Comp.vue.verter.tsx` does NOT satisfy `import "./Comp.vue"` (tsgo never probes a `.verter.` segment → TS2307, GATE 5). **But the carrier path is in the USER namespace, NOT a reserved one — path-collision-free is NOT resolution-unambiguous (see the Carrier-path conflict model below).** A user can have a real `Foo.vue.tsx` on disk, or a same-stem `Foo.svelte.ts` rune module beside a `Foo.svelte` component; both are RESOLUTION conflicts that Verter **detects and fails closed** rather than silently shadowing or surfacing a wrong edge.
- **Public-API carrier (`CarrierApi`) = `{name}.vue.verter.ts` / `{name}.svelte.verter.ts`** — the Verter-reserved `.verter.` infix is correct and necessary HERE, and ONLY here. This `.ts` surface is **redirect-reached** (project-reference redirect / cross-package `.d.ts`-equivalent — §2.6), **never bare-probed**, so a reserved infix does not break any probe; it is *required* because a bare `{name}.svelte.ts` API carrier WOULD collide with a rune module (GATE 5 records tsgo probing `.svelte.ts` **before** `.svelte.tsx`, so a `.svelte.ts` carrier identity would be shadowed by / collide with a same-stem rune module). No real on-disk file carries a `.verter.ts` segment.

**Carrier-path conflict model — the carrier path is in the USER namespace, so a model conflict FAILS CLOSED; Verter NEVER overlay-shadows a real user file (cross-referenced from §2.6 ownership).** The `{name}.vue.tsx` / `{name}.svelte.tsx` component-carrier path is **path-collision-free** against a different real source extension in the normal case, but it is **NOT a Verter-reserved namespace** — a user may legitimately have a real file at that exact path, or a same-stem rune module that resolution reaches first. Such a case is a **resolution conflict**, detected and fail-closed:
- **A real user file at the exact carrier path ⇒ CONFLICT → fail closed (never overlay-shadow).** Before Verter serves a carrier at a target path (`Foo.vue.tsx`), if a real file already occupies that **exact path on disk**, that is a CONFLICT: the source is marked **`Ambiguous`/conflicted** (the §2.1/§2.6 `Ambiguous` state), Verter **does NOT place the overlay** (it never shadows the user's real `Foo.vue.tsx`), and **produces no external-TS result for that source** — Verter-native (non-external-TS) features may still answer. The `path_is_carrier` / `all_adapter_module_extensions()` classifier (above) names the candidate carrier path; the disk-occupancy check is the conflict gate.
- **Same-stem Svelte rune ⇒ AMBIGUOUS → fail closed (DETECTED, not a silently-wrong edge).** Because GATE 5 proved tsgo probes `.svelte.ts` **before** `.svelte.tsx`, if a real same-stem `Foo.svelte.ts` rune module exists beside a `Foo.svelte` component, a bare `import "./Foo.svelte"` is **AMBIGUOUS** — the rune shadows the carrier. Verter **detects** this same-stem collision (the rune module and the component carrier share the exact stem under `path_is_carrier`) and **fails closed** (the source is `Ambiguous`, no external-TS result, no silently-wrong edge surfaced to the user). This **replaces** the prior "documented wrong-edge / accepted TS1192" disposition: the collision is no longer surfaced as a wrong edge — it is a detected ambiguity that produces no result. The component carrier still resolves the bare import normally whenever **no** same-stem `.svelte.ts` rune exists (the normal case, since rune-module stems differ from component stems).
- **Non-fail-closed future path (not in the shipped `--api`).** A static **module-resolution-map API** (an upstream tsgo capability the shipped `--api` does NOT expose — §2.3/§2.8) would let Verter **redirect the bare import to a non-colliding carrier path without shadowing** any real file, dissolving both conflicts above without failing closed. Record it as the **future enhancement**; **fail-closed is the behavior until/unless that API exists.**
- *Guards:* `carrier_never_shadows_real_user_file` (a real file at the exact `{name}.vue.tsx`/`{name}.svelte.tsx` carrier path ⇒ `Ambiguous` + no overlay placed + no external-TS result, asserted for `.vue` AND `.svelte`; the user's real file is never shadowed) and `same_stem_svelte_component_rune_fails_closed` (a `Foo.svelte` component beside a real same-stem `Foo.svelte.ts` rune ⇒ DETECTED ambiguity, no silently-wrong edge — fail closed). Registered to Block 1 (ownership/`Ambiguous`) and Block 4/5 (the per-engine overlay-placement gate).

(The `.vue.tsx`/`.svelte.tsx` and `.verter.ts` examples below read for every adapter.) The plugin:
- declares the carrier extension via `extraFileExtensions` (script kind TSX/JSX per source lang) and exposes project-owned carrier-source files through `getExternalFiles`/`readDirectory` so the **configured project** owns the virtual companion;
- serves the **carrier TSX** as the *companion's* `getScriptSnapshot`/`getScriptVersion`/`readFile`/`fileExists`/script-kind (the companion's content IS the carrier; the source file itself stays the user's source);
- redirects module resolution for carrier-source specifiers (`./C.vue` → the `C.vue.tsx` component IDE carrier — matching the bare-import identity tsgo's probe forces, so both engines resolve the in-project bare import to the SAME surface; the macro-derived `.verter.ts` API surface is the cross-package redirect target of §2.6, not the in-project bare import) via `resolveModuleNameLiterals` (the modern hook; not the legacy `resolveModuleNames`) **without rewriting generated text** (CodeTransform-is-source-of-truth). NB: tsserver's `resolveModuleNameLiterals` returns a `resolvedFileName` directly (a true resolution map), so unlike tsgo it is not basename-probe-bound — but Verter targets the same `.vue.tsx` identity on both engines for parity;
- leaves `paths`/`baseUrl`/`types`/`lib`/`jsx`/project-references owned by tsserver's real configured project.

**Descriptor reconciliation (current vs target).** The committed `VirtualFileNaming` descriptor (`packages/typescript-plugin/src/generated/virtual-file-naming.ts`, mirrored from `crates/verter_session/src/framework/descriptor.rs`) ALREADY renders the gate-proven **component IDE carrier** identity — Vue `ide: .tsx`/`.jsx` → `App.vue.tsx`, Svelte `ide: .tsx` → `App.svelte.tsx` (so the IDE-carrier half needs no descriptor change; the doc's earlier `.verter.`-component claim was the doc being out of sync with both the gate AND the descriptor). The **public-API / import-surface** half differs: the descriptor currently renders `importSurface: .ts` → `App.vue.ts` / `App.svelte.ts`, but GATE 5 shows a bare `App.svelte.ts` is exactly a Svelte rune-module extension and is probed before `.svelte.tsx`, so the Svelte import surface MUST move to the reserved `.svelte.verter.ts` infix (Vue's `.vue.ts` has no Vue rune-module collision, but the API carrier uses the reserved `.verter.` infix uniformly across adapters for one clean, collision-proof rule). **Block 4 lands this `importSurface` descriptor change** (the IDE-carrier rows already match) under `virtual_file_naming_ts_freshness` (byte-pinned) + `carrier_companion_suffix_collision_free`; it cannot land guard-silent. If a later decision instead moves identity to the bare source path, that too is a descriptor + byte-pinned-mirror change landing with the same guards.

**TWO carrier-projection policies — the contract spans BOTH `VirtualPathPolicy::{Suffix, SelfFile}` (D3, framework parity).** Verter's adapter substrate (`verter_session::framework`, `VirtualPathPolicy`) already defines two ways a source carrier file maps to its engine surface, and the membership/overlay/redirection/leak-suppression contract must cover **both** (the above describes only `Suffix`):
- **`Suffix` (companion ≠ source path)** — the common case (Vue components and Svelte components). The **component IDE carrier** identity is `{name}.vue.tsx` / `{name}.svelte.tsx` and the **public-API carrier** identity is `{name}.vue.verter.ts` / `{name}.svelte.verter.ts` (the GATE-5 split above). The overlay serves carrier content **at the companion path** and falls through to disk for the bare source path; `.x`→component-carrier redirection applies (the §2.9/GATE-4 + GATE-5 extension-probing — `Foo.svelte` → `Foo.svelte.tsx`, identical to Vue's `Foo.vue.tsx`); the source `.x` is NOT a TS document; B2 suppresses companion-path results (both the `.vue.tsx`/`.svelte.tsx` IDE carrier and the `.verter.ts` API carrier are Verter-owned companion paths the user never opens).
- **`SelfFile` (the carrier IS the source path, rewritten in place)** — Svelte **rune modules** (the REAL `.svelte.ts`/`.svelte.js` user source) and any adapter surface whose engine identity equals its own canonical path, serving `<prelude> + <rewritten source bytes>`. This is **distinct from the `Suffix` companion** and leak-suppression does NOT apply to it. Here: engine identity = **the source path itself** (no companion, no infix — the rune module keeps its real `.svelte.ts` path); the overlay serves **rewritten content for that real path** (it does NOT fall through to disk — it overrides the on-disk bytes); **redirection is a no-op** (the import already targets the real path, no `.x`→companion hop); the per-line specifier rewrite the rune path performs is an **explicit, sanctioned exception** to §2.7's "carrier text stable / no post-hoc rewriting" (which holds for `Suffix` companions but NOT for `SelfFile`, where in-place rewrite is the mechanism); and **B2 leak-suppression must NOT suppress results on a `SelfFile` path** — it is a real user document (the inverse of the companion rule), so its diagnostics/edits map through `ProviderPositionMapper` (prelude/rewritten regions fail-closed) and surface normally.
- **Collision invariant (gate-proven) — path-collision-free, NOT resolution-unambiguous.** The two `Suffix` identities are **path-collision-free against a *different* real adapter source/module extension** for two distinct reasons: the **component IDE carrier ends in `.tsx`**, which is never a Svelte rune-module extension (`*.svelte.ts`/`*.svelte.js`) — so `Foo.svelte.tsx` is not a *different-extension* rune module (GATE 5 proves a `.svelte.tsx` carrier and a real `.svelte.ts` rune module coexist); the **`.ts` API carrier carries the reserved `.verter.` infix** precisely because a bare `{name}.svelte.ts` WOULD collide with a rune module (GATE 5 records `.svelte.ts` probed before `.svelte.tsx`), and the API carrier is redirect-reached so a reserved infix breaks no probe. **Path-collision-free is NOT resolution-unambiguous, and the carrier path is in the USER namespace:** a real same-path file OR a same-stem rune module is a **RESOLUTION conflict that Verter detects and FAILS CLOSED** (the Carrier-path conflict model above; cross-referenced in §2.6) — never an overlay-shadow of a real user file, never a silently-wrong edge. The descriptor's `path_is_carrier` / `all_adapter_module_extensions()` split is the authority that disambiguates a generated companion path from a real rune-module path. **Same-stem edge (GATE 5 D2) — FAIL CLOSED, not a documented wrong-edge:** a component carrier and a rune module that share the EXACT same stem (`Widget.svelte.tsx` + `Widget.svelte.ts`) make the bare `import "./Widget.svelte"` ambiguous (tsgo probes the `.ts` rune first — the underlying tsgo behavior GATE 5 records as TS1192); this is inherent to Svelte's rune-module naming and is NOT fixed by any infix (a `.svelte.verter.tsx` component carrier is unreachable by the bare probe anyway), so **Verter detects the same-stem collision and fails closed** (the source is `Ambiguous`, no external-TS result — no silently-wrong edge surfaced). The component carrier stays `.svelte.tsx` and resolves the bare import normally **whenever no same-stem `.svelte.ts` exists** (the normal case). The non-fail-closed dissolution (a future module-resolution-map API redirecting to a non-colliding path) is recorded in the conflict model above. *Guard:* `carrier_contract_covers_selffile_and_suffix` (the membership/overlay/redirection/leak rules are exercised for BOTH policies, e.g. a Vue component (`Suffix`) AND a Svelte rune module (`SelfFile`)); `carrier_companion_suffix_collision_free` (the component carrier ends in `.tsx` ≠ any *different* rune-module extension, and the `.ts` API carrier's reserved `.verter.` infix equals no real adapter source/module extension — asserted for `.vue` AND `.svelte` incl. the `.svelte.ts` rune-module form; this guard covers PATH-collision-freedom only, NOT resolution ambiguity — the Fix-1 fail-closed guards below cover that); `component_carrier_is_bare_import_probe_compatible` (GATE 5 — the component IDE carrier identity is a path tsgo's `.tsx`/`.ts` basename probe reaches; a `.verter.`-infixed *component* identity fails the bare `.x` import); `carrier_never_shadows_real_user_file` + `same_stem_svelte_component_rune_fails_closed` (the resolution-conflict fail-closed pair from the Carrier-path conflict model above — a real file at the exact carrier path, and a same-stem rune beside a component, both fail closed).

**Full host-proxy matrix (each hook tested for `.vue`, helpers, type-reference directives, package `exports`, directory scans):** `getScriptSnapshot`, `getScriptVersion`, `getScriptKind`, `readFile`, `fileExists`, `resolveModuleNameLiterals`, `resolveTypeReferenceDirectiveReferences`, `readDirectory`, `directoryExists`, `getDirectories`, `realpath`, `getCanonicalFileName`. A partial proxy silently constructs the wrong Program graph.

**Critical: the plugin does not compile Vue, and a missing/stale carrier must never poison tsserver's caches.** Plugin host APIs are **synchronous** and tsserver **caches their results into the Program and the module-resolution cache** — including *negative* results. If the plugin returns "no snapshot" / `fileExists=false` for a `.vue` companion that the project owns before Verter's async carrier store is warm, tsserver records a sticky failed resolution (`TS2307`) and a missing file, and will not spontaneously re-resolve. So:
- The plugin reads carriers from a Verter-published **content-addressed carrier snapshot store + atomic manifest (epoch)** — never a per-read synchronous callback into Rust.
- **Two-phase publish:** content files are written to the store first, *then* the manifest epoch is advanced. A file is never advertised through `getExternalFiles` before its content exists.
- For a path the plugin *knows* is a project `.vue` (from `getExternalFiles`), a cold read **bounded-blocks** on the manifest epoch (a short, bounded wait on a condition — never a busy-spin, never an unbounded block) rather than returning a negative result; for a previously-known file it returns the **last-good** snapshot/version, not `undefined`.
- After the store warms a path that tsserver had already probed cold, Verter forces a `projectService` "file changed" notification for those paths to **evict tsserver's negative resolution cache**.
- LSP responses are gated **in Verter** (the §2.7 version-gate), not inside the plugin read path: a query whose required snapshot is not yet synced returns no result and retries once — it does not let the plugin fabricate a degraded answer.

Verter remains the parse/codegen/VFS authority; the store is an engine mirror. A genuinely unavailable carrier ⇒ no TS result (never parse raw SFC text, never emit bogus `TS2307`).

### 2.3 tsgo (TS≥7, LSP surface) — the official `--api` path, FS-overlay project membership
**Not** the tsserver plugin model (tsgo cannot dynamically link third-party code into its server process). Drive the user-installed tsgo's `--api`. This section specifies the **OWNED mode** — Verter spawns its own `--api` instance and the FS overlay backs carriers. **OWNED is the correctness BASELINE and the architectural center of the tsgo backend: it is the actual tsgo path, works in every editor, and is what the rest of this doc means by "the tsgo backend."** The **SHARED mode** (attach to the editor's own tsgo, carriers injected as LSP overlay documents on the shared session — §2.10) is an **optional layered optimization** built ON TOP of this OWNED `--api` backend, enabled only where a full editor TS-LSP proxy exists (VS Code today); it is never on the core path. In OWNED mode the plain LSP `didOpen` overlay is **not** the membership mechanism (the FS overlay + `open_project` is); in the SHARED optimization an LSP overlay document on the *shared* session is the carrier-injection channel (§2.10) — both are bindings of the same carrier contract, OWNED being the primary one. **The TS≥7 distribution is the npm `typescript` package at v7** — e.g. `typescript@7.0.1-rc` (the Go-port "Corsa" `typescript` release; per-platform native binaries `@typescript/typescript-{platform}-{arch}`; `--api` client exported at `typescript/unstable/sync`). **This mechanism is empirically verified (Block-0 gate = GO, 17/17 + GATE 5 6/6; reproducible repo gate at `tools/tsgo-api-gate/`, report `tools/tsgo-api-gate/GATE-REPORT.md`).** The install ships an **official generated JS API client** (`{typescript|@typescript/native-preview}/unstable/sync`, class `API`, plus an `unstable/async` JSON-RPC twin for a concurrent driver) that spawns `tsgo --api --cwd … --callbacks=…` over MessagePack — **no hand-rolled wire**. *(The recorded gate run used the `@typescript/native-preview 7.0.0-dev.20260526.1` dev-preview channel. `npm view typescript@7.0.1-rc` confirms the production package carries the platform tsgo binary + the SAME `./unstable/sync`/`./unstable/async` exports, so the **surface is npm-confirmed equivalent** — but BEHAVIOR against the prod package is a **gate obligation, not an assumption**: the gate selects the installed `typescript` whose **major is ≥ 7** (and accepts `@typescript/native-preview` as a fallback source), and Block 5 RUNS it against `typescript@7.0.1-rc` to prove behavior there (B4), not only against native-preview.)* The surface is marked `unstable`/generated, so it is **version-pinned and re-gated on bumps** by re-running the gate harness (the §2.8 capability handshake).

**Membership mechanism (CORRECTED — this is the shipped API, not the #2824 proposal).** The shipped public `updateSnapshot` does **NOT** take an inline `open_files: [{ uri, content, default_project, version }]` payload (that was the typescript-go #2824 *proposal* shape). The actual param is `{ open_project?: tsconfig_path, file_changes?: { changed?[], created?[], deleted?[] } | { invalidate_all: true } }`. The carrier is made a **configured-project member via the FS OVERLAY**, which is a *better* fit for Verter than the proposal shape (it maps 1:1 onto the VFS and needs no special per-file open call):

- `new API({ tsserver_path: <user tsgo exe>, cwd: workspace_root, fs: { read_file, file_exists, get_accessible_entries, realpath, directory_exists } })` — the FS callbacks ARE the VFS overlay (`read_file` ≈ `WorkspaceRead::read_file`).
- `update_snapshot({ open_project: <abs tsconfig path> })` resolves and **holds the configured-project Program**. The off-disk carrier becomes a real **root file** of that Program because the overlay's `read_file`/`file_exists` serve its content and `get_accessible_entries` injects it into its directory listing so the tsconfig's `include` enumeration discovers it (GATE 1: `carrier_in_program_rootfiles = true`, `scriptKind = TSX`).
- **The overlay is sparse:** return carrier content for the carrier path; return `undefined` for `node_modules`/libs/real `.ts` so they **fall through to real disk** (`null` forces not-found). Verter mirrors nothing.
- Association is **queryable**: `snapshot.get_default_project_for_file(carrier)` → the real tsconfig (GATE 1: not an inferred project). The negative control proved that with **no `open_project`**, `get_projects()` is `[]` and the carrier errors "no project found for file" — today's inferred path provably cannot pass, so this is the RED baseline the new fixtures encode.
- **Engine file identity = a single, stable, path-collision-free virtual carrier path** (the component IDE carrier of §2.2 — `Comp.vue.tsx` / `Comp.svelte.tsx`, *not* a temp path and never a *different* real source extension; path-collision-free ≠ resolution-unambiguous — a real same-path file or a same-stem rune is a resolution conflict that fails closed, §2.2 Carrier-path conflict model; the redirect-reached public-API surface is the separate `.verter.ts` carrier), so module identity, project membership, rename/reference identity, and source-mapping all key off one path. The path MUST be one tsgo's appended-extension probe reaches for the bare `.x` import (GATE 5 — hence `.vue.tsx`/`.svelte.tsx`, NOT a `.verter.`-infixed *component* path which the probe never reaches), and MUST round-trip **byte-identically** through both tsgo's path handling and Verter's `ProviderPositionMapper`/`CanonicalPath` (§2.6 portability).

**Carrier discovery — making the companion a Program root, by the verified overlay mechanisms (C/round-2 D3, round-3 codex).** The shipped `--api` exposes `{ open_project, file_changes, fs-callbacks }` and **no explicit `rootNames`-override**, so "root-set injection" must be realised through the FS overlay, which backs **every** read tsgo makes — *including the tsconfig file itself*. The deciding factor is whether the user's config **enumerates the companion's `.tsx`/`.ts` extension**, not whether the glob is "directory-style":
  - **When the config's enumeration ALREADY matches the companion extension** — the default include, a directory/bare-star glob (`"src"`, `"src/**/*"`), OR an **extension-specific glob that matches `.tsx`/`.ts`** (`"src/**/*.tsx"`) — the overlay's `read_file`/`file_exists` (+ `get_accessible_entries` augmenting the directory listing) make the companion a discovered root file directly. This is the case Block 0's fixture (`include: ["src/**/*.tsx"]`, which enumerates the `.tsx` companion) exercised and proved.
  - **When the config does NOT enumerate the companion extension** — a `.vue`-specific glob (`"src/**/*.vue"`), or a `files` list, or any glob whose extensions exclude `.tsx`/`.ts` — `get_accessible_entries` cannot help (it cannot override a `files` list, and a non-matching glob filters the injected name out), so the adapter **virtualizes the tsconfig** as a **first-class identity model**, not a casual content overlay:
    - **The virtualized config is its own cache identity.** It carries its **own virtual-config hash** (over the user config's content hash ∪ the injected companion-path set) and its own derived **`project_identity`** distinct from the user config's, so a virtualized-config Program never aliases a non-virtualized one in any §2.7 cache slot. The overlay serves a `read_file` of `tsconfig.json` whose `include`/`files` is augmented with the companion paths (a Verter-computed overlay of the user's config content, never written to disk; the user's real config is unchanged). tsgo then parses the virtualized config and the companion is a root file.
    - **Injected roots are config/options-diagnostic-invisible (batch promises config/options parity).** The companion entries Verter injects into `include`/`files` are **mapped/suppressed** so they NEVER surface in user-visible config-file-parse / options diagnostics (`get_global_diagnostics` / options diagnostics / config-file parse diagnostics, §2.4) — a virtualized config must produce the **same config/options diagnostic set** the user's real config would; only the real-config entries are user-visible, the injected roots are Verter's and are stripped from any config/options diagnostic before it maps back. Apart from the injected `include`/`files` entries the virtualized config is byte-identical to the user's.
    - **Explicit invalidation.** The virtualized config is invalidated and recomputed when ANY of: the user's `tsconfig.json` content changes (its content hash advances), the owned companion-path set for that project changes (a carrier-source file added/removed/renamed), or an `extends`-chain ancestor changes — each advances the virtual-config hash and re-roots the virtualized-config Program. A pure carrier-*text* edit does NOT touch the virtual config (the path set is unchanged) — only the §2.7(b) engine notify fires.
  Either way the companion is a member of the `open_project` Program; the choice is driven by whether the user's config enumerates the companion extension (the §2.6 ownership resolver records which). **Block 5 must prove both** mechanisms: the **companion-extension-enumerated** case is **gate-proven** (Block 0's fixture used `include: ["src/**/*.tsx"]`, an extension-specific glob that matches the `.tsx` companion — this is that case, NOT a directory glob); the `.vue`-specific-`include` and `files`-list cases via the tsconfig-virtualization path are the unproven ones.

**`.vue` → carrier module redirection rides the FS overlay (C13 — shipped reality).** This binary exposes FS callbacks for resolution but **no dedicated static module-resolution-map endpoint yet**. So for `import X from "./Comp.vue"`, Verter answers `read_file`/`file_exists` for the resolved carrier path; tsgo owns all normal resolution (`paths` alias + project-reference redirect were both proven on the off-disk carrier — GATE 1/2). The redirection contract is still **keyed and bounded** so it cannot shadow real resolution:
- **Logical key:** `(project, containing_uri, module_literal, resolution_mode, resolve_env_hash)` → `{ resolved_carrier_path, script_kind }`, realised by answering the FS overlay for that carrier path.
- **Rule:** TypeScript's own resolver runs **first**; Verter only supplies a redirect when resolution reaches a `.vue`/carrier/helper specifier TS cannot resolve by extension. Verter **never reimplements** general module resolution.
- **Lifecycle:** the redirection set is diffed/invalidated **independently of carrier text** — it changes only when the project file set, `.vue` path set, config, or import signatures change.
- **The bare `./X.vue`-specifier redirection is now PROVEN** (gate probe + GATE 4 + GATE 5): a bare `import "./Comp.vue"` resolves because tsgo appends `.tsx`/`.ts` to the basename and the overlay answers the **component IDE carrier** path `Comp.vue.tsx` (serving the bare path itself fails with `TS2307` — the redirection MUST target the carrier extension). *(The production adapter answers at exactly this `.vue.tsx`/`.svelte.tsx` component identity — GATE 5 proved a `.verter.`-infixed *component* path is NOT reached by the probe and a `.vue.tsx` path IS, with types flowing. The reserved `.verter.ts` infix is the SEPARATE redirect-reached API carrier, never this bare-import target.)* Block 5 carries this case in the production adapter; if a future tsgo ships a dedicated static-resolution-map endpoint, migrate the redirection onto it (strictly the same `(containing, literal, mode) → resolved` mapping) — at which point a non-bare-probed component identity would become possible. It is unnecessary for the *normal* case (where `.vue.tsx`/`.svelte.tsx` is path-collision-free against a different real extension), but it is exactly the **non-fail-closed dissolution** of the §2.2 Carrier-path conflict cases (a real same-path file or a same-stem rune): the map could redirect the bare import to a non-colliding carrier path without shadowing, replacing the fail-closed behavior those cases require until it ships.

### 2.4 The TSC (batch typecheck) surface — a separate backend, not the LS plugin
The `vue-tsc`-equivalent whole-project typecheck is a **first-class deliverable with its own backend**, because **a tsserver language-service plugin does NOT load for the `tsc` command line** (LS plugins affect interactive editing only). The §2.2 plugin therefore cannot serve TS<7 batch typecheck at all, and the perf benchmark (a cold full-project run, §2.7) is a *TSC-surface* measurement.

The no-fork TSC path per engine:
- **TS≥7 (tsgo):** the same `--api`/`open_project` Program drives batch diagnostics over the configured Program with carriers as FS-overlay members (the same membership as §2.3, proven in Block 0). A `vue-tsc`-equivalent emits the **complete** diagnostic set, not semantic-only: per file the **syntactic** and **semantic** diagnostics (`get_syntactic_diagnostics` + `get_semantic_diagnostics`), plus the program-level **global/options/config diagnostics** (`get_global_diagnostics`, options diagnostics, config-file parse diagnostics) and any **declaration** diagnostics when the user config emits declarations — all classified through `ProviderPositionMapper` (generated-only spans suppressed, §2.7). Mapping a tsgo carrier diagnostic back to the `.vue` is the same fail-closed mapping as the LSP surface. This reuses one backend for both LSP and TSC on tsgo, and is what the §2.7 cold benchmark targets.
- **TS<7 (tsserver / `tsc`) — the MIRROR-HOST / compiler-host model ONLY.** Since no plugin loads, batch typecheck drives a **`ts.CompilerHost`/tsc-API driver** that re-roots reads so generated carriers live in a **mirror tree** — **the generated carriers NEVER touch the user's working tree** (no source-tree writes, no co-located sidecars; the user's checkout is read-only as far as Verter's batch is concerned). The precise filesystem/config model:
  - **Mirror-host carrier placement.** For `src/Foo.vue`, the carrier is materialised into a Verter-owned mirror root (e.g. `<mirror>/src/Foo.vue.tsx`, the descriptor-owned component IDE carrier of §2.2), not next to the source. (On this TS<7 batch path the host `resolveModuleNames` override is a true resolution map — D7 — so the carrier path is not basename-probe-bound; Verter uses the same `.vue.tsx`/`.svelte.tsx` component identity as the engine surfaces for consistency, and the `.verter.ts` API carrier for referenced-project `.d.ts` emit per build mode below.) The driver's host `readFile`/`fileExists`/`getSourceFile` serve the mirror carriers and fall through to the real user tree for everything else; relative imports inside a carrier (`./bar`, `../lib/x`) resolve **identically to the same specifier from the source's own directory** because the host's `resolveModuleNames`/`resolveModuleNameLiterals` re-roots resolution into the user tree (or the mirror is overlaid via `rootDirs`), so `baseUrl`/`paths`/relative/`node_modules` resolution is byte-for-byte the user's — with **zero working-tree mutation**.
  - **A `.x` → companion module redirection** is required so `import C from "./C.vue"` resolves to the carrier. **`paths`/`baseUrl` do NOT solve this (B3):** per the TS module-resolution docs, `paths` mappings apply only to **non-relative (bare) module specifiers** — a relative `import "./C.vue"` is never matched by a `paths` entry. The mirror-host model uses the **host `resolveModuleNames`/`resolveModuleNameLiterals` override** on the `ts.CompilerHost`/tsc-API driver to map the `.x` specifier to its companion (resolver-version-independent — it does not rely on stock `tsc`'s appended-extension probing); `rootDirs` may overlay the mirror as a secondary aid. Verter's import lowering may *also* reference the companion via `CodeTransform` (never text-splice). `paths` is reserved for bare/aliased specifiers only.
    - **Proof scope (D7):** the host `resolveModuleNames` override (the mechanism above) is **resolver-version-independent** and does NOT depend on stock `tsc`'s appended-extension behavior, so the tsgo GATE-4 extension-probing result is NOT assumed to transfer to the JS `tsc` resolver. The Block-4 spike validates that the host-override carrier resolution matches the user's own diagnostics on a `composite`+reference+`paths` fixture.
  - **The generated tsconfig (consumed by the driver, served from the mirror) `extends` the user config** and changes only what is required: it adds the carrier companions to a `files` list (or `include`), and **preserves `rootDir`, `outDir`, `composite`, `declaration`, `declarationDir`, `types`, `lib`, `paths`, and `references` verbatim from the user config**. The injected companion entries are **config/options-diagnostic-invisible** exactly as the §2.3 tsconfig-virtualization identity model requires (the batch promises the user's own config/options diagnostic set); `rootDir` stays valid because the mirror re-roots the carriers under it.
  - **Emit/invocation mode is reference-dependent (the `noEmit` + references incoherence is fixed here).** A referenced project is consumed through its emitted `.d.ts`, and `noEmit` produces no such boundary — so forcing `noEmit` on a referenced project breaks cross-project resolution (and contradicts §2.6's `disableSourceOfProjectReferenceRedirect: true` mode, which is exactly "use the `.d.ts` boundary"). Therefore:
    - **No project references:** invoke the driver in `noEmit: true` diagnostics-only mode — nothing written.
    - **With project references:** invoke in **build mode** (`tsc -b`-equivalent through the driver), which respects `composite` and **emits each referenced project's real `.d.ts`** so the referencing project resolves it the way the user's own build does. `noEmit` is **not** forced on referenced projects; the leaf being type-checked may still be `noEmit`. Emitted `.d.ts`/`.tsbuildinfo` are written into the **mirror tree** (a Verter-owned `tsBuildInfoFile`/`outDir` under the mirror root), never the user's working tree — the Block-4 spike validates the mirror-emitted boundaries match the user's own `tsc -b` diagnostics.
    - **§2.6's source-of-project-reference redirect is a LANGUAGE-SERVICE (LSP) feature, not a CLI-build one** — do not conflate the two surfaces. On the **batch** path, build mode consumes a referenced project **through its emitted `.d.ts` boundary regardless of the redirect setting** (build mode does not source-redirect); `disableSourceOfProjectReferenceRedirect` only affects the LSP/editor surface (§2.3, where the in-memory source carrier can be the resolution target). So on the batch path the model is uniform: build mode emits each referenced project's `.d.ts` (including the `CarrierApi`-equivalent declaration for a referenced `.vue`) and every cross-project import resolves through it. The LSP surface (§2.3/§2.6) is where the default-vs-`disable…` distinction (source carrier vs `.d.ts`) actually applies. No virtual-`.d.ts` channel is needed because build mode produces the real one in the mirror.
  - **Project references** are otherwise followed as the user declares them; a referenced project's carriers are materialised in **its own mirror subtree** under its own generated tsconfig, and the referencing project's generated tsconfig points `references[].path` at the **referenced project's generated (mirror) tsconfig**.
  - *(This mirror-host physical-carrier path is also the §2.8 fallback for any TS≥7 workspace where `--api` batch diagnostics prove insufficient.)*
  - **Why mirror-host, not source-tree sidecars (the rejected alternative):** co-located gitignored sidecars would touch the user's working tree (perturbing file watchers, other tools, or a read-only checkout) — that intrusiveness is exactly why the source-tree sidecar model is **rejected** (see §4). The Block-4 spike validates the mirror-host model against the user's own `tsc -b` diagnostics on a `composite`+reference+`paths` fixture (both with and without `disableSourceOfProjectReferenceRedirect`); it does not bake off against sidecars, which are off the table.

**Minimal/cheap Verter footprint for the TSC surface (A6).** The batch typecheck must run a **lean** Verter — only what carrier codegen + driving the engine needs — because the cold run's only beat-the-reference lever is doing less non-checker work (§2.7). The TSC footprint **includes**: the framework adapter's carrier codegen, the VFS/source read, ownership resolution (§2.6), the generated-tsconfig/compiler-host driver, and the `ProviderPositionMapper` for diagnostic mapping. It **excludes** the LSP/interactive machinery: the editor session/`SyncCoordinator`, the LSP server + document registry, the interactive query de-dupe/cancellation lanes (§2.7), the SHARED-mode attach path (§2.10), the **eager-`CarrierApi` auto-import INDEX** (§2.7 — auto-import is an editor feature; the *index* is not built for batch), inlay/semantic-token/hover scaffolding, and the `ProviderSurfaceStore`'s editor-rename history. The reduced host instantiates only the carrier-store **cache layer** (the §2.1/Block-2 content-addressed slots — including the leaf diagnostic-carrier slot it reads, the provisional `CarrierBatch` slot or the shared `CarrierIde` slot per §2.1), not the full LSP `ProviderSurfaceStore` with rename history (reconciling A6's exclusion with B6's storage slot).
- **Which carrier(s) batch generates — depends on references (D5, reconciling A6 with §2.4's build mode).** The **leaf** project under check uses the leaf diagnostic carrier — the **`CarrierBatch` minimal-diagnostic carrier if Block 2 keeps it as a distinct role, else the shared `CarrierIde`** (per the §2.1 provisional decision). But when project references are present, build mode **emits each referenced project's `.d.ts`** (§2.4), and a referenced project's `.vue`/`.svelte` needs a **declaration carrier** to emit from — that is the **`CarrierApi`** surface (the macro/adapter-derived public declaration). So batch generates `CarrierApi` (the `.d.ts`-producing surface) for **referenced** projects' carrier sources, and the leaf diagnostic carrier for the leaf being diagnosed. (`CarrierBatch`/`CarrierIde` is diagnostics-only and not declaration-equivalent, so it cannot stand in for a referenced project's emit — `CarrierApi` is the distinct declaration role.) The eager-`CarrierApi` *index* is still excluded (that's the auto-import accelerator); batch generates `CarrierApi` *carriers* on demand for the reference closure it must emit declarations for.
Concretely the TSC entry point constructs a reduced host (carrier codegen + VFS + cache layer + engine driver) rather than the full LSP host, so a `vue-tsc`/`svelte-check`-equivalent run carries no per-keystroke/interactive overhead. This is the footprint the §2.7 cold perf gate measures.

Both TSC variants are fail-closed and version-gated exactly like the LSP surface; a `.vue` with `NoProject`/`Ambiguous` ownership is excluded from the batch run rather than checked under a synthesised config.

### 2.5 VFS-driven incrementality; one Program per tsconfig (reuse PROVEN — Block 0)
The VFS is the authority for source + carrier artifacts + source-maps + generations + project ownership. Provider sync is a pure **projection**:
1. source edit/disk change enters the VFS → bump generation;
2. ownership resolves to a `ProjectBinding` (or `NoProject`/`Ambiguous`/`SyntheticScratch`) per the §2.6 algorithm;
3. carrier looked up by its content-addressed key (§2.7); if absent, compiled **once** from the shared parse into IDE carrier + source-map + (lazily) IDE TSX, with the public-API surface eagerly indexed per §2.7;
4. the sync planner computes per-project **deltas** (changed/removed carriers + resolution-map delta + fs generation), AND forwards every dependency-closure generation bump (§2.7 — a dependency change is the engine's to re-check, not Verter's to suppress);
5. the adapter applies **one atomic batch per project**;
6. queries are allowed only after the engine acknowledges the matching generation/epoch.

One persistent workspace session, with **one configured Program per tsconfig, reused — now PROVEN for tsgo** (Block 0, GATE 3): a carrier-only edit pushed as `update_snapshot({ open_project, file_changes: { changed: [carrier] } })` flips diagnostics on the **same stable project handle** (`p.<canonical-tsconfig-path>`, unchanged across the edit); an **unchanged dependency keeps its source-file identity** (retained, not re-parsed); and a redundant identical delta returns an **empty `changed_files`** because tsgo **content-hashes** and reports nothing changed (content-addressed incremental invalidation — it does not blindly trust the `changed` hint). tsserver configured projects reuse Program structure by construction. Correctness still **never** depends on Program identity: if a future engine rebuilds, results stay correct (just slower) and the §2.7 perf gate is re-derived per §2.8. Ordinary `.vue` edits bump only that carrier version (push a per-file `file_changes.changed` delta); tsconfig/lib/types/project-reference changes bump the **project** generation and invalidate the whole configured project (`invalidate_all` or re-`open_project`). No owner / ambiguous owner ⇒ no external TS result (Verter-native non-TS features may still answer).

### 2.6 Project ownership, project references, and solution-style configs
Ownership resolution must match TypeScript's own configured-project semantics, **not** literal glob membership of the `.vue` path (which fails because `.vue` is not a known extension — §1). The resolver:

1. **Parses configs with TS-equivalent rules** — `files` / `include` / `exclude`, `extends` chains, and the implicit default include.
2. **Computes `.vue` ownership by the SAME extension rules TypeScript applies — `.vue` is NOT picked up by an extension-specific glob.** Automatic supported-extension expansion (`.ts`/`.tsx`/`.d.ts`, `.js`/`.jsx` iff `allowJs`, **plus declared `extraFileExtensions`**) happens ONLY when an `include` glob's last segment has no extension and no extension-bearing wildcard — i.e. a directory glob (`"src"`) or a bare star (`"src/**/*"`). An extension-specific glob like `"src/**/*.ts"` matches **only** `.ts` and does **not** expand to `.vue`. So a `.vue` is owned by a configured project ONLY when one of these holds: (a) the **default include** (no `files`/`include`); (b) a **no-extension directory/bare-star glob** that covers it (`"src"`, `"src/**/*"`); or (c) a **glob or `files` entry that explicitly covers `.vue`** (`"src/**/*.vue"`). Under `"include": ["src/**/*.ts"]`, the sibling `.vue` is **NOT** a member ⇒ `NoProject`. (This is also why the tsgo carrier-discovery of §2.3 works for the component IDE carrier — `Foo.vue.tsx`/`Foo.svelte.tsx`: its final `.tsx` extension matches a `.tsx` glob or the default include, so the carrier is enumerated even though the bare `.vue` is not — and a project whose config does NOT enumerate the carrier extension reaches it via §2.3 tsconfig-virtualization. Note these are two SEPARATE facts: the `.vue` SOURCE ownership follows the §2.6 extension rules above (a `.ts`-only glob does not own the `.vue`), while the COMPANION reachability is the §2.3 discovery mechanism; the ownership resolver records the source-ownership case and the adapter selects the matching discovery mechanism. The redirect-reached `.verter.ts` API carrier is NOT enumerated — it is resolved on demand via the §2.6 project-reference redirect, so its reserved infix never needs to match a glob.)
3. **Solution-style (referenced-only) roots resolve to a leaf project**, never to the solution root: the owner is the leaf configured project whose include/files claims the file (by the rules in step 2). Two leaves claiming one file with no deterministic winner ⇒ `Ambiguous` ⇒ fail closed (this must be rare; a common monorepo layout has exactly one leaf per file).
4. **Carrier-path conflict ⇒ `Ambiguous` ⇒ fail closed (the carrier path is in the USER namespace — §2.2).** Even when steps 1–3 resolve a clean `ProjectBinding`, a carrier cannot be served if its target path collides with real user resolution. The carrier-companion path (`{name}.vue.tsx` / `{name}.svelte.tsx`) is **not a Verter-reserved namespace**, so the ownership resolver records two model-conflict cases that downgrade the source to `Ambiguous` (no external-TS result; Verter-native features may still answer) — the full model + guards (`carrier_never_shadows_real_user_file`, `same_stem_svelte_component_rune_fails_closed`) live in §2.2's Carrier-path conflict model: (a) a **real user file at the exact carrier path** — Verter NEVER overlay-shadows it; (b) a **same-stem Svelte rune** (`Foo.svelte.ts` beside `Foo.svelte`) that the GATE-5 `.svelte.ts`-before-`.svelte.tsx` probe reaches first — a DETECTED ambiguity, never a silently-wrong edge. The non-fail-closed dissolution (a future static module-resolution-map API redirecting to a non-colliding carrier path) is the recorded future enhancement; fail-closed holds until it ships (§2.2/§2.8).

**Project references / `composite` / `disableSourceOfProjectReferenceRedirect`.** A `.vue` in package `B` imported from package `A` is reachable through **two** Programs with **redirect-dependent** resolution, which the single-valued ownership of step 2 cannot express alone. Per the TypeScript docs, in 3.7+ the **source of truth is the referenced project's TypeScript source**, and `disableSourceOfProjectReferenceRedirect` reverts to the pre-3.7 behaviour where the `.d.ts` is the module boundary:
- **DEFAULT (redirect ON, `disableSourceOfProjectReferenceRedirect: false`):** `A`'s import of `B`'s `.vue` resolves to `B`'s **SOURCE** — i.e. `B`'s full IDE carrier (`CarrierIde`, §2.1) — so `A` sees `B`'s live source types. The adapter must make `B`'s carrier visible to `A`'s Program (publish it into both `B`'s and `A`'s Programs).
- **`disableSourceOfProjectReferenceRedirect: true` (pre-3.7 boundary):** `A` resolves to `B`'s **declaration output** — for a `.vue` that is the **macro-derived public-API surface** (`CarrierApi`), which Verter presents as `B`'s emitted-`.d.ts`-equivalent (no `.d.ts` is written to disk; the surface is served virtually to `A`'s Program and content-addressed). `B`'s own Program still uses its full IDE carrier.
- Each referenced project is published with its **own** ownership, API surfaces, and invalidation generation. Where no faithful mapping exists, **fail closed** for the cross-package query rather than guess.

**Ownership ≠ discovery — two steps, bridged by the §2.3 overlay mechanisms.** Step 2 decides whether a `.vue` is *owned* (a member of a configured project). Making its *component IDE carrier* reachable by the engine is a separate step with **two concrete mechanisms by config shape** (fully specified in §2.3): for **any config whose enumeration matches the carrier's `.tsx` extension** (the default include, a directory/bare-star glob, OR an extension-specific `.tsx` glob — the case Block 0 proved), the FS overlay surfaces it directly; for a `.vue`-specific `include` or a `files` list that does NOT enumerate `Foo.vue.tsx`, the adapter **virtualizes the tsconfig** (the first-class identity model of §2.3 — own virtual-config hash/`project_identity`, injected roots config/options-diagnostic-invisible, explicit invalidation; overlay-serves a `read_file` of `tsconfig.json` with `include`/`files` augmented by the companion paths — never touching the user's real config). For tsserver, `getExternalFiles` returns the companion for the configured project (same effect). So "owned" (§2.6) and "discoverable by the engine" (§2.3/§2.2) are always reconciled: ownership is computed by the §2.6 extension rules; the companion is then surfaced by the matching §2.3 mechanism, independent of glob shape.

Verter's `verter_workspace` ownership must be extended to encode (a) the carrier extension as a member-extension, and (b) reference-graph awareness; until it does, the §1 "no owner ⇒ no TS" rule would mis-fire on legitimate `.vue` files. This is Block 1/3 work, gated by the §3 ownership fixtures (including the `.vue`-specific-`include` and `files`-list discovery cases).

### 2.7 Performance: do less *non-checker* work — and never less *checker* work than correctness requires
Both engines are checker-bound on the same tsgo, so equal work is at parity. The durable win is doing less work **around** the checker, with a hard line: Verter may skip **re-codegen** and redundant **transport**, but it must **never** suppress an engine re-check that a type change requires. The two decisions are split:

**(a) Carrier *regeneration* skip (Verter's decision — safe).** Keyed by the orthogonal env dimensions the carrier text depends on: `source_content_hash + parse_env_hash + (codegen) compile profile + file_language_row + helper/runtime_version` (R21 — no single bundled hash). If this key is unchanged, **do not regenerate the carrier and do not re-send its text to the engine.** This is strictly less work than vize, which regenerates virtual TS per request. "Carrier-hash-stable" means *reuse the cached carrier text* — it does **not** mean "the type result is still valid."

**(b) Engine re-check / notification (the engine's decision — Verter must not suppress it).** Once a carrier is a real configured-project member, the engine's Program owns cross-file type invalidation. A carrier can be byte-identical while its *types* changed — a dependency `.d.ts` edit, a sibling `.vue`'s public-API surface change, an `@types`/`lib`/tsconfig change, a `paths` remap, a package upgrade. Therefore Verter **forwards every dependency-closure VFS-generation bump** to the engine and **never** gates a re-check on "carrier text unchanged." Concretely, the carrier-regeneration cache (a) carries a separate `import_signature_hash` (the hash of the carrier's resolved import surface); when a dependency's generation advances, the dependent's `import_signature` is re-evaluated and the engine is notified even though the carrier *text* is cache-stable. This mirrors the project's fact-based-cache discipline (`ReadSetSignature` — cache correctness is read-side authoritative, revalidated against the live view on every warm hit; never cache on path alone).

**Source-map changes (C3 — corrected).** A `CodeTransform`-built source-map is derived from the same chunk list that produces the text, so a map change with byte-identical carrier text is essentially impossible *except* a helper-preamble metadata-boundary move. The previous "map changed ⇒ don't re-check" claim is **withdrawn**: `map_hash` is part of the version-gate identity (below), so a `map_hash` change **invalidates every cached mapped result** keyed by the old map. Raw engine results are cached by `(carrier content_hash, project epoch)`; mapped results are cached by `map_hash`. On a map-only change, mapped outputs are dropped and raw spans are re-mapped through the new map **only if** the carrier content and project epoch still match — never remap a stale diagnostic through a new map.

**Other systems-layer wins:**
- **No post-hoc import rewriting** — the static resolution overlay (§2.3) keeps carrier text stable across graph changes.
- Per-project **batched** `publish_snapshot` (not per-file IPC).
- **Singleflight** carrier generation per canonical file and `ensure_surface(project, source, role, generation)`; concurrent hover/diagnostic/completion join one build.
- **Async wherever the engine permits; the tsserver/JS-LS exception is explicit (A4 + user-review P1) — not a "switch to async if bottlenecked" deferral.** The interactive lane is **async, cancellable, priority-laned from the start** wherever the engine supports it: **tsgo** via the **`unstable/async` (JSON-RPC) client** with an **active-file priority lane** that preempts background work, so a keystroke's hover/completion/diagnostic is never head-of-line-blocked behind a project-wide pull. **Explicit exception (A4, D8-tightened): the tsserver / in-context JS language service drives SYNCHRONOUS language-SERVICE APIs** — the `ts.LanguageService` host-callback methods and tsserver's `LanguageServiceHost` are synchronous *by API contract* (a host read returns a value, not a promise), so the *checker invocation* for one query executes synchronously and cannot be "made async." (Precise, not sloppy: the tsserver *protocol* is request/response and cancellable, and Verter still runs the tsserver subprocess transport I/O asynchronously; it is the language-SERVICE API — not "the protocol" — that is synchronous.) Preemption uses the **TS cancellation token** (`--cancellationPipeName` for the tsserver process; the host's `getCancellationToken`/`ThrottledCancellationToken` for the in-context LS) instead of async cancellation. (The tsgo `unstable/sync` client is likewise acceptable only for the Block-0 gate and the batch/TSC baseline, where one-request-at-a-time is fine.) Query **de-dupe + cancellation** keyed by `(project, provider_uri, carrier_offset, feature, content_hash, map_hash, required_version, feature_param)` — the FULL fail-closed query identity: `required_version` keeps a query against a newer snapshot from joining a stale in-flight slot (a byte-identical re-sync advances the generation while preserving `content_hash`/`map_hash`), and `feature_param` (e.g. a rename's new-name hash) keeps two parameterized queries at the same offset distinct. Cancellation is wired to the engine, not just response-dropping: tsgo via the async client's cancellation mechanism (verified against the installed client at the §2.8 handshake — JSON-RPC cancel if the transport is JSON-RPC, LSP `$/cancelRequest` only if genuinely LSP), tsserver/JS-LS via the TS cancellation token; a superseded generation cancels in-flight engine work. The de-dupe registry ELIDES the duplicate engine query for an identical in-flight key and shares one cancellation token across the join group; broadcasting the leader's RESULT to joiners is the engine backend's concern (the registry is de-dupe + shared cancellation, not itself a result rendezvous).
- **Version-gated, multi-file-aware responses (C12).** A stale result is dropped, never mapped through a non-matching source-map. Provider epoch is encoded into script versions; the engine/project epoch is captured **before and after** each request and the result is dropped if **any touched provider file or map** changed mid-flight (not just the queried file — rename/find-references/definition return multi-file spans that can span epochs). **Every returned span/edit is classified** as `source-mappable` (mapped back to the `.vue`), `generated-only` (a synthetic helper region with no `.vue` correlate — **suppressed**), or `external` (a real on-disk `.ts` — returned as-is). Generated-only spans never escape.

**Surface generation — lazy IDE, EAGER public-API index, on-demand materialization under redirect (C8 + round-2 D4).** Full IDE TSX (`CarrierIde`) is generated lazily for open/diagnosed/queried carriers. The macro-derived **public-API surfaces (`CarrierApi`) for ALL project-owned `.vue` files are generated and import-indexed up front** — TypeScript auto-imports and find-all-references draw from the project's indexed export surfaces (the auto-import provider project), so a closed `.vue` whose API surface is missing would vanish from completions and references. **But "lazy `CarrierIde`" must not starve the default project-reference redirect:** under the DEFAULT redirect (ON, §2.6), a cross-package import of `B/Foo.vue` from `A` resolves to `B`'s **source** carrier (`CarrierIde`), so resolution of such an import is itself a **materialization trigger** — the referenced `CarrierIde` is generated on demand when `A`'s Program resolves the import (it is "lazy" only in the sense of *not generated before any consumer needs it*, never "absent when a member Program references it"). Equivalently: the eager index covers `CarrierApi` for every owned `.vue` AND the `CarrierIde` head of every `.vue` that is *imported across a redirect-ON reference*. Only the IDE TSX of a carrier that no member Program references stays lazy; the lightweight API surface is always eager. **Materialization-ordering rule (round-3 codex caveat):** the on-demand `CarrierIde` must be generated and published to the overlay **before** the engine first observes `file_exists`/`read_file` for it — otherwise a cold read returns "absent" and the engine can pin a sticky negative resolution (the same poisoning class as §2.2 on tsserver). The redirection/sync path therefore materializes the referenced carrier as part of resolving the import (synchronously w.r.t. the engine's FS probe), never lazily-after-the-probe.

- Background population is **priority prewarm, not correctness** (active file + dependency closure first); `Arc`-cached source-map tables.

**Cold-path strategy — minimize the per-carrier non-checker overhead so cold can BEAT, not just match, the reference (user-review P1).** On a cold full-project run the carrier-regeneration cache is empty, so every carrier is codegen'd once; the only way to beat a reference that regenerates virtual TS per request is to make Verter's *one* cold codegen + sync cheaper than the reference's per-request work AND amortize it across the batch. The cold pipeline therefore:
- **Parallel carrier codegen through `verter_scheduler` (A9).** Do not hand-roll thread pools — route through **`verter_scheduler`**, which already owns BOTH the **rayon CPU pool** and the **tokio I/O runtime** and provides the CPU-vs-I/O routing (`/scheduler`: `submit_request`/`submit_batch`, the scheduler-owned `cpu_pool` + the host-owned `HostCpuPool` coordinator shared by every host batch API). The shared parse + carrier codegen (CPU-bound, embarrassingly parallel) is submitted as CPU-lane work and fans out to a single batched engine snapshot rather than per-file opens; engine transport I/O (the `--api`/stdio channel) and the §2.7 async interactive lane are I/O-lane work. Stated once for the whole doc: **all carrier codegen / source-map build / hashing → `verter_scheduler`'s CPU (rayon) lane; all engine/transport I/O + the interactive lane → its I/O (tokio) lane** — never block a tokio worker on CPU codegen or a rayon worker on engine I/O. This reuses the existing scheduler (the shared owner layer per CLAUDE.md), not a second pool.
- **A minimal-diagnostic carrier for the TSC surface — the PROVISIONAL `CarrierBatch` role (§2.1).** *(Provisional: kept only if Block 2 measures a material cold-perf gain over sharing the single `CarrierIde` surface for the batch; otherwise this minimal carrier is merged into `CarrierIde` and the cold path reads `CarrierIde` directly. The cold-path win below holds either way — a merged single surface simply forgoes this one lever.)* If kept, the batch carrier carries everything type-checking observes — **the full script surface AND every type-observable template-expression construct** (interpolations, directive/attribute expression values, `v-for`/`v-if`/`v-bind`/`v-on`/`v-model`/`v-slot` expressions) plus the import/redirection shape — but OMITS the interactive-only IDE embellishments (inlay/semantic-token scaffolding, hover-only helper regions) that have no type-checking role, so it codegens strictly less per file than the full `CarrierIde` while **preserving identical type-checking semantics**. The strong equivalence guard (`carrier_batch_typechecks_same_as_ide`) asserts the batch and `CarrierIde` surfaces produce the **same diagnostic set** on a corpus (a distinct-role equivalence if `CarrierBatch` survives, trivially true if merged), so the minimal carrier can never silently drop a template-expression check (honoring the Carrier IDE TS Surface Principle: every supported template expression is still type-checked).
- **Lazy source-map construction on the cold path** — the source-map table for a carrier is built only if/when a diagnostic actually lands in it (most cold-clean files never need their map materialized), versus eagerly building every map.
- **A precomputed overlay/resolution manifest** handed to the engine once, so the FS-overlay + `.vue`→companion redirection set is not recomputed per file during the cold sweep.
These let the cold TSC run do measurably less non-checker work than a per-request regenerator while running the identical tsgo checker.

**Carrier pre-compile-to-warm — a FLAGGED, measured experiment, NOT a baseline (A7).** An opt-in experiment (behind a flag, default off): proactively compile/warm carriers into the VFS-resident carrier store **ahead of the engine's demand** (e.g. on workspace open, warm the active file's import closure, or the whole project for a batch run), so the engine's first touch hits a ready carrier instead of triggering codegen. The eager-`CarrierApi` index (below) already does a bounded version of this for auto-import; the experiment generalizes it to the full carrier set (`CarrierIde`, plus the provisional `CarrierBatch` if Block 2 keeps it — §2.1). It is **implementation-validated, not assumed**: shipped only if measured to improve cold/warm latency without a memory regression on the perf corpus; the baseline design never *depends* on pre-warming for correctness (carriers are still generated lazily-on-demand if the flag is off). Pre-warm runs on the rayon pool (A9) and respects the §2.7 cache invalidation (a pre-warmed carrier is just a cache fill, subject to the same content-hash/dependency invalidation).

**Perf gates (C16 + user-review P1 — four measurable gates, "best of the best" precision: pinned corpus, protocol, percentiles, memory ceilings, and overhead attribution).** **The BATCH performance REFERENCE is `vize` — the fastest TS7 reference — measured on the SAME corpus + thread count; `vue-tsc`/`svelte-check` are the CORRECTNESS/COMPATIBILITY baselines ONLY, never the performance bar.** The historical headline "20–84×" was multi-threaded-`vize` vs single-threaded-`vue-tsc` on a partial checker (≈13× at equal threads on a 15k-SFC corpus) — that figure is `vize`-over-`vue-tsc` *context*, not Verter's target. Verter's batch gates measure **Verter vs `vize`**; `vue-tsc`/`svelte-check` remain the correctness oracle the diagnostic SET is checked against. It is not a single number to "beat" blindly. The gates are measurable, not hand-wavy — every "beat/parity" below is a numeric threshold against the stated reference on the **same machine, same run**.

**Corpus (fixed, pinned, hermetic).** A committed, version-pinned synthetic corpus (no third-party checkout — Testing-Hermeticity): **15,000 SFCs** across **~600 modules**, with a realistic shape — a mean of **~8 imports/SFC**, cross-file type edges (props/emits types imported across files), at least one **`composite` project-reference closure** (so the redirect-ON `CarrierIde` and `disable…:true` `CarrierApi` boundaries are both exercised), a `paths`/`baseUrl` alias layer, and a `lib`/`@types` floor. The corpus is split into the four gate workloads below; its content hash is recorded with each result so a corpus change is never silently a "perf improvement".

**Tooling protocol (identical for Verter and the reference).** Same machine, same tsgo binary, **fixed identical thread count** (record the exact N; default = physical core count), warm OS file cache, **≥ 5 runs per gate** reporting **median + p95** (interactive gates additionally p50/p99). **The PERFORMANCE reference is `vize` for the batch gates (1–2)** — measured on the same corpus + thread count + tsgo binary — **and the editor's own TS LSP for the interactive gates (3–4)**. **`vue-tsc`/`svelte-check` are the CORRECTNESS oracle, not a performance bar**: the batch diagnostic SET is asserted equal to `vue-tsc`/`svelte-check` for compatibility, while wall-time is compared against `vize`. (Hermeticity: `vize` is an external reference invoked for benchmarking only — never embedded, named, or depended on in Verter source/tests/fixtures, per §6.) **Cold vs warm is explicit:** *cold* = a fresh process with an empty carrier-regeneration cache and no Program (every carrier codegen'd once); *warm* = the persistent session with the carrier cache populated and the Program retained, then the measured edit/query applied.

**Memory ceilings (a regression here fails the gate).** Record **peak RSS** and **steady-state RSS after the cold full-project run** for Verter and the batch reference `vize`; Verter's peak RSS must be **≤ 1.15×** `vize`'s on the same corpus/threads (the carrier store + source-map tables are the only structural additions and must stay bounded), and the pre-warm experiment (A7) must show **no RSS regression** vs the flag-off baseline or it does not ship.

**Overhead attribution (every gate breaks its wall-time into checker vs non-checker).** Each result attributes time to **(i) Verter codegen** (carrier generation), **(ii) source-map build**, **(iii) hashing / cache lookup / sync transport**, and **(iv) checker time** (the tsgo/tsserver invocation). Since both tools run the identical checker, (iv) is ~constant across tools at equal threads; the gate's lever and its pass/fail margin live entirely in (i)+(ii)+(iii) (the non-checker budget). The cold gate's "beat" is achieved iff Verter's (i)+(ii)+(iii) per-file is **measurably below** `vize`'s per-request regeneration cost (the batch reference).

The four gates (each on its corpus slice, at the fixed thread count). **Batch gates (1–2) compare against `vize`; interactive gates (3–4) against the editor's own TS LSP. `vue-tsc`/`svelte-check` are the diagnostic-set correctness oracle for the batch gates, not the timing bar:**
1. **Cold full-project typecheck** (TSC surface) — **floor = parity with `vize`** (Verter's median wall-time **≤ 1.00× ± 3%** of `vize`'s on the same corpus/threads; (iv) is shared so this bounds the non-checker budget) — **shipping below parity is a fail**; **stretch = BEAT `vize` cold** (median **≤ 0.85×** `vize`) via the cold-path strategy above (the leaf diagnostic carrier — the provisional `CarrierBatch` if Block 2 keeps it, else `CarrierIde` — plus parallel codegen + lazy maps + precomputed manifest, i.e. (i)+(ii)+(iii) below `vize`'s per-request work). The gate records the floor (must hold) and the stretch (tracked, drives the cold-path work). The emitted diagnostic SET is separately asserted equal to `vue-tsc`/`svelte-check` (correctness oracle).
2. **Warm batch re-typecheck** (≥ 95% of carriers unchanged; a small dependency edit) — target: **beat `vize`** (median **≤ 0.5×** `vize`, which re-regenerates virtual TS per request) via the carrier-regeneration skip (a) — while still forwarding every dependency-closure re-check (b), so correctness is unchanged. Diagnostic set still equals `vue-tsc`/`svelte-check`.
3. **Single-file-edit latency** (interactive; edit the active SFC, measure to updated diagnostics) — target: **beat** the editor's own TS LSP at **p50 and p95** (skip re-codegen of untouched carriers + reused Program where Block 0 proves reuse), with **p99 ≤ that reference's p99** (no tail regression from cache revalidation).
4. **IDE query latency** (hover/completion/diagnostics on the active file) — target: **beat** the editor's own TS LSP at **p50/p95/p99** via de-dupe + cached `Arc` source-map tables + reused Program; the active-file priority lane (§2.7 async) keeps a keystroke query off the head-of-line behind a project pull.

A **diagnostics scheduler** backs these: active-file diagnostics first, affected-set next, full-project only for the TSC/CLI surface; cancellation tokens wired to the engine; never a synchronous whole-project semantic pull on every keystroke.

Invalidation safety: VFS generation + content hash + source-map hash + import-signature + project generation + provider synced-version are authoritative. **Never cache on path alone.**

### 2.8 No-fork, capability-gated, fail-closed
**Installed workspace TypeScript ALWAYS wins.** Resolve the user's TypeScript first by reusing the existing discovery path (`verter_type_runtime/src/discovery.rs`: walk up to `<workspace>/node_modules/typescript`, then tsdk, then global — one discovery path, not a second): if the workspace has a `typescript` install, Verter uses **that exact install**, regardless of version/dist-tag, and never overrides it. The **`typescript` package major version** decides the backend — **major `< 7`** ⇒ the user's `tsserver`; **major `≥ 7`** ⇒ tsgo, where a `typescript` package at major ≥ 7 (e.g. `typescript@7.0.1-rc`) declares the per-platform native binary as the optional dependency **`@typescript/typescript-{platform}-{arch}`** (the `--api` client is at `typescript/unstable/sync`). Binary selection is documented exactly: the platform/arch triple (incl. musl vs gnu on Linux), the optional-dependency binary package above, version/integrity match against the `typescript` package, PnP/workspace-hoisting layouts, and **no-network** behaviour. *(The `@typescript/native-preview` dev-preview channel — binary `@typescript/native-preview-{platform}-{arch}` — is the same engine/API and is accepted as a fallback **source** by the same discovery when no installed `typescript` with major ≥ 7 is present, but the canonical user-install target is an installed `typescript` whose major is ≥ 7.)*

**No bundling — the no-TypeScript fallback is package-DOWNLOAD only (A3).** Verter **never bundles, ships, forks, or patches a TypeScript engine binary.** Two cases: (i) **the workspace HAS TypeScript** — use the user's install, **regardless** of version (their `tsserver`/JS-LS for `<7`, their `typescript@7.x`'s tsgo for `≥7`); never override it (the always-wins rule above). (ii) **the workspace has NO TypeScript** — Verter **downloads the npm `typescript` package** into a Verter-managed location and uses that — **download only, never a bundled/shipped/forked binary.** **Version policy for the no-TS case (D10): download `typescript` at the `rc` dist-tag** — the current TS7 channel (e.g. `typescript@rc` resolving to `typescript@7.0.1-rc`), consistent with the project's tsgo-preferred posture, so a TS-less workspace gets the fast native tsgo path rather than an arbitrary `<7` JS lib. **When TS7 becomes the `latest` dist-tag, the no-TS download retargets `latest`** (the policy is "the current TS7 channel," which is `rc` today and `latest` once TS7 ships stable). The fallback unit is **download-the-package** (which for v7 carries the platform tsgo binary as its optional dependency), so the engine is always a real npm `typescript` release, version-resolvable and integrity-checked, never a Verter fork/bundle. (On a WASM host, where tsgo can't run — §2.11 — the no-TS fallback is instead the in-context JS language service over a **fetched/downloaded** JS `typescript`, the same download-not-bundle discipline.) The download respects offline/no-network — **fail closed when offline** (no external-TS until the package is available) — and is version-matched by construction (the downloaded package's tsgo binary matches that package version). This satisfies the no-fork/no-bundle constraint while still giving a TS-less workspace a working engine.

The native `--api` is shipped but marked `unstable`/generated, so the tsgo backend stays **capability-gated by a handshake** (`capabilities()`, §2.1/§2.3) that re-runs the Block-0 spike assertions against the installed binary. The required-capability set splits into **what the recorded Block-0 gate already proved** and **what the Block-5 handshake additionally proves** (the gate used the SYNC client, so async/cancellation is NOT among the already-proven set):
- **Proven by the recorded Block-0 gate** (sync client, `@typescript/native-preview 7.0.0-dev.20260526.1`; the SAME surface is npm-confirmed on `typescript@7.0.1-rc`, whose behavior Block 5 re-proves by running the gate against it — B4): `{ off-disk virtual files visible to a configured project via FS-overlay callbacks (read_file/file_exists/get_accessible_entries), explicit open_project configured-project ownership, file→configured-project association queryable (get_default_project_for_file), sparse read-through overlay, bare-.x→component-carrier extension redirection through the overlay (GATE 4), the production component-carrier identity .vue.tsx/.svelte.tsx is bare-import-probe-reachable while a .verter.-infixed *component* identity is NOT (GATE 5), a .svelte.tsx component carrier coexists with a real .svelte.ts rune module (GATE 5), content-addressed incremental snapshot updates (update_snapshot file_changes) with Program reuse, project-scoped diagnostics/hover/definition from the configured Program }`.
- **Additionally REQUIRED, proven at the Block-5 handshake (not yet exercised by the committed gate):** `{ a virtualizable tsconfig (the overlay can serve tsconfig.json content — for §2.3 companion root-injection under .vue-specific include/files), the typescript/unstable/async client with working request cancellation (for the §2.7 interactive lane), root-set injection under a .vue-specific include / files-list }`. The async/cancellation capability is **part of the gate** (a sync-only API must NOT pass the Block-5 handshake): it verifies `unstable/async` is importable and its cancellation mechanism works — the exact cancellation method is verified against the installed client (the async client's documented cancel, not assumed to be LSP `$/cancelRequest`; §2.7's `$/cancelRequest` mention applies only where the transport is genuinely LSP). Block 5 extends `tools/tsgo-api-gate/` to cover these (an async-client probe + a tsconfig-virtualization probe), so they graduate from "required" to "gate-proven". The gate runs on **every tsgo version bump**: if a future build regresses a required capability, **do not ship a "nearly correct" tsgo LSP path on that build** — fail closed (no external TS results for TS≥7 on that surface), keep non-TS features alive, and **upstream the minimal missing *generic* API shape** (never Vue-specific concepts). For the **TSC surface only**, the mirror-host + generated-`extends`-tsconfig path (§2.4) is a no-fork fallback independent of `--api` batch diagnostics, so a `--api` regression need not strand TS≥7 *batch typecheck*. **Never fork, patch, or bundle a TS engine** — the no-TS fallback is package-DOWNLOAD only (above), not an exception to no-bundle.

### 2.9 `.vue`-import from plain `.ts`/`.js` — first-class enhanced DX on BOTH engines
A plain `.ts`/`.js` file that does `import Comp from "./Comp.vue"` (or `import { x } from "./Comp.vue"`) must get the **full enhanced type experience** at that import and at every use of the imported symbol — correct `$props`/emits/slots/expose typing, hover, go-to-definition into the SFC, find-all-references back into and out of it, completion, rename — exactly as if `Comp.vue` were a `.ts` module. This is a **primary IDE-DX bar** (it is the entire point of the existing `@verter/typescript-plugin`), and it must be first-class on **both** TS<7 and TS≥7. The two engines reach it by **different mechanisms** (the same split as the rest of this doc), unified behind the §2.1 contract:

- **TS<7 (tsserver) — the in-process plugin (existing model, kept first-class).** The `@verter/typescript-plugin` (§2.2) makes `import "./Comp.vue"` resolve (via `resolveModuleNameLiterals`) to the descriptor-owned component IDE carrier (`Comp.vue.tsx`/`.svelte.tsx`) served as the `.vue`'s script snapshot, so the user's `.ts` Program sees a real typed module. (tsserver's `resolveModuleNameLiterals` returns a `resolvedFileName` directly — a true resolution map — so it is not basename-probe-bound like tsgo; Verter still targets the same `.vue.tsx`/`.svelte.tsx` component identity on both engines for parity, and the separate `.verter.ts` API carrier is the cross-package redirect target of §2.6.) This is the load-bearing path for plain-script `.vue` imports on tsserver and stays first-class (the full host-proxy matrix + readiness protocol of §2.2 apply).

- **TS≥7 (tsgo) — NOT a plugin; the `--api` FS-overlay + carrier-extension redirection (PROVEN).** tsgo **cannot load in-process LS plugins** (typescript-go #2824: "we can't dynamically link third-party code into the server process"), so the plugin model is unavailable. The capability is served by the FS overlay, and the **exact resolution mechanism is verified** (gate probe + GATE 4 + GATE 5): when the user's `A.ts` (a member of the `open_project` Program) resolves a bare `import "./Comp.vue"`, **tsgo's own resolver appends `.tsx`/`.ts` to the `Comp.vue` basename and probes `Comp.vue.tsx`/`Comp.vue.ts`** — Verter's overlay answers `read_file`/`file_exists` for that probe with the carrier content. **The carrier's canonical engine/store identity IS `Comp.vue.tsx` (the component IDE carrier of §2.2)** — exactly the path the probe reaches; `path_is_carrier` classifies it. (GATE 5 falsified the alternative: a `.verter.`-infixed *component* path — `Comp.vue.verter.tsx` — is NOT reached by tsgo's basename probe → TS2307. The reserved `.verter.ts` infix belongs only to the redirect-reached API carrier, never this bare-import target.) Critically (probe Strategy A): serving TSX content at the **bare** `Comp.vue` path does **NOT** work (tsgo emits `TS2307`); the redirection MUST be served at the carrier-extension probe `<specifier>.tsx`/`.ts`, resolving to the `.vue.tsx`/`.svelte.tsx` component carrier (§2.2/§2.3). No module-resolution-map endpoint is needed. Hover/definition/references/rename/completion then ride the §2.7 query path with `ProviderPositionMapper` mapping any span inside the carrier back to the `.vue`. **No plugin, no fork** — the plain-`.ts` importer is just another configured-project member whose bare `.vue` import tsgo resolves to the overlay-served component carrier.

**Cross-engine, cross-framework DX contract (the bar, mapped through the carrier source-map):** for a carrier-source file (`.vue`/`.svelte`/…) imported from a plain script, on **either** engine — (1) the imported default/named symbols carry the component's **real public surface as the framework adapter defines it** (Vue: `$props`/emits/slots/expose; Svelte: the component's props/events/slots/snippets per the Svelte adapter; future adapters per their own surface) — the *contract* is "the adapter's real public types flow," not any Vue-specific shape; (2) **go-to-definition** from the `.ts` use lands in the source file (mapped from the carrier); (3) **find-all-references** spans both the `.ts` importer(s) and the source (script + template/markup); (4) **rename** of an exported member updates the source and every `.ts` importer; (5) **completion** of `Comp.`'s members and **auto-import** of the component into a `.ts` file work (auto-import relies on the eager `CarrierApi` index, §2.7); (6) unmapped synthetic/carrier-only regions **fail closed** (never a mis-mapped result). Items (4)/(6) reuse the cross-file rename fail-closed machinery already in `ProviderSurfaceStore`. The per-adapter public-surface *semantics* are framework-specific (the adapter computes them); the DX *contract* above is framework-neutral.

**Module-specifier mapping for inserted imports (completion/auto-import/code-action edits).** Because the engine's file identity for the SFC is the **carrier companion** (the `Comp.vue.tsx` component carrier, or the `Comp.vue.verter.ts` API carrier under a cross-package redirect), an auto-import or quick-fix that the engine generates will, left raw, insert the **companion** specifier (`./Comp.vue.tsx` / `./Comp.vue` -as-`.tsx`, `./Comp.vue.verter` for the API carrier, or even a bare `./Comp`), not the user-facing `./Comp.vue`. A **module-specifier rewrite layer** is therefore required on inserted-import text edits (the same direction as position mapping, applied to the specifier string): any engine-produced import whose target is a `.vue`/`.svelte` companion is rewritten to the bare `.vue`/`.svelte` specifier before the edit reaches the user; an edit whose specifier cannot be mapped to a user-facing form **fails closed** (the edit is suppressed, not mis-inserted). This is the one place §2.9 needs logic beyond raw redirection, and it is part of the contract (item 5). **CodeTransform note (D9):** this rewrites the **engine's output** — a tsserver/tsgo code-action `TextEdit`/`WorkspaceEdit` specifier string destined for the **user's source file** — NOT any `CodeTransform`-built carrier text; so it does not violate "CodeTransform is the single source of truth" (which governs modifications to *Verter's generated carrier*, never an LSP edit the engine emits for user source). The carrier text itself stays CodeTransform-authored and stable.

**`tsconfig` association for the plain script.** The plain `.ts`/`.js` importer must itself be a member of a configured project (§2.6) for the redirect to apply — a `.ts` outside every tsconfig is `NoProject` and gets no enhanced `.vue` types (fail closed), same as any other file. The `.vue` it imports is discovered/owned per §2.6 + §2.3 root-set injection.

**Verification — the tsgo half is ALREADY PROVEN; tsserver + the full contract land in the blocks.** The committed gate's **GATE 4** (`tools/tsgo-api-gate/`) shows a plain `Consumer.ts` importing a **bare `"./Exported.vue"`** (overlay serving only the `Exported.vue.tsx` companion, the bare path asserted absent on disk) resolve with zero diagnostics and the companion's exported type flow into the `.ts` — i.e. the tsgo redirection + types-flow of contract item (1) is green NOW. The remaining contract items (2)–(6) on both engines, and the module-specifier rewrite layer, land with Block 4 (tsserver, via the plugin) and Block 5 (tsgo, via the gate-proven overlay). *Guard:* `carrier_dx_enhanced_both_engines_both_frameworks` (the contract (1)–(6) holds on tsserver AND tsgo, with inserted-import specifiers rewritten to `.vue` or the edit failing closed).

### 2.10 Shared-engine mode — an OPTIONAL optimization layered on the OWNED `--api` backend
The tsgo backend (§2.3) is the **OWNED `--api` path** — that is the correctness baseline and architectural center. **SHARED mode is an OPTIONAL layered optimization built on top of it**, selected per session by capability negotiation and enabled **only where a full editor TS-LSP proxy exists** (VS Code today). It is never on the core path; OWNED always works without it.
- **OWNED mode (the backend itself)** — Verter spawns its own `--api` instance (`new API({ tsserver_path, cwd, fs })`); the FS-overlay backs carriers. The **portable baseline** (works in every editor, version-controlled by Verter, stable lifecycle) and the actual tsgo backend. This is what Block 0/GATE 1–4 proved.
- **SHARED mode (optional optimization)** — Verter attaches to the editor's already-running tsgo and drives carriers against the **editor's own Program** (one engine, no double-checking, instant consistency with what the editor sees). It is an **opt-in enhancement layered on OWNED**, enabled only under the gating preconditions below (editor attach bridge + full TS-LSP proxy); it is **secondary to OWNED**, not co-equal, and OWNED is always the fall-back. **Full carrier parity is a property of the combined tsgo adapter (OWNED + the SHARED optimization with OWNED fall-over), and OWNED alone already provides it**: SHARED cannot serve configs that need tsconfig-virtualization (Verter doesn't own the editor's FS), so for those it routes to OWNED. The OWNED baseline, not the optional optimization, guarantees parity.

Both modes serve the **identical** §2.3/§2.7/§2.9 carrier/redirection/query contract; they differ only in **how carrier content reaches the Program** and **which Program it is**. The adapter abstracts the difference behind a `CarrierInjectionChannel` so every higher layer (§2.5 sync, §2.7 queries, §2.9 DX) is mode-agnostic.

**Attach handshake (verified from the shipped Go binary + typescript-go source + the VS Code extension):** the tsgo LSP server exposes `custom/initializeAPISession` (handler `(*lsp.Server).handleInitializeAPISession` → `api.NewSession(s.session, …)`, minting a second API session that **holds the LSP server's existing `project.Session`** — the same projects/Programs the editor checks) returning a pipe; Verter connects with **`API.fromLSPConnection({ pipe })`** (`typescript/unstable/async`; attach is async-only — matches the §2.7 async interactive lane). The VS Code "TypeScript (Native Preview)" extension bridges the pipe to third parties via `ExtensionAPI.initializeAPIConnection(pipe?)` (command `typescript.native-preview.initializeAPIConnection`). This is typescript-go #2824's auxiliary-server model.

**The carrier-injection mechanism in SHARED mode (verified — and DIFFERENT from OWNED mode; the §2.10 design hinge).** An attached `API<true>` has **no content channel of its own**: `fs` callbacks are a SPAWN-only option (`ClientSpawnOptions.fs`); `LSPUpdateSnapshotParams` is `{ openProject? }` only; there is no `openFile`/`overlay`/`Document` API method. So Verter **cannot** push carriers through the attached API client. The verified path is the **LSP text-document overlay on the shared `project.Session`**: because `api.NewSession(s.session)` shares the LSP server's session, and that session's overlay store is what the checker reads through (`overlayFS.getFile` → overlay-map-then-disk; `SnapshotFS.GetFileByPath` → overlays→disk; the frozen compiler host's `GetSourceFile`/`ReadFile` consult the snapshot FS), a carrier opened as an **LSP `textDocument/didOpen` (then `didChange`) document on that same connection becomes a member of the shared Program** and is then visible through the attached `API<true>` `Program`/`Checker`. Therefore SHARED mode's `CarrierInjectionChannel` is:
1. **Open each generated carrier companion** (the component IDE carriers `.vue.tsx`/`.svelte.tsx` and the API carriers `.vue.verter.ts`/`.svelte.verter.ts` — §2.2 GATE-5 identities) **as an LSP overlay document** (`didOpen` with the carrier text, `didChange` on edits, `didClose` on retract) on the editor's tsgo LSP connection — driven by Verter's editor-side LSP client / the editor extension that already owns that connection. Content is the same `CodeTransform`-built carrier (CodeTransform-source-of-truth); never a text-splice.
2. **Program-eligibility** is established the same way as §2.3/§2.6: the companion must be a member of the relevant configured project. Where the user's config does not enumerate the companion, the §2.3 **tsconfig-virtualization** is unavailable in SHARED mode (Verter does not own the editor's FS), so SHARED mode requires the companion to be reachable as a root/included file or via an import edge.
   - **Fail-over granularity is the REDIRECT-ON PROJECT-REFERENCE CLOSURE, never per-file and not even per-single-tsconfig (B1, round-6 hardened).** A shared Program that imports a carrier only available in OWNED mode would have a broken/missing edge (and cross-file queries — references/rename/auto-import — would be silently incomplete). Per-file fail-over is therefore forbidden; but **per-single-configured-project is also insufficient** because project references create **cross-project** carrier edges:
     - **Intra-project:** if **any** carrier owned by a configured project cannot be made program-eligible in the shared session, **that entire project's carriers route to OWNED** (one `--api` Program for that tsconfig); no intra-project edge is split.
     - **Cross-project under DEFAULT redirect (the hard case):** §2.6 says with source-of-project-reference redirect ON, project A importing project B's `.x` resolves to **B's source carrier (`CarrierIde`) inside A's Program** — so A and B's carriers are entangled in one Program slice. If B must use OWNED (e.g. B needs tsconfig-virtualization, unavailable in SHARED) while A is SHARED-eligible, the A→B carrier edge would split across two engines. **The mode-selection unit is therefore the redirect-ON reference CLOSURE: A plus every project reachable through its `references[]` whose `.x` it imports under redirect-ON share ONE mode** — if any member of the closure must use OWNED, the whole closure uses OWNED.
     - **Cross-project under `disableSourceOfProjectReferenceRedirect: true`:** the edge is the emitted-`.d.ts`-equivalent `CarrierApi` boundary (§2.6), which **decouples** the Programs — A only needs B's API surface, not B's live carrier in A's Program — so the closure rule applies **only to redirect-ON references**; a `disable…:true` reference does not force A and B into one mode.
     - A closure served in OWNED still benefits from SHARED reads for *unrelated* projects/closures in the workspace. Fail-closed: a closure whose carriers can't be made eligible in either mode yields no external-TS result for those carriers. *(This is the §2.10 ↔ §2.6 cross-wiring: SHARED/OWNED selection is redirect-aware.)*
3. **Module redirection** (`import "./Comp.vue"` → component carrier) works identically: tsgo appends `.tsx`/`.ts` and the overlay-opened `Comp.vue.tsx` component-carrier document answers the probe (the §2.9/GATE-4/GATE-5 mechanism — the carrier is present, now as an LSP overlay instead of an FS-overlay answer).
4. **Queries** (`Program`/`Checker`, diagnostics, hover, definition, references, rename, completion) run through the attached `API<true>`; results map back through `ProviderPositionMapper` exactly as in OWNED mode.

**Read side.** Beyond carriers, the attached session gives Verter the editor's **already-loaded** on-disk `.ts`/`.d.ts` + the editor's open overlays for free — cross-file context that OWNED mode would re-read/re-check. SHARED mode thus does strictly less duplicate work.

**Overlay precedence / conflict model (verified) — gated by the §2.2 same-path conflict check.** Both read paths check the overlay map before disk, so an overlay-opened companion *would* win over any on-disk file of the same path; there is no second content source on the API protocol to conflict with. **But because the engine overlay would shadow a real same-path file, SHARED mode runs the SAME §2.2 Carrier-path conflict gate as OWNED mode BEFORE `didOpen`-ing a companion:** if a real user file already occupies the exact carrier path (`Foo.vue.tsx`), Verter **does NOT open the overlay** for it — the source is `Ambiguous`/conflicted and yields no external-TS result (Verter never shadows the user's real file; §2.2/§2.6 step 4). In the **normal case** (the common one), Verter owns the carrier companion paths (the component IDE carriers `.vue.tsx`/`.svelte.tsx` and the API carriers `.vue.verter.ts`/`.svelte.verter.ts`), the editor never opens them (it opens the bare `.vue`/`.svelte` source and real `.ts`), and **the user has no on-disk file at the companion path** — so there is no contention and the overlay is placed; `path_is_carrier` plus the disk-occupancy check keep a Verter-generated companion (`Foo.vue.tsx`) distinct from a real user `Foo.tsx` AND detect the same-path conflict. If both the editor and Verter would (legitimately) hold the same path, the **single shared overlay store** holds one document. (The same-path conflict is the §2.2 fail-closed case; the same-stem Svelte-rune conflict fails closed identically.)

**Carrier-path leak suppression in SHARED mode requires Verter to PROXY the editor's TS LSP connection — and that requirement gates when SHARED is offered (B2, round-6 hardened).** Injecting companions as `didOpen` documents into the **editor's own** tsgo session means the editor's tsgo now *knows about* `Foo.vue.tsx` (the component carrier) and `Foo.vue.verter.ts` (the API carrier) and can surface results keyed to those virtual paths **on the editor's OWN channels — including UNPROMPTED ones Verter never queried**: `textDocument/publishDiagnostics` (push, per-document, on the engine's own cadence), `workspace/symbol`, `workspace/diagnostic`, call/type hierarchy, document symbols, code actions / quick fixes, completion-resolve `additionalTextEdits`, organize-imports, `workspace/applyEdit`, semantic tokens, inlay hints, implementation/type-definition, and file-rename edits. A `Foo.vue.tsx` (or `Foo.vue.verter.ts`) diagnostic or symbol reaching the user is exactly the leak B2 must prevent. **This is only enforceable if Verter is the LSP MIDDLEWARE/PROXY sitting in front of the editor's TS language server** — i.e. every TS LSP message between the editor UI and the shared tsgo passes through Verter, so Verter can map-back-or-suppress *every* channel above, not just the results it requested through the attached `API<true>`. **Therefore SHARED mode is offered ONLY when this proxy topology holds**; this is a STRONGER requirement than "the editor exposes the attach pipe" and further constrains SHARED availability (see Editor availability). When Verter cannot proxy the full TS LSP connection, **SHARED is not offered for that workspace — it runs OWNED** (where companions live in Verter's own Program and never touch an editor channel). Within the proxy topology, the suppression is concrete and total:
- (a) **diagnostics** (push or pull) for a companion path are intercepted and **mapped back to the source carrier (`.vue`/`.svelte`) via `ProviderPositionMapper` or suppressed** — never shown against a companion path (the `Foo.vue.tsx`/`Foo.svelte.tsx` component carrier or the `Foo.vue.verter.ts` API carrier); (b) **definition/references/rename/highlights/hover/implementation/type-definition/call+type-hierarchy/document+workspace-symbols** results landing in a companion are mapped to the source (script + markup) or dropped (the OWNED §2.7 fail-closed mapping); (c) **inserted-import specifiers** in **code actions, completion-resolve `additionalTextEdits`, organize-imports, `workspace/applyEdit`, and file-rename edits** targeting a companion run through the §2.9 **module-specifier rewrite layer** → bare `.vue`/`.svelte` or the edit fails closed; (d) **semantic tokens / inlay hints / generated-only spans** for companion regions are suppressed.
- **No companion path is ever a user-visible document or an un-mapped result on ANY channel.** The editor opens `.vue`/`.svelte` (the source surface) and real `.ts`; the companions exist only inside the shared Program as Verter-injected overlays, and the proxy guarantees every engine response naming a companion path is Verter's to translate or swallow before the editor UI sees it.
- *Guard:* `shared_mode_requires_full_ts_lsp_proxy` (SHARED is selected only when Verter proxies the editor's entire TS LSP connection; otherwise OWNED) **and** `shared_mode_no_unmapped_carrier_path_leak` (under the proxy, no companion-path span/edit/diagnostic — on any enumerated channel — reaches the editor UI un-mapped). The honest scope: the second guard is checkable precisely *because* the first establishes the proxy; without the proxy, SHARED is not offered, so there is nothing to leak.

**Lifecycle.** The attached session dies when the editor restarts/changes tsgo; Verter detects disconnect, **re-attaches and re-opens its companion overlays**, and meanwhile **fails over to OWNED mode (per redirect-ON reference closure, per B1)** so no query is ever served without a valid Program (fail-closed). Mode is renegotiated on reconnect.

**Editor availability — TWO requirements, both editor-side (this is why SHARED is opt-in, not the universal default).** SHARED needs BOTH: (1) the editor exposes the **attach pipe** (`custom/initializeAPISession`) so Verter can `API.fromLSPConnection` — **VS Code** exposes it via the Native Preview extension's `ExtensionAPI`; and (2) Verter can **proxy the editor's TS LSP connection** for the B2 leak suppression. Where Verter ships the editor's TS integration itself (or sits as LSP middleware), (2) holds; where the editor owns its own TS LSP client and Verter cannot interpose, (2) fails and **SHARED is not offered — OWNED runs instead** (correct, just a second Program). Other matrix editors (Neovim/Helix/Zed/Lapce) today provide neither bridge, so they run OWNED until their tsgo integration exposes the pipe **and** admits a Verter proxy. This dual requirement is the honest reason SHARED is an optional, where-available optimization layered on the universal OWNED baseline, not a default and not co-equal with OWNED.

**Decision & invariants (OPTIONAL optimization, OWNED baseline):** SHARED mode is an **opt-in enhancement** enabled only where the editor exposes the attach bridge AND Verter can proxy the editor's TS LSP connection (B2) AND the §2.8 handshake passes on the attached session; **OWNED mode is the always-available correctness baseline and the architectural center** — the backend works fully without SHARED ever engaging. The selection is per-session and dynamic (re-evaluated on reconnect), and defaults to OWNED. **Correctness is mode-independent**: both produce real configured-Program results mapped fail-closed; the only differences are memory/consistency (SHARED wins where available) and editor availability (OWNED is universal). The tsgo backend is built on the **`--api` `Checker` surface** (not the legacy `tsgo --lsp --stdio` path Verter uses today) — the same client code path serves both spawn and attach, so the optional SHARED optimization layers on cleanly. **Block 5 builds OWNED mode (the backend) first (gate-proven); the SHARED optimization is layered on afterward** behind the same `CarrierInjectionChannel`, with `tools/tsgo-api-gate/` extended by **two attach probes**: (a) `API.fromLSPConnection` against a tsgo LSP server Verter starts in-harness, proving the attached `API<true>` reads a shared on-disk file; (b) a companion opened as an LSP `didOpen` overlay on that connection becoming a member of the shared Program and type-checking through the attached `Checker` (the SHARED-mode carrier-injection proof). *Guards:* `tsgo_shared_mode_carrier_injection` (an LSP-overlay companion is a member of the attached session's Program and types flow, or the closure fails over to OWNED mode); `shared_mode_failover_is_per_reference_closure` (B1 — fail-over selects per redirect-ON reference closure, never per-file or per-single-tsconfig; no cross-project redirect-ON edge is split across two engines); the B2 leak-suppression guards are scoped to what Verter actually controls — see the §2.10 leak-suppression block and the proxy-topology requirement above.

### 2.11 WASM / playground — the TS JS language service IN-CONTEXT (third instantiation, accepted boundary)
Verter's Rust core compiles to **WASM** (the playground; `crates/verter_wasm` → `wasm32-unknown-unknown`, `--target web`, verified to export carrier-gen + Verter-native analysis). A WASM host **cannot spawn a subprocess**, which sets a hard, **accepted** platform boundary — not a gap to engineer around:

- **tsgo is native-only (ACCEPTED, verified).** `typescript@7.x` / `@typescript/native-preview` ship the tsgo checker only as **native platform binaries** (no `wasm`/`wasi` optional dependency exists; the launcher is `child_process`-only). There is **no WebAssembly build of tsgo and Verter does not attempt one.** Therefore **TS≥7 external type-check is unavailable in WASM** — the playground gets carrier-gen + Verter-native analysis and (below) the TS<7-style JS language service, never a fabricated TS≥7 result (fail-closed).
- **The WASM type-check engine = the TypeScript JS `LanguageService` run IN-CONTEXT.** TypeScript's language service (`ts.createLanguageService(host)`) is **pure JavaScript and runs in the browser** (the TS Playground and monaco do exactly this; `typescript`'s `package.json` `"browser"` field blanks `fs`/`os`/`path`, and `@typescript/vfs` provides the in-memory `System`/host). The playground instantiates it **directly in the WASM/browser host — no subprocess** — fed by Verter's **carrier-serving `LanguageServiceHost`**.
- **It REUSES the §2.2 carrier-serving CORE — NOT identical host hooks (the architectural consequence, D4).** This instantiation shares the §2.2 **carrier-serving CORE** (the carrier store/overlay, the §2.9 `.x`→companion redirection, the §2.9 specifier-rewrite, the fail-closed `ProviderPositionMapper`), but it **implements `LanguageServiceHost` directly** rather than feeding tsserver's plugin-host hooks — so it is the in-context `ts.LanguageService` sharing the CORE, NOT the identical tsserver/plugin host hooks. Concretely it exposes `extraFileExtensions` for the carrier source extension(s) and serves the carrier as the descriptor-owned companion — the `.vue.tsx`/`.svelte.tsx` component IDE carrier (and the `.verter.ts` API carrier where reached) — via `getScriptSnapshot`/`readFile`/`fileExists`/`getScriptKind` and the `.x`→component-carrier redirection. (The in-context JS `ts.LanguageService` resolves bare `.x` imports through the same appended-extension probing as tsgo, so the component carrier identity must be the probe-reachable `.vue.tsx`/`.svelte.tsx` here too, GATE 5.) So the **JS-language-service backend has two instantiations over ONE shared CORE**: **(a) the Node tsserver plugin** on native (§2.2), and **(b) the in-context `ts.LanguageService`** in WASM/playground (§2.11) — they reuse the CORE, not the same host hooks (the §2.2 tsserver negative-cache mechanics are instantiation-(a) concerns and do not all carry to (b)). The configured-project-membership fix, the enhanced plain-`.ts`-imports-`.vue`/`.svelte` DX (§2.9), and the framework-agnostic carrier mechanism (Vue AND Svelte) therefore **all apply in the playground** — this is the §2.1 contract with a third engine instantiation, the contract itself unchanged.
- **Lib `.d.ts` provisioning.** The lib `.d.ts` set (`lib.es*.d.ts`, `lib.dom.d.ts`) is not in the WASM module; the playground **fetches/loads** it and feeds it through the in-memory host (the standard `@typescript/vfs` browser pattern).
- **Fail-closed matrix posture.** In WASM, where the external engine is the in-context JS LS, TS-version-appropriate results come from it; where no engine applies (TS≥7), the surface returns carrier-gen + Verter-native only, never wrong external-TS.
- **Status: a designed path with a WASM spike obligation.** The in-context LS over Verter carriers is architecturally sound and its dependencies are browser-proven, but it is **not yet implemented** (Verter's only TS transport today is the native subprocess). A **WASM spike** (a Block; see §3) must prove: browser-load `typescript.js` with Node built-ins stubbed; supply lib `.d.ts`; feed `getIde` (`.vue.tsx`/`.svelte.tsx`) + `getPublicApi` (`.vue.verter.ts`/`.svelte.verter.ts`) carriers through the host; `.x`→component-carrier redirection + specifier-rewrite; position-map back; acceptable worker perf/memory. *(Also note: `analyzeWithAudit` is `wasm32`-stubbed and `processStyle` is Node-only today — the WASM "Verter-native" column is near-complete, not 100%; the spike records which `analyze_*` entry points are `cfg`-gated.)* Until the spike lands, the doc describes WASM external-TS as designed-but-unproven (it does not block native).

See §2.A (platform × surface × engine matrix) for the full per-platform capability table.

### 2.A Platform × surface × engine matrix
The authoritative capability table (verified; fail-closed where an engine is unavailable). Columns: **Carrier-gen** (IDE/TSX + compile + public `.d.ts`); **Verter-native analysis** (lint, code-actions, symbols, CSS, type-IR — `/architecture`); **Ext-TS LSP**; **Ext-TS TSC batch**, each split TS<7 / TS≥7.

| Host | Carrier-gen | Verter-native | Ext-TS LSP <7 | Ext-TS LSP ≥7 | TSC batch <7 | TSC batch ≥7 |
|---|---|---|---|---|---|---|
| **Native win/mac/linux** (`@verter/native`) | YES | YES | YES — tsserver Node plugin subprocess (§2.2) | YES — tsgo `--api` subprocess, OWNED baseline (+ optional SHARED, §2.3/§2.10) | YES — mirror-host `ts.CompilerHost` driver (§2.4) | YES — tsgo batch / mirror-host driver (§2.4) |
| **WASM / playground** (`wasm32`, `--target web`) | YES | **PARTIAL** (sync codegen/lint/code-actions/symbols/CSS/type-IR YES; `analyzeWithAudit` + `processStyle` Node-only — unavailable) | **SPIKE-GATED — UNAVAILABLE until Block 7** (designed: in-context `ts.LanguageService`, §2.11; fail-closed until proven) | **NO — tsgo native-only (accepted); fail-closed** | **SPIKE-GATED — UNAVAILABLE until Block 7** (designed: in-context LS `createProgram`/`getEmitOutput`, §2.11; fail-closed until proven) | **NO — tsgo native-only (accepted); fail-closed** |

A WASM module running inside a Node host is **not** a distinct external-TS capability: the module still can't spawn; any external TS there comes from the surrounding Node process running the native path. The editor-plugin WASM targets (`wasm32-wasip1`/`wasip2` for Lapce/Zed thin LSP clients) are unrelated to the playground core and embed no engine.

---

## 3. Block decomposition

Serial, one block at a time (per project discipline). Each block lands via the standard cycle: TDD red→green, 3/3 review (1 fresh-claude + 2 primed codex), fix cycle, land, independent confirm. **Block 0 is GO (the tsgo `--api` gate passed).** **Blocks 1–4 (contract + ownership + carrier store + VFS-driven sync + the tsserver first-real-backend + the TS<7 mirror-host TSC path) form ONE atomic landing unit on the integration branch** — the project-bound contract becomes live exactly when the tsserver backend does, with the inferred path deleted at that same boundary (the no-dual-path discipline above); they are decomposed into Blocks 1–4 for review/TDD granularity, but no commit ships the contract live over the inferred path. The tsgo path (Block 5) extends the now-live contract.

**No-dual-path discipline — the contract lands WITH its first real backend; there is NO compat-adapter-over-inferred interim (C11 — read this before the blocks).** The project-bound contract is **never live over the still-running inferred path** — not even on the integration branch. There is **no standalone "contract over inferred" block** and **no compat adapter** that satisfies the contract by calling today's inferred path. Instead the contract + ownership resolver + VFS-driven project-bound sync + the **first real backend (the tsserver backend, Block 4 below)** land as **ONE atomic landing unit (Blocks 1–4)**, and the inferred sync path (`compilerOptionsForInferredProjects`, `configure_paths`, the synthetic inferred-project carrier opens, the tsgo `rootUri`-only init) is **deleted at that SAME boundary**. The "config-less op unrepresentable" invariant therefore becomes live exactly when the first real backend does — at no commit is the contract a seam over a live inferred backend. tsgo (Block 5) and the remaining cutover/cleanup (Block 6) extend the now-live contract; each backend's own inferred knobs are deleted with it. At no commit is there a second *live semantic* path, and at no commit is there an inferred backend reachable behind the contract.

**Integration-branch landing — no half-cutover, no compat adapter, ever (C/P2-F).** Because the contract only lands with a real backend (above), there is no transitional "adapter over inferred" state to confine. The work still lands on an **integration branch** per the project's worktree/commit discipline: every block lands there, wip is **squashed to one commit per block/phase** (no plan-phase vocabulary in the squashed message), and **only the completed, SHIM-FREE state** (with the inferred path fully deleted and `no_fallback_to_inferred_anywhere` green) **fast-forwards** to the user's branch / `main`. The user's branch never carries a compat adapter or a half-cutover. This makes "no dual paths in the shipped tree" a branch-topology invariant, not just a per-commit aspiration.

**Architecture guards (C15 — mandatory; CLAUDE.md guard-per-CRITICAL).** Each block lands its guard(s) in the same change. Minimum set, by block, below; a `(CRITICAL)`-grade rule this workstream introduces (the project-bound invariant, the no-suppress-engine-recheck cache rule, the no-inferred-fallback rule, the virtual-file-naming reconciliation) must each be guarded or name its planned guard in this doc.

### Block 0 — GATE: tsgo `--api` capability gate — **DONE, GO (17/17 + GATE 5 6/6), reproducible in-repo**
**Resolved AND committed as a reproducible gate.** A real `tsconfig.json` with `baseUrl` + `paths` (`@/* → ./src/*`) + `jsx` + `jsxImportSource` + `types` + `typeRoots` + a **project reference**; an off-disk-only `.tsx` carrier (the model of a `.vue.tsx` companion) served purely through the FS overlay, against the **user-installed** tsgo via its official `--api` JS client (the TS≥7 distribution = `typescript@7.x`, e.g. `typescript@7.0.1-rc`; the recorded run used the identical-API `@typescript/native-preview 7.0.0-dev.20260526.1` channel). The harness, **fully-committable** hermetic fixture (declaration output in `packages/shared/lib/`, not the gitignored `dist/`), and report live in the repo at **`tools/tsgo-api-gate/`** (`run-gate.mjs` / `harness.mjs` / `control.mjs` / `incremental.mjs` / `companion-identity.mjs` / `fixture/`, report `GATE-REPORT.md`); re-run with `node tools/tsgo-api-gate/run-gate.mjs` (exit 0 = GO; discovery selects the installed `typescript` whose major is ≥ 7, falls back to native-preview). Proven:
1. **GATE 1** — resolves **identically to an on-disk TSX**: off-disk carrier is a real Program root file (scriptKind TSX), `get_default_project_for_file` → the real tsconfig (not inferred), **no false `TS2307`** (the `@/*` alias AND the project reference both resolve on the off-disk file), **no `TS2304`** (tsconfig `types`/`typeRoots` global in scope), clean carrier = zero diagnostics, off-disk ≡ on-disk twin.
2. **GATE 2** — correct **diagnostics** (`TS2345` with exact span via `program.get_semantic_diagnostics`), **hover** (`get_type_at_position` + `type_to_string` → `"string"`), **definition** (reaches the path-aliased target AND across the project reference) from the configured Program's `checker`.
3. **GATE 3** — a carrier-only edit (`update_snapshot({ open_project, file_changes: { changed: [carrier] } })`) flips diagnostics (TS2345→TS2322→clean) on the **same stable project handle**, with an unchanged dependency retained (same source-file identity) — incrementality proven observably (not via a build-dependent private field).
4. **GATE 4** — a plain `Consumer.ts` imports a **bare** `"./Exported.vue"` and resolves to the overlay-served `Exported.vue.tsx` companion (bare path asserted absent) with zero diagnostics; the companion's exported type flows into the `.ts`.
5. **GATE 5 (production companion identity — the load-bearing P0, `companion-identity.mjs`, 6/6)** — proves the production component-carrier identity against real tsgo: (a) a `.verter.`-infixed *component* path (`CompA.vue.verter.tsx`) does **NOT** satisfy `import "./CompA.vue"` (TS2307 — REJECTED); (b) `CompB.vue.tsx` **does** satisfy the bare `.vue` import, types flow (`"string"`); (c) a `Widget.svelte.tsx` component carrier and a REAL `state.svelte.ts` rune module coexist in one directory, correct types both ways, no clash; (d) tsgo probes `.svelte.ts` **before** `.svelte.tsx` (so the redirect-reached `.ts` API carrier must avoid a bare `.svelte.ts` identity — the reserved `.verter.ts` infix); (e) a same-stem `Same.svelte.ts` rune shadows a `Same.svelte.tsx` carrier on the bare import (the underlying tsgo behavior is TS1192; not fixed by any infix — Verter's response is to DETECT this same-stem collision and FAIL CLOSED per §2.2's Carrier-path conflict model, not surface a wrong edge).
- **Negative control** — with no `open_project`, `get_projects()` is `[]` and the carrier errors "no project found for file"; today's inferred path provably cannot pass.

**Carrier-discovery note (informs Block 5):** the fixture's `include` is extension-specific (`src/**/*.tsx`), so the carrier is discovered because its `.tsx` extension matches that glob — exactly why the production component IDE carrier (`Foo.vue.tsx` / `Foo.svelte.tsx`) is discoverable: its final `.tsx` extension matches a `.tsx` glob the same way (and why a bare `.vue` would not be; see §2.6). The redirect-reached `.verter.ts` API carrier is not enumerated (it is resolved on demand via the §2.6 reference redirect). The fixture keeps `src/components/` on disk via a `.gitkeep` so the overlay can inject the off-disk carrier into the directory enumeration.

**Mechanism correction folded into §2.3:** membership rides the **FS overlay** + `open_project`, NOT the `open_files[].default_project` proposal shape. The bare `./X.vue`-specifier redirection is **PROVEN by GATE 4** (a bare `"./Exported.vue"` import resolving to the overlay-served `.vue.tsx` companion). *Guard:* `tools/tsgo-api-gate/run-gate.mjs` is the **capability gate re-run on every tsgo version bump** (the §2.8 handshake) — Block 5 wires it into the gated test set so a tsgo bump that regresses any capability fails CI. *Residual Block-5 verification (NOT bare-`.vue`, which is proven):* root-set injection via tsconfig-virtualization under a `.vue`-specific `include` / `files`-list config (§2.3/§2.6), and the §2.9 module-specifier rewrite layer.

### Block 1 — The project-bound provider contract + ownership resolver (lands in the Blocks 1–4 atomic unit)
Define the three-layer contract (§2.1: `ProjectResolver` / `CarrierRegistry` / `EngineBackend`) with explicit `ProjectBinding`/`NoProject`/`Ambiguous`/`SyntheticScratch` states, and the §2.6 ownership resolver (TS-equivalent `files`/`include`/`exclude`/`extends` with the **correct extension-expansion rule** — a `.vue` is owned only via the default include, a no-extension directory/bare-star glob, or a glob/`files` that explicitly covers `.vue`; an extension-specific `*.ts` glob does NOT own it; plus solution-style leaf resolution and reference-graph awareness). Make every LSP feature depend on the contract. **No interim compat adapter and no inferred backend underneath — the contract goes live only when its first real backend (the Block-4 tsserver backend) lands in the same atomic unit** (the no-dual-path discipline); the inferred sync path is deleted at that boundary, not wrapped. *Legacy deletions:* none in Block 1 in isolation — the inferred-path deletion is part of the Blocks 1–4 atomic landing (it is gone before the unit fast-forwards). *Verification:* a provider op cannot be constructed without a resolved project; queries carry expected content-hash + map-hash; the ownership fixtures assert the **exact** extension semantics (below). **New real-provider fixtures (paths/baseUrl/types/lib/jsx/moduleResolution/project-refs/multi-root/solution-style) that the OLD inferred overlay FAILS** (red baseline, captured before the inferred path is deleted in the same unit). *Guards:* `provider_op_requires_resolved_project` (a provider op is uninstantiable without a `ProjectBinding`); `carrier_ownership_extension_rules` — **adapter-parameterized (asserted for `.vue` AND `.svelte`, and any registered adapter's carrier-source extension)**, asserts ALL of (note: **TypeScript `include` has NO brace expansion**, so multi-extension coverage is SEPARATE entries, never `*.{vue,svelte}`): `include: ["src"]` (directory glob) **owns** `src/Foo.vue` and `src/Foo.svelte`; `include: ["src/**/*"]` (bare star) **owns** them; `include: ["src/**/*.ts"]` (extension-specific) does **NOT** own them ⇒ `NoProject`; `include: ["src/**/*.ts", "src/**/*.vue", "src/**/*.svelte"]` (separate per-extension entries) **owns** them; default-include (no `files`/`include`) **owns** them; an `exclude`d carrier source ⇒ `NoProject`. **The two §2.2 Carrier-path conflict guards land here on the ownership/`Ambiguous` half:** `carrier_never_shadows_real_user_file` (a real file at the exact `{name}.vue.tsx`/`{name}.svelte.tsx` carrier path downgrades the source to `Ambiguous` — `.vue` AND `.svelte`) and `same_stem_svelte_component_rune_fails_closed` (a `Foo.svelte` component beside a real same-stem `Foo.svelte.ts` rune ⇒ DETECTED ambiguity ⇒ `Ambiguous`); the per-engine **overlay-placement** half of both guards lands in Blocks 4/5.

### Block 2 — The carrier surface store + the corrected carrier cache (+ the `CarrierBatch` provisional measurement)
**Extend the existing `ProviderSurfaceStore`** (`crates/verter_lsp/src/provider_surface_store.rs` — already the generation-stamped, content-addressed, fail-closed surface store with the `CarrierIde`/`CarrierApi`/`Shadow`/`Real` roles and the dual content/source-hash oracle), adding the project-owner column, `map_hash`, `import_signature_hash`, and wiring the currently-reserved roles. It is the **sole** record of provider content/maps/ownership (C9 — no second store). **Block 2 is also where the PROVISIONAL `CarrierBatch` role is MEASURED and decided (§2.1/§2.7):** measure whether a distinct minimal-diagnostic batch carrier yields a **material cold-perf gain** over sharing the single `CarrierIde` surface for the batch run. **If it does**, add `CarrierBatch` as its own storage slot with a content-addressed cache key (the §2.7(a) self-content env dims — a distinct `compile_profile`, so its key differs from the `CarrierIde` slot for the same source) and the same dependency-driven engine-notify invalidation (§2.7(b)); a source edit then invalidates both its `CarrierIde` and `CarrierBatch` slots, the cold TSC path (§2.4) reads `CarrierBatch`, the interactive path reads `CarrierIde`, coexisting in one store keyed by `(canonical, role, content_hash, env-dims)`. **If it does not**, `CarrierBatch` is **merged into `CarrierIde`** (no separate slot) and the cold path reads `CarrierIde`. Implement the **split cache** (§2.7) regardless: (a) carrier-regeneration skip keyed on self-content env dims; (b) engine notify driven by dependency-closure generation + `import_signature_hash` — **never suppressed by carrier-text stability**. *Legacy deletions:* none yet (but if any ad-hoc `CachedTsx` path duplicates the store, fold it in here). *Verification:* unchanged `.vue` content ⇒ zero **re-codegen** (the beat-vize lever); **a dependency `.d.ts`-only change (carrier text byte-identical) STILL notifies the engine and re-checks the dependent** (C2 discriminator — must fail against a self-content-only cache); a `map_hash` change invalidates cached mapped results (C3); **the `CarrierBatch` measurement is recorded** and, if kept, a `CarrierBatch` slot is stored/invalidated independently of its `CarrierIde` peer (B6); **the leaf batch carrier and `CarrierIde` produce the SAME diagnostic set** on the Block-2 fixture set (the equivalence gate — landed HERE because Block 2 is where the batch carrier is decided/stored, so nothing downstream can depend on an unverified batch surface); store is the single source for the tsserver manifest + tsgo overlay. *Guards:* `dependency_change_rechecks_unchanged_carrier` (the C2 false-green discriminator); `map_hash_change_invalidates_mapped_results` (C3); `single_provider_surface_store` (no second content-addressed surface store exists); `carrier_batch_role_stored_and_invalidated` (B6 — applies only if the measurement keeps `CarrierBatch` as a distinct role: it then has its own slot + invalidation, distinct from `CarrierIde`; trivially satisfied if merged); **`carrier_batch_typechecks_same_as_ide`** (lands in Block 2 — where the batch carrier is decided/stored — as a distinct-role equivalence if `CarrierBatch` survives, trivially true if merged; it is **RE-RUN in Block 6 on the perf corpus**, §2.7, so the minimal carrier can never silently drop a type-observable template-expression construct on real-world inputs either).

### Block 3 — VFS-driven project-bound sync (lands in the Blocks 1–4 atomic unit) — **DONE (additive seam landed)**
Migrate `ProjectSync` path-centric calls to project-bound snapshot publishing (§2.5): ownership resolution → per-project atomic delta batches → generation/epoch-gated queries → version-gated/fail-closed responses (multi-file epoch capture + span classification, C12); query de-dupe + shared cancellation (wired to each engine, §2.7); **eager `CarrierApi` index + lazy `CarrierIde`** (C8). **No interim inferred-path adapter underneath** — this sync drives the real backend that lands with it in the same atomic unit (Block 4); the inferred path is being deleted in this unit, not kept live beneath the seam. *Legacy deletions:* the inferred `ProjectSync` call paths are migrated to the project-bound seam as part of the unit (the inferred backend they called is deleted with Block 4). *Verification:* stale/torn/mixed-epoch results dropped; generated-only spans suppressed; closed `.vue` components appear in auto-import/find-refs (C8); no-owner ⇒ no TS result. *Guards:* `generated_only_spans_suppressed`; `stale_generation_result_dropped`; `closed_carrier_in_autoimport_index`.

**Landed state (`crates/verter_lsp/src/external_ts_sync.rs`):** the seam is built ADDITIVELY (the inferred sync path stays live + untouched until Block 4 cuts over + deletes it). It is PURE (no live engine — unit-testable without a provider), built on the Block-1 contract DTOs (`PublishSnapshot`/`SnapshotFile`/`Query`/`ProjectResolution`) and the Block-2 store (`ProviderSurfaceStore`):
- (1) `ProjectSyncBatch::for_binding(&ProjectBinding, …)` + `plan_publish_for_resolution` — a per-project atomic delta batch is constructible ONLY from a resolved `ProjectBinding` (the owner URI is read FROM it; `NoProject`/`Ambiguous`/`SyntheticScratch` ⇒ no binding ⇒ no batch). `PlannedFile::from_snapshot(&ProjectBinding, &snapshot)` reads content from the single store and FAILS CLOSED unless the snapshot's `project_owner` matches the binding (no foreign / owner-less surface leaks in); its `ScriptKind` is derived from the provider-path extension (the syntactic fact TS keys on), not the role.
- (2) `SpanClass` + `classify_provider_range` (RANGE-based — a TS result is a span/edit) over the `SpanMapperView` trait, keyed on the typed `SpanSubjectKind` (derived from the contract `SnapshotRole` via `from_role` — the structural companion-vs-real authority, NEVER a path-suffix sniff). Implemented for the production `ProviderPositionMapper` (`tsx_range_to_carrier` returning `None` — including a range straddling the generated/source boundary — is the fail-closed `GeneratedOnly` primitive): companion-mapped ⇒ `SourceMappable`, companion-unmapped/straddling ⇒ `GeneratedOnly`/suppressed, `External` subject ⇒ `External`/as-is.
- (3) `RequestEpoch::capture_project(store, project)` captures EVERY project-owned surface's `(generation, content_hash, map_hash)` (via `ProviderSurfaceStore::current_project_surface_paths`) before+after — multi-file by construction (a change/close/appearance of ANY project surface drops the result, order-independent). `returned_paths_all_fresh` validates each engine-RETURNED path against the before-capture; a returned path the store knows as a virtual surface but that was absent from the before-capture fails closed (the store — not a caller heuristic — decides companion-vs-external).
- (4) `QueryDedupeRegistry` — query de-dupe keyed `(project, provider_uri, carrier_offset, feature, content_hash, map_hash, required_version, feature_param)` (the full `Query` fail-closed identity, incl. `required_version` so a newer-snapshot query never joins a stale slot and `feature_param` so two same-offset renames stay distinct), the join group sharing one `verter_scheduler::cancellation::CancellationToken` so a supersession cancels the in-flight engine work, leader-retire gated on a monotonic slot id. It elides the duplicate engine query and shares cancellation; result delivery to joiners is the backend's concern.
- (5) `EagerApiIndexPlan` — eager `CarrierApi` for every owned source (closed components included; open state is not a filter); `CarrierIde` stays lazy.

The three guards live as standalone integration tests in `crates/verter_lsp/tests/` (matching the Block-2 convention) and are framework-agnostic (Vue AND Svelte). A test-only `ProjectBinding::new_for_test` (behind `verter_session`'s `test-util` feature, enabled only via a downstream `[dev-dependencies]`) lets downstream tests mint a binding without standing up a full resolver — never available in a production build, so the witness discipline holds.

### Block 4 — tsserver plugin backend (the FIRST real backend; + the TS<7 mirror-host TSC path) — completes the Blocks 1–4 atomic unit
This is the **first real backend**: landing it makes the Block-1 contract live and **deletes the inferred path in the same atomic unit** (no commit ships the contract over inferred). Harden the **existing `@verter/typescript-plugin`** (§2.2) into the thin synchronous reader over the Block-2 store + atomic manifest epoch: the full host-proxy matrix, the **gate-proven companion identities** reconciled with the `VirtualFileNaming` descriptor (C4 — the descriptor mirror is updated to the §2.2/GATE-5 scheme: component IDE carrier `.vue.tsx`/`.svelte.tsx`, redirect-reached API carrier `.vue.verter.ts`/`.svelte.verter.ts`, byte-pinned), the two-phase publish + bounded-block + negative-cache-eviction readiness protocol (C10). **Also land the TS<7 TSC batch path** (§2.4: the **mirror-host / `ts.CompilerHost` driver** + generated `extends`-tsconfig — generated carriers NEVER touch the user's working tree), since the plugin does not load for `tsc` (C7). *Legacy deletions (part of the atomic-unit cutover):* `compilerOptionsForInferredProjects`, `configure_paths`, synthetic inferred-project carrier opens, the tsserver inferred-project knobs (deleted at this boundary — the plugin replaces them, and there was never a compat adapter to remove). *Verification:* the Block-1 real-provider fixtures go **green on tsserver** (paths/baseUrl/types/lib/jsx/project-refs/rename/references/diagnostics/completions); a cold-read of an unwarmed `.vue` does **not** pin a sticky `TS2307` (C10 discriminator); **the §2.9 plain-`.ts`-imports-`.vue` DX contract (1)–(6) goes green on tsserver via the plugin**; the TSC batch path type-checks a project of carriers via the mirror-host build-mode driver with the generated tsconfig (zero working-tree writes); stale-map fail-closed. *Guards:* `no_inferred_project_knobs_on_tsserver` (the inferred knobs are gone); `virtual_file_naming_ts_freshness` (the descriptor mirror stays byte-pinned through the GATE-5 component/API identity reconciliation); `carrier_companion_suffix_collision_free` (the component IDE carrier ends in `.tsx` ≠ any rune-module extension, and the `.ts` API carrier's reserved `.verter.` infix equals no real adapter source/module extension — `.vue` AND `.svelte` incl. the `.svelte.ts` rune-module form); `component_carrier_is_bare_import_probe_compatible` (§2.2/GATE-5 — the component IDE carrier identity is a path the engine's `.tsx`/`.ts` basename probe reaches; a `.verter.`-infixed *component* identity fails the bare `.x` import → TS2307); `tsserver_cold_read_no_sticky_ts2307` (C10); `carrier_never_shadows_real_user_file` + `same_stem_svelte_component_rune_fails_closed` (§2.2 Carrier-path conflict model, the tsserver overlay-placement half — the plugin does NOT place the carrier companion / `getExternalFiles` entry when a real user file occupies the exact carrier path, and a same-stem rune fails closed; `.vue` AND `.svelte`); `tsc_batch_writes_no_working_tree_files` (the mirror-host driver materialises carriers/emit only under the mirror root, never the user tree); `carrier_dx_enhanced_both_engines_both_frameworks` (§2.9, the tsserver half).

### Block 5 — tsgo `--api` backend (Block 0 = GO) — extends the now-live contract
The tsgo adapter (§2.3) on the user-installed `typescript@>=7` `--api`, built as the **OWNED `--api` backend with the SHARED optimization layered on, behind one `CarrierInjectionChannel`**:
- **OWNED mode (the backend, build first):** `new API({ tsserver_path, cwd, fs: <VFS overlay> })` (the `typescript/unstable/sync` client for the batch baseline; **`unstable/async`, cancellable, for the interactive lane** per §2.7) + `update_snapshot({ open_project })` + sparse read-through FS overlay + `.vue`→carrier redirection through the overlay; carrier discovery by the §2.3 mechanism for the config shape (enumeration when the config matches the companion extension, the **first-class tsconfig-virtualization identity model** when it does not — §2.3/§2.6). The bare `./X.vue` redirection is PROVEN (GATE 4); Block 5 additionally verifies tsconfig-virtualization root-set injection under a `.vue`-specific `include` / `files`-list config (incl. its own virtual-config hash/`project_identity` and config/options-diagnostic invisibility of injected roots), the **async client + cancellation** (§2.8 handshake), and the **module-specifier rewrite layer** (§2.9).
- **SHARED optimization (optional, layered on afterward):** `API.fromLSPConnection({ pipe })` against the editor's tsgo session (VS Code via the Native Preview extension's `ExtensionAPI`), carriers injected as **LSP `didOpen`/`didChange` overlay documents on the shared `project.Session`** (§2.10), enabled ONLY when BOTH the attach-pipe AND Verter proxying the editor's TS LSP connection (B2) hold, with **fail-over to OWNED mode per redirect-ON reference closure** (B1) when any carrier in the closure cannot be made program-eligible in the shared session or the attach drops. An opt-in enhancement where available; **OWNED is the universal baseline and is always sufficient on its own.**

*Legacy deletions:* the current tsgo LSP `didOpen` carrier-membership semantics (OWNED mode replaces it with FS-overlay; SHARED mode's LSP-overlay is a *different*, shared-session mechanism), the post-hoc carrier import rewriting (`provider_sync.rs`), the tsgo inferred-project init (`tsgo/ipc.rs` `rootUri`-only). *Verification:* the real-provider fixtures go **green on tsgo in OWNED mode**; `tools/tsgo-api-gate/run-gate.mjs` (incl. **GATE 4 — the bare-`.vue` import + types-flow — and GATE 5 — the production component-carrier identity `.vue.tsx`/`.svelte.tsx` + the `.verter.`-component rejection + Svelte rune-module coexistence**) **runs against the PRODUCTION package `typescript@7.0.1-rc` (not only the recorded `@typescript/native-preview` channel) and goes GO (B4)** — the npm metadata confirms `typescript@7.0.1-rc` carries the platform tsgo binary + the `./unstable/sync`/`./unstable/async` exports, but BEHAVIOR is proven only by running the gate against that exact installed package; this run is wired into the gated test set; **two new attach probes go green** — (a) `API.fromLSPConnection` reads a shared on-disk file from a Verter-started tsgo LSP server, (b) an LSP-overlay companion becomes a member of the attached session's Program and types flow (the SHARED-mode carrier-injection proof); the **§2.9 DX contract (1)–(6) goes green on tsgo** in OWNED mode (and in SHARED mode where attach is available); version-pin + capability-gate handshake in place. *Guards:* `no_inferred_project_init_on_tsgo`; `tsgo_capability_gate_on_version` (CI runs `tools/tsgo-api-gate/run-gate.mjs` — incl. GATE 5; the adapter refuses a tsgo build that fails the handshake); `component_carrier_is_bare_import_probe_compatible` (§2.2/GATE-5 — the tsgo overlay answers the bare `.x` import at the `.vue.tsx`/`.svelte.tsx` component carrier the basename probe reaches; a `.verter.`-infixed *component* identity fails); `carrier_never_shadows_real_user_file` + `same_stem_svelte_component_rune_fails_closed` (§2.2 Carrier-path conflict model, the tsgo overlay-placement half — the FS overlay (OWNED) / the LSP `didOpen` overlay on the shared session (SHARED) does NOT serve the carrier at the exact carrier path when a real user file occupies it, and a same-stem rune fails closed; `.vue` AND `.svelte`); `tsgo_shared_mode_carrier_injection` (§2.10 — an LSP-overlay companion is a member of the attached session's Program and types flow); `shared_mode_failover_is_per_reference_closure` (B1); `shared_mode_requires_full_ts_lsp_proxy` + `shared_mode_no_unmapped_carrier_path_leak` (B2); `carrier_dx_enhanced_both_engines_both_frameworks` (§2.9, adapter-parameterized — see D3 below).

### Block 6 — Cutover + cleanup + perf gate
Convert ALL LSP features to the project-bound query path; delete every remaining legacy path; replay/restart state from the surface store only. (The bulk inferred-path deletion already happened in the Blocks 1–4 atomic unit; this block sweeps any residual.) *Legacy deletions:* any residual inferred-project knobs, ad-hoc carrier-companion sync left outside the store, import-rewriting, any fallback-to-inferred behavior. *Verification:* the full real-provider conformance suite green across **both** engines AND the TSC surface; the **four perf gates** (§2.7, measured to the precision pinned there: fixed corpus + fixed thread count; cold full-project = parity floor / beat-cold stretch; warm batch / single-edit / IDE-query = beat the reference at the stated percentiles + memory ceilings), with the cold path using the leaf diagnostic carrier (the provisional `CarrierBatch` if Block 2 kept it, else the shared `CarrierIde`); a **cross-platform** real-provider test (Windows drive-casing/UNC, case-insensitive collision, symlink/pnpm realpath — C14) green on all three OSes; zero dual paths remain. *Guards:* `no_fallback_to_inferred_anywhere` (a global guard that no inferred-project construction survives in the LSP/TSC path); `carrier_batch_typechecks_same_as_ide` (**LANDED in Block 2** — where the batch carrier is decided — and **RE-RUN here on the perf corpus**, §2.1/§2.7: the leaf batch carrier and `CarrierIde` produce the SAME diagnostic set, so the minimal carrier never drops a type-observable template-expression check; a distinct-role equivalence if `CarrierBatch` survives Block 2, trivially true if merged); `tracked_paths_are_portable` (extended to cover generated carrier/companion paths).

### Block 7 — WASM / playground in-context language service (§2.11; gated spike → impl)
Bring the configured-project + enhanced-DX improvements to the **playground** via the TS JS `LanguageService` run in-context, reusing the §2.2 carrier-serving CORE as instantiation (b) — sharing the CORE (store/overlay + redirection + specifier-rewrite + mapper), implementing `LanguageServiceHost` directly, NOT the identical tsserver host hooks (§2.11/D4). **Spike first** (the §2.11 obligation): in a browser/worker, load `typescript.js` with Node built-ins stubbed (the `"browser"` field / `@typescript/vfs`), supply the lib `.d.ts` set in-memory, instantiate `ts.createLanguageService(carrierServingHost)`, feed `getIde` (`.vue.tsx`/`.svelte.tsx`) + `getPublicApi` (`.vue.verter.ts`/`.svelte.verter.ts`) companions for the open SFCs, prove the `.x`→component-carrier redirection + specifier-rewrite + position-mapping work and a real diagnostic/hover/definition flows, at acceptable worker perf/memory. Then implement: factor the shared CORE so the Node-plugin and in-context instantiations reuse it; wire the playground to the in-context LS. *Legacy deletions:* none (additive — the playground gains real configured-project TS where it previously had `declare module '*.vue'` wildcards). *Verification:* a playground/WASM test where a `.vue`/`.svelte` carrier type-checks through the in-context LS with the §2.9 DX contract holding (mapped back, fail-closed); TS≥7 in WASM is asserted **unavailable** (tsgo native-only — accepted) and the surface returns carrier-gen + Verter-native only there. *Guards:* `wasm_in_context_ls_carrier_membership` (a carrier is a member of the in-context LS Program and types flow); `wasm_tsgo_unavailable_fail_closed` (no fabricated TS≥7 result in WASM). *(Framework-neutral: this benefits BOTH Vue and Svelte carriers in the playground.)*

---

## 4. Rejected alternatives
- **Inferred-project overlays** (today's path) — cannot express configured-project membership; the source of the bug. The Block-0 negative control proves it directly (no `open_project` ⇒ "no project found").
- **Import-rewriting as the semantic integration** — destabilises carrier text + fights incrementality; violates CodeTransform-is-source-of-truth.
- **A whole-filesystem proxy worker feeding the engine over IPC** (the stopped block's heavy design) — duplicates project state, floods transport, makes invalidation *harder* than TypeScript's own Program graph; over-engineered and likely **slower**. The shipped tsgo `--api` proves the *targeted* sparse overlay suffices.
- **A plain LSP `didOpen` overlay into a CONFIG-LESS / inferred project as the membership mechanism** — rejected on a precise criterion, not "LSP bad": a `didOpen` into an inferred/config-less project cannot apply the real tsconfig (today's bug). Two things are explicitly NOT this rejected pattern: (i) OWNED mode's `--api` `open_project` + FS overlay (the proven §2.3 path); (ii) SHARED mode's `didOpen` overlay **on the editor's already-configured shared `project.Session`** (§2.10), where the document lands in the editor's REAL configured Program, not an inferred one — that is a verified, accepted carrier-injection channel, distinct from a config-less `didOpen`.
- **Forking / patching / bundling tsgo** (golar-style) — version skew against the user's `typescript` is a correctness bug; cannot drive the user's install. **Rejected outright, with no exception:** the no-TypeScript fallback is **package DOWNLOAD only** (download the npm `typescript` at the `rc` dist-tag, §2.8), never a forked/patched/bundled/shipped binary.
- **Source-tree (co-located, gitignored) sidecar carriers for the TS<7 batch path** — writing `Foo.vue.tsx` next to `Foo.vue` would touch the user's working tree (perturbing file watchers, other tools, or a read-only checkout). **Rejected** in favour of the §2.4 **mirror-host / `ts.CompilerHost`** model, which re-roots reads so generated carriers live in a Verter-owned mirror tree and relative imports resolve via a host `resolveModuleNames` override — zero working-tree writes. There is no sidecar-vs-mirror bake-off; sidecars are off the table.
- **Routing IDE/typecheck through Verter's internal semantic resolver** — creates a second TypeScript that diverges on overloads, contextual typing, module resolution, JSX, libs, refactors. (This independently validates the typeinfo/semantic-engine exclusion: the IDE surface must be *real* TypeScript semantics.)

---

## 5. Risks & open questions
- **~~Primary risk: tsgo `--api` sufficiency~~ — RESOLVED (Block 0 = GO, 17/17).** Residual: the surface is `unstable`/generated, so a version bump could regress a required capability → mitigated by the version-pinned capability-gate handshake (§2.8) that re-runs the committed gate harness (`tools/tsgo-api-gate/`); a regressed build fails closed for TS≥7, never ships degraded.
- **Cross-file type invalidation correctness (the highest-severity class).** The carrier cache must skip only *re-codegen*, never an engine re-check a dependency change requires (§2.7 split + `import_signature_hash`); the discriminating guard `dependency_change_rechecks_unchanged_carrier` is the defense. A regression here produces stale-but-plausible diagnostics — the worst IDE failure mode.
- **tsserver negative-resolution-cache poisoning** — a cold/missing carrier read can pin a sticky `TS2307` (§2.2 two-phase publish + bounded-block + eviction; guard `tsserver_cold_read_no_sticky_ts2307`).
- **Project references / `disableSourceOfProjectReferenceRedirect` / solution-style configs** — a `.vue` reachable through two Programs with redirect-dependent resolution. §2.6 now specifies the **correct** per-mode surface: DEFAULT (redirect ON) ⇒ `A` sees `B`'s **source** carrier (`CarrierIde`); `disable…: true` ⇒ `A` sees `B`'s **`.d.ts`-equivalent** API surface (`CarrierApi`). Ambiguous ⇒ fail closed. Monorepo correctness rests on a `composite` + reference + cross-package `.vue`-import fixture run **both** with and without the flag (Block 0/5 fixture obligation).
- **TSC surface (no plugin on `tsc`) + the `noEmit`/references coherence** — the **mirror-host / `ts.CompilerHost`** + generated-`extends`-tsconfig path (§2.4) is the no-fork TS<7 batch route and the TS≥7 fallback; **generated carriers NEVER touch the user's working tree** (source-tree sidecars are rejected, §4 — no bake-off). Relative-import fidelity is solved by the host `resolveModuleNames` re-root into the user tree (resolver-version-independent); the `noEmit`-vs-project-references hole is fixed (no-refs ⇒ `noEmit` diagnostics-only; with-refs ⇒ build mode so referenced `.d.ts` boundaries are actually emitted into the mirror, coherent with both §2.6 redirect modes). Residual: the **pre-Block-4 spike** validates the mirror-host model matches the user's own `tsc -b` diagnostics on a `composite`+reference+`paths` fixture, both with and without `disableSourceOfProjectReferenceRedirect`.
- **`.vue`→carrier redirection + carrier discovery** — redirection rides the FS overlay (TS-resolver-first + keyed/bounded, §2.3); the bare-`./X.vue` redirection is **PROVEN (GATE 4)**. Carrier discovery uses the matching §2.3 mechanism by config shape (enumeration when the config matches the companion extension; **tsconfig-virtualization** when it does not). The one **unproven** case → Block-5: tsconfig-virtualization for root-set injection under a `.vue`-specific `include` / `files`-list config.
- **Carrier path is in the USER namespace — resolution-conflict fail-closed (§2.2/§2.6).** The component-carrier path (`{name}.vue.tsx` / `{name}.svelte.tsx`) is **path-collision-free against a *different* real source extension but NOT a Verter-reserved namespace**, so two model conflicts fail closed rather than shadow real user resolution: (a) a real user file at the **exact carrier path** ⇒ `Ambiguous`, no overlay placed (Verter never shadows it); (b) a **same-stem Svelte rune** (`Foo.svelte.ts` beside `Foo.svelte`, the GATE-5 `.svelte.ts`-before-`.svelte.tsx` probe) ⇒ DETECTED ambiguity, no silently-wrong edge. Guards `carrier_never_shadows_real_user_file` + `same_stem_svelte_component_rune_fails_closed`. The non-fail-closed dissolution is the future static module-resolution-map API (an upstream tsgo capability not in the shipped `--api`), recorded as the future enhancement; fail-closed holds until it ships.
- **Plain-script `.vue`-import DX under tsgo (first-class requirement, §2.9)** — importing a `.vue` from a plain `.ts`/`.js` and getting enhanced types is an in-process `getExternalFiles` plugin capability on tsserver, but tsgo **cannot load in-process plugins** (typescript-go #2824), so under tsgo it is served by the `--api` FS-overlay + companion-extension redirection (PROVEN, GATE 4). Its own cross-engine DX contract + fixtures (§2.9), a primary IDE-DX bar.
- **SHARED-engine mode (OPTIONAL optimization on the OWNED baseline, §2.10) — attach to the editor's own tsgo.** An **opt-in enhancement layered on the OWNED `--api` backend**, enabled only where the editor exposes the attach bridge AND Verter can proxy the editor's TS LSP connection (B2): one Program shared with the editor, no double-checking, instant consistency. It is **secondary to OWNED, not co-equal** — OWNED is the universal baseline and always sufficient. **Verified constraint that shaped the design:** an attached `API<true>` has NO content channel (`fs` is spawn-only; `LSPUpdateSnapshotParams` is `{openProject?}`; no `openFile`/`overlay` method) — so carriers are injected as **LSP `didOpen` overlay documents on the shared `project.Session`** (verified: shared session's overlay store is what the checker reads), NOT through the API client. Residual risks: (a) **editor availability** — only VS Code (Native Preview `ExtensionAPI`) exposes the pipe today; other editors run OWNED until they expose it (an editor-side gap, tracked per editor); (b) **program-eligibility in the shared session** — if any carrier in a **redirect-ON reference closure** can't be made program-eligible, **the whole closure falls over to OWNED** (per B1 — never per-file, never per-single-tsconfig); (c) **`unstable`/async-only/lifecycle-coupled** — re-attach + re-open overlays on editor tsgo restart, fail over to OWNED meanwhile. All fail-safe: SHARED never yields a wrong result, only falls back to OWNED. Gated by the Block-5 attach probes + `tsgo_shared_mode_carrier_injection` + `shared_mode_failover_is_per_reference_closure` + `shared_mode_requires_full_ts_lsp_proxy`.
- **Cross-platform identity** — one canonical identity layer shared by VFS + mapper + both adapters; drive-casing/UNC/case-collision/symlink/pnpm tested (§2.6/§2.7; guard `tracked_paths_are_portable` + a cross-platform real-provider test).
- **tsgo interactive concurrency** — the interactive lane is async/cancellable/priority-laned from the start (`unstable/async`, §2.7); the `unstable/sync` client is used only for the Block-0 gate and the batch baseline.
- **Durable transactional membership store (Q2) — DEFERRED to the ledger→read-path cutover.** The Block-4 tsserver backend lands the membership-correctness floor: the Q1 sealed carrier-sync gateway (`reconcile_carrier_source`) fusing the membership decision with the provider-commit behind a sealed `CarrierProviderCommit` receipt; every carrier-sync site (incl. `workspace_scanner`) routed through the single `MembershipReconciler`; compile-fail-to-empty retract; publish/prune atomic-or-rollback; the carrier-store mutators sealed `pub(in external_ts)` with a CI bypass guard. The architect's Q2 robustness layer — making the durable on-disk manifest the SINGLE membership authority with the in-process `MembershipLedger` demoted to a derived projection, non-advertising `Transitioning`/`Retracting` middle states, source-keyed serialization + `source_seq`/`prior_hash` CAS, and lease-atomic `commit_if_fresh` — is **deferred**. Safe today because production `getExternalFiles` is served cross-process from the on-disk store `ready_files` (writes individually atomic + gated to the `Owned` reconciler decision) and the in-process ledger has ZERO non-test production readers (its sole `external_files_for_project` caller is `#[cfg(test)]`, locked by the `ledger_is_off_the_serve_path` guard), so MOST Q2 gaps (non-transactional store↔ledger ordering, stale-lease ledger records) are not tsserver-visible and self-heal via the pending-retry path. ONE exception is honestly recorded (Block-4 verification): a failed durable-store RETRACT on a document DELETE (rare, durable-store-I/O-failure-gated) is warn-and-dropped today — the delete removes the document, the live `SourceDelta` publish path never prunes, and the only pruning (`ProjectAuthoritative`) publish is test-only — so on an actual retract I/O failure a stale `ready_files` row persists and stays tsserver-visible (B's pending-retry covers re-sync of LIVE documents, not deletes). This specific case IS tsserver-visible-on-failure and is the precise gap Q2's `Retracting`-state + retract-retry closes; it MUST land with Q2. Q2 becomes load-bearing — and MUST land — at the ledger→read-path cutover (when the in-process view becomes the live `getExternalFiles` authority, or same-source concurrent transitions / session-advance-during-await become tsserver-observable). Deferral per the round-3 architect verdict (un-primed codex, relayed via the orchestration coordinator, 2026-06-25); the substantive basis (store-authoritative, zero production ledger readers) is re-validated at the Block-4 3/3 review.
- **tsserver cross-file imported-type rename — Block-H program-membership gap.** Renaming a `<Child prop=…>` usage whose prop type is an IMPORTED type (declared in a third file) should edit BOTH the parent usage and the third-file member. Verter's half is complete and proven: it advertises BOTH the parent and child carriers in the on-disk store `ready_files` (the `getExternalFiles` serve set). But tsserver does not materialize the advertised parent `.vue.tsx` into a queryable program SourceFile at query time (`getValidSourceFile` ⇒ "Could not find source file"), so the rename's first provider hop errors, the declaration stays `Unknown`, and the merged-edit completeness gate fails closed (no usage-only partial). This is the SAME cross-file program-membership gap tracked for tgo (the `rename_cross_file_imported_prop_tgo_member_parity` sibling); the tsserver canary `rename_cross_file_imported_prop_tsserver` is `#[ignore]`'d on it (remove when Block-H program-membership lands). Out of Block-4 scope (membership-correctness, not provider program-materialization).
- **Block-4 review follow-ups (deferred to Q2 / pre-Q2, recorded as debt — NOT absorbed into Block 4).** The fresh 3/3 + verification surfaced these MINOR, non-blocking items, deferred to keep Block 4 bounded; all land in/before the Q2 ledger→read-path cutover: (1) owner-MOVE store ordering is publish-first (compensated rollback only on an in-process prune `Err`), leaving a transient/crash-window cross-project DUPLICATE `ready_files` row on a SUCCESSFUL move — pre-Q2 mitigation: owner-change-gated PRUNE-FIRST (fail-closed gap, never a duplicate); (2) the stale-owner prune + its compensation run on same-owner content edits too, so a transient prune I/O blip can over-retract a just-published healthy carrier — gate the prune/compensation on a detected owner change; (3) the in-process `commit_sync_transition` / `carrier_close_state` carrier-commit primitives are not receipt-gated (defense-in-depth only — NOT serve-path-visible; the on-disk store mutators ARE sealed + guarded) — fold under the receipt or extend the allowlist guard; (4) confirm the gateway's `NativeProjectResolver` path-derivation and the reconciler's `WorkspaceProjectResolver` membership decision read one published snapshot (both fail closed to absent; the reconciler owns the final decision).

---

## 6. Hermeticity & scope notes
- vize/corsa-bind, Volar, golar, tsgo (#2824, PRs #711/#2620) are **external public references** used for analysis only — never named or embedded in Verter source/tests/fixtures.
- The campaign's private validation projects are never referenced in code/tests/commits; deviations route through the campaign ledger.
- `typeinfo` + Verter's internal semantic resolver are **out of scope** and must never appear in this workstream's design or implementation (they do not use tsserver/tsgo; LSP/TSC does not route through them).
- **Behave ~identically to real TypeScript, plus framework parse/validation (A8 — reaffirmed; the framework-agnostic reframe does NOT dilute it).** The external engine produces **real TypeScript semantics** for the carrier; Verter is **never a second type resolver** (the §4-rejected internal-resolver path stays rejected for both Vue and Svelte and every future adapter). On top of TS's verdict, the framework adapter adds its own **parse/template/markup validation** (Vue template diagnostics, Svelte rune/markup diagnostics) — additive, never a reinterpretation of TS's result — and everything maps back fail-closed. The framework-neutral generalization (A5) only moves the carrier *producer* behind an adapter; the carrier is still real TSX type-checked by real TypeScript, identically across frameworks.
- **Forward-compat (A10 — NOT designed in this cutover, but not precluded):** the TSC surface (§2.4) is intended to become **CLI-usable** — a `vue-tsc`/`svelte-check`-equivalent binary driving the same lean carrier-codegen + engine path (the §2.4/A6 minimal footprint is already the right shape for a CLI). And the carrier/compile path stays **bundler-friendly** (the unplugin integration consumes the same framework-adapter carrier codegen). Neither is built here; the design must not foreclose them (e.g. the §2.4 reduced-host footprint and the framework-neutral carrier contract are deliberately CLI/bundler-shaped). Track as follow-on workstreams.

---

## 7. Review-hardening log (process note, not technical proof)
This doc was hardened after a four-reviewer adversarial pass (an adversarial reviewer that verified the doc's file:line claims against the live source; two independent adversarial second opinions; and a cold-brief reviewer given only the problem statement, whose reconstruction matched the architecture — corroborating the *direction*). Convergence is recorded as process context; the architecture is justified by the falsifiable invariants and the §3 fixtures, not by agreement. Material corrections made (with the section that now owns each):

- **Membership is injected, not inherited** (§1, §2.6) — `.vue` is not a configured-project member by default on either engine; Verter's glob-based ownership had to be corrected, and a per-engine injection + a TS-equivalent ownership algorithm specified. The earlier "rooting ⇒ membership + Program-reuse both fall out" framing was the central over-claim.
- **Carrier cache split** (§2.7) — "unchanged carrier hash ⇒ don't notify the engine" was a false-green on cross-file type changes; corrected to skip *re-codegen* only, never an engine re-check, via a separate `import_signature_hash` + dependency-closure forwarding, matching the project's read-side-authoritative cache discipline.
- **Source-map "don't re-check" withdrawn** (§2.7) — a `map_hash` change invalidates cached mapped results; raw vs mapped result caches separated.
- **tsserver identity reconciled** (§2.2) — the descriptor-owned companion identity (the shipping plugin + the `VirtualFileNaming` CRITICAL descriptor), not a bare `.vue` path; the existing `@verter/typescript-plugin` is the Block-4 starting point; readiness/negative-cache-poisoning protocol added. *(SUPERSEDED by Final-convergence item 7 + GATE 5: the production component IDE carrier is `.vue.tsx`/`.svelte.tsx` (bare-import-probe-reachable, path-collision-free against a different real extension — but the path is in the user namespace, so a same-path/same-stem resolution conflict fails closed, §2.2); only the redirect-reached `.ts` API carrier uses the reserved `.verter.` infix — `.vue.verter.ts`/`.svelte.verter.ts`. An earlier round's "the component companion suffix is the `.verter.` infix" claim is REFUTED — GATE 5 shows `Comp.vue.verter.tsx` is not reached by the bare `.vue` probe.)*
- **TSC surface separated** (§2.4) — LS plugins do not load for `tsc`; the batch typecheck path is a first-class deliverable with its own per-engine design.
- **tsgo mechanism corrected to the SHIPPED API** (§2.3) — FS-overlay + `open_project`, not the `open_files[].default_project` proposal; folded from the Block-0 spike (GO, 17/17), which also flipped Program-reuse/incrementality from a proof obligation to a proven fact.
- **Ownership of project references / solution-style configs** (§2.6), **eager API-surface index** (§2.7, auto-import/find-refs), **multi-file version-gating + span classification** (§2.7), **cross-platform canonical identity** (§2.6/§2.7), **block ordering + no-dual-path interim** (§3), **architecture-guard column** (§3), and **measurable four-gate perf** (§2.7) were all added or corrected from the panel.
- **Existing repo machinery reused, not re-built** — `ProviderSurfaceStore` (extended, not duplicated) and `discovery.rs` (reused) were called out to avoid a second store / second discovery path.

**Second hardening round (6 further findings — 4 P1, 2 P2 — all resolved):**
- **Block-0 proof made reproducible in-repo (P1-A)** — the gate harness, hermetic fixture, and report were relocated from session scratchpad into `tools/tsgo-api-gate/` and **re-run against the user-installed tsgo (GO, 17/17)**; the doc citations now point at the repo, and the gate is wired as the version-bump capability handshake (Block 5).
- **Project-reference redirect direction CORRECTED (P1-B)** (§2.6) — verified against the TypeScript docs: DEFAULT (redirect ON) resolves to the referenced project's **source** (so `CarrierIde`); `disableSourceOfProjectReferenceRedirect: true` resolves to the **`.d.ts`** boundary (so `CarrierApi`). The prior text had these reversed.
- **`include` extension-expansion CORRECTED (P1-C)** (§2.6, Block 1) — verified against the TypeScript docs: auto supported-extension expansion applies only to a no-extension directory/bare-star glob; `"src/**/*.ts"` matches only `.ts`, so a sibling `.vue` is `NoProject`. The ownership rule and the `carrier_ownership_extension_rules` guard now assert the exact three-case behaviour.
- **TS<7 TSC physical-carrier model pinned (P1/P2-D)** (§2.4) — co-located gitignored sidecar carriers (relative imports resolve with no path-mapping) + a generated `extends`-tsconfig preserving `rootDir`/`outDir`/`composite`/`declaration`/`paths`/`references`; working-tree-intrusiveness flagged for a pre-Block-4 spike. *(Superseded in round 3 by the reference-dependent emit mode; and **superseded by Final convergence**: sidecars are rejected outright — the model is mirror-host ONLY.)*
- **Intro/§2.3 tsgo-membership contradiction fixed (P2-E)** (§1) — the stale "explicit `default_project` association" line now matches §2.3's FS-overlay + `open_project` mechanism.
- **Staged interim confined to an integration branch (P2-F)** (§3) — the compat-adapter-over-inferred interim never reaches mainline; blocks land on an integration branch, squash to one commit per phase, and only the shim-free cutover fast-forwards to the user's branch / `main`. *(Superseded by Final convergence: there is **no** compat-adapter-over-inferred interim at all — the contract lands only with its first real backend in the Blocks 1–4 atomic unit; the integration-branch + squash + FF-only-when-shim-free discipline stays.)*

**Third hardening round (user binary correction + a second adversarial re-review's defects + two user DX requirements — all resolved):**
- **TS≥7 binary identity corrected to `typescript@7.0.1-rc`** (§1, §2.3, §2.8, §3, gate) — the shipping TS≥7 distribution is the npm `typescript` package at v7 (platform binaries `@typescript/typescript-{plat}-{arch}`; `--api` at `typescript/unstable/sync`), NOT `@typescript/native-preview` (a dev-preview channel with the **identical** `--api`). Verified via npm. The gate's discovery selects the installed `typescript` whose **major is ≥ 7** (and falls back to `@typescript/native-preview` as a source when none is present); the recorded GO run used native-preview because that is what was installed. The `typescript@7.0.1-rc` `--api` **surface** is npm-confirmed equivalent, but its **behavior is a gate obligation, not assumed** — Block 5 PROVES it by running the gate against that exact installed package (B4).
- **Block-0 fixture made committable for real** (gate) — the referenced package's declaration output was under `dist/`, which the repo's root `.gitignore` excludes (the fixture would not commit intact — the same class of break P1-A targeted). Moved to `packages/shared/lib/`; verified with `git add -n` that all 16+ fixture files are trackable; gate re-run GO.
- **`noEmit` + project-references incoherence fixed (D2)** (§2.4) — a referenced project consumed via the driver needs its emitted `.d.ts`; forcing `noEmit` produces no boundary and contradicts §2.6's `disable…: true` mode. Now: no-refs ⇒ `noEmit` diagnostics-only; with-refs ⇒ build mode so referenced `.d.ts` boundaries are actually emitted — coherent with both redirect modes. *(Superseded by Final convergence: no sidecar bake-off — the pre-Block-4 spike validates the **mirror-host model only** against the user's `tsc -b`; emit lands in the mirror tree, never the working tree.)*
- **Carrier-discovery vs ownership mismatch fixed (D3)** (§2.3, §2.6) — `get_accessible_entries` cannot make tsgo enumerate a component carrier under a `.vue`-specific `include` or a `files` list; the authoritative mechanism is **root-set injection** of the carrier (the §2.3 first-class tsconfig-virtualization identity model), with directory-enumeration augmentation as a secondary aid. Ownership (§2.6) and discovery (§2.3) reconciled as two steps bridged by injection. *(Identity note, GATE 5: the discovered/injected component carrier is `Foo.vue.tsx`/`Foo.svelte.tsx`, not a `.verter.`-infixed path — the earlier `.verter.`-companion wording here is superseded by Final-convergence item 7.)*
- **Lazy-`CarrierIde` vs default-redirect import gap fixed (D4)** (§2.7) — a cross-package import under redirect-ON is a materialization trigger; the referenced `CarrierIde` is generated on demand, never absent when a member Program references it.
- **Cold-perf raised from parity to a beat-vize-cold stretch (user-review P1)** (§2.7) — added a cold-path strategy (parallel codegen, minimal-diagnostic carrier, lazy source-maps, precomputed manifest) and split the cold gate into a parity floor + a beat-cold stretch.
- **Batch diagnostics made complete (user-review P1)** (§2.4) — TS≥7 batch emits syntactic + semantic + global/options/config (+ declaration when emitting), not semantic-only, for `vue-tsc` parity.
- **Async/cancellable interactive lane mandated from the start (user-review P1)** (§2.7) — the interactive lane uses `unstable/async` with an active-file priority lane; sync is only for the gate/batch baseline.
- **NEW first-class requirement: `.vue`-import from plain `.ts`/`.js` enhanced DX on BOTH engines (user)** (§2.9) — tsserver via the existing in-process plugin; tsgo via the `--api` FS-overlay + `.vue`→companion redirection (tsgo cannot load in-process plugins, #2824).

**Fourth hardening round (a round-3 codex re-review's 5 real holes + an empirical probe + the IDE-reuse research — all resolved):**
- **bare-`.vue` redirection PROVEN + GATE-4 overclaim corrected (codex #8)** (§2.3, §2.9, §3, gate) — the prior GATE 4 imported `./Widget.carrier` (a carrier path), NOT a bare `./X.vue`, so it did not prove the real DX and contradicted §2.3/Block-5's "bare-`.vue` untested." A 3-strategy probe established the mechanism (tsgo appends `.tsx`/`.ts` to the `.vue` basename and resolves the overlay-served **companion**; serving the bare path itself yields `TS2307`). GATE 4 was rewritten to import a real bare `"./Exported.vue"` (companion served at `Exported.vue.tsx`, bare path asserted absent) — **GO**. This both proves §2.9 and closes the §2.3 bare-`.vue` open question.
- **Module-specifier rewrite layer added (codex #8)** (§2.9) — because the engine identity is a carrier companion, inserted imports (auto-import/quick-fix) would write the companion specifier (the `./C.vue.tsx` component carrier, or `./C.vue.verter` for the API carrier) instead of the user-facing `./C.vue`; a rewrite layer maps them back to the bare `.vue` or fails closed. *(Identity per GATE 5 + Final-convergence item 7: component carrier `.vue.tsx`/`.svelte.tsx`, API carrier `.verter.ts`.)*
- **Batch `tsc -b` semantics corrected (codex #2)** (§2.4) — the source-of-project-reference redirect is a **language-service** feature; on the CLI/batch path `tsc -b` consumes referenced projects through emitted `.d.ts` **regardless** of the flag. Removed the wrong "batch default-redirect sees source carriers" claim; the source-vs-`.d.ts` distinction applies to the LSP surface (§2.3/§2.6) only.
- **Root-set-injection mechanism NAMED (codex #3)** (§2.3, §2.6) — the shipped `--api` has no `rootNames` override, so the two concrete overlay mechanisms are specified: directory/bare-star `include` → companion surfaced by `read_file`/`get_accessible_entries`; `.vue`-specific `include` / `files` list → **tsconfig virtualization** (the overlay serves a `read_file` of `tsconfig.json` with `include`/`files` augmented by companion paths, never touching the user's real config). Stale directory-enumeration language removed.
- **`CarrierBatch` role added to the taxonomy (codex #5)** (§2.1, §2.7) — the minimal-diagnostic batch carrier is now a named role with a `compile_profile`, guarded (`carrier_batch_typechecks_same_as_ide`) so it preserves every type-observable template-expression check (Carrier IDE TS Surface Principle honored).
- **Async/cancellation folded into the capability gate (codex #7)** (§2.8) — `typescript/unstable/async` + working request cancellation are now **required** capabilities (a sync-only API must not pass); the async client's cancellation method is verified at the handshake (not assumed to be LSP `$/cancelRequest`).
- **Lazy-`CarrierIde` materialization-ordering note (codex #4)** (§2.7) — the referenced carrier is materialized to the overlay **before** the engine's first FS probe, so on-demand generation cannot pin a sticky negative resolution.
- **NEW §2.10 — attach to the editor's OWN tsgo session (user research Q)** — verified from the shipped Go binary + typescript-go source + the VS Code extension: `custom/initializeAPISession` mints a second API session sharing the editor's `project.Session`; the VS Code Native Preview extension exposes `ExtensionAPI.initializeAPIConnection()` → `API.fromLSPConnection({ pipe })`. *(Final convergence keeps this as an **optional optimization** layered on the OWNED baseline — the round-5 "first-class co-equal" promotion is demoted back to optional/secondary; the mechanism is unchanged.)*
- **Stale check-count fixed** — the doc/README said "15/15"/"15 checks"; the gate now has 17 checks (GATE 1–4) and the doc says 17/17, README is number-free.

**Fifth hardening round (round-4 close-out codex residuals + the user's "API hook must be FIRST-CLASS" directive):**
- **§2.10 SHARED-mode carrier-injection mechanism settled (user directive).** *(Positioning superseded by Final convergence: SHARED is an OPTIONAL optimization layered on the OWNED baseline, NOT a co-equal/first-class mode — the mechanism facts in this entry stand; only the "co-equal/preferred" framing is demoted to "optional, secondary to OWNED".)* The tsgo backend has the OWNED backend (spawn-our-own, FS overlay, portable baseline) with the SHARED optimization (attach to the editor's tsgo) layered behind one `CarrierInjectionChannel`. A primary-source investigation settled the load-bearing mechanism: an attached `API<true>` has **NO content channel** (`fs` is spawn-only; `LSPUpdateSnapshotParams` is `{openProject?}`; no `openFile`/`overlay`/`Document` API method) — so SHARED-mode carriers are injected as **LSP `didOpen`/`didChange` overlay documents on the shared `project.Session`** (verified: the shared session's overlay store is exactly what the checker reads through). This corrects the round-4 §2.10 claim ("open companions via the API client") which the close-out codex flagged as lacking an injection channel. SHARED is an opt-in enhancement, offered only where the editor exposes the attach bridge AND Verter can proxy the TS LSP connection (B2); OWNED is the universal baseline + per-reference-closure/lifecycle fail-over (B1) and is always sufficient on its own; correctness is mode-independent. Block 5 builds OWNED (the backend) first, then layers the SHARED optimization, with two new attach probes + guards.
- **Carrier-discovery framing corrected (close-out codex #2)** (§2.3) — the enumeration mechanism applies to ANY config whose enumeration matches the companion's `.tsx`/`.ts` extension (default include, directory/bare-star, OR an extension-specific `.tsx`/`.ts` glob — which is what Block 0 used); tsconfig-virtualization covers configs that don't enumerate the companion. The earlier "directory/bare-star only" framing was inaccurate.
- **Async/cancellation demoted to the Block-5 handshake (close-out codex #4)** (§2.8) — the recorded gate ran the SYNC client, so async+cancellation is NOT among the already-proven capabilities; it is a REQUIRED capability proven at the Block-5 handshake (a sync-only API must not pass), with the async client's cancellation method verified against the install.
- **Stale bare-`.vue` "unproven" residuals removed (close-out codex #6)** (§3 Block 0, §5) — both now state the bare-`.vue` redirection is PROVEN by GATE 4; the only residual tsgo discovery case is tsconfig-virtualization under a `.vue`-specific `include`/`files` list.
- **`CarrierBatch` equivalence guard placement (close-out codex #3, re-placed)** (§3 Blocks 2 + 6) — `carrier_batch_typechecks_same_as_ide` **lands in Block 2**, where the batch carrier is decided/stored, so nothing downstream can depend on an unverified batch surface; it is **RE-RUN in Block 6 on the perf corpus**. *(This supersedes the earlier "assigned to Block 6" note — Block 6 is the re-run on real-world inputs, not the first landing.)*

**Sixth hardening round (10 user directives A1–A10 + a re-review's B1–B6 + round-5 close-out residuals — all folded):**
- **A5 FRAMEWORK-AGNOSTIC reframe (doc-wide)** (header, §1, §2.2, throughout) — the architecture keys off the **carrier** + source carrier file, not `.vue`; the only framework-specific pieces are the carrier-source extension/redirection specifier and the SFC→carrier codegen (the per-framework adapter, `/framework-adapters`, companion naming via `VirtualFileNaming`). Vue + Svelte are the first two adapters; `.vue` tokens read as example carrier-source extensions. Aligned with the Carrier IDE TS Surface Principle (Vue AND Svelte).
- **A1 WASM/playground matrix + boundary** (§2.11, §2.A) — tsgo is **native-only (accepted, verified: no wasm/wasi build)**; the WASM/playground type-check engine is the TS JS `LanguageService` run **in-context**, reusing the §2.2 host-proxy as a **second instantiation** ((a) Node plugin native, (b) in-context LS WASM). Carrier-gen + Verter-native always work in WASM; TS≥7 fail-closed there. Full platform × surface × engine matrix in §2.A; WASM spike = Block 7.
- **A2 SHARED / OWNED** (§2.10) — *(Superseded by Final convergence: SHARED is an OPTIONAL optimization layered on the OWNED baseline, not "preferred/co-equal"; OWNED is the universal baseline + fail-over **per redirect-ON reference closure**, never per-project.)*
- **A3 no-bundle, AUTO-DOWNLOAD** (§2.8) — Verter never bundles TS; uses the user's install regardless of version; when none, auto-downloads the npm `typescript` package (which carries the platform tsgo binary for v7), not a shipped binary.
- **A4 async-everywhere + explicit tsserver/JS-LS sync exception** (§2.7) — async/cancellable/priority-laned for tsgo; the tsserver/in-context JS LS uses its synchronous API + TS cancellation token.
- **A6 minimal/cheap Verter for TSC** (§2.4) — the batch footprint excludes LSP/interactive machinery (editor session, LSP server, query lanes, SHARED attach, eager auto-import index, inlay/semantic scaffolding); a reduced host of carrier-codegen + VFS + engine driver, tied to the leaf diagnostic carrier. *(Final convergence: that leaf carrier is the **provisional** `CarrierBatch` if Block 2 keeps it, else the shared `CarrierIde`.)*
- **A7 pre-compile-to-warm experiment** (§2.7) — flagged (default off), implementation-validated/measured, never a baseline correctness assumption.
- **A8 behave ~identically to real TS + framework validation (reaffirmed)** (§6) — the framework-neutral reframe does not dilute the "real TS semantics, never a second resolver, fail-closed" principle; the adapter adds parse/markup validation additively.
- **A9 use `verter_scheduler` (tokio + rayon)** (§2.7) — route through `verter_scheduler` (which owns both pools) per the user's clarification, not hand-rolled pools: CPU codegen/source-map/hashing → its CPU (rayon) lane; engine I/O + interactive lane → its I/O (tokio) lane.
- **A10 future CLI + bundler forward-compat** (§6) — the TSC surface should become a `vue-tsc`/`svelte-check`-equivalent CLI and the carrier path stays unplugin/bundler-friendly; not built here, not precluded (the §2.4/A6 footprint is already CLI-shaped).
- **B1 SHARED fail-over is PER REDIRECT-ON REFERENCE CLOSURE, not per-file or per-single-tsconfig** (§2.10) — per-project is insufficient because redirect-ON project references entangle A's and B's carriers in one Program slice; the selection unit is the redirect-ON reference closure (a `disable…:true` reference decouples via the `.d.ts`-equivalent `CarrierApi` and is exempt). *(Round-6 hardening of the round-5/initial-B1 per-project answer.)*
- **B2 SHARED carrier-path leak suppression** (§2.10) — `didOpen`-ing companions into the editor's own session means the editor's tsgo could surface carrier-path diagnostics/edits/import-specifiers directly to the user; Verter's editor-side layer intercepts all companion-path results (map-back-or-suppress, specifier-rewrite, generated-only suppression). Guard `shared_mode_no_unmapped_carrier_path_leak`.
- **B3 `paths` removed as the relative-import solution** (§2.4) — `paths`/`baseUrl` match only bare specifiers (TS docs). *(Final convergence: relative `.x` carrier-source imports resolve via the **mirror-host compiler-host `resolveModuleNames`/`rootDirs` override** on the TS<7 batch path, and via tsgo's overlay-answered extension-probing on the tsgo path — NOT source-tree co-location, which is rejected with sidecars.)*
- **B4 prod-package gate obligation** (§2.3/§2.8/Block 5) — `typescript@7.0.1-rc`'s surface is npm-confirmed, but behavior is a gate obligation: Block 5 RUNS the gate against `typescript@7.0.1-rc`, not only the recorded native-preview channel.
- **B5 stale gate docs fixed** (`GATE-REPORT.md`, `README.md`) — both now state the bare-`.vue` redirection is GATE-4-proven, not deferred.
- **B6 `CarrierBatch` decided in Block 2** (§3 Block 2) — *(Final convergence: `CarrierBatch` is **PROVISIONAL** — Block 2 MEASURES whether it earns a distinct slot; if kept it gets its own store slot + content-addressed cache key (distinct `compile_profile`) + independent invalidation, if not it is merged into `CarrierIde`. Guard `carrier_batch_role_stored_and_invalidated` applies only in the kept case, trivially satisfied if merged.)*
- **Round-5 close-out residuals** (§2.3/§2.6) — the carrier-discovery framing corrected to "any config whose enumeration matches the companion extension" (Block 0 used an extension-specific `.tsx` glob, not a directory glob); the "full carrier parity" overclaim moved from SHARED-alone to the combined adapter.

**Seventh hardening round (round-6 re-review: 2 P0 + Svelte/host-proxy/footprint P1 — all folded):**
- **D1 (B1) SHARED fail-over is the REDIRECT-ON REFERENCE CLOSURE** (§2.10, +Block 5/§5/§7 stale "per-file"/"per-project" text fixed) — per-project is insufficient because redirect-ON project references entangle A's and B's carriers in one Program slice; a `disable…:true` reference decouples via the `.d.ts`-equivalent and is exempt. (Resolves the P0 stale contradiction AND the cross-project hole.)
- **D2 (B2) leak suppression REQUIRES a full TS-LSP-proxy topology** (§2.10) — the editor's own tsgo can surface companion-path results on UNPROMPTED channels (push diagnostics, workspace-symbol, call/type hierarchy, code actions, completion-resolve, organize-imports, applyEdit, semantic tokens, inlay, file-rename); suppression is enforceable only if Verter proxies the entire TS LSP connection, so SHARED is offered ONLY under that topology (further constraining availability), else OWNED. Guard `shared_mode_requires_full_ts_lsp_proxy` + the rescoped leak guard.
- **D3 (A5) Svelte truly first-class** (§2.2 two carrier policies, §2.9 neutral DX contract, Block-1/5 adapter-parameterized guards) — the carrier contract now spans BOTH `VirtualPathPolicy::{Suffix, SelfFile}` (Svelte rune modules `.svelte.ts`/`.svelte.js` are `SelfFile`: engine identity = source path, content rewritten in place, no companion, redirection a no-op, B2 must NOT suppress them — a sanctioned exception to "no post-hoc rewriting"); the §2.9 public-surface contract is "the adapter's real public types flow" (Vue `$props`/emits; Svelte runes/snippets), framework-neutral; guards renamed `carrier_ownership_extension_rules` / `closed_carrier_in_autoimport_index` / `carrier_dx_enhanced_both_engines_both_frameworks`, asserted for `.vue` AND `.svelte`.
- **D4 host-proxy "two instantiations" softened** (§2.2/§2.11) — they reuse the shared carrier-serving CORE (store + redirection + mapping), NOT identical host hooks ((a) feeds tsserver's plugin host; (b) implements `LanguageServiceHost` directly), so §2.2's negative-cache mechanics don't all carry to (b).
- **D5 (A6) TSC carrier-generation reconciled with references** (§2.4) — leaf uses the leaf diagnostic carrier (the **provisional** `CarrierBatch` if Block 2 keeps it, else `CarrierIde` — Final convergence); referenced projects under build mode need `CarrierApi` declaration boundaries (the `.d.ts`-producing surface), generated on demand; the eager `CarrierApi` *index* stays excluded. The reduced host instantiates the carrier-store cache layer (incl. the leaf diagnostic-carrier slot) but not the full LSP store — reconciling A6 with B6.
- **D6 (§2.A) matrix made fail-closed** — WASM Ext-TS<7 reads "SPIKE-GATED — UNAVAILABLE until Block 7" (not near-YES); Verter-native "PARTIAL" (analyzeWithAudit/processStyle Node-only).
- **D7 (B3) TS<7 `tsc` extension-probing not assumed from tsgo GATE 4** (§2.4) — gated for TS<7 specifically or uses the compiler-host `resolveModuleNames` override (resolver-version-independent).
- **D8 (A4) tightened** — the language-SERVICE API is synchronous (not "the protocol"); Verter still runs tsserver transport I/O async.
- **D9 (§2.9) CodeTransform caveat** — the specifier-rewrite targets the engine's `WorkspaceEdit` output for user source, not CodeTransform-built carrier text; no rule violation.
- **D10 (A3) no-TS version policy** — *(SUPERSEDED by Final-convergence item 3 below; recorded for history.)* The round-6 draft said "default to a tsgo-bearing `typescript@>=7` (WASM: a *bundled* JS `typescript` for the in-context LS)". The controlling rule is now precise: the installed `typescript` whose major is ≥ 7 wins; the no-TS fallback **downloads** the npm `typescript` package at the **`rc`** dist-tag (TS7 channel; retargets `latest` once TS7 is stable) and **fails closed offline** — and WASM's in-context LS uses a **fetched/downloaded** JS `typescript`, never a bundled one (download-only, no fork/patch/bundle).

**Final convergence + freeze (the controlling record — supersedes any earlier-round phrasing in this log where they differ).** Eight user-decided values are now final and applied doc-wide:
1. **SHARED = OPTIONAL layered optimization; OWNED `--api` = the correctness BASELINE and architectural center.** SHARED is an opt-in enhancement layered on the OWNED backend, enabled only where a full editor TS-LSP proxy exists (VS Code today), never on the core path. This **supersedes** the round-4/round-5 "SHARED promoted to a first-class co-equal binding mode / preferred binding" framing wherever that phrasing reads as co-equal — SHARED is secondary, OWNED is primary and always sufficient. (The *mechanism* facts those rounds recorded — LSP-overlay-on-shared-session injection, the proxy-topology requirement, redirect-ON-closure fail-over — are unchanged; only the positioning is demoted.)
2. **TS<7 batch = MIRROR-HOST / `ts.CompilerHost` model ONLY.** Source-tree (co-located gitignored) sidecars are **rejected** (§4) — generated carriers never touch the user's working tree, and there is **no sidecar-vs-mirror bake-off**. This **supersedes** the round-2/round-3 "co-located gitignored sidecar carriers" model and the "bake off sidecars vs mirror-host" spike framing (P1/P2-D, D2); the spike now only validates the mirror-host model against the user's `tsc -b`.
3. **Precise TS acquisition.** Installed workspace TS ALWAYS wins. The no-TS fallback **downloads** the npm `typescript` package at the **`rc` dist-tag** (the current TS7 channel, retargeting `latest` once TS7 ships stable) and **fails closed when offline**; it is **download-only, never fork/patch/bundle** (the §4 fork/patch/bundle rejection has no no-TS exception). WASM's in-context LS uses a **fetched/downloaded** JS `typescript`, not a bundled one. This sharpens A3/D10.
4. **tsconfig-virtualization = first-class identity model** (§2.3): own virtual-config hash + `project_identity`; injected roots are config/options-diagnostic-invisible (batch promises config/options parity); explicit invalidation on user-config / companion-path-set / `extends`-ancestor change.
5. **`CarrierBatch` = PROVISIONAL** (§2.1/§2.7/Block 2): kept only if Block 2 measures a material cold-perf gain over sharing the single `CarrierIde` surface; otherwise merged into `CarrierIde`. The `carrier_batch_typechecks_same_as_ide` equivalence gate holds either way.
6. **Stronger no-dual-path** (§3): the project-bound contract lands **only with its first real backend** (tsserver, in the Blocks 1–4 atomic unit) and the inferred path is deleted at that same boundary — **no compat-adapter-over-inferred interim, even on the integration branch**. This **supersedes** the round-1/P2-F "compat adapter over inferred ... confined to the integration branch" framing: there is no such adapter to confine.
7. **Gate-proven companion identities (§2.2, GATE 5 — supersedes the earlier-round `.verter.`-for-everything framing).** The shipped `--api` has no module-resolution-map endpoint, so a bare-import-probed carrier must live where tsgo's appended-extension basename probe reaches it. Two distinct identities:
   - The **component IDE carrier** (`CarrierIde`) is `{name}.vue.tsx` / `{name}.svelte.tsx` — bare-import-probe-reachable (tsgo appends `.tsx`/`.ts` to the full `Comp.vue` basename) and **path-collision-free against a *different* real extension** because it ends in `.tsx`, whereas Svelte rune modules are `*.svelte.ts`/`*.svelte.js` (never `.tsx`). GATE 5 proves `.vue.tsx`/`.svelte.tsx` resolves the bare `.x` import (types flow) and coexists with a real `.svelte.ts` rune module; it also proves a `.verter.`-infixed *component* identity is **REJECTED** (TS2307 — never probed). **Path-collision-free is NOT resolution-unambiguous, and the carrier path is in the USER namespace:** a real file at the exact carrier path, or a same-stem rune, is a RESOLUTION conflict Verter DETECTS and FAILS CLOSED (`Ambiguous`, no external-TS result — never an overlay-shadow of a real user file, never a silently-wrong edge; §2.2 Carrier-path conflict model). **This corrects the earlier-round "the component companion uses the `.verter.` infix" framing, which would have broken the gate-proven bare-import mechanism AND would not have fixed the only real rune collision (the same-stem edge below) anyway.**
   - The **public-API carrier** (`CarrierApi`) is `{name}.vue.verter.ts` / `{name}.svelte.verter.ts` — the reserved `.verter.` infix is correct and necessary ONLY here. This `.ts` surface is **redirect-reached** (project-reference redirect / cross-package `.d.ts`-equivalent — §2.6), **never bare-probed**, so the reserved infix breaks no probe; it is *required* because a bare `{name}.svelte.ts` API carrier would collide with a rune module (GATE 5 records tsgo probing `.svelte.ts` before `.svelte.tsx`).
   - **Same-stem edge (GATE 5 D2) — DETECTED ambiguity, FAILS CLOSED (not a documented wrong-edge):** a component carrier and a rune module sharing the EXACT same stem (`Widget.svelte.tsx` + `Widget.svelte.ts`) make the bare `import "./Widget.svelte"` ambiguous (the `.ts` rune is probed first — tsgo's TS1192). This is inherent to Svelte's rune-module naming and is NOT fixed by any infix; because the carrier path is in the USER namespace, **Verter detects the same-stem collision and fails closed** (`Ambiguous`, no external-TS result — never a silently-wrong edge), per §2.2's Carrier-path conflict model. The component carrier stays `.svelte.tsx` and resolves the bare import **whenever no same-stem `.svelte.ts` exists** (the normal case, since rune-module stems differ from component stems). The non-fail-closed dissolution is the future module-resolution-map API (§2.2/§2.8).
   Guards `carrier_companion_suffix_collision_free` + `component_carrier_is_bare_import_probe_compatible`.
8. **WASM in-context LS shares the carrier-serving CORE, NOT identical tsserver/plugin host hooks** (§2.2/§2.11/D4) — it implements `LanguageServiceHost` directly; the §2.2 negative-cache mechanics are instantiation-(a)-only.

**Final convergence sign-off round (four bounded fixes — applied doc-wide, supersede any earlier phrasing where they differ):**
- **Carrier path is in the USER namespace ⇒ model conflict FAILS CLOSED; never overlay-shadow a real user file** (§2.2 Carrier-path conflict model, cross-referenced from §2.6 step 4; §5; §7 items above). The `{name}.vue.tsx`/`{name}.svelte.tsx` component-carrier path is NOT a reserved namespace. (a) A real user file at the exact carrier path ⇒ `Ambiguous`, the overlay is NOT placed, no external-TS result (Verter never shadows the user's real file). (b) A same-stem Svelte rune (`Foo.svelte.ts` beside `Foo.svelte`, the GATE-5 `.svelte.ts`-before-`.svelte.tsx` probe) ⇒ a DETECTED ambiguity that fails closed — **this supersedes the prior "documented wrong-edge / accepted TS1192" disposition** (the collision is no longer surfaced as a wrong edge). The non-fail-closed dissolution is a future static module-resolution-map API (upstream tsgo capability, not in the shipped `--api`) that could redirect to a non-colliding carrier path without shadowing; fail-closed holds until it ships. Guards `carrier_never_shadows_real_user_file` + `same_stem_svelte_component_rune_fails_closed` (Block 1 ownership/`Ambiguous` + Block 4/5 overlay-placement gate).
- **path-collision-free ≠ resolution-unambiguous** (§2.2, header, §2.3, §5, §7) — every "collision-free" claim is now precise: the carrier path is path-collision-free against a *different* real source extension in the normal case, but a real same-path file OR a same-stem rune is a RESOLUTION conflict that is detected and fails closed. `carrier_companion_suffix_collision_free` covers path-collision-freedom only; the two fail-closed guards above cover resolution ambiguity.
- **Batch perf reference = `vize`, not `vue-tsc`/`svelte-check`** (§2.7) — the batch performance gates (1–2) compare wall-time against `vize` (the fastest TS7 reference, §1) on the same corpus/threads/binary; the interactive gates (3–4) against the editor's own TS LSP. `vue-tsc`/`svelte-check` are retained explicitly as the diagnostic-set **correctness/compatibility oracle**, never the timing bar. RSS ceiling and overhead-attribution compare against `vize`. Hermetic: `vize` is an external benchmarking reference only, never embedded/named in Verter source/fixtures (§6).
- **`run-gate.mjs` runs FOUR gate scripts** — the driver comment now names `harness`/`control`/`incremental`/`companion-identity` (it formerly said "three"); behavior unchanged, gate re-run GO.

Gate alignment (also frozen): the §2.3/Block-0/Block-5 tsgo membership is **FS-overlay + `open_project`** (never `open_files[].default_project`); the SHARED fail-over unit is the **redirect-ON reference closure** (never per-file / per-project / per-single-tsconfig); the **production component IDE carrier identity is `{name}.vue.tsx` / `{name}.svelte.tsx`** (bare-import-probe-reachable, path-collision-free against a different real extension — but in the user namespace, so a same-path/same-stem resolution conflict FAILS CLOSED, §2.2 — GATE 5) and the **redirect-reached `.ts` API carrier is `{name}.vue.verter.ts` / `{name}.svelte.verter.ts`** (a `.verter.`-infixed *component* identity is REJECTED); the **check-count is 17/17 (GATE 1–4) plus GATE 5 6/6 (`companion-identity.mjs`)** across the doc + `GATE-REPORT.md` (`README.md` is intentionally number-free); engine selection uses the installed `typescript` whose **major is ≥ 7** (the no-TS fallback downloads `typescript@rc`, fails closed offline); the gate harness imports the tsgo `--api` via the **PUBLIC export** (`@typescript/native-preview/unstable/sync`, parameterized for `typescript/unstable/sync`).
