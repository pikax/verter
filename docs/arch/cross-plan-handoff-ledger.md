# Cross-plan hand-off ledger

Formal receiving rows for the **non-LSP** cross-plan hand-offs transferred at the external-ts-engine plan close-out (`842f95a2e`). The source rows are marked `TRANSFERRED`/`MOVED` in [`external-ts-engine-architecture.md`](./external-ts-engine-architecture.md) → "Plan close-out — external-ts-engine debt ledger disposition (842f95a2e)"; the **LSP-bound** transfers are received separately in [`lsp-workstream-ledger.md`](./lsp-workstream-ledger.md) → "Received close-out transfers".

Per the SB6c-roadmap §4 rule 5, a departed row leaves the open debt set ONLY with a formal receiving row that carries `source id + owner + trigger + acceptance bar + ruling`; a named-target hand-off with no owning row would still be open (silent-park = reopen). Every row below is that owning row, so the external-ts-engine open debt set is genuinely empty. Each disposition was ruled by the codex-sol architect (grand-plan-aware, forward-compat verified) in the close-out CODEX-A re-adjudication, except the perf-lane rows carried by the recorded perf-A / rmpy decider rulings as noted.

| Source id | Owner | Trigger | Acceptance bar | Ruling |
|---|---|---|---|---|
| external-ts §2.7.1 Axis-B `verter-tsc` peak RSS (cold + warm-incremental `peak_rss_ratio`) | perf/audit instrumentation — `verter_tsc` + `verter_audit` | the `verter-tsc`/tsgo audit substrate can emit an engine-attributed (process-tree or per-PID) resident-set figure via a `verter-tsc --audit/json` mode | axis-B cold + warm-incremental `peak_rss_ratio` gates on the tsgo-engine-attributed peak RSS (not the wrapper-PID `VmHWM`, which misses the tsgo child), portable across Linux/macOS/Windows; the misleading wrapper-PID figure is never gated | codex-sol (CODEX-A close-out re-adjudication) |
| external-ts §2.7.1 Axis-A per-PID peak RSS (`process_rss_peak_bytes`) | `verter_audit` + `verter_napi` instrumentation | the audit footprint per-PID peak-RSS sampler is armed on the native compile path | `process_rss_peak_bytes` reads the real compiler resident set (not null) on an axis-A run and the per-PID peak-RSS metric gates (the Node-process maxRSS is never substituted) | codex-sol (CODEX-A close-out re-adjudication) |
| external-ts §2.7.1 Axis-A full non-checker time aggregate (`nonCheckerMs`) | compiler/audit instrumentation — `verter_compiler` + `verter_napi` | the compile `RequestAuditRecord` carries the parse/transform/transport phase timings | `nonCheckerMs` is non-null on an axis-A run and the full non-checker aggregate (parse + transform + transport on top of the already-gated codegen + source-map emit time) gates | codex-sol (CODEX-A close-out re-adjudication) |
| external-ts §2.7.1 Svelte (`.svelte`) perf corpus + carrier-extension discovery | Svelte block B8 (Svelte LSP/IDE) | gated on B8 — once Svelte LSP/IDE support exists | a pinned hermetic `.svelte` perf corpus + carrier discovery generalized to the registered carrier extensions (`.vue` + `.svelte`) + the Svelte axis-A/axis-B workloads, landing only once Svelte support exists so the numbers measure a real engine, not a stub | codex-sol (CODEX-A close-out re-adjudication) |
| external-ts §2.7.1 cluster-aware bootstrap | perf-gate methodology / tooling | before/with arming the gate baseline | the gate's bootstrap resamples whole per-run `WorkloadSample` clusters (or run-level percentile scalars) instead of pooled per-operation values, so the CI reflects the true between-run variance (today's pooled resample biases toward a false RED — the safe direction) | codex-sol (CODEX-A close-out re-adjudication) |
| external-ts Block 6 ROW 1 — surface-store REPLAY consolidation (`SurfaceReplaySnapshot`) | surface-store / host-session plan — `verter_type_runtime` + `verter_lsp` | the surface store first owns the load-vs-open replay axis (`CachedFileMode {Open, Load}`, dispatched `load_file` vs `open_file`) — a cross-crate identity/schema change | the carrier/declaration surface-content replay set (`files` + `carrier_registrations`) moves off `ResilientProvider.DesiredState` to a `verter_type_runtime`-defined store-derived `SurfaceReplaySnapshot` DTO (`path_configs`/`workspace_folders` stay in `DesiredState` as transport metadata) | codex-sol (CODEX-A close-out re-adjudication) |
| external-ts Block 6 ROW 2 — surface-store carrier OWNERSHIP / §2.7 producer-wiring | surface-store / cache-producer plan — `verter_lsp` / `verter_session` / `verter_workspace` + host facts | the owner-bearing producers exist (R21 per-file env dims, file-language identity, compile-profile/helper version, resolved import facts, forward-dependency-closure generation, project/env recheck generation) | the live carrier publish wires through the owner-bearing `record_carrier_surface` (sets `project_owner` + constructs `PublishedCarrierSurface { regen_key, engine_recheck }`) with a live owner-gated reader landed in the SAME change, no fabricated partial cache state | codex-sol (CODEX-A close-out re-adjudication) |
| external-ts Repo-toolchain FF3 — CLAUDE.md `## Dependencies Policy` no-Python WORDING tightening | **USER** (rmpy / toolchain-policy) | user steer | **USER-OWNED — resolve per user steer.** A keyword-scanning / static guard is OUT by explicit user directive; the exact strictness (ban repo-owned Python regardless of extension/shebang; the breadth of the `.claude` exemption) is PENDING USER STEER. A legitimately parked owning row whose owner is the user — not an unowned claim | codex-DEFER'd (rmpy confirm FEED-FORWARD ledger, FF3) + user directive (static guard OUT) |
| external-ts Repo-toolchain Q1 — `compile_entry` shared-substrate extraction | PERF-A render lane | PERF-A render-lane consolidation | the shared compile substrate is extracted behind `compile_entry` so the RuntimeRender render-only lane and the HostBacked path share ONE substrate | codex-DEFER'd (perf-a decider ruling, Q1 = LAND-NOW-WITH-GUARD+DEFER) |
| external-ts Repo-toolchain Q2a — unplugin double-upsert cleanup | PERF-A / unplugin cleanup | PERF-A / unplugin cleanup pass | the unplugin double-upsert (verified a gated no-op at deferral time) is removed | codex-DEFER'd (perf-a decider ruling, Q2a = LAND-NOW+DEFER) |
| external-ts Repo-toolchain Q2b — RuntimeRender warm-cache / warm-state | PERF-A render lane | PERF-A render-lane warm-state work | warm-state caching is added to the RuntimeRender render-only lane | codex-DEFER'd (perf-a decider ruling, Q2b = LAND-NOW+DEFER) |

FF3 is a parked **user-owned** row (its owner is the user, its trigger is user steer): it satisfies rule 5 as an owning row with an owner, and its acceptance is deliberately "resolve per user steer," not a claim of completed work. Every other row above names a concrete receiving plan/crate owner with a landing trigger and an acceptance bar. No source row was silently deleted — each traces back to its `TRANSFERRED`/`MOVED` marker in `external-ts-engine-architecture.md`.
