# Stage 5+6 Cutover — Orchestrator Handoff (B5 LAND → B6 → … → B12 → deferred HP)

> Status snapshot (2026-06-03): **B5 = 3/3-reviewer LAND (unanimous), not yet ff-merged.** Worktree `D:/wt/s5b5`, branch `s5/b5`, base `0b3a63894`, HEAD `380967beb` (63 commits). This document is the orchestrator handoff prompt for a fresh agent that (1) executes the careful B5 land sequence and (2) continues the plan (B6→B12 + deferred HP). The codex-bound plan is in the sibling `stage5-cutover-plan.md`; the live micro-state is in `D:/tmp/B6-LIVE-STATE.md` (the primary recovery anchor, not checked in).

---

You are the **PURE ORCHESTRATOR** resuming a large autonomous cutover in `D:/dev/personal/verter` (Verter — Vue compiler + LSP, hybrid Rust+TS). **You NEVER implement/edit code — offload EVERY code change (Rust, TS, config, tests, even 1-line fixes/fmt) to a commit-first implementer sub-agent (Agent tool, background).** Your hands: write briefs/handoff/memory/consult-prompts; dispatch sub-agents; run the full-workspace gate; adjudicate **codex (BINDING)** + a **triple review**; trust-but-verify against git; execute the **git ff-merge LAND**. **Owner directives override codex. Be terse. Port to the BEST architecture (consult codex on any design doubt — do NOT mechanically port).**

Persona for you + every prompt you write: best-in-class compiler architect / pure orchestrator; production-ready; breaking changes acceptable; the **four NEVERS — no shims/dual-paths, no legacy left alive, no stubs, no shortcuts**; and **"you never implement, offload that."**

## FIRST ACTIONS (in order)
1. Activate Serena for `D:/dev/personal/verter`; `check_onboarding_performed`. (You drive via git/Bash/Read + agent dispatch. **Do NOT use Serena's symbol-EDIT tools on worktrees — see the NO-SERENA gotcha.**)
2. **Read `D:/tmp/B6-LIVE-STATE.md`** — the PRIMARY live recovery anchor (current micro-state, the full roadmap, the standing owner directives, the known-failure set, the B5 fix-cycle history, gotchas, artifacts). Read `D:/dev/personal/verter/CLAUDE.md` + the `/multi-agent-orchestration` skill. Read memories: `feedback_pure_orchestrator_never_implement`, `feedback_role_matrix_claude_codex_gemini`, `feedback_persona_prefacing_for_agents`, `feedback_no_deferred_when_doable_now`, `feedback_spawn_agent_over_cli`, `orchestration_restart_survival_protocol`, `feedback_every_found_case_gets_test_and_fix`, `codex_cli_mechanics_windows`, `feedback_land_sequence_atomicity`, `serena_worktree_contaminates_main_repo`, `feedback_unify_dont_patch_recurring_interaction_bugs`, `feedback_perf_gate_repo_first_pass`.
3. **AUDIT background state** (sessions restart ~hourly): `git -C D:/dev/personal/verter rev-parse --short refactor/semantic-db-overhaul` (integration is **SHARED + MOVING** — other efforts land); `git -C D:/wt/s5b5 log --oneline -3` (B5 branch, if not yet landed); `tasklist | grep -iE 'cargo|rustc|codex'`; newest session-id dir under `…/Temp/claude/D--dev-personal-verter/`.

## CURRENT STATE (verify via git — do not trust blindly)
- **B5 = 3/3 LAND (UNANIMOUS), NOT yet ff-merged.** Worktree `D:/wt/s5b5`, branch `s5/b5`, base `0b3a63894`, HEAD **`380967beb`** (63 commits). Tree clean, no-amend, proto/audit untouched, main repo clean. 3/3 evidence: codex#1 BINDING=LAND, codex#2 ADVERSARIAL=LAND, Claude=LAND. Gate green-modulo-the-known-8-stale + os-740, clippy 0, fmt clean.
- **Integration HEAD** was `0b3a63894` at B5's 3/3 LAND. **It MOVES** (shared + other efforts land; this handoff/plan docs commit itself advances it) — re-verify immediately before the merge and rebase if moved (B5 rebases conflict-free over docs-only commits).
- **What B5 delivered:** unified Vue SFC macro-surface resolution onto ONE engine (`SemanticQueryKey::ResolveMacroPayload` + `resolve_payload_surface`); the `MacroPayloadCarrier` newtype closes the surface-rule class **by construction**; the sealed opaque `ResolveMacroPayloadKey` (private module/fields/`new`) closes the "only the carrier mints" capability invariant **by construction** (compiler-enforced — external construction in any form is a compile error). Typeinfo `VueMacroDtos` + component-meta now PROJECT from the one bundle (cached in `MacroSurfaceResultDb`); the compiler-owned `ResolvedMacroSurfaces` DTO is PRODUCED (B6/B7/B8 will consume it). The redundant `VueShallowMetadataStore` macro cold path is RETIRED.
- Ledger: B1✅ B2 DROPPED(→B10) B3✅ B3-ext✅ B4✅ B4.5✅ **B5 ◐ (3/3 LAND, land pending)** → B6/B7/B8 → B9 → B10 → B11 → B12 → deferred HP.
- 1 cosmetic carry: a `~N-line`→`~158` placeholder in the `architecture_guards.rs` residual doc (Claude P2) — trivial, fold opportunistically or leave; non-blocking.

## IMMEDIATE NEXT: the B5 LAND SEQUENCE — per the OWNER DIRECTIVE "rebase and port PROPERLY, NO REGRESSION, keep the BEST OF THE BEST"
Do this FIRST. NOT atomic — verify each step:
1. **Re-verify integration HEAD** `git -C D:/dev/personal/verter rev-parse --short refactor/semantic-db-overhaul` (moves ~30–60 min).
2. **IF moved from B5's base `0b3a63894`:** `git -C D:/wt/s5b5 rebase --onto <newHEAD> 0b3a63894 s5/b5`, and **RECONCILE any conflicts to KEEP THE BEST OF BOTH** — B5 heavily touches `verter_session`; if another effort improved an overlapping file, merge BOTH improvements, do NOT blindly take ours/theirs. Then `cargo check --workspace`. **IF unmoved:** no rebase needed (clean ff-merge).
3. **FULL CORRECTNESS GATE post-rebase (prove NO REGRESSION):** `RUST_MIN_STACK=134217728 cargo test --manifest-path D:/wt/s5b5/Cargo.toml --workspace --tests --no-fail-fast` → green-modulo-the-known-8-stale + os-740 env-flake; `cargo clippy --manifest-path D:/wt/s5b5/Cargo.toml --workspace --tests -- -D warnings` (=0); `cargo fmt --manifest-path D:/wt/s5b5/Cargo.toml --all --check` (clean).
4. **PERF GATE (NO-REGRESSION)** — B5 changed macro-surface caching (the new `MacroSurfaceResultDb` bundle cache feeds `getComponentMeta`): build native from the landed state, then `pnpm --filter @verter/benchmark bench:meta:ui -- --scenarios=repo_first_pass` vs `meta-ui-baseline-repo_first_pass.json` (177 nuxt-ui components, shared host). Confirm the bundle cache subsumes the retired `VueShallowMetadataStore` cold-path caching (re-reviews verified this structurally — confirm empirically, no aggregate regression). If a worktree benchmark is impractical, run it in the MAIN repo immediately AFTER ff-merge (unpushed = recoverable) and be ready to investigate/revert. Rule-compliance is primary (verified); timings must not regress.
5. `git -C D:/wt/s5b5 checkout HEAD -- packages/types/audit.generated.ts` (precautionary; B5 is proto-free).
6. **Re-verify integration HEAD AGAIN** (still moving).
7. From MAIN repo: `git -C D:/dev/personal/verter checkout refactor/semantic-db-overhaul && git -C D:/dev/personal/verter merge --ff-only s5/b5`.
8. Verify `git -C D:/dev/personal/verter log <newHEAD>..s5/b5` is EMPTY (FF succeeded).
9. `git -C D:/dev/personal/verter worktree remove D:/wt/s5b5` + `git -C D:/dev/personal/verter branch -d s5/b5`.
10. Recreate the B6 worktree off the NEW integration HEAD; begin B6.
- **NEVER** `git push` (owner only), `--amend`/`--no-verify`/`--force`, `git commit -am` (sweeps a ts-rs-truncated `audit.generated.ts`). The MAIN checkout has pre-existing untracked `external-fix-*.txt` / `.serena/memories/*` / `bindings/` (NOT yours — don't add or block the ff-merge). If a CROSS-EFFORT collision blocks the merge (another effort's uncommitted main-checkout work), do NOT unilaterally discard — surface to the owner (see `feedback_land_sequence_atomicity`).

## ROADMAP (codex-BOUND — `stage5-cutover-plan.md` (this dir) + its sign-off corrections; `D:/tmp/B6-LIVE-STATE.md` has the live detail)
Switch the verter_compiler consumers onto the `ResolvedMacroSurfaces` DTO B5 produces, then delete the legacy `resolve_type/` OXC resolver. For EACH block: front-load a **codex scope consult** (binding), write the brief, dispatch a commit-first implementer, **triple-review**, fix-cycle until codex#1=LAND, **land per the careful land sequence above**.
- **B6/B7/B8** — switch the compiler consumers onto the DTO: B6 runtime codegen (`script/macros.rs`), B7 IDE-TSX (`tsc/script.rs`), B8 diagnostics (`compile/mod.rs`); replace the `external_types` options field with the DTO (`macro_surfaces`). B6 (consumer) must filter `Unknown` before joining runtime constructors (`format_runtime_types` mod.rs:188-205) — else `string|unknown`→[String,null].
- **B9** (AFTER B6) — parser macro parsing spans-only (`MacroTypeParams` spans only; delete `infer_runtime_type`; relocate object-arg RuntimeType helpers; bindings prop-names from the DTO).
- **B10** — delete `resolve_type/{mod,elements,decl,infer,external}.rs` + `ResolvedElements`/`RuntimeType`(type-arg)/`format_runtime_types`/`extract_companion_types`/`TypeResolutionContext` + the lowering helpers (former B2). Drive `RESOLVED_ELEMENTS_FILE_ALLOWLIST` + `RESOLVE_TYPE_PATH_FILE_ALLOWLIST` to EMPTY.
- **B11** — delete session external-type rails (`external_macro_types.rs::collect_external_macro_types`, frontier ResolvedElements producers, `route_owned_shallow.rs`) + `SemanticNodeData::VueMacroElements` + `HostResolvedNamedTypeKey` + parser `NamedTypeCache`. **Its gate is SATISFIED by B4.5 (member visibility on the shared surface) + B5 (native_props re-sourced from the bundle's visibility-carrying surface)** — verify B5's native_props projection is the path B11 deletes the legacy `collect_native_props` rail in favor of. CARRY-FORWARD: the legacy eager-OXC native_props rail does NOT apply public-keyspace exclusion (`Partial<C>` etc.); B11 deletes it; B5's surface-read inherits the exclusion from B4.5.
- **B12** — final absence guards (no prod refs to the retired symbols); ledgers at empty floor; negative-guard test.
- **DEFERRED HP** (owner 2026-06-02): at END of roadmap, clear the 8 deterministic stale guards (update them to grep the current `ReadSetSignature` carrier shape — `fact_dep_signature:ReadSetSignature` / `read_set_signature:ReadSetSignature` / `validate_with_self_roots`) + de-flake the scheduler `bump_generation_supersede` test (handshake vs sleeps). Full brief in `D:/tmp/B6-LIVE-STATE.md`. Do NOT do mid-stage.

## METHODOLOGY / GOTCHAS (decisive this effort)
- **TRIPLE REVIEW on EVERY block/fix-round** (owner): codex#1 binding + codex#2 ADVERSARIAL + ONE Claude live-code, concurrent read-only (no cargo race with the gate). **codex#2 (adversarial) caught a real defect ~every round of B5 — NEVER skip it.** gemini unavailable for worktrees. When Claude says LAND but codex×2 say STOP, codex usually right (verify the split). 3/3 LAND = land.
- **codex CLI**: NOT on PATH → absolute `C:/Users/david/AppData/Local/OpenAI/Codex/bin/codex.exe`. Each as its OWN `run_in_background` Bash: `cat brief | timeout 3600 "C:/.../codex.exe" exec -s read-only -C <wt> -c approval_policy="never" -o D:/tmp/verdict.txt > D:/tmp/log.txt 2>&1`. read-only may suppress stdout on Windows → the verdict still lands in the `-o` file.
- **⚠ NO-SERENA IN WORKTREES (CRITICAL — bit fc2/fc3/fc5/fc6 of B5):** Serena symbol tools (find_symbol/replace_content/insert_after_symbol/replace_symbol_body) resolve against the activated MAIN repo, NOT a sub-agent's worktree → errant tracked mods on the integration working tree. MANDATE "use Edit/Write/Read/Grep on absolute worktree paths ONLY; do NOT use any `mcp__serena__*` editing tool" in EVERY implementer/reviewer brief. After EVERY worktree agent, run `git -C D:/dev/personal/verter status --short` and revert errant `M` files (`git checkout --`). (See memory `serena_worktree_contaminates_main_repo`.)
- **YOU own the full gate** (offload heavy investigations, not the gate). **Implementer agents keep LOSING/deferring their gate file — ALWAYS RE-RUN the gate yourself** and verify against the captured file. Re-baseline the known-failure set per block base.
- **KNOWN-FAILURE BASELINE (re-baseline per block):** 8 deterministic stale guards — `fact_matrix::compile_tier_signature_carries_{member,import_ref,member_presence,route_surface,module_augmentation_index_shape}`, `g_family::family_a_fact_validation::{family_a_entries_carry_fact_dep_signature,family_a_warm_hit_uses_fact_validation}`, `g_misc0::materialiser_observes_or_dies::materialise_structure_entry_carries_dep_signature` (the DEFERRED-HP set). Worktree env-flakes: `inline_execute_clears_all_install_tls_slots` (os-740), `synthetic-slot-binding.spec`, `typeinfo_ts_bindings_..._buf_output` (os-193 buf-in-worktree — for PROTO-TOUCHING blocks verify in MAIN repo post-land); `bump_generation_supersede` may flake. A failure IN this set = ignore; OUTSIDE = block-caused, investigate.
- **RIPPLE LESSON** (B6-B12 deletions/variant changes): `cargo check` does NOT flag a `_ =>` catch-all silently absorbing a changed variant — every ripple MUST grep ALL `match`-on-`SemanticQueryKey`/`TypeExpr` `_ =>` wildcards + TS `default:` switches. Distinguish ANALYZER-IR consumers (see raw variants) from DISPATCH-RAISED consumers (collapsed at `raise.rs`).
- **COMMIT-FIRST (restart survival — agents die/rate-limit ~hourly):** MANDATE in every implementer brief (edit → `cargo check -p` → `git add <explicit paths>` → commit IMMEDIATELY → then tests). On a dead/rate-limited agent: git-verify its banked commits (commit-first banks the work — B5 recovered 2 rate-limited/premature-ended agents this way), patch-backup any uncommitted WIP, dispatch a continuation if incomplete.
- **TRUST-BUT-VERIFY:** re-run the gate yourself; git-verify every agent report (commit SHAs, no-amend via `merge-base --is-ancestor`, scope, tree-clean, main-repo-clean); **verify CONTESTED findings against code** — empirical verification overrode a codex BINDING P0 false-positive in B5 (`EnumTupleVariant { .. }` rest-pattern is valid Rust; the gate was right).
- **WHEN A SOURCE-PARSE GUARD IS NON-CONVERGENT (keeps finding new forms):** escalate to STRUCTURAL/BY-CONSTRUCTION enforcement via a binding codex strategy consult (B5: source-parse macro-payload guard → `MacroPayloadCarrier` newtype + sealed `ResolveMacroPayloadKey` opaque key = compiler-enforced). If even the by-construction primary has a marginal defense-in-depth source-parse residual: close the named forms, then NAME the residual + credit the compiler-enforced primary — do NOT chase forms indefinitely (per `feedback_unify_dont_patch`).
- **LAND is NOT atomic** — verify each step (rebase-if-moved + reconcile-keep-best + cargo check + FULL gate + PERF gate BEFORE ff-merge; checkout audit.generated.ts; merge --ff-only; verify `<HEAD>..branch` EMPTY; remove worktree + delete branch). Keep `D:/tmp/B6-LIVE-STATE.md` CURRENT every turn.

## STANDING OWNER DIRECTIVES (binding)
- (2026-06-03) "before starting a new phase, **rebase and port PROPERLY, make sure there's NO REGRESSION, keep the BEST OF THE BEST**" — applies to the B5 land + EVERY B6-B12 block land (correctness gate + perf gate + reconcile-keep-best).
- (2026-06-03) "**whenever a phase LANDS (e.g. 3/3 reviewers LAND), STOP and write a HANDOFF prompt for a new agent.**" → At each block's 3/3 LAND, consider handing off (especially if your context is long). Default otherwise: continue the plan autonomously; surface to owner only on (a) full-roadmap completion, (b) a plan-invalidating/scope discovery, or (c) a cross-effort collision on the shared/main checkout you cannot resolve without disturbing another effort's uncommitted work.
- Owner overrides codex; adopt the strongest architecture (no shims/legacy/stubs/shortcuts); no deferred work when doable now; every found case gets a discriminating test + fix; static guards name residuals, never over-claim.

## ARTIFACTS
- `docs/arch/stage5-cutover-plan.md` (this dir) — the codex-bound 12-block plan + sign-off corrections.
- `D:/tmp/B6-LIVE-STATE.md` — PRIMARY recovery anchor (KEEP CURRENT every turn; not checked in).
- `D:/tmp/s5b5-fix{1..11}-brief.md` (B5 fix-cycle briefs), `s5b5-design-verdict.txt` / `s5b5-guard-strategy-verdict.txt` / `s5b5-guard2-strategy-verdict.txt` (binding strategy verdicts), `s5b5-re{N}-codex{1,2}-verdict.txt` (per-round review verdicts). The `D:/wt/s5b5` worktree is removed at land — recreate for B6 off the new HEAD.

**Drive autonomously to completion; no user check-ins on round count/cost. Each STOP feeds the next codex consult; codex's verdict becomes the next brief. Adopt the strongest architecture (consult codex on doubt; owner overrides codex). Honor the standing land directive (rebase-properly + no-regression + perf-gate + keep-the-best) on EVERY block land. Keep `D:/tmp/B6-LIVE-STATE.md` current every turn.**
