# Determinism matrix status — Verter Semantic Signature Kernel

The §5.9 mandatory determinism matrix of
[`docs/arch/signature-kernel.md`](../../arch/signature-kernel.md), as it is
actually driven by the tree.

**Registration is executable, not prose.** The row table lives in
`crates/verter_session/tests/cases/g_block/semantic_determinism_matrix.rs` and
is enumerated against the contract's own §5.9 table by
`determinism_matrix_enumerates_every_5_9_row`: a perturbation that is lost,
invented, renamed, or left without a driver fails there. This document is a
reader's index over that registration; it is never the authority, and it
records no run counts, timings or machine-bound numbers — those belong to a
run report, not to the tracked tree.

## Comparison bases

Every replay driver compares on **two independent bases**, so a
schedule-dependent answer cannot hide behind an unstable print, nor behind an
observation that stays fixed while the generated bytes drift:

1. **Completed observation** — the semantic-graph projection rendered to stable
   text (kinds, names, literal values, structural order). Never node ids,
   pointers or allocation ordinals.
2. **Generated-bytes digest** — the implementation's own content digests over
   the artifacts it generated from the upserted sources.

## Row status

| Row | Perturbation | Status |
|---|---|---|
| DET-01 | Same files arrive forward / reverse / shuffled | Ready — `det_01_forward_reverse_random_arrival` |
| DET-02 | Lazy load vs. eager preload; queried in different orders | Ready — `det_02_lazy_vs_eager_query_order` |
| DET-03 | An unrelated type/literal is interned or queried first | Ready — `det_03_unrelated_interning_first` |
| DET-04 | Worker counts 1/2/4/8, randomized delays/work stealing, duplicate publishers | Ready — `det_04_worker_counts_1_2_4_8`, reinforced by `signature_kernel_interned_identities_are_schedule_independent` (duplicate publishers and opposite intern orders at each worker count) |
| DET-05 | Cold, warm, partial persisted cache, edit/revert, cancelled/retried work, epoch rebuild | **Ignored** — the cold/warm/edit-revert subset IS driven in the ignored body; the partial-persisted-cache, cancelled/retried-work and epoch-rebuild axes have no public drivable surface |
| DET-06 | Hash seed changes, equal prefixes, forced full stable-fingerprint collisions | Ready — `det_06_hash_seed_and_forced_collisions` |
| DET-07 | Duplicate shapes with distinct origins, anonymous/virtual units, recursive binders | Ready — `det_07_duplicate_origins_and_anonymous_units` |
| DET-08 | Contextual body demands with the same descriptor/type arguments | **Ignored** — a narrower (contextual) return demand fails closed with `UnmodeledDemandPoint`; only the canonical whole-return demand point is answerable, so distinct coexisting result memo entries under one descriptor cannot be replayed |
| DET-09 | Policy changes with resident parent caches | **Ignored** — no public surface mutates effective semantic policy on a live host; the formatting-only-edit axis IS driven in the ignored body |
| DET-10 | Logical checkout relocation with fixed mappings | Ready — `det_10_relocation` |
| DET-11 | Authored overload/intersection order deliberately changed | Ready — `det_11_authored_reorder_detected` |
| DET-12 | Same output requested after different preceding unrelated outputs | Ready — `det_12_unrelated_preceding_outputs` |

Two further drivers ride the same file without being §5.9 rows:
`global_contributor_ingest_order_is_declaration_authority` (opposite ingest
order of two `declare global` contributors yields one precedence) and the
schedule-independence driver named under DET-04.

## The three ignored rows

An ignored row is **not** an empty body. Each asserts the known state — the
drivable subset, or the typed refusal that keeps the row undrivable — so the
change that un-ignores it inherits a real body rather than a stub, and the
registration assertion requires the reason to name the **missing public
surface** rather than a coordination identifier.

| Row | What must exist before it can claim Ready |
|---|---|
| DET-05 | A public cache-persistence surface (to load a PARTIAL persisted cache), a deterministic public request-cancellation drive, and a public epoch-rebuild entry point. `SignatureStore::replace_epoch` and its retirement policy are exercised directly by `signature_kernel::lifetime_tests`, but the store is crate-private and not reachable from a consumer-level replay. |
| DET-08 | A demand lattice that models narrower-than-whole-return demand points. The body asserts the current typed refusal (`FlowReturnFailure::UnmodeledDemandPoint`) and FAILS the moment a contextual demand answers — that failure is the un-ignore signal, so the row cannot rot silently. |
| DET-09 | A public surface for mutating effective semantic policy on a live host, plus the parent-cache invalidation that follows it. |

## §5.4 stable-key table

The same file carries the §5.4 stable-key registration. It is enumerated
against the **live** `SemanticNodeData` declaration by
`stable_key_table_enumerates_every_semantic_node_data_category`: a new variant
cannot land without a registration row in the same change.

Every variant now has a `VerterStableV1` encoding. Four rows carry a
`residual` — an identity input the encoder **approximates** rather than
consumes, so the row's identity claim is weaker than its `inputs` column reads:

| Variant | Residual |
|---|---|
| `DeferredCallable` | The encoder walks the carrier type arguments only; the deferred subject reference is not an encoding input. |
| `ImportType` | The encoder consumes the authored specifier text, not a resolved module logical identity. |
| `RawFallback` | The encoder serialises the opaque payload's `Debug` form rather than a stable reference to the failed input. |
| `SyntheticBinding` | The encoder serialises the content-free binding id's `Debug` form; the synthesizing operation's owner/role anchor is not an encoding input. |

## Class 3 admission

An order-induced difference (§5.8 class 3) is admitted to
[`semantic-difference-ledger.md`](semantic-difference-ledger.md) only with the
contract's counterfactual: only semantic union traversal order changed, in an
isolated cache namespace (parents included), and restoring the stable order
recovers the recorded observation. No class-3 row exists.

Serialising node allocation to make a replay agree is forbidden by §12 and is
not how any row above is satisfied.
