# Semantic-difference ledger — Verter Semantic Signature Kernel

The four-class release authority of `docs/arch/signature-kernel.md` §5.8.
Every row recorded here is executable evidence: each names its subject,
both sides of the difference, and where the recorded bytes live. Classes:

1. **exact agreement** — Verter's completed observation equals the
   checker's byte-for-byte or through the one canonical checker-syntax
   projection.
2. **presentation-only** — the printed forms differ while every
   binding/callable effect agrees (verified, not assumed).
3. **`VerterStableV1` order-induced** — a difference attributable to
   semantic union traversal order, with the causal proof §5.8 demands
   (order-only counterfactual in an isolated cache namespace).
4. **independent semantic difference / incompleteness** — anything
   else, including checker-version-to-checker-version differences and
   Verter's typed gaps. Rows stay here until classified.

A row is re-classified only by moving it, never by deleting it. The
bundled `SDL-5..27` row that once stood for every owed corpus row was
SPLIT into `SDL-5`..`SDL-12`, one per owed capability, when the corpus
driver began asking its probes the question a consumer asks; no row left
the ledger in that split. Every row of the split has since moved:

- `SDL-5` to Class 1 — a defect in the kernel's intersection-signature
  dedup, not an owed capability;
- `SDL-6` to Class 1, once class expressions, the checker's mixin rule and
  the instantiation of declared signatures were implemented;
- `SDL-7` and `SDL-9` to Class 1, once the corpus lane printed at the
  checker's altitude (the semantics already agreed);
- `SDL-8` to Class 1, once signatures carried their type predicate;
- `SDL-10` to Class 1 — a path-walker defect;
- `SDL-11` to Class 1 — a signature-utility defect, plus a display-only
  flag the checker column never needed;
- `SDL-12` to Class 3 — its members agree, and its order is the mandated
  `VerterStableV1` order, proven by an order-only counterfactual.

`SDL-13`, recorded later, moved to Class 1 once the flow-return lane
answered under the function's own `strictNullChecks`, and `SDL-1` once a
declared union's reduction read `boolean` as `true | false`. `SDL-4`, recorded
with the corpus, moved to Class 1 once the awaited relations applied the
checker's own recursive-thenable rules (TS1062 in the runtime relation,
TS2589 in the lib conditional) and published the error type each operation
recovers with as a typed carrier. The corpus driver's flip law
(`signature_corpus_flip_law_fires_in_both_directions`, and
`diagnostic_rows_flip_on_the_checker_recovery_in_both_directions` for a row
that records a diagnostic) makes each re-classification deliberate: a
`MatchesChecker` row fails when the live answer stops matching, and an owed
row fails when the live answer STARTS matching.

`SDL-14`..`SDL-18`, recorded with the declaration-heritage fix, moved to
Class 1 once call resolution read TypeScript's candidate order, merged
declarations kept every signature, indexed accesses read optional
properties the checker's way, class heritage resolved through a value, and
mapped types instantiated over their type variable. `SDL-19`..`SDL-23` are
pre-existing differences measured on the way; `SDL-24` is presentation-only.
`SDL-19` has since moved to Class 1, once a namespace member path and a
global declared in several files resolved to their declarations.
`SDL-20`, `SDL-21` and `SDL-23` have since moved to Class 1, once the
canonical intersection distributed over a union, the relation read
intersection targets, mapped operands and settled `keyof`s, and the path
walker read tuple lengths, rest positions and apparent members. `SDL-25`,
measured on that work, is presentation-only; `SDL-26` and `SDL-27`,
measured on it too, have since moved to Class 1, once a class constructor
type carried its `prototype` property and the path walker read index
signatures. `SDL-78`, measured on that work, is
presentation-only, and `SDL-79` and `SDL-80`, measured on it too, agree. `SDL-28` and `SDL-29`, measured with
the flow-return lane's loop and relation work, are presentation-only,
`SDL-30` is order-induced, and `SDL-31` recorded that lane's remaining
residuals.
`SDL-2` and `SDL-3` moved out of Class 4 into the checker-version history
note: both sides answer `never` on 7.0.2.
`SDL-22` moved to Class 1 once a class heritage expression, and a module
constant's call or class-expression initializer, resolved to its value.
`SDL-24` moved to Class 1 once an indexed access that reads a named
declaration printed it by that name.
Five of `SDL-31`'s residuals — the object-literal union normalisation,
the user intersection alias, the mapped index source, the generic call
over a conditional argument and the consumers of a flow-return value —
moved to Class 1 once the lane widened object-literal unions with their
siblings' missing properties, reduced intersection-alias applications,
related a mapped source through its index signature and lowered call
arguments and consumers in the frame. Its arithmetic operands,
destructuring elements, member-path narrowing, logical writes and
uninitialized `let` reads moved to Class 1 once the lane evaluated
operators, binding elements, member-path assignments and logical
operands along the checker's flow graph and read an uninitialized
local's auto type. Its last residual, the evolving array's `push`, moved
to Class 1 once the lane followed the checker's evolving-array
operations, so `SDL-31` has no residual left and Class 4 is empty.
`SDL-32`..`SDL-41`, measured on that work, are exact agreement except
`SDL-40`, which is presentation-only.
`SDL-42`..`SDL-54`, measured with the closure-capture, callee-signature
and template-literal work, are exact agreement except `SDL-52`, which is
presentation-only.
`SDL-55`..`SDL-57`, measured with the callee schedule's argument,
type-position and local-arrow chains, are exact agreement.
`SDL-58` and `SDL-59`, measured with the instantiated-return work, are
exact agreement.
`SDL-60`..`SDL-72`, exact agreement, record the differences measured and
resolved with the global-resolution work: declaration-file namespaces, a script's block-scoped
`var`, values assigned with `export =`, widening reads of a module `const`,
globals a library declares, module objects, values of ambient modules,
functions declared in a namespace, `static readonly` literals and nullish
declarations without `strictNullChecks`, the wrapper a project declares,
a member call on a shadowing binding, global and uninstantiated namespaces
with callable `export =` values, and merged function declarations.
`SDL-73`..`SDL-77`, exact agreement, record the enum
literal types: their print, their relations, the widening of a fresh
member, narrowing by a member, and member value evaluation.
`SDL-81`..`SDL-85`, measured with the class self-reference work (static
members and literal methods reading their own declaration, abstract
construct signatures, local function declarations), are exact agreement
except `SDL-83`, which is presentation-only.
`SDL-86` and `SDL-87`, presentation-only, record the print of a returned
enum object, whose reverse mapping the lane now carries, and of a returned
class value.

Companion evidence: [`determinism-matrix.md`](determinism-matrix.md)
(the §5.9 replay status) and [`performance-gates.md`](performance-gates.md)
(the §12 structural gates and what is not measured).

## Class 1 — exact agreement

| Subject | Evidence |
|---|---|
| The 344 `u6_flow_shape_corpus_rows_tests` checker columns | Re-measured on the installed TypeScript 7.0.2 executable on the implementing host: 344/344 byte-identical to the recorded columns (the `E01_spread_any` row verified through its `IsAny` leg: `any`→`null` is error-free, so the leg firing `true` is the print). The port from `tsgo 7.0.0-dev.20260526.1` changed no recorded checker value. |
| The oracle snapshot tree (93 files) | Regenerated with the pinned 7.0.2 engine via the `oracle_gen` binary: every snapshot rewrote byte-identically, zero stale files pruned. Recorded in `docs/evidence/signature-kernel/manifest.json` (`port_witness`). |
| Signature corpus `MatchesChecker` rows | **25 of 26**: `SV01`, `SV02`, `SV03`, `SV04`, `SV05`, `SV06`, `SV07`, `SV08`, `SV09`, `SV10`, `SV11`, `SV12`, `SV13`, `SV14`, `SV15`, `SV16`, `SV17`, `SV18`, `SV19`, `SV20`, `SV21`, `SV22`, `SV23`, `SV24`, `SV26` — the union-valued and mixed-arm `then` residuals, the construct-signature intersection instance side, the mixin construct intersection over a class expression, the type-predicate and assertion signatures, both generic-default applications, explicit type arguments, the nested alias instantiation, the constrained indexed access, both 7.0.2 grouping witnesses, the transparent intersection group, all ten Awaited residuals, and the literal/generic union probe. The live answers are structural matches of the recorded 7.0.2 observations (`SV21` records a diagnostic, matched by the typed recovery carrier naming it — see `SDL-4` below), compared through the typed checker-syntax projection at the altitude the checker prints (utility and conditional applications reduced; interfaces, classes and the alias applications the checker names kept by name, with omitted defaulted arguments filled). |
| `SV03_construct_intersection` (formerly `SDL-5`) | Moved from Class 4. `InstanceType<typeof CtorA & typeof CtorB>` is `B` on 7.0.2 and the live answer was `A`: the kernel's intersection-signature dedup compared signatures with their RESULTS IGNORED, so `new () => A` and `new () => B` collapsed onto the first. The checker's `appendSignatures` compares with `ignoreReturnTypes: false`, re-measured on the pinned 7.0.2 for both construct (`InstanceType`) and call (`ReturnType`) intersections, and for the converse: two signatures identical including their result still collapse. Held by `signature_kernel::discovery_tests::intersection_keeps_signatures_that_differ_only_in_their_result`. |
| `SV04_mixin_intersection` (formerly `SDL-6`) | Moved from Class 4. `InstanceType<ReturnType<typeof Mixin<typeof BaseCtor>>>` over `function Mixin<S extends new (...args: any[]) => Base>(Base: S) { return class extends Base { extra = 1; }; }` prints `Mixin.(Anonymous class) & Base` on 7.0.2; the live answer was a typed gap (`Opaque(Miss)`, degraded). Four missing pieces, each measured on the pinned 7.0.2: (1) a class expression in a function body had no type — the flow lane now composes its constructor type (construct signatures from the declared constructor, else the base constructor's; static members) over an instance that carries the class's own identity (`ClassExpressionInstance`, printed `Mixin.(Anonymous class)`) and its own members over the base instance, intersected with a type-variable base (`getBaseTypeVariableOfClass`); (2) `typeof f<T>` over a function DECLARATION was `Opaque(Miss)` — an instantiation expression now instantiates every signature the declaration carries whose type-parameter list accepts the arguments (`ReturnType<typeof G<string>>` is `string`); (3) `InstanceType` and `ConstructorParameters` read authored signature nodes only and refused the composed mixin candidate — they now read the shared candidate list's node form; (4) the kernel kept every mixin member's own construct signature — it now applies `resolveIntersectionTypeMembers`: a mixin constructor contributes no signature of its own and mixes its instance into the others', and when every constructor is a mixin the first keeps its signature (`typeof CtorB & typeof MixA` constructs `B & A` from `[s: string]`). Held by `signature_discovery_tests::mixin_construct_intersections_follow_the_checker_mixin_rule` and the `flow_return_coverage_tests` class-expression rows (`mixin_class_expression_composes_with_its_instantiated_base`, `declared_mixin_factory_instance_is_its_constructor_result_over_the_base`, `instantiation_expression_instantiates_a_declarations_signatures`). The `Mixin.` qualifier is the reference instantiating the factory's clause — a reference read inside the factory's body stays `(Anonymous class)` — held by the `flow_return_class_tests` rows (`bare_factory_return_reads_each_parameter_at_its_constraint`, `class_expression_reference_in_its_own_body_prints_unqualified`). |
| `SV05_generic_default`, `SV06_default_references_binder` (formerly `SDL-7`) | Moved from Class 4. The semantics already agreed: `Instantiate` binds an omitted parameter to its declared default, instantiated under the arguments before it (`WithDefault` resolves to `{ value: string }`, `Chain<number>` to `{ self: number; others: number[] }`). The difference was the comparison altitude. 7.0.2 names an alias application the alias constructs by the alias, with the omitted defaults filled (`WithDefault<string>`, `Chain<number, number[]>`), while the corpus lane stopped at the bare reference (`DeclRef(WithDefault)`) or reduced the application to its expansion. The lane now prints at the checker's altitude: the alias application, its defaults filled through the same binding `Instantiate` applies. Held by `signature_corpus_tests::the_print_altitude_names_declaration_applications_as_the_checker_does` (42 prints measured on 7.0.2: object, function, mapped, array, tuple, union and intersection aliases by name; conditional, bare-parameter and primitive aliases, and a union collapsed to one member, as what they resolve to; a generic interface with its default filled; an alias of a non-generic interface, class or alias as that declaration (`type ToFace = Face` prints `Face`, `type ToToObj = ToObj` prints `ObjNoGen`), while an alias of a generic interface application keeps its own name (`ToGFace`, `PromAlias<number>`); the mapped utilities `Partial` / `Readonly` / `Pick` / `Record` / `Omit` by their names, an alias of a homomorphic one taking the mapped name unless its declared source is a union (`type P<T> = Partial<T>` prints `P<{ a: 1 }>` as `Partial<{ a: 1; }>`, `type MpA<T> = Mp<T>` prints `Mp<{ a: 1; }>`, `type PU = Partial<Face \| ObjNoGen>` prints `PU`) and an alias of a keyed one keeping its own (`PickA<{ a: 1; b: 2; }>`, `Rec<"x">`)). |
| `SV07_type_predicate`, `SV08_assertion_signature` (formerly `SDL-8`) | Moved from Class 4. 7.0.2 prints `(x: unknown) => x is Foo` and `(x: unknown) => asserts x is Bar`. The live answer used to be the callable surface with its parameter intact and only the RETURN lost — `{ (x: unknown) => Opaque(Miss) }`, degraded — because the predicate annotation lowered to an unsupported-syntax raw fallback. (The earlier `{  }` wording was a rendering artifact: the corpus diagnostic renderer printed no call signatures.) Signatures now carry TypeScript's `TypePredicate` beside a `boolean` (type predicate) or `void` (assertion) return, and the corpus comparison applies the checker's display rule that an object whose only content is one call signature prints as a function type. Re-measured on 7.0.2: `ReturnType<typeof isFoo>` is `boolean`, `ReturnType<typeof assertBar>` is `void`, and `isT<string>` over `isT<T>(x: unknown): x is T` prints `(x: unknown) => x is string`. Held by `project_semantic_dispatch::signature_predicate_tests`. |
| `SV10_nested_instantiation` (formerly `SDL-9`) | Moved from Class 4. The same altitude rule: `type G<U> = F<U[]>` constructs its type through `F`, and 7.0.2 names the OUTERMOST alias, `G<boolean>`; the expansion `{ f: boolean[] }` already agreed. The lane prints the alias application, so no presentation-only difference remains to classify. |
| `SV11_constrained_substitution` (formerly `SDL-10`) | Moved from Class 4. Not a constraint defect: call-site inference binds `T` to `{ a: 1; extra: string }`, and the witness return `{ a: 1; extra: string }['a']` reduced to `1` under every demand except a Shallow one (`published(Shallow)` and the relation engine's `structural_transit()`). The path walker ran the EMPTY-path surface synthesis on a projected path's terminal, so every surfaceless member type (`1`, `string`, `string[]`, `[1, 2]`, `{ b: 1 } \| 1`, `() => void`) became the empty surface `{}`, relation reads included. A projected terminal is now its own answer unless it has a surface, and a reference whose body contributes none keeps the reference. Held by `project_semantic_dispatch::projected_terminal_surface_tests`. |
| `SV23_awaited_deferred_generic`, `SV24_awaited_constrained_generic` (formerly `SDL-11`) | Moved from Class 4. `SV23`'s checker column was never display-only: `ReturnType` of a generic signature reads the checker's base signature, each type parameter at its constraint (`unknown` when unconstrained), so `Awaited<unknown>` = `unknown` IS the answer and the row now compares against it. `SV24` had two defects. The signature utilities instantiated every type parameter at `unknown` and ignored its constraint (`ReturnType<typeof f>` for `f<T extends string>(v: T): T` was `unknown`; 7.0.2 prints `string`). And instantiating a builtin runtime nominal (`Promise<number>`, `Date`, …) produced a miss instead of its own application carrier, in production `normalize_node_for_structural_fact_demand` as well as in the lane. Both utility routes (the whole result and the `ReturnType<…>['k']` member lookup) now read the base signature: sibling constraints substituted for `N - 1` rounds, what is still referenced erased to `any`, a circular constraint (TS2313) dropped, a default never applied, each re-measured on 7.0.2. A runtime nominal instantiates to its carrier, which the callable realizer and the event-name enumerator read as a complete non-callable, as they read the miss. Held by `project_semantic_dispatch::base_signature_tests`, `component_meta_flow_return_admission_tests::return_type_member_of_constrained_callee_publishes_the_base_constraint` and the builtin-nominal cases in `meta_resolve::callable_view::tests`. |
| `SDL-13` — semantic policy on the flow-return lane (`strictNullChecks`, `noImplicitAny`) | Moved from Class 4, where the live answer was `string \| undefined` for `leaf` and `string \| null` for `(v: string \| null) => v` under BOTH settings. The flow-return key states the function's OWN project's policy — `FlowReturnPolicy::nullability` (`strictNullChecks`) and `FlowReturnPolicy::no_implicit_any` (`noImplicitAny`), each read from that file's effective options and also folded into `type_env_hash` — and the lane answers under it: an optional parameter, an optional member read, an optional chain, an optional destructured element, a bare return and a fall-through add `undefined` only under `strictNullChecks`; with it off every union the evaluation builds runs the erased algebra (`NullabilityPolicy::Erased`: `null` / `undefined` are dropped beside any other member — family identity on `ReduceUnion` and recorded on the canonical stamp, so the two settings share no memo entry, node or pre-seal skip), a value entering the flow is erased through its inline structure (a declared `{ a: string \| null }`, `(string \| null)[]`, `[string \| null]` or `(x: string \| null) => void` reads `{ a: string }`, `string[]`, `[string]`, `(x: string) => void`) and through a generic application's type arguments (`Box<string \| null>` is `Box<string>`, which distributes over `string` alone; `Record<"k", string \| null>` is `Record<"k", string>`; `wrapOf(v)` over `Wrap<T \| null>` is `Wrap<string>`), an application whose union loses its nullable members is that union unless the checker still names it by its alias (`Opt<string>` over `T \| undefined` is `string`, `Opt<string \| number>` is `string \| number`, `OptNum<string>` over `T \| undefined \| number` stays `OptNum<string>`), a conditional's `null extends T` check decides in the function's own relation environment (`IsNullable<string>` is `true`), the lone fresh literal left behind widens, and a bare `null` / `undefined` / `void` value is the checker's WIDENING nullable type: alone in a return or a yield (sync or async) it widens to `any`, nested in an object or array literal it widens with the literal (`{ a: null }` is `{ a: any }`, `[null]` is `any[]`, `[null, 1]` is `number[]`, `{ a: null } as const` is `{ readonly a: any }`), and a `const` initialised to one is declared `any`; a declared `null` / `undefined` (a parameter, an annotated local, an assertion) never widens. Under both settings a free `undefined` reads as the `undefined` type and `void x` as `undefined` whatever its operand: a call that IS the operand is never entered into control flow (the checker binds only a statement's own call or a comma operand), so it needs no certification and narrows nothing, and `void (x = v);` retypes `x` as the bare write does. An unannotated `let` / `var` with no initializer or a bare `null` / free `undefined` one is AUTO-TYPED under `noImplicitAny`: it holds `undefined` (no initializer) or its initializer's value until a write retypes it, the widening fact riding the reaching-type product so it joins per PATH — where a widening path meets a path holding a declared nullable or the initializer-less `undefined` the value is that non-widening one, and only an all-widening join widens to `any`. Without `noImplicitAny` such a variable — and under both settings one initialised to `void 0` or a nullable conditional — is DECLARED as its initializer's widened type (`any` with no initializer, `null` / `undefined` strict and `any` off), which no write retypes. An array literal is structural in both halves (the shared classifier's `ValueDescent::Array`, one child site per element): `[v]` over a parameter or a local reads the frame, the value is `E[]` with `E` the subtype-reduced union of the elements read at a mutable location (a spread contributing its source's elements — a union source every arm's, a string literal's `string` —, a hole the widening `undefined`, `[]` `never[]` or `any[]` off), and under `as const` the readonly tuple of the pinned elements (nested literals const, a spread spliced with its optional elements `\| undefined` when strict). An unannotated declaration initialised to an empty array literal is the checker's EVOLVING array under `noImplicitAny` (`autoArrayType`; not through parentheses — `const a = ([])` is `never[]` on 7.0.2): its element type grows only at a `push` / `unshift` call on it (each argument's literal base type, a spread's element types, nothing for `never`) and at an element write `a[i] = v` under a number-like index, and every other reference FINALIZES it there — the array of the elements added so far, `any[]` with none (`const a = []; return a` is `any[]` and `[a]` `any[][]`, with TS7034 at the declaration and TS7005 at the read; `const b = a; a.push(1)` leaves `b` `any[]`; `a.push(1); a.push("s")` is `(string \| number)[]`) — while the array goes on evolving past it. A write to it follows the `autoArrayType` rule: an unparenthesized `[]` starts a new evolving array, any other value is held when assignable to `any[]` (else `any[]`), and a path holding an ordinary value joins an evolving one finalized under subtype reduction (`let a = []; if (c) a = [1]; return a` is `any[]`). Paths meet by the union of their elements, a conditional or logical expression branching at its test in statement and value position; in a loop each reference's head type is its entry type joined with one pass of the iteration in which it reads its own entry type and every other reference its own head type (`a.push(a)` over `a.push(1)` is `(number \| number[])[]`); and a nested function reads a captured one as its declared `any[]`, unless it is a `let` past its last assignment, whose array the function continues and evolves (`let a = []; a.push(1); return () => { a.push("s"); return a; }` is `() => (string \| number)[]`). Without `noImplicitAny` it is declared the ordinary `[]` (`never[]`, `any[]` off), which no operation retypes. A statement-position `yield x` is a demand origin like a return argument, so a yield of a local, a narrowed parameter or an object reads its value. Every join is subtype-reduced — a return join, an array literal's elements and a generator's yields alike, array types relating covariantly in their element (`number[]` beside `any[]` is `any[]`, `string[]` beside `(string \| number)[]` is `(string \| number)[]`) — and compares an object or tuple literal BEFORE its bare `null` / `undefined` members widen, as the checker widens only the reduced union (`{ a: null }` beside `{ a: "s" }` is `{ a: string }` off, where the widened `{ a: any }` would have absorbed its peer), after the checker's discriminant shortcut: an arm whose first unit-typed property is a different unit type from its peer's is never absorbed (`[null] as const` beside `["s"] as const` stays `readonly [any] \| readonly ["s"]` off, though `null` relates to `"s"` there). Every relation the evaluation opens is decided under the function's own project, whichever request demanded it and with no request at all. Measured on the pinned 7.0.2 checker (`tsc --noEmit --strict`, with `--strictNullChecks false` and `--noImplicitAny false` in each combination, the type read from the checker's own error message quoting `ReturnType<typeof f>` — TS2741, or TS2322 against a `never` probe — and the `.d.ts` only for a `never` answer; the `.d.ts` printer re-derives a nested literal's type from its initializer — `{ a: [undefined] }` prints `{ a: undefined[] }` there while the checker holds `{ a: any[] }` — so the checker is the basis), on and off: `leaf` `string \| undefined` / `string`; `nullable` `string \| null` / `string`; `twoArms` `"a" \| null` / `string`; `fallthrough` `1 \| undefined` / `number`; `nested` `{ a: string \| null }` / `{ a: string }`; `loneNull` `null` / `any`; an optional member read `string \| undefined` / `string`; a declared `string \| null` member read `string \| null` / `string`; a same-project caller of `leaf` `string \| undefined` / `string`; `{ a: null }` `{ a: null }` / `{ a: any }`; `[null]` `null[]` / `any[]`; `{ a: [undefined] }` `{ a: undefined[] }` / `{ a: any[] }`; `let x = null; return x` and `const y = undefined; return y` `null` and `undefined` / `any`; `const y: null = null; return y` `null` / `null`; `function* g() { yield null; }` `Generator<null, void, unknown>` / `Generator<any, void, unknown>` (async: `AsyncGenerator<…>` alike); `return undefined` and `return void 0` `undefined` / `any`; `(v: string) => [v]` `string[]` / `string[]`; `(v: string \| null) => [v]` `(string \| null)[]` / `string[]`; `[]` `never[]` / `any[]`; `(v: string) => [v] as const` `readonly [string]`; `function* (v: string) { const n = 1; yield v; yield n; }` `Generator<string \| 1, void, unknown>`; `return void f()` over an `any` callee `undefined` / `any`; `let x = null; if (c) x = "s"; return x` `string \| null` / `string` / `null` / `any` (the four projects: strict, off, strict without `noImplicitAny`, off without it); `let x; if (c) x = "s"; return x` `string \| undefined` / `string` / `any` / `any`; `let x = null; if (c) x = v; else x = null; return x` over `v: null` `null` / `null` / `null` / `any`; `Opt<string>` `Opt<string>` / `string`; `Box<string \| null>` `{ v: string }` off; `if (c) return { a: null }; return { a: "s" }` `{ a: null } \| { a: string }` / `{ a: string }`; `if (c) return [null] as const; return ["s"] as const` `readonly [null] \| readonly ["s"]` / `readonly [any] \| readonly ["s"]`. The live answers equal every measured row — the 22-row first table and its caller, 44 nested-literal / local / declared-structure rows, 12 yield and bare-`undefined` rows, 70 array-literal rows, 36 generator rows, 21 `void` rows, 31 generic-application rows, 11 conditional-application prints and 21 object-join rows on both settings, and the 40 auto-typed and declared-local rows on all four `strictNullChecks` × `noImplicitAny` projects (six of them TS2322 programs without `noImplicitAny`, whose declared type the checker keeps), one object-join print presentation-only (`SDL-28`) — and the subtype reduction `{ x: string } \| { x: null }` / `{ x: string }` follows the callee's own project from a caller in the other one. Held by `project_semantic_dispatch::flow_return_null_policy_tests` (`flow_returns_follow_their_own_projects_strict_null_checks`, `nested_and_local_nullish_values_widen_with_strict_null_checks_off`, `yields_and_bare_undefined_returns_follow_strict_null_checks`, `array_literals_infer_from_their_element_values`, `generator_yields_of_parameters_and_locals_join_like_returns`, `yields_the_join_does_not_model_never_publish_a_partial_yield_type`, `void_values_are_undefined_whatever_the_operand`, `implicit_any_and_auto_typed_locals_follow_both_policies`, `flow_policy_states_the_functions_own_no_implicit_any`, `shared_type_applications_follow_the_functions_own_algebra`, `conditional_applications_decide_under_the_functions_own_algebra`, `object_arms_reduce_to_their_supertype_before_widening`, `reduce_union_null_policies_do_not_warm_hit`, `canonical_stamp_is_scoped_to_its_null_algebra`, `relation_verdicts_follow_the_functions_own_file`, `relation_verdicts_without_a_request_use_the_functions_own_file`) and `flow_return_coverage_tests::an_empty_array_initializer_is_the_checkers_evolving_array`, and the 127 evolving-array and 4 array-join rows on all four projects (`evolving_array_operations_grow_the_element_type`, `a_reference_finalizes_an_evolving_array_where_it_reads`, `writes_to_an_evolving_array_follow_the_auto_array_rule`, `evolving_arrays_join_where_paths_meet`, `evolving_arrays_in_loops_take_the_checkers_loop_head`, `closures_read_a_captured_evolving_array_by_the_capture_rule`, `array_joins_reduce_by_the_covariant_subtype_relation`); an arithmetic binary expression takes the checker's operand rule (`i + s` over `number` and `string` is `string`, `b + i` over `boolean` is the error `any`, `a - i` over `bigint` and `number` is `any`), an assignment or update through a type assertion is no assignment (`(x as any) = 5` leaves `x` narrowed as it was, while `x! = 5` retypes it), writes to ordinary variables in a loop take each reference's loop head rather than the loop refusal (`let x: string \| number = 0; while (n-- > 0) x = "s"` is `string \| number`), and a logical expression whose right operand writes branches at its left operand (`c && a.push(1)` is `number \| false`, `number` off), held by the 72 measured rows of `arithmetic_expressions_take_the_checkers_operand_rule`, `assignments_through_a_type_assertion_are_not_assignments`, `loop_writes_take_each_references_loop_head` and `logical_expressions_that_write_branch_at_their_left_operand`. A loop types each written reference at its head from its own write dependencies (`loop_heads_follow_each_references_dependencies`, one pass per head: twelve independent counters take thirteen passes), a test's updates apply where the test evaluates (`test_updates_apply_where_the_test_evaluates`), and a closure extends past a `let`'s last assignment by the assignment's statement extent, member writes not counting (`closures_extend_past_the_last_assignment_by_its_extent`). An unannotated `let` is declared as its widened initializer's type and a type assertion's type is not fresh, so `x = 1` over `let x = 0 as 0 \| 1 \| 2` is `1` and a loop entered at the declared type keeps it (`i++` over `0 as 0 \| 1` leaves `0 \| 1`): `assignments_reduce_against_an_asserted_declared_type` (25 rows), `loops_writing_only_their_induction_variable_take_the_loop_head` (5 rows). An array, readonly array or tuple reads its members through the global wrapper and its elements at an integer literal (`a.pop()` over `const a = []; a.push(1)` is `number \| undefined`, `t[1]` over `[number, string]` `string`): `array_members_read_through_the_apparent_wrapper` (25 rows). |
| `SDL-4` — recursive thenable (`SV21_awaited_recursive_thenable`) | Moved from Class 4, where the live answer was the typed gap `Opaque(Miss)`: the lib conditional could not follow the thenable's self-reference (a declaration's body lowers `Rec` inside `Rec` as the `RecursiveRef` sentinel) and every async position degraded. Measured on the pinned 7.0.2 checker (`tsc --noEmit --strict --target es2022`; each type read through the TUPLE wrapper `[<probe>]` quoted by TS2322, because the checker's error type keeps both the two-step wrapper and the `IsAny` leg silent — SV21's probe prints nothing through either and `[any]` through the tuple). **The lib conditional** (TS2589, `Type instantiation is excessively deep and possibly infinite.`, at the `Awaited` reference): `Awaited<Rec>` (the corpus probe `Awaited<ReturnType<typeof witness>>`), `AwaitedOf<Rec>` over `type AwaitedOf<T> = Awaited<T>`, `Awaited<Promise<Rec>>`, `Awaited<PingThen>` (`PingThen` promises `PongThen`, which promises `PingThen`), `Awaited<SelfOrText>` (`then(onfulfilled: (v: SelfOrText \| string) => void)`), the generic `Awaited<GenericRec<string>>` (`then(onfulfilled: (v: GenericRec<T>) => void)`) and the growing `Awaited<GrowThen<string>>` (`then(onfulfilled: (v: GrowThen<[T]>) => void)`) are each `any`; the error type absorbs every union and branch on the way. The checker's limits, measured over chains of distinct thenables `C0 → … → number`: a tail run (a single callback into a non-union value) answers at 999 steps and is `any` at 1000; a run entered through a callback union answers at 998 further steps and fails at 999; a nested step (an optional or nullable callback, a union-typed `then`, every `Promise`, or a union value; both at once count twice) answers at 97 steps (some shapes also report TS2589 there while keeping the value) and is `any` at 98 — the same counts whether `Awaited` is written directly, through a generic alias, through a signature or over `ReturnType`. **The runtime relation** (`getAwaitedTypeNoAlias` and its `awaitedTypeStack`; TS1062, `Type is referenced directly or indirectly in the fulfillment callback of its own 'then' method.`, at the `await`, the async function's name, or the failing `yield` operand): `await` of `Rec`, `Promise<Rec>`, `PingThen` and `GenericRec<string>` is `any` (and `return [await r]` is `Promise<any[]>`), returning them from an async function `Promise<any>`, and so is the self-referential alias `type P = Promise<P>` returned; a recursive arm of an awaited union is DROPPED (`await` of `SelfOrText` and returning it are `string`, of `Rec \| number` `number`); an async generator's failing YIELD contributes no yield type (`yield r`, `yield r; yield r` and `yield r; yield ping` are `AsyncGenerator<never, void, unknown>`, `yield a` over `Rec \| number` is `AsyncGenerator<number, …>` and over `SelfOrText` `AsyncGenerator<string, …>`, while `yield await r` is `AsyncGenerator<any, …>`) while its failing RETURN is the error type (`return r` is `AsyncGenerator<never, any, unknown>`, `yield 1; return r` `AsyncGenerator<number, any, unknown>`); a sync generator awaits nothing (`Generator<Rec, void, unknown>`); a declared `Promise<Rec>` return keeps its declared type (TS1062 there checks the annotation). Two DISTINCT yields of which one fails (`yield r; yield n`, either order, or `yield r; yield "s"`) crash tsgo 7.0.2 itself (a nil-pointer panic in `getUnionTypeEx`), so that program has no oracle; the INTERNAL CONTRACT is the rule every answered program follows — the failing yield drops out of the join (`AsyncGenerator<number, void, unknown>`). Controls, no diagnostics: a thenable promising `number` is `number`; `GenericBox<number>` (promising its argument) is `number`; `interface Chain { next: Chain }`, `GenericTree<string>`, `Holder['box']` (a thenable inside `Holder` promising `Holder`) and a thenable holding itself inside the value it promises keep their own types. **Implementation**: the typed carrier `QueryError::CheckerRecovery(CheckerDiagnostic { code, operation })` is the checker's error type after one diagnostic — the §22 error type (it dominates the absorbers and relates like `any`), raised as its recovery (`any`), a complete answer. A self-reference inside a declaration's body records the instantiation it stands for (`RecursiveRef { name, args }`: `G<[T]>` inside the body of `G<string>` is the back-edge to `G<[string]>`), minted at the back-edge for an application with arguments as for one without, so both relations follow it as that application's carrier. The path walk follows the same carrier: a member read or an indexed access whose member is the self-reference reads the declaration and walks on through it (on 7.0.2 `a.next` over `interface Chain { next: Chain; v: number }` is `Chain`, `a.next.next.v` `number`, `Chain['next']` `Chain` and `Chain['next']['v']` `number`), where the walk used to end in a miss. The lib conditional evaluates its recursion the way the checker does — tail steps as a counted run, nested steps counted on the path — and answers the `ExcessivelyDeepInstantiation` / `LibAwaited` carrier at either limit (`LIB_AWAITED_TAIL_STEPS`, `LIB_AWAITED_NESTED_STEPS`), and at an application that recurs on its own path (it can never reach a value, so the count is certain to run out). Each runtime relation runs its TAIL steps — a `Promise<V>` payload, a thenable promising a single value, a declaration carrier's body, an awaited application's argument — in one loop, and re-enters its query only for a nested unwrap (a union arm); every operand a run reaches is on the path (the checker's `awaitedTypeStack`), and a step to an operand already on it fails with TS1062; the failure is dropped from a union and from an async generator's yield join, and otherwise the operation continues with the `RecursiveFulfillmentCallback` carrier naming `AwaitOperand` or `AsyncReturnPayload`. Answers built over a TS1062 failure are complete but ReturnOnly: what is in flight is a fact about the path, not the operand. The diagnostic rides wherever the checker's type is its error type; where the checker keeps a value (a dropped union arm or yield, a 97-step nested chain) the answer is that value. **Chain length and the operational budget.** The checker's runtime relation has no depth rule of its own: a chain of distinct thenables `C0 → … → number` awaits to its value at any length (5000 steps measured, 1.3 s; at 8, 40 and 200 steps `await` and an async return are `Promise<number>`, a yield `AsyncGenerator<number, void, unknown>`, an async generator's return `AsyncGenerator<never, number, unknown>`). The tail loop costs no query depth, so the same chains answer here. The operational budget is a separate, typed incompleteness for work this substrate cannot finish: every tail step is charged to the connected demand's work budget (262,144 units), and a cold chain charges 16 units per element plus 6 (measured at 15, 200, 1000 and 5000 elements, and at the boundary under a reduced budget: 50 elements answer within 806 units and 51 do not), so chains of up to 16,383 elements answer and a longer one stops as a typed partial. Genuinely unbounded work stops the same way: neither `await` of a `GrowThen<string>` nor returning one from an async function terminated within 25 minutes (0.9 GB and climbing) on tsc 7.0.2, so no oracle answer exists, and the relation stops at the budget without claiming the checker's diagnostic (reaching it is the cost of the bound: the `await` and the async return of one `GrowThen<string>` take 45 s together in release). A budget-cut relation read is never a payload: the async and async-generator wraps publish the typed gap over it, where they used to publish the thenable the relation stopped at (`Promise<C14>`). Cost, measured on the implementing host: the run is a loop, so its stack does not grow with the chain (release: a 128 KiB thread overflows and 160 KiB answers 15 and 1000 elements alike, 256 KiB answers 5000; debug: 448 KiB overflows and 512 KiB answers 15, 1000 and 5000 alike); time in release 0.24 s at 500 elements, 0.51 s at 1000 and 3.7 s at 5000, in debug 40 ms at 15, 0.34 s at 200, 2.0 s at 1000 and 17.4 s at 5000, at 4.5 queries per element whatever the length. The per-element time stays near flat as the declaring file grows (release: 0.48 ms at 500 elements, 0.51 ms at 1000, 0.68 ms at 2000, 0.73 ms at 5000, and 0.86 ms at 5000 beside 3000 more declarations); it was 104 s / 366 s at 5000 while four per-query steps did work linear in the file or the path — the request overlay's completion re-compared the file's facts on every serve, a plain script's artifact key re-hashed its source on every read, each instantiation copied every declared type name into a shadowing set, and the awaited path check scanned every operand reached — each now constant per query and held by a work-count test (`request_view_reuse_tests::{repeated_completion_of_one_content_version_promotes_once, a_script_artifact_key_reuses_the_snapshot_parse_identity}`, `resolver_core::scope_shadowing::tests::a_shadow_set_views_its_bundle_without_copying_names`, `project_semantic_dispatch::build::awaited_path_tests::the_awaited_path_counts_each_entry_until_its_run_truncates`). Held by `flow_return_coverage_tests::{awaiting_a_recursive_thenable_recovers_with_any_under_ts1062, returning_a_recursive_thenable_recovers_with_promise_any_under_ts1062, a_recursive_arm_is_dropped_from_an_awaited_union, the_authored_awaited_of_a_recursive_thenable_is_the_ts2589_recovery, a_declared_promise_of_a_recursive_thenable_keeps_its_declared_type, non_recursive_thenables_and_non_thenable_cycles_keep_their_answers, recursive_thenable_answers_do_not_depend_on_demand_order, async_wrap_over_a_self_referential_promise_recovers_under_ts1062, async_generator_yields_of_a_recursive_thenable_follow_the_checker, a_failing_yield_beside_another_yield_drops_out_of_the_join, a_generic_self_referencing_thenable_follows_its_recorded_instantiation, a_growing_thenable_hits_the_checker_limit_in_the_lib_conditional_only, a_long_thenable_chain_awaits_to_its_value_within_the_work_budget, lib_awaited_tail_runs_stop_at_the_checker_limit, lib_awaited_nested_steps_stop_at_the_checker_limit, an_awaited_recursive_thenable_in_an_array_literal_is_the_recovery_element, a_member_read_through_a_self_reference_walks_on_through_the_declaration, an_indexed_access_of_a_self_reference_is_the_declaration}` and `signature_corpus_tests::diagnostic_rows_flip_on_the_checker_recovery_in_both_directions`. |
| `SDL-1` — conditional labeled-break join (`flow_return_lexical_tests::flow_return_conditional_labeled_break_joins_the_write`) | Moved from Class 4, where the live answer kept the declared `boolean` arm (`number \| boolean`) and TypeScript 7.0.2 `tsc` prints `number \| true` (tsgo `7.0.0-dev.20260526.1` printed `number \| boolean`). A declared union's reduction (`getAssignmentReducedType`) reads `boolean` as `true \| false` and keeps each constituent SOME member of the assigned value is assignable to (`typeMaybeAssignableTo`), a `boolean` both of whose literals survive staying `boolean`; a fresh boolean literal the reduction keeps stays fresh, so a lone `x = true` read at a return widens to `boolean` while a join keeps `true`. The live answer is `number \| true`. Held by that test and by `flow_return_null_policy_tests::declared_boolean_unions_keep_the_assigned_constituent` (13 rows × 4 projects: `if (c) { x = true } else { x = "s" }` over `string \| number \| boolean` is `string \| true`, `x = b` over `b: boolean` is `boolean`, `x = c ? "s" : 2` is `string \| number` where the whole-union relation used to degrade; `true \| false` is the lane's spelling of `boolean`, the equivalence the oracle normalizer applies). |
| Flow-return lane: loops, unreachable code, discarded writes, relations and joins | Every row measured on the pinned TypeScript 7.0.2 checker in the four `strictNullChecks` × `noImplicitAny` projects, read from the checker's message quoting `ReturnType<typeof f>` (a conditional-type probe for a relation), and equal to the live answer. LOOPS type each reference they write at the head as the checker's loop label does — the entering state joined with every back edge (the body's end and each `continue`, after a `for` update; a `do…while`'s under its test) of one pass in which the reference reads its entering type and every written reference its value depends on its own head type — and the body then runs once from that head, a `for…of` / `for…in` binds its element, and an inferred binding whose initializer the loop feeds from itself through a union-declared binding is `any` as on the checker (TS7022): `flow_return_null_policy_tests::loops_iterate_to_the_checkers_fixed_point` (36 rows). UNREACHABLE returns and yields are aggregated with every reference at its declared type, and a break an abrupt `finally` replaces keeps its implicit `undefined`: `unreachable_returns_and_yields_read_declared_types` (14 rows). DISCARDED values apply their whole-binding writes in evaluation order (`void (x = v)` in any position, conditional arms, sequence operands, members, elements, unselected initializers), and a compound write retypes to the base type of what it held: `discarded_values_apply_their_writes_in_evaluation_order` (23 rows). With `strictNullChecks` off an equality with `null` / `undefined` narrows nothing: `strict_null_checks_off_equality_with_nullish_narrows_nothing` (5 rows). RELATIONS: a mutable array is covariant in its element, a tuple relates by the checker's arity rules (fixed, optional, rest, `readonly`): `array_and_tuple_relations_follow_the_checker` (61 rows). JOINS reduce by the checker's strict subtype relation (`any` not below `unknown`, `readonly` not below mutable, strict arity, optional not below required, index signatures inferred only for object literals, classes through derivation), so `if (c) return [null]; return ["s"]` is `string[]` off: `joins_reduce_by_the_checkers_strict_subtype_relation` (40 rows), and a conditional expression reduces like a return join: `conditional_expressions_reduce_like_return_joins` (17 rows). `satisfies` contextually types its literal operand (literal contexts keep literals, tuple contexts make tuples; corpus `X21`, `CC09`): `satisfies_contextually_types_its_literal` (39 rows). EQUALITY narrows by the checker's literal-comparison rules: `x == null` selects both nullish members and `x != null` removes both, on a member path as on a binding; with `strictNullChecks` `unknown` reads as `{} \| null \| undefined` under a nullish or truthiness test and a return join absorbs a primitive into the `{}` member; `==` against a non-nullish literal narrows as `===` except over `unknown` and an empty object arm, which it only filters, where `===` reads the literal; a `case` clause reads the literal over `unknown` and filters over `{}`: `equality_narrows_by_the_checkers_literal_comparison_rules` (43 rows, corpus `N76`). A narrow that takes the compared literal from the compared VALUE is the checker's fresh literal, widening where a bare literal would (`if (x === "s") return x` over `x: string` is `string`), while a declared or `case` literal stays: `an_equality_narrow_from_the_compared_value_is_a_fresh_literal` (20 rows). An index signature never satisfies a required named member and satisfies an optional one without relating its value (`{ [k: string]: string }` is not assignable to `{ a: string }`), and `any extends unknown` takes the true branch alone: `index_signatures_and_any_relate_by_the_checkers_rules` (14 rows). The corpus loop rows `D05`, `N52`–`N54`, `X58`–`X60` and `X82` match the checker. |
| Declaration heritage signatures (`project_semantic_dispatch::heritage_signature_tests`) | TypeScript 7.0.2's `resolveObjectTypeMembers` gives an interface or class with `extends` its OWN signatures first, then each base's in clause order, with no identical-signature dedup and no mixin composition. Measured: over `interface A { (): string }` and `interface C extends A { (): number }`, `c()` is `number` and `ReturnType<C>` is `string`; over two bases `B1`, `B2` that both extend `interface A { (a: string): 'A' }`, `ReturnType<DM>` for `interface DM extends B1, B2 {}` is `"A"` (the root reached twice is the last) where `ReturnType<B1 & B2>` is `"B2"`; two identical inherited signatures both stay (`Parameters<DD>` is `[y: string]`, `Parameters<DA & DB>` is `[x: string]`); a mixin constructor base keeps its own construct signature (`InstanceType<MX>` is `{ b: 1; }`, `InstanceType<MA & MB>` is `{ a: 1; } & { b: 1; }`). The live answers were reversed (`c()` `string`, `ReturnType<C>` `number`): the kernel answered a heritage body — an intersection NODE, bases then own body — with the intersection rules, and the shallow walker kept its own base-first, identity-deduplicated lists. The body is now minted under its own composite category, `SignaturesOfType` answers that category with `heritage_signatures`, and the walker's flush takes the kernel's lists. Construct signatures, an own overload set over a base set, generic heritage and generic signatures, an alias of the declaration, merged declarations with heritage, class `implements` and `extends`, and relations are measured and agree. Held by `heritage_signature_tests`, `signature_kernel::discovery_tests::heritage_lists_own_signatures_first_and_drops_nothing`, and the Vue emits contract's own-first order (`host_resolve_creo_tests`, `host_resolve_tests::vue_public_projection_orders_inherited_callable_emits_own_before_base`). |
| Relations through an indexed access (`project_semantic_dispatch::indexed_access_relation_tests`) | 7.0.2 resolves an indexed access over a type that is not generic where it is written: `2 extends Box['lit']` is `"y"`, `Box['u'] extends 1` is `"n"`, `5 extends Obj['nested']['b']` is `"y"`, and `pick(2)` over `pick(x: Box['lit']): 'lit'` and `pick(x: number): 'num'` is `"lit"`. The relation engine treated every indexed-access operand as an unread operator and answered `Unknown`, so each such conditional stayed deferred and the overload call failed closed. It now relates the type the access reads — for a target, a source, both, a read nested in a member, and overload applicability. An access that reads an OPTIONAL property reads its `undefined` (`SDL-16`). |
| `SDL-14` — overload resolution candidate order (`project_semantic_dispatch::merged_declaration_signature_tests`) | Moved from Class 4, where a call tried candidates in list order (`m()`, `mm.f()` and `m2()` were `"first"`, `s('a')` was `"str"`). A merged declaration's signatures are now one group minted `CompositeList::merged_overload_group` (one arm per declaration, each arm that declaration's own overloads, `CompositeList::overload_group`), which `SignaturesOfType` answers with `merged_declaration_signatures`: every declaration's signatures in declaration order, each attributed to the merged symbol and to its own declaration. Call resolution alone reads them in the kernel's `resolution_order`, TypeScript's `reorderCandidates`: a later declaration's group before an earlier one's, and a signature with a parameter WRITTEN as a literal type (`null` included; recorded on the parameter as `declared_literal` and carried through instantiation) before the rest. The signature utilities and conditional inference keep declaration order. Measured on 7.0.2 (`tsc --ignoreConfig --noEmit`, the answer read from TS2322): `m()`, `mm.f()`, `ReturnType<M>` and `ReturnType<MM['f']>` are `"second"`; `mo.f('s')` over `f(x): 'o1'; f(x): 'o2'` then `f(x): 'o3'` is `"o3"`; three declarations give `3`; a class and an interface `"iface"`; a namespace merged into a function adds no signature (`"fn"`); a module augmentation in another file is a later declaration (`x()` `"b-file"`, `x.f()` `"b-f"`); `m2()` over `interface M2 extends HA { (): 'first' }` and `interface M2 { (): 'second' }` is `"second"` while `ReturnType<M2>` is `"base"`; `s('a')`, `sf('a')` and `gf('a')` over `gf<T>(x: T)` and `gf(x: 'a')` are `"lit"`, `s('b')` is `"str"`; a union parameter (`uf('a')`), an alias (`af('a')`) and a template literal (`tl('ab')`) are not specialized (`"str"`); `tf(true)` is `"true"`, `nf(1)` `"one"`, `nlf(null)` `"null"`, `udf(undefined)` `"any"`; a specialized base signature stays ahead of the derived declaration's own (`sd('a')` `"base-lit"`); an instantiated type parameter is not written as a literal (`gb('a')` over `GB<'a'>` with `(x: string): 1; (x: T): 2` is `1`). The live answers equal every row. Held by `merged_declaration_signature_tests` (`a_later_merged_declaration_is_tried_first`, `a_module_augmentation_is_a_later_declaration`, `a_specialized_signature_is_tried_first`). |
| `SDL-15` — identical overloads of a merged method (`merged_declaration_signature_tests::identical_overloads_of_a_merged_method_all_stay`) | Moved from Class 4, where `Parameters<MDX['f']>` over `interface MDX { f(x: string): string }` and `interface MDX { f(y: string): string }` was `[x: string]`: the merged method's overload group was an ordered intersection answered with the intersection rules, which drop an identical repeat. It is now the merged group of `SDL-14`, which keeps every declaration's signatures (TypeScript's `getSignaturesOfSymbol`). Measured on 7.0.2: `Parameters<MDX['f']>`, `Parameters<OD['f']>` (one declaration's identical overloads) and `Parameters<typeof fd>` (function overloads) are `[y: string]`, and `Parameters<G<number>['f']>` over a generic merged method is `[y: number]`. The live labels and types equal them. |
| `SDL-16` — optional read through an indexed access (`project_semantic_dispatch::indexed_access_relation_tests`) | Moved from Class 4, where the type-level read of `Box['opt']` over `opt?: 3` answered `3` and a relation over such a read was left undecided. An indexed access now reads a property the checker's way: under the DECLARING project's `strictNullChecks` an optional property or tuple element reads its type plus `undefined`; with it off the read erases `null` / `undefined`; `exactOptionalPropertyTypes` does not change the read. A property NAVIGATION step (a member read, the component-meta member-path route) still reads the declared type, whose optionality the property carries; the intrinsic-element surface now addresses a member by that route instead of by `Tag['name']`, so an optional attribute publishes its declared type as before. A finite key set reads every key: a key union, `keyof T` over an enumerable `T` (through an alias too), and an intersection of those (`'opt' & keyof Box` is `'opt'`). A member read through an intersection distributes over the contributors' `null` / `undefined` arms as the checker's intersection does: a nullish arm every contributor carries stays, any other meets a disjoint arm and drops. The relation engine decides on the read (the special case that left it undecided is deleted), and three relation defects the decided reads exposed are fixed with it: an intersection source none of whose arms is assignable to an object target relates as the object its arms compose (`QA & QB extends { qa: 1; qb: 2 }` was a wrong `"n"`); comparability reads a member optional on one side with its implied `undefined` instead of skipping it; and the intersection-collapse classifier applies the checker's empty-discriminant rule (`isDiscriminantWithNeverType`: a literal type — unit, `boolean`, or a union of those — on one side, no common value, not optional on both sides) instead of requiring two unit types. Measured on 7.0.2 over the tests' fixtures, strict / `--strictNullChecks false` (`--exactOptionalPropertyTypes` equals strict): `Box['opt']`, `G<Box>`, `Get<Box, 'opt'>`, `Pick<Box, 'opt'>['opt']`, `Readonly<Box>['opt']`, `Box['opt' & keyof Box]` and `{ o?: 3 }['o']` are `3 \| undefined` / `3`; `Box[keyof Box]` and `Box[K]` `1 \| 2 \| 3 \| 4 \| undefined` / `1 \| 2 \| 3 \| 4`; `Tup[1]` over `[1, 2?]` `2 \| undefined` / `2`; `Required<Box>['opt']` `3`; `undefined extends Box['opt']` `"y"`; `Box['opt'] extends 3` `"n"` / `"y"`; `(H & { onClick?: (payload: QB) => void })['onClick']` over `H.onClick?: (payload: QA) => void` `(((payload: QA) => void) & ((payload: QB) => void)) \| undefined` / the intersection alone; `NU['a']` `({ qa: 1; } & { qb: 2; }) \| null`; `GT<string>['a']` `(string & { qa: 1; }) \| undefined`; `QA & QB extends { qa: 1; qb: 2 }` `"y"`; a predicate narrow of `{ v: OptionalBoxed['k'] }` by `x is { v: 'b' }` returns `{ v: string; }` on both settings, `{ v: number }`, `{ v: 'a' \| 'c' }`, `{ v?: 'a' }` against a required `'b'`, `{ v: boolean }` against `1` and `{ v: 'a' \| null }` against `'b' \| undefined` collapse likewise, while `{ v: 'a' \| 'b' }` against `'b' \| 'd'`, `{ v: string }` against `number` and optional on both sides keep the intersection; `x === b` over `x: OptionalHolder["kind"]` is TS2367 quoting `'"a" \| undefined' and '"b"'`; `StatusForString` over an optional `status` is `{ kind: "text"; value: string; } \| undefined`. The live answers equal every row, and the component-meta click payload of `HTMLAttributes & { onClick?: … }` is the handler intersection. Held by `indexed_access_relation_tests` (`an_access_reading_an_optional_property_reads_its_undefined`, `an_intersection_member_read_distributes_its_nullish_arms`, `an_intersection_source_relates_as_the_object_it_composes`, `a_narrow_over_a_member_read_through_an_indexed_access_decides_as_the_checker`), `flow_gap_retraction_tests::predicate_narrow_over_an_optional_member_read_decides_as_the_checker`, `relation_nominal_authority::comparable_treats_unreduced_operands_as_undecided`, `typeinfo_tests::basic::component_like_conditional_aliases_select_concrete_branches` and `meta_tests::project_local_intrinsics_tag_members_value_intersect_conflicting_fallback`. |
| `SDL-17` — class heritage from a value (`project_semantic_dispatch::class_value_heritage_tests`) | Moved from Class 4, where a class declaration whose `extends` names a value the type space does not declare (`class KE extends Ctor` over `declare const Ctor: new () => …`) had no base (`Opaque(Miss)`). Such a base now resolves through the value, as `getBaseConstructorTypeOfClass` and `resolveBaseTypesOfClass` do: the base constructor type is the value's type; its construct signatures, instantiated with the clause's type arguments, give the static side its members and, for a class without its own constructor, its construct signatures returning the derived instance; the instance side inherits the FIRST signature's return when the constructor type is an object type (not a union) and the return is a valid base type. Measured on 7.0.2: `KE['x']`, `InstanceType<typeof KE>['x']` and `KE2['x']` (a class extending `KE`) are `1`; `ReturnType<KE>`, `ReturnType<KE2>` and `ReturnType<IK>` (an interface extending `KE`) `"base-call"`; `KP['p']` `"p"`, `(typeof KP)['st']` and `(typeof KP2)['st']` `"static"`, `ConstructorParameters<typeof KP>` `string`, `number`; over overloaded constructors `KO['o']` is `"str"` (TS2510) and `ConstructorParameters<typeof KO>[0]` `number`; over `GCtor<string>` `KG['val']` and the constructor parameter are `string`; a declared constructor replaces the inherited ones (`ConstructorParameters<typeof KD>` is `[]`); a union of constructors gives no base type (`KU extends { a: 1 }` `"n"`, `ConstructorParameters<typeof KU3>[0]` `never`), a value without a construct signature none (`KNC`, TS2507), and a construct result that is not an object type none (TS2509: over `new () => string` `KS extends string` is `"n"` and `{ own: 1 } extends KS` `"y"`, over a union result `{} extends KUU` is `"y"`). The live answers equal every row. A heritage EXPRESSION — a call (`extends Mixin(Base)`), a namespace member path, a class-expression constant — is `SDL-22`. |
| `SDL-18` — homomorphic mapped instantiation (`project_semantic_dispatch::homomorphic_mapped_tests`) | Moved from Class 4, where `Partial<string>` stayed an unreduced mapped shell and `Partial<Face \| Obj>` evaluated to `{}` (the constituents' COMMON keys). A mapped type declared over `keyof T` for a type parameter `T` now records it (`MapperKey::over_type_variable`, set by every lowering and by the builtin homomorphic utilities, kept through substitution), and its evaluation instantiates as `instantiateMappedType`: a union maps each constituent, a primitive passes through (with an `as` clause too), `never` is `never`, `unknown` maps to `{}`, and without an `as` clause an array maps its element and a tuple each element, `+?`, `-?` and `readonly` applying to the elements (`+?` adds `undefined` under `strictNullChecks` unless `exactOptionalPropertyTypes` carries it on the element's flag; with the option off the element erases it). A mapping written over a concrete type keeps mapping that type's own keys. The relation engine reads an optional tuple element with its implied `undefined` (the missing type under exact optionality). Measured on 7.0.2, strict / `--strictNullChecks false` / `--exactOptionalPropertyTypes`: `Partial<string>`, `Mp<string>`, `P<string>` (`type P<T> = Partial<T>`) and `MpR<string>` (an `as` clause) are `string`; `Partial<undefined>`, `Partial<null>`, `Partial<symbol>`, `Partial<bigint>`, `Partial<object>` and `Partial<1>` are the argument; `Partial<boolean>` is mutually assignable with `boolean`; `Partial<never>` and `Mp<never>` are `never`; `Partial<Face \| Obj>`, `Partial<U>` and `P<Face \| Obj>` are mutually assignable with `{ a?: 1 } \| { o?: 1 }`, and likewise `Mp`, `MpS`, `Partial<Face \| string>`, `Required<{ a?: 1 } \| { b?: 2 }>` and `Readonly<Face \| Obj>` with their per-constituent maps; `keyof Partial<Face \| Obj>` is `never`; `Partial<number[]>[number]` is `number \| undefined` / `number` / `number \| undefined`; `Partial<[1, 'two', 3?]>[0]` `1 \| undefined` / `1` / `1 \| undefined`; `Required<…>[2]` `3`; `Readonly<…>[2]` and `Mp<…>[2]` `3 \| undefined` / `3` / `3 \| undefined`; `Readonly<number[]> extends number[]` `"n"`; `Partial<readonly [1, 2]> extends readonly [1?, 2?]` `"y"` and `extends [1?, 2?]` `"n"`; `[] extends Partial<Tup>` `"y"`; `[(1 \| undefined)?] extends [1?]` `"y"` / `"y"` / `"n"`; a mapping written inline in a generic signature or a generic function's body instantiates at a call (`g('s' as string)` over `g<T>(x: T): { [K in keyof T]: T[K] }` is `string`, `g([1] as number[])` `number[]`, its `?` twin `(number \| undefined)[]`, and `h('s' as string)` over `function h<T>(x: T) { const y: { [K in keyof T]: T[K] } = null!; return y; }` `string`). The live answers equal every row; the union rows compare the value the application evaluates to, because the checker prints `Partial<Face \| Obj>` by name and the corpus lane keeps that name. |
| `SDL-19` — namespace member values and cross-file globals (`project_semantic_dispatch::namespace_member_value_tests`, `flow_return_global_tests`) | Moved from Class 4, where `NS1.g()` over `declare namespace NS1 { function g(): 'one' }`, `NS.g()` over two namespace blocks, and `gsf('x')` / `ReturnType<typeof gsf>` over a function declared in two global scripts were typed gaps (`Opaque(Miss)` / `Opaque(UnmodeledPosition)`), and `gs()` over an interface declared in two global scripts answered `"ga"`. A namespace's value member is its own declaration under its qualified name (`NC.Inner.f`), functions as well as variables: an AMBIENT namespace (`declare namespace`, every namespace inside one, and every namespace inside a `declare global` / `declare module` block) exports each member, written `export` or not, and an ordinary namespace only the members it exports. A `typeof` path reads the longest prefix that names such a declaration, ahead of the surface of a function or class the namespace merges with. A member declared in several namespace blocks, a global function declared in several `declare global` blocks, and a global function declared in several files are merged declarations of `SDL-14` — one arm per block or file, in declaration precedence order (a `var` declared in several files stays one variable typed by its first declaration). A global interface read from a later declaring script lists the earlier scripts' declarations first. Measured on 7.0.2 (`tsc --ignoreConfig --noEmit`, the answer read from TS2322): `NS1.g()` is `"one"`, `NC.c` `"c"`, `NC.Inner.d` `"d"`, `NC.Inner.h()` `"h"`, `NC.Inner.Deep.e` `"e"`; over a namespace merged into `declare function nf(): 'nf'`, `nf.v` is `"nfv"`, `nf.w()` `"nfw"` and `nf()` `"nf"`; over a namespace merged into `declare class NK`, `NK.k` is `"nk"`, `NK.kf()` `"kf"` and `NK.s` `"static"`; `Impl.y()` over an exported function with a body is `1`; `NO.o(1)` and `ReturnType<typeof NO.o>` are `"o-num"`; `A.f`, not written `export` in a `declare namespace`, returns `1`, `D.E.e` in a namespace inside an ambient one `"e"`, while `typeof B.hidden` (TS2339) and `typeof C.amb` (TS2551) are not exported and stay a typed miss live; `NS.g()` and `ReturnType<typeof NS.g>` are `"ns2"`, `NB.k()` `"k2"`, `NB.k(1)` and `ReturnType<typeof NB.k>` `"k2-num"`, and `NT.t()` over two overloads of one block `"t1"`; over the scripts `ga.ts`, `gb.ts` and the modules `gma.ts`, `gmb.ts` in program order, `gs()` and `ReturnType<GS>` are `"gb"`, `gsf('x')` and `ReturnType<typeof gsf>` `"gsf-b"`, `GN.gn()` `"gn-b"`, `GN.gc` `"gc"`, `gmf()` `"gmf-b"`, and `gm()` / `ReturnType<GM>` `"b-file"`, every answer naming the other file with the contents swapped; `dg()` over two `declare global` blocks of one module is `"second"` and `dg(1)` / `ReturnType<typeof dg>` `"second-num"`; `splitFn()` / `splitFn("x")` over a script's `(): number` and a module's `declare global` `(x: string): string` are `number` / `string`, and `orderFn()` the later file's `"b"`. The live answers equal every row. |
| `SDL-20` — intersection over a union (`project_semantic_dispatch::intersection_distribution_tests`) | Moved from Class 4, where the canonical intersection kept `1 & (1 \| 2)` and `({ k: 1 } & { k: 1 \| 2 })['k']` read it. The canonical intersection now runs `getIntersectionType`'s reductions: a redundant supertype is removed (a primitive beside its literal or template, `boolean` beside `true`, `symbol` beside a unique symbol, `void` beside `undefined`, `{}` beside a definitely non-nullable member — the written pair `string & {}` stays), and an intersection with a union arm distributes into the union of each constituent's own canonical intersection (at most 64 constituents), keeping the written intersection as the printed origin when a constituent stays an object intersection and the union is wider than the intersection. A union's written intersection arm reduces the same way (the flow-return join), a relation relates a written intersection whose canonical intersection differs as that type (the recursive pair walk, the shallow prefilter and the identity unwrap alike), and a projected read that ends at an intersection with a scalar arm is that type, not a one-level surface. Measured on 7.0.2 (`--strict`): `1 & (1 \| 2)` is `1`, `('a' \| 'b') & ('b' \| 'c')` `"b"`, `string & ('a' \| 1)` `"a"`, `(1 \| 2 \| 3) & (2 \| 3 \| 4) & (3 \| 4)` `3`, `(1 \| 'a') & (number \| boolean)` `1`, `(true \| 1) & boolean` `true`, `string & (string \| number)` `string`, `{} & (1 \| 2)` `1 \| 2`, `QA & QA` `QA`, `1 & unknown` `1`, `number & string` `never`; `('a' \| QA) & string` is `"a" \| (QA & string)`, `(1 \| QA) & 1` `1 \| (QA & 1)`, `(QA \| 1) & QB` prints `(1 \| QA) & QB` and `(QA \| QB) & Z` prints `(QA \| QB) & Z`; `({ k: 1 } & { k: 1 \| 2 })['k']` and `((QA \| QB) & Z)['z']` are `1`, `keyof ((QA \| QB) & Z)` `"z"`, `((QA \| QB) & Z) extends QA & Z` `"n"` and `QA & Z extends ((QA \| QB) & Z)` `"y"`; a written intersection in a tuple element, an object member, an array element, a relation operand or a declared member read relates as the type it constructs (`[number & string] extends [never]`, `(string & ('a' \| 1)) extends 'a'`, `T1[0] extends 1` over `type T1 = [1 & (1 \| 2)]` are `"y"`; `2 extends O1['a']` over `type O1 = { a: 1 & (1 \| 2) }` is `"n"`). Every live answer equals the checker's; the print of a written intersection read through a declared member is `SDL-25`. |
| `SDL-21` — relation operands (`project_semantic_dispatch::relation_operand_tests`, `relation_nominal_authority::comparable_treats_unreduced_operands_as_undecided`) | Moved from Class 4, where the conditionals stayed deferred, `keyof Partial<Face>` and `keyof D0` stayed carriers, `Extract<Partial<Face \| string>, string>` missed and comparability of `keyof SingleKey` answered `Undecided`. The relation now takes every declaration carrier, mapped type and `keyof` through its canonical frame (not only a pair with a nominal side), relates an intersection TARGET arm by arm before it splits an intersection source (`unionOrIntersectionRelatedTo`'s order), reads a homomorphic mapped operand over a closed object as the object its keys map to — interned for the relation only, so no member is published and no member edge recorded — and reads a `keyof` whose keys settle as that key set. A `keyof` over a declaration, an application or a mapped type is evaluated at a demand for its value: a single key prints as the key, a mapped type's keys are its key space (`keyof X` for a homomorphic one), a builtin keyed utility's the keys it produces, and a union of two or more keys over a declaration or user application stays the `keyof` origin the checker prints. `Extract` / `Exclude` settle a declaration or builtin-application source first (a union application distributes), filter an object, array, tuple, callable or template constituent like a scalar, and a single surviving constituent is the source as written. Measured on 7.0.2 (`--strict`, and `--strictNullChecks false` for `Div`): `{ qa: 1; qb: 2 } extends QA & QB` `"y"`, `QA extends QA & QB` and `1 extends QA & QB` `"n"`, `{ x: QA } extends { x: QA & QB }` `"n"` and the reverse `"y"`, `((QA \| QB) & Obj) extends QA & Obj` `"n"`, `QA & Obj extends ((QA \| QB) & Obj)` `"y"`, `Div['req'] extends Div['both']` `"y"` under both settings, `Rec extends Rec2` `"y"`; `Partial<Face>` extends and is extended by `{ a?: 1 }` (`"y"`), `Partial<Face> extends { a: 1 }` and `extends { a?: 2 }` `"n"`, `Required<{ a?: 1 }>` and `{ a: 1 }` extend each other, `Readonly<Face> extends Face`, `Readonly<Face> extends { readonly a: 1 }`, `Required<Partial<Face>> extends Face` and `Pick<Face, 'a'> extends Face` are `"y"`; `keyof Partial<Face>` is `"a"`, `keyof Readonly<Face \| Obj>` `never`, `keyof D0` `"p"`, `keyof Required<{ a?: 1; b: 2 }>`, `keyof Pick<Face2, 'a' \| 'b'>` and `keyof Record<'x' \| 'y', 1>` the two keys, `keyof (Face2 \| One)` `"a"`, `keyof Face2`, `keyof Partial<Face2>` and `keyof Obj2` extend and are extended by their keys, `keyof G<1> extends 'g'` and `keyof Required<Face2> extends 'a'` `"n"`; `Extract<Partial<Face \| string>, string>`, `Extract<Partial<Face> \| string, string>` and `Extract<PF, string>` are `string`, `Extract<{ a: 1 }, object>` `{ a: 1; }`, `Exclude<{ a: 1 }, object>` `never`, `Extract<Face, { a: 1 }>` `Face`, `Exclude<Partial<Face \| string>, string>` and `{ a?: 1 }` extend each other, `Partial<Face \| string> extends string` `"n"`; `x === b` over `x: keyof SingleKey` or `keyof Partial<SingleKey>` and `b: "b"` is TS2367 while `x === a` is accepted. Every live answer equals the checker's; comparability still answers `Undecided` for a `keyof` over an import that does not resolve. The print of `Exclude<Partial<Face \| string>, string>` is `SDL-25`, `keyof (typeof D0)` is `SDL-26`. |
| `SDL-22` — class heritage from a value EXPRESSION (`project_semantic_dispatch::class_value_heritage_tests::a_value_expression_base_reads_the_expression_value`) | Moved from Class 4, where a class declaration whose `extends` is an expression the declaration facts cannot name (`class KM extends Mixin(Base) {}`, `class KT extends Tagged(Base) {}`), or a constant initialized with a class expression (`const CE = class { … }; class DCE extends CE {}`), had no base (`Opaque(Miss)`), and so had `InstanceType<typeof KMV>['mixed']` over `const KMV = Mixin(Base)`. The heritage expression is now an indexed program expression whose value registers under a synthetic value (`KM:extends`, never an authorable name), so the base resolves through the value rule of `SDL-17`; a call's value reads through call resolution, and a type-level read of an indexed call (a module constant's initializer read by `typeof`) serves the call's result instead of attaching a hold to a flow frame that happens to be open around it; a class expression that adds nothing an index record cannot serve (no static block, no write in a field initializer) is no unserved callable, so a factory returning one proves; and a constant initialized with a non-generic class expression holds that class's constructor over its instance shape. Measured on 7.0.2: `KM['b']` and `InstanceType<typeof KM>['b']` are `"base"`, `KM['mixed']` and `KM2['mixed']` (a class extending `KM`) `"m"`, `KM extends { b: 'base'; mixed: 'm' }` `"y"`, `ConstructorParameters<typeof KM>` `[]`; `KT['tag']` `"t"` and `KT['b']` `"base"`; `KN['inner']` over `extends NSC.Inner` `"i"`; `DCE['ce']` `"ce"`; `InstanceType<typeof KMV>['mixed']` `"m"` and `InstanceType<typeof KTV>['tag']` `"t"`. The live answers equal every row. |
| `SDL-23` — tuple `length`, rest positions and apparent members (`project_semantic_dispatch::tuple_length_and_apparent_member_tests`, `apparent_type_tests::a_primitive_member_read_is_project_scoped_and_never_cached`) | Moved from Class 4, where every read below was `Opaque(Miss)`. The path walker reads a tuple's `length` as its possible lengths (`number` with a rest element), a position at or past a rest element as every element from the rest start on (the rest contributing its element type), and any other key of a tuple, an array or a primitive off the global wrapper `getApparentType` names — `Array` / `ReadonlyArray` over the element union, `String`, `Number`, `Boolean`, `BigInt`, `Symbol` — read from the requesting project's registered library through the one lookup signature discovery already made. A wrapper read names no project of its own, so it is transaction-local: never served from the shared memo to another project's request. Measured on 7.0.2 (`--strict`): `Tup['length']` over `[1, 'two', 3?]` is `2 \| 3`, `[1, 2]['length']` and `(readonly [1, 2])['length']` `2`, `[1?, 2?]['length']` `0 \| 1 \| 2`, `[]['length']` `0`, `[1, ...string[]]['length']` `number`, `Partial<[1, 2]>['length']` `0 \| 1 \| 2`, `Required<Tup>['length']` `3`; `[1, ...string[]]` at `1` and `5` is `string`, `Partial<[1, ...string[]]>[1]` `string \| undefined` and `[0]` `1 \| undefined`, `[1, ...string[], 2]` at `0` `1` and at `1` and `2` `string \| 2`, `[...number[], 'end']` at `0` and `3` `number \| "end"`, `[1, 2?, ...string[]][1]` `2 \| undefined`, and the broad `number` reads the union of every element; `string['length']`, `Partial<string>['length']`, `'abc'['length']`, `string[]['length']` and `(readonly 1[])['length']` are `number`, and `string['charAt'] extends (pos: number) => string`, `number['toFixed'] extends (fractionDigits?: number) => string` and `true['valueOf'] extends () => boolean` are `"y"`. The lane registers the members read, verbatim from 7.0.2's `lib.es5.d.ts`, as the probe project's library; a project without one declares no global `String`, so its reads stay misses. Every live answer equals the checker's. |
| `SDL-26` — the `prototype` property of a class constructor type (`project_semantic_dispatch::class_prototype_property_tests`) | Moved from Class 4, where `keyof (typeof D0)` over `class D0 { p = 1 as const; private q = 2; protected r = 3; static s = 4 }` was `"s"`: the static side was published without `prototype`, which only the path walker's member step synthesized, and for any constructor-shaped surface. A class constructor type now carries the property the checker gives it (`getTypeOfPrototypeProperty`) — the class instance with `any` for each type parameter, first among its members, replacing a declared static `prototype` and the base constructor's — on the composed static side of a declared class and on a class expression's constructor, so `keyof`, indexed access, property reads and relations read it as any other property; the walker synthesizes nothing. Measured on 7.0.2 under each `strictNullChecks` × `noImplicitAny` setting (all four agree): `keyof typeof D0` is `"prototype" \| "s"`, `keyof typeof G` over `class G<T> { t!: T; static gs = 1 }` `"gs" \| "prototype"`, `keyof typeof A0` over an abstract class `"as" \| "prototype"`, `keyof typeof D1` over `class D1 extends D0` `"prototype" \| "s"`, `keyof typeof Empty` and `keyof typeof SP3` over `declare class SP3 { static prototype: 'lit'; x: 1 }` `"prototype"`, `keyof ReturnType<typeof mk>` over a function returning `class { ce = 1 as const; static z = 2 }` `"prototype" \| "z"` (a generic class expression's `"prototype" \| "w"`); `typeof D0.prototype` is `D0`, `typeof G.prototype` `G<any>`, `(typeof D0)['prototype']['p']` and `typeof D0.prototype.p` `1`, `ReturnType<typeof mk>['prototype']['ce']` `1` and the generic class expression's `['prototype']['v']` `any`; `(typeof D0)['prototype']` relates both ways with `D0`, `(typeof G)['prototype']` with `G<any>` and `G<string>`, `(typeof A0)['prototype']` with `A0`, `(typeof D1)['prototype']` with `D1` and not with `D0`, `(typeof SP3)['prototype']` with `SP3` and not with `'lit'` (a declared `static prototype` is TS2699 in a class and its type is not read), `(typeof D0)['prototype']` not with `Q0`, the same shape declared again, whose private member is its own (`SDL-79`), `(typeof D0)['prototype' \| 's']` with `number \| D0`; `typeof D0 extends { prototype: D0 }` is `"y"`, `extends { prototype: D1 }` `"n"`, `typeof G extends { prototype: G<string> }` `"y"`. `(typeof D0)['prototype']` prints `D0`, `(typeof G)['prototype']` `G<any>` and `(typeof SP3)['prototype']` `SP3`, as on 7.0.2. Every live answer equals the checker's. One producer per class kind: the composed static side of a declared class and a class expression's constructor (the class as authored, every parameter erased to `any`) carry the property; a constructor type that is not a class reads its apparent `Function`'s, in the path walker and in the relation alike (`(new () => ProtCtor) extends typeof ProtCtor` is `1` on 7.0.2 and live). A constructor type that is not a class has no `prototype` of its own and reads the apparent `Function`'s, as a function type does: an object with construct signatures only has the `Function` apparent type too (the checker's `NewableFunction`, which extends it). Measured on 7.0.2 (all four settings): `{ new (): Foo }['prototype']`, `(new () => Foo)['prototype']`, `{ new (): Foo; (): 1 }['prototype']`, `{ (): 1 }['prototype']` and `{ new (): Foo; s: 2 }['prototype']` are `any`, as are the property reads `typeof ctor.prototype`, `typeof callOnly.prototype` and `typeof both.prototype`; `{ new (): Foo }['length']` and `typeof ctor.length` are `number` and `{ new (): Foo }['name']` `string`; `keyof` those types lists no apparent member (`never`, and `"s"` for `{ new (): Foo; s: 2 }`) (`class_prototype_property_tests::a_constructor_type_reads_the_apparent_function_members`). |
| `SDL-27` — indexed access through an index signature (`project_semantic_dispatch::index_signature_access_tests`) | Moved from Class 4, where every read below was `Opaque(Miss)`: the path walker's index and member steps read a surface's named members only. A key no property names now reads the index signature that applies to it, TypeScript's `getPropertyTypeForIndexType` over `getApplicableIndexInfo`: a non-`string` index applies when the key is assignable to its key type through the relation authority (a number to a number index, a symbol to a symbol index, a string matching a template to that template), a number index also to a numeric string name and to `` `${number}` ``, several applicable ones intersect, a `string` index applies when no other does to a key assignable to `string` or `number`, and a symbol key no symbol index takes reads the `string` index (the type 7.0.2 recovers with after TS2538). The order is the checker's `getIndexedAccessType`: a string-index-only object (`isStringIndexSignatureOnlyType`: no property, one index key type, `string`) reads its `string` index for any key assignable to `string \| number`; any other object reads its own property, then the members its apparent `Function` type (an object with call or construct signatures) and the global `Object` add, then the index signature (a non-public member that names the key, or an apparent type that does not settle, keeps the typed miss); a heritage body is the one object it declares, read off its composed surface. A union or intersection object is read as the composite (`getPropertyOfType` over it, then its index infos): every arm string-index-only reads the composite's `string` index; a union whose every arm has a property, an apparent member or an applicable index signature for the name, one of them a property or an apparent member, reads each arm's (`createUnionOrIntersectionProperty`); an intersection reads the arms that declare the name, else their apparent members; otherwise the composite's index signatures — every arm's for an intersection, intersected per key type, and for a union the key types every arm declares, their types unioned (`getUnionIndexInfos`). Each arm reports the surface it settled on through its own step, so the decision adds no resolution of its own; a primitive reads its wrapper's through the one global lookup of `SDL-23`, and the global `Object`'s members, read in the subject's own project through the same lookup, come last; the read follows the declaring project's `strictNullChecks` as a property read does. The relation relates two templates of the same texts over identical placeholders as the one type the checker interns. Measured on 7.0.2 under each `strictNullChecks` × `noImplicitAny` setting (all four agree unless stated): over `interface SN { [index: number]: string }`, `interface SS { [key: string]: 1 }` and `type TN = { [n: number]: 2 }`, `SN[number]`, `SN[0]`, `SN['0']`, `SN['1.5']`, `SN[-1]`, `SN['-1']` and `SN[0 \| 1]` are `string`, `SS[string]`, `SS['x']`, `SS[number]`, `SS[0]`, `SS['x' \| 'y']`, `SS['x' \| 0]` and ``SS[`p${string}`]`` `1`, `TN[number]`, `TN[0]` and `TN['1']` `2`; `M['a']` over `interface M { a: 'A'; [k: string]: 'A' \| 'S' }` is `"A"` and `M['b']`, `M[string]`, `M[number]` and `M['a' \| 'b']` `"A" \| "S"`; over `interface MN { 0: 'zero'; [n: number]: 'N' \| 'zero'; [k: string]: 'N' \| 'zero' \| 'S' }` `MN[0]` and `MN['0']` are `"zero"`, `MN[1]` and `MN[number]` `"N" \| "zero"`, `MN['x']` and `MN[string]` `"N" \| "S" \| "zero"`; `TL['data-x']` and ``TL[`data-${string}`]`` over ``[k: `data-${string}`]: 'D'`` are `"D"`, over two template indexes `TT['ax']` is `"A"` and `TT['ab']` `never`; `SY[symbol]` and `SY[typeof uniq]` over `[s: symbol]: 'Y'` are `"Y"`, `SS[symbol]` and `SS[typeof uniq]` `1` with TS2538; `RO[number]` over a readonly index is `"R"`, `Dict<3>['x']` and `Dict<3>[string]` `3`, `{ [k: string]: 4 }['z']` `4`; `SS['toString']`, `SS['constructor']` and `SS['hasOwnProperty']` are `1`, `CS['bind']`, `CS['call']` and `CS['x']` over `interface CS { (): 1; [k: string]: 'CS' }` `"CS"`; with a library whose `Object` declares `toString(): string`, `M['toString']`, `X['toString']` and `SN['toString']` are `() => string` while `M['zz']` is `"A" \| "S"` and `X['zz']` `1`; `SU['x']` over `[k: string]: 1 \| undefined` is `1 \| undefined` with `strictNullChecks` and `1` without; `X['b']` over `interface X extends SS { a: 1 }` is `1` and `Y[0]` over an interface re-declaring an inherited number index `"n"`; with a library declaring `String`'s `readonly [index: number]: string`, `string[number]`, `string[0]` and `'abc'[number]` are `string`. `SN['x']`, `SN['01']` and `TL['other']` are TS2339, `SN[string]` and `TL[string]` TS2537, `SN[symbol]` TS2538, `SY['x']` and `string['x']` TS2339, `(SN \| SS)[0]` and `(SS \| { y: 2 })['x']` TS2339 — the error type, and the typed miss live. Over composites: `(SS \| { x: 2 })['x']`, `(SS \| SS2)['x']`, `(SS \| { x: 2 } \| SS2)['x']`, `(SS \| SS2)[number]`, `(SS \| SS2)['toString']` and `(SS \| SS2)[symbol]` (TS2538) are `1 \| 2`, `(SS & SN)[0]`, `(SS & SN)[number]`, `(SNS \| SN)[0]`, `(SN \| { 0: 'z' })[0]` and `(SS & SN & { a: 1 })[5]` `string`, `(SS & SN)['x']`, `(SS & { y: 1 })['x']`, `(SS & { a: 'A' })['b']` and `(SS & Fn)['x']` `1`, `(SS & SS2)['x']`, `(SS & SS2)['toString']` and `(SS & CS)['bind']` `never`, `(SNS \| SS)[0]` `"n" \| "s" \| 1`, `(SS & { x: 2 })['x']` `2`, `(TL & SS)['data-x']` `"D"`, `(SS \| { x: 2 })['toString']` and `(SS & { y: 1 })['toString']` `() => string`, `(SS \| { toString: 5 })['toString']` `5 \| (() => string)`, `(CS & { y: 1 })['bind']` and `(SS & Fn)['bind']` `Function`'s `bind`; `NS['toString']` over `interface NS { [n: number]: 'N'; [k: string]: 'S' }` is `() => string` and `NS['q']` `"S"`. Every live answer equals the checker's. An arm is read as the checker reads it: a tuple declares its fixed positions and `length`, an array none of its positions, both reading the rest through `Array`'s number index (a union member past a tuple's fixed positions reads its rest, else `undefined`); a union arm of a union flattens into it and a union arm of an intersection distributes it (the checker's `getIntersectionType`, even where the canonical construction keeps the written intersection as its printed origin); an intersection arm of a union is one object, read off its composed surface. Measured on 7.0.2 (all four settings, with a library declaring `Object`, `String`, `Array` and `ReadonlyArray`): `([1] \| SS)[0]`, `([1] \| SS)['length']`, `(string[] & SS)['x']`, `((SS \| SN) & { a: 1 })['a']`, `([1] & SS)[0]` and `([1] & SS)[5]` are `1`; `(string[] \| SS)['length']` and `(string[] & SS)['length']` `number`; `(string[] & SS)[0]`, `(string[] & SS)[number]` and `(string[] \| { 0: 'z' })[0]` `string`; `((SS \| SS2) & { a: 1 })['b']`, `([1, 2] \| SS)[1]`, `((SS & SN) \| SS2)['x']`, `((SS & SN) \| SS2)[0]`, `([1] \| { 0: 2 })[0]` and `(UU \| { x: 2 })['x']` over `type UU = SS \| SS2` `1 \| 2`; `([1, 2] \| [3])[0]` `1 \| 3`; `([1] \| string[])[0]` `string \| 1`; `([1] \| SS)['x']`, `(string[] \| SS)[0]`, `(string[] \| SS)['x']`, `((SS \| SN) & { a: 1 })['b']`, `([1] \| SS)[5]`, `(U \| { x: 2 })['x']` and `(U & { a: 1 })['b']` over `type U = SS \| SN`, `(I \| SN)['a']` and `(I \| SN)[0]` over `type I = SS & { a: 1 }`, and `(string \| SS)[0]` are TS2339 and `(readonly string[] \| SS)[number]` and `([1] \| SS)[number]` TS2537 — the error type, and the typed miss live. |
| `SDL-79` — property accessibility in the relation (`project_semantic_dispatch::member_accessibility_relation_tests`) | Measured on this work: the relation compared a private or protected property pair by type alone, so `class Q0`, redeclaring `D0`'s shape with its own `private q`, related both ways with `D0`. The property comparison decides accessibility first, as the checker's `propertyRelatedTo` does: a private property on either side relates only to the same declaration (the member's declaring file and span); a protected target property only to one declared in a class derived from the target property's declaring class (`isValidOverrideOf`, the same declaration included); a protected source property never to a public one. A member's declaring class is the class node whose body declares it directly — a class element or a property-declaring constructor parameter — read from the file's syntactic class index, which records every class declaration and class expression at index time; derivation reads a file-scope class's ancestry from the class-heritage ancestry authority, and a class with no `extends` clause derives from itself alone, while any other class with a heritage clause keeps the pair undecided. A mixin's or a derived class expression's instance relates through its base arm first, the base its instantiation supplies. Measured on 7.0.2 (all four settings): `Same<D0, Q0>`, `D0` to `Q0` and back, `PV` to `PV2`, `PV` to and from `PB`, `{ x: number }` to and from `PV`, `PV` to `Sub` (`Sub extends PV`) and `G<1>` to `G<2>` are `"n"`; `PV` to itself, `Sub` to `PV`, `PV & { z: 1 }` to `PV`, `PV` to `{}`, `G<1>` to `G<1>` and to `G<number>` are `"y"`; `SubPR`, `SubPR2` (redeclaring `protected x`), `SubPub` (redeclaring it public) and `Grand` to `PR` are `"y"`; `PR` to `PR2`, `PB` to `PR` and back, `PR` to `SubPR2` and to `SubPub`, `SubPR2` to `PR2`, an interface and a type alias with a public `x` to `PR`, and `PR` to the interface are `"n"`; `keyof D0` is `"p"` and `keyof PV` `never`. Class expressions (`project_semantic_dispatch::class_owner_tests`, measured on 7.0.2 in all four settings with `--noEmit`, since declaration emit refuses a protected member of an exported anonymous class): over `function mk() { return class { protected z = 1 } }`, a twin `mk3`, `mk2` returning `class extends (mk()) { protected z = 2 }`, `mkp` returning `class extends PR { protected x = 2 }`, `mkq` returning `class { constructor(protected z: number) {} }` and `mkr` extending `mkq()` and redeclaring it, instances read through `InstanceType<ReturnType<…>>`: `mk2` to `mk`, `mkp` to `PR` and `mkr` to `mkq` are `"y"`; `mk` to `mk2`, `mk3` to `mk` and back, `PR` to `mkp`, `mkq` to `mk` and back, and `mkq` to `mkr` are `"n"`; over `const CE = class extends PR { protected x = 4 }` and `const CO = class { protected x = 4 }`, `CE` to `PR` is `"y"` and `PR` to `CE`, `CO` to `PR` and back, and `CO` to `CE` `"n"`; over the mixin `function Tagged<B extends Ctor>(base: B) { return class extends base { protected tag = 't' as const } }` with `class TB { protected tag = 'x' as const }`, `Tagged(TB)` to another `Tagged(TB)` and to `TB` are `"y"` and `TB` to `Tagged(TB)` `"n"`. Every live answer equals the checker's (`mk3` to `mk` and `PR` to `mkp` had been undecided conditionals). Classes declared inside a function body (`LS extends LB`, `LO` redeclaring `LB`'s shape, `LP extends PR`, a class expression extending `LB`, read through the tuple the function returns) measure `"y"` for `LS` to `LB`, `LP` to `PR` and the class expression to `LB`, and `"n"` for `LB` to `LS`, `LO` to `LB`, `PR` to `LP` and `LB` to the class expression; their instances are not evaluated yet, so each probe stays an undecided partial demand (`class_owner_tests::a_function_local_class_declares_its_own_members`, ignored until they evaluate). The comparable relation reads the same rule through its member descent, in either direction (comparability holds when either direction relates): a shared property whose accessibility relates in neither direction proves the types disjoint. Measured on 7.0.2 over `declare const` values: `d0 === q0`, `pv === pv2`, `pr === pr2`, `pr === pb` and `pv === pb` are TS2367 and `d0 as Q0`, `pv as PV2`, `pr as PR2`, `pr as PB`, `pb as PR` and `pv as PB` TS2352, while `subPR === pr`, `pr === subPR2`, `subPR2 as PR` and `pr as SubPR2` are accepted; `pr2 === subPR2` is TS2367 and `subPR2 as PR2` TS2352, while `pr === subPub`, `subPub as PR` and `pr as SubPub` are accepted; the comparable verdict is disjoint for the refused pairs and overlapping for the accepted ones (`member_accessibility_relation_tests::the_comparable_relation_reads_property_accessibility_both_ways`). The comparable descent composes a class body's surface, and a class body composes as a heritage overlay however it is reached, so an overriding member's own declaration and accessibility shadow the inherited one's: `keyof SubPub` is `"x"` and `SubPub['x']` `number` on 7.0.2 and live (the composed surface had carried the base's `protected x`, so `keyof SubPub` was `never`). The files the ancestry walk reads join the relation build's self-roots, and an edit to the base class's file retracts the answer (`flow_gap_retraction_tests::protected_member_relation_retracts_when_the_base_class_file_changes`: `{ x, … }` inside `x === pr` over `Sub \| number` keeps the `Sub` arm while `PR`'s `x` is protected and drops it once `PR`'s `x` is public, as on 7.0.2). Open, each with an ignored test asserting the checker's answer (`class_owner_tests`): a class expression extending a class expression (`CE2` and `CE3` to `CE1` are `"y"`, `CE1` to `CE2` and `CE2` to `PR` `"n"` on 7.0.2) stays an undecided conditional, since a non-file-scope class with an `extends` clause does not read its base; a file-scope class extending a mixin application (`class G extends Tagged(TB)`: `G` to `TB` `"y"`, `TB` to `G` `"n"`) panics in the relation's SCC close (`the machinery root frame is always its SCC's root`), independent of the accessibility rule; a function-local class extending one (`"y"` / `"n"` likewise) waits for local classes to evaluate. |
| `SDL-80` — equality narrowing against a value that is not a literal (`project_semantic_dispatch::equality_value_narrowing_tests`) | Measured on this work: `if (x === d0)` over a declared value `d0` kept the typed `FlowGap(GuardNarrowing)` and the declared type. An equality whose other operand is a name the frame leaves free, or a static member path rooted at one, now narrows the compared binding as the checker's `narrowTypeByEquality` does: the value lowers as the flow expression it is, and the positive edge keeps the arms comparable to its type (`filterType` by `areTypesComparable`) — an arm the relation authority proves disjoint is dropped, an arm assignable to the value or the value to it is kept; the negated edge keeps the declared type (no such value is a unit type); on the strict positive edge `unknown` and a type with a `{}` arm read `object` for an object value and the value's own type for a primitive, `object` or `{}` value; `any` stays `any`. The comparable relation gained the two rules the filter needs: two surfaces each requiring a property the other proves absent are disjoint (read once their shared members are, so an unread member keeps the pair undecided), and two arrays relate by their elements. Measured on 7.0.2 under each `strictNullChecks` × `noImplicitAny` setting (the four agree but for the `\| null` a `return null` adds): over `class D0 { p = 1 as const; private q = 2; protected r = 3 }`, its twin `Q0`, `class PR { protected x = 1 }`, `class SubPR2 extends PR { protected x = 2 }` and interfaces `A { kind: 'a'; a: 1 }`, `B { kind: 'b'; b: 2 }`, `C { c: 3 }`: `D0 \| Q0` against `d0` reads `D0` for `===`, `==` and `d0 === x` and `D0 \| Q0` for `!==`; `SubPR2 \| Q0` against `pr: PR` `SubPR2`; `D0 \| string` against `d0` `D0`; `D0 \| A` against `ns.inner.d: D0` and against `dn: D0 \| undefined` `D0`; `A \| B` against `a` `A`, and `A \| B` for `!==`; `A \| C` against `c` `C`; `A \| B \| C` against `ab: A \| B` `A \| B`; `string[] \| number[]` against `arr: number[]` `number[]`; `(() => void) \| number` against `fn` `() => void`; `'a' \| number` against `s: string` `"a"`, and `number \| "a"` for `!==`; `unknown` against `d0` `object`, against `s` `string`, loosely against `d0` `unknown`; `{} \| number` against `d0` `object`; `any` stays `any`. Every live answer equals the checker's. Measured over `declare const` values: `ka === kc` (`KA { kind: 'a'; a: 1 }`, `KC { c: 3 }`), `sa === na` and `rsa === na` (`string[]`, `number[]`, `readonly string[]`) are TS2367, `ka === kac` (`KAC` adding `c: 3`), `oa === ob` (`{ a?: 1 }`, `{ b?: 2 }`) and `sa === rsa` are accepted, and the comparable verdicts agree. The other operand may also be a literal-typed value, a call whose callee is a free name or a static member path rooted at one, or another reference a narrow lands on (then each narrows by the other's type, read at the test), and the subject may be a member reference, which narrows by the value and narrows its parent by the checker's `narrowTypeByDiscriminant` rule: only when the member is a discriminant (the arms' member types differ and one is a literal type), keeping the arms whose member type is comparable to the member's union type narrowed by the value; the literal-operand equality (`x.kind === "a"`) takes the same parent rule. A unit value takes the literal equality's rules with its literal pinned; any other value filters by comparability, applies `replacePrimitivesWithLiterals`, removes on the negated edge only the unit arms equal to a unit value, and keeps under `==` the arms a primitive value coerces. Two scalar types (primitives, literals, template literals) related in neither direction are not comparable. Measured on 7.0.2 (`project_semantic_dispatch::equality_value_narrowing_tests`, all four settings agree but for the `\| null` and the erased nullish arms): `'a' \| 'b'` against `k: 'a'` is `"a"` (`"b"` for `!==`), `string` against `k` `"a"` (`string` for `!==`), `string` against `kab: 'a' \| 'b'` `"a" \| "b"`, `'a' \| 'b' \| 'c'` for `!==` unchanged, `number \| string` against `one: 1` `1`, `1 \| 2` for `!==` `2`, `boolean` against `t: true` `true` (`false` for `!==`), `D0 \| null` against `nul: null` `null`, `D0 \| undefined` against `und` and `==` `null \| undefined` read, `unknown` against `k` `"a"`, `string \| number == k` `"a"`, `== one` `1`; `x.k === a` over `HA \| HB` (`k: A` / `k: B`) narrows `x.k` to `A` and keeps `x`, `x.kind === k` over `A \| B` selects `A` (`B` for `!==`) and reads `x.kind` as `"a"`, `x.k === s` over `{ k: string } \| { k: number }` keeps `x`; `x.k === 'a'` over that pair keeps both arms, `x.kind === 'a'` over `A \| B \| C` (`C`'s `kind: string`) is `A \| C` and `!==` keeps all three (the lane had narrowed the parent in all three); `{ x, y }` inside `x === y` over `A \| B` and `B \| D0` is `{ x: B; y: B; }`, over `'a' \| 'b'` and `'b' \| 'c'` `{ x: "b"; y: "b"; }`, inside `x !== y` over `'a' \| 'b'` and `'b'` `{ x: "a"; y: "b"; }`, `x === o.v` narrows `o.v` too; `x === get()` and `get() === x` over `D0 \| Q0` with `get(): D0` are `D0`, `x === getK()` is `"a"`, `x === getS()` over `'a' \| number` is `"a"`; `return x === y` over `x: "a" \| "b"`, `y: "a"` infers `x is "a"`, and over two `string` parameters or `string \| number` and `string` no predicate (`signature_predicate_inference_tests`). Every live answer equals the checker's; the corpus rows `N65_enum_member_discriminant`, `N79_equality_against_const_literal_binding`, `N80_equality_against_const_literal_target_narrows` and `N81_equality_against_let_widened_target_does_not_narrow` move to `MatchesChecker`. Open, each with an ignored test asserting the checker's answer: a function value against an object arm and an array against an object (`fnObj`, `arrObj`, `objArr`, `objFn`: `() => void`, `number[]`, `A`, `A` on 7.0.2) keep the typed gap; a `const` initialized with a literal reads as its literal where the checker widens it as a sole return (`constRead` is `string`). Keeps the typed gap: a value read through a call whose callee is not provably closed (the control-test certification), a value rooted at a computed or optional access. |
| `SDL-24` — an indexed access that reads a named declaration (`project_semantic_dispatch::indexed_access_name_tests`) | Moved from Class 2, where the checker-probe lane printed the declaration's body where 7.0.2 prints its name: `H['req']` over `interface H { req: QA }` as `{ qa: 1 }`, `Div['req']` over the `SDL-21` fixture as `{ qa: 1 } & DeclRef(QB)`, and a class constructor's `prototype` as the class's surface. An indexed access whose terminal is a named declaration (a class, an interface, a generic application, or a union or intersection of them) now stays that access where it is lowered, and the printing demand reads it at navigate altitude, so it prints by the name; every other consumer still reads it at its own altitude. Measured on 7.0.2: `H['req']` is `QA`, `Div['req']` `QA & QB`, `H['both']` `QA \| undefined`, `Div['both']` `(QA & QB) \| undefined`, `W['d']` over `type W = { d: Decl }` and `(typeof Decl)['prototype']` `Decl`, `(typeof GDecl)['prototype']` `GDecl<any>`. |
| `SDL-31` residual — object-literal union normalisation (`project_semantic_dispatch::flow_return_null_policy_tests`) | Moved from Class 4, where the live answer kept each literal's own members (`{ a: number } \| { a: number; b: number }`). A widened union of object literals gives each literal its siblings' missing properties as `?: undefined`: `c ? { a: 1 } : { a: 1, b: 2 }` is `{ a: number; b?: undefined } \| { a: number; b: number }`, the checker's answer in all four `strictNullChecks` × `noImplicitAny` projects. Held by `flow_return_null_policy_tests::widened_object_literal_unions_take_their_siblings_properties` and `flow_return_null_policy_tests::a_normalized_object_literal_join_is_the_checkers_type`. |
| `SDL-31` residual — user intersection alias (`project_semantic_dispatch::flow_return_null_policy_tests`) | Moved from Class 4, where the live answer kept the application. `type NN<T> = T & {}` over `string \| null` is `string` on the checker and live. Held by `flow_return_null_policy_tests::user_intersection_aliases_reduce_their_applications`. |
| `SDL-31` residual — mapped index source (`project_semantic_dispatch::flow_return_null_policy_tests`, corpus `E02`) | Moved from Class 4, where the live answer stayed an undecided conditional. `Record<string, string> extends { a: string } ? "y" : "n"` is `"n"` on both sides: the mapped source relates through its index signature, which never satisfies a required named member. Held by `flow_return_null_policy_tests::mapped_sources_relate_through_their_index_signatures`; the U6 corpus row `E02` is `MatchesChecker`. |
| `SDL-31` residual — generic call over a conditional argument (`project_semantic_dispatch::flow_return_null_policy_tests`) | Moved from Class 4, where the live answer was the typed miss. `id(c ? a : b)` over `declare function id<T>(v: T): T` is `{ x: string; }` on both sides. Held by `flow_return_null_policy_tests::call_consumers_read_the_frames_values`. |
| `SDL-31` residual — consumers of a flow-return value (`project_semantic_dispatch::flow_return_null_policy_tests`, `flow_return_lexical_tests`) | Moved from Class 4, where each degraded live behind a typed gap. `twin(c).a`, `pick(twin(c))` (`"u"`: the overloaded callee is tried under the subtype relation first), `first([twin(c)])` and an `in` test answer the checker's types clean. Held by `flow_return_null_policy_tests::call_consumers_read_the_frames_values` and `flow_return_lexical_tests::flow_return_leaf_answered_call_forms_publish_any_not_a_carrier`. The `{ a: any; } \| { a: any; }` print of `first([twin(c)])` without `strictNullChecks` stays `SDL-28`'s Class 2. |
| `SDL-31` residual — arithmetic operands (`project_semantic_dispatch::flow_return_null_policy_tests`) | Moved from Class 4, where an operand reading a frame binding was the typed gap `FlowGap(UnmodeledExpression)`. An arithmetic, unary or update operand, and `xs[i]` over an array, tuple or string, read frame bindings by the checker's operator and element rules (`const n = i; i = n + 1` is `number`). Held by `flow_return_null_policy_tests::operators_over_frame_bindings_type_by_the_checkers_rules`. |
| `SDL-31` residual — destructuring elements (`project_semantic_dispatch::flow_return_null_policy_tests`) | Moved from Class 4, where an element read the unmodelled-binding marker. Elements in `for…of`, declarations, parameters, assignment patterns and top-level declarations read the checker's binding-element type, defaults, rest and nesting included. Held by `flow_return_null_policy_tests::destructured_bindings_read_the_checkers_binding_element_type`. |
| `SDL-31` residual — member-path narrowing (`project_semantic_dispatch::flow_return_null_policy_tests`) | Moved from Class 4, where `o.y = "b"; return o.y` was `"a" \| "b"` live. A member-path assignment narrows to the assignment-reduced value (`"b"`), survives calls, and is dropped by a write to a prefix or the root. Held by `flow_return_null_policy_tests::member_path_assignments_narrow_like_the_checker`. |
| `SDL-31` residual — logical writes (`project_semantic_dispatch::flow_return_null_policy_tests`) | Moved from Class 4, where a write in a logical operand (`c && (x = "s")`) kept its typed gap. The write applies on the edge the operator selects, including writes inside `if` tests and comparison operands inside logical assignments. Held by `flow_return_null_policy_tests::logical_operands_follow_the_checkers_flow_graph` and `flow_return_null_policy_tests::writes_in_conditions_follow_the_checkers_flow_graph`. Loop-test and conditional-expression-test writes are pinned at the checker's answer by the skipped `flow_return_null_policy_tests::open_condition_writes_match_the_checker`. |
| `SDL-31` residual — uninitialized `let` (`project_semantic_dispatch::flow_return_null_policy_tests`) | Moved from Class 4, where `let x; if (c) { x = 1; return x }` kept the conditional-definition degradation. A read in the arm or loop body that assigned it gives the assigned value, and without `noImplicitAny` the declared `any`. Held by `flow_return_null_policy_tests::uninitialized_locals_read_the_checkers_auto_type`. |
| `SDL-31` residual — evolving array operations (`project_semantic_dispatch::flow_return_null_policy_tests`) | Moved from Class 4, where `const a = []; a.push(1); return a` kept its typed gap. An unannotated declaration initialised to an empty array literal grows its element type at each `push` / `unshift` call and number-like element write and is finalized where any other reference reads it (`a.push(1); a.push("s")` is `(string \| number)[]`), on all four projects. Held by `flow_return_null_policy_tests::{evolving_array_operations_grow_the_element_type, a_reference_finalizes_an_evolving_array_where_it_reads, writes_to_an_evolving_array_follow_the_auto_array_rule, evolving_arrays_join_where_paths_meet, evolving_arrays_in_loops_take_the_checkers_loop_head, closures_read_a_captured_evolving_array_by_the_capture_rule}`. |
| `SDL-32` — `for…of` iterables (`project_semantic_dispatch::flow_return_null_policy_tests`) | `for…of` over strings, tuples, arrays and the `[Symbol.iterator]` protocol reads the checker's element type. Held by `flow_return_null_policy_tests::for_of_over_a_non_literal_iterable_reads_its_element_type` and `flow_return_null_policy_tests::for_of_follows_the_symbol_iterator_protocol`. `Set` and `Map` read the lib globals and are re-checked with the global-declaration work. |
| `SDL-33` — `new`-expression members and operator initializers (`project_semantic_dispatch::flow_return_null_policy_tests`) | `new C().m()`, `new C().p`, `!0`, `true && 1` and `1!` initializers give the checker's answer. Held by `flow_return_null_policy_tests::operators_over_frame_bindings_type_by_the_checkers_rules`. |
| `SDL-34` — assignment values (`project_semantic_dispatch::flow_return_null_policy_tests`) | A value-position `=` is its right-hand side's fresh type (`c ? (x = "s") : 0` is `"s" \| 0`). Held by `flow_return_null_policy_tests::assignment_values_are_their_right_hand_side`. Chained, compound, member and wrapped assignment values are pinned at the checker's answer by the skipped `flow_return_null_policy_tests::open_assignment_values_match_the_checker`. |
| `SDL-35` — element keys (`project_semantic_dispatch::flow_return_null_policy_tests`) | An element access keyed by a binding follows the checker's key identity. Held by `flow_return_null_policy_tests::element_access_references_follow_the_checkers_key_identity`. Index-signature reads and keyed guards are pinned at the checker's answer by the skipped `flow_return_null_policy_tests::open_element_keys_match_the_checker`. |
| `SDL-36` — statement calls (`project_semantic_dispatch::flow_return_null_policy_tests`) | A statement call keeps the narrowings in force when its callee's signatures neither assert nor return `never`. Held by `flow_return_null_policy_tests::statement_calls_keep_narrowings_their_callee_cannot_change`. Assertion method calls are pinned at the checker's answer by the skipped `flow_return_null_policy_tests::open_narrowings_match_the_checker`. |
| `SDL-37` — correlated destructuring (`project_semantic_dispatch::flow_return_null_policy_tests`) | A test of a destructured discriminant narrows its siblings and the destructured source for `const`, `const` loop and unassigned array-parameter patterns and destructured-source aliases, and a closure an arm creates reads the narrowed sibling. Held by `flow_return_null_policy_tests::destructured_discriminants_narrow_their_siblings_and_source`, `flow_return_null_policy_tests::instanceof_tests_of_non_discriminant_members_narrow_only_themselves` and `flow_return_null_policy_tests::correlated_narrowings_the_lane_does_not_carry_degrade`. A `switch` over an element, flat object parameters and generic parents degrade, pinned at the checker's answer by the skipped `flow_return_null_policy_tests::open_narrowings_match_the_checker`. |
| `SDL-38` — catch variables (`project_semantic_dispatch::flow_return_null_policy_tests`) | A catch variable reads its annotation, else `unknown` under `useUnknownInCatchVariables` and `any` without it. Held by `flow_return_null_policy_tests::catch_variables_follow_use_unknown_in_catch_variables`. |
| `SDL-39` — inferred predicates (`project_semantic_dispatch::flow_return_null_policy_tests`) | A `boolean` return of an element read infers no type predicate. Held by `flow_return_null_policy_tests::element_reads_infer_no_return_predicate`. A local `boolean` alias (`localBool`) is pinned at the checker's answer by the skipped `flow_return_null_policy_tests::open_narrowings_match_the_checker`. |
| `SDL-41` — object-literal roots (`project_semantic_dispatch::flow_return_null_policy_tests`) | Member reads and calls off an object literal, getters included, read the literal's member. Held by `flow_return_null_policy_tests::member_reads_off_an_object_literal_read_its_member_type`. An `as const` root and a literal holding a write are pinned at the checker's answer by the skipped `flow_return_null_policy_tests::open_assignment_values_match_the_checker`. |
| `SDL-42` — a guard over a binding another closure writes (`project_semantic_dispatch::captured_declared_type_tests::a_guard_over_a_binding_a_closure_writes_narrows_as_the_checker_does`, `…::a_closure_under_a_guard_reads_a_capture_another_closure_writes_at_its_declared_type`, `flow_gap_retraction_tests::a_let_capture_outside_its_container_is_complete_and_warm`, the `let_*_closure_write` / `let_write_after_creation` matrix cells) | Not previously recorded; found by the class-gaps work. A guard over a `let`, parameter or `var` some closure assigns took `FlowGap(GuardNarrowing)` in its own function — every statement call of a local closure (`set();`) was an unprovable effect — and `FlowGap(ClosureCapture)` inside a closure, where a `let` another closure writes (or one written after the creation) read no declared type. The checker reads a call's control-flow effects only through a callee declared with an explicit type (`getExplicitTypeOfSymbol`), so a statement call (or a discarded sequence operand) through a frame binding whose declared type shows no `asserts` or `never` signature — an unannotated `const` / `let` / `var` with an initializer, an unannotated parameter or one annotated `any` or with a function type, a nested function declaration — is inert and leaves every narrowing in place, and a lone nested declaration annotated `never` ends the path; a capture outside its extended container — a parameter, `let` or `catch` parameter some nested function assigns is never past its last assignment — reads its declared type in the body (an annotated `let` its annotation, an unannotated `let` / `var` its initializer's widened type, through every nesting depth), and the body's own guards narrow it from there. Measured on 7.0.2, every `strictNullChecks` × `noImplicitAny` setting: `letOwn`, `letOwnCall`, `paramOwn`, `varOwnCall` (guard, call of the writing closure, read) are `string \| 0`, `callAssign` (`x = "s"; set(); return x`) `string`; inside a closure `letIn`, `letInCall`, `letInBefore`, `paramIn`, `varIn` are `() => string \| 0`, `letInAnn` / `letAnnAfter` `() => string \| number \| boolean`, `letInLit` `() => "a" \| 0`, `hopLetMiddleWrites` / `hopParamMiddleWrites` `() => () => string \| number`, `hopLetPast` `() => () => number`; `letInNarrowedOuter` / `paramInNarrowedOuter` / `varInNarrowedOuter` are `(() => string \| number) \| undefined`, `() => string \| number` with `strictNullChecks` off; the G6/G7 cells `() => "a" \| "b"`. The intervening call does not undo a narrowing on the checker. A call through such a binding in its own frame or inside a closure: `bClosureCall` is `number[]`, `closureAssignsAnn` `(string \| number)[]`, `nestedFuncCall`, `annotatedVoidCallee`, `paramCallee` (`cb: () => void`), `paramUnannotated`, `paramDefaultCallee`, `discardedCall` (`(set(), x)`) `string \| 0`, `nestedFuncNever` `string`, `callInClosure` and `nestedDeclInClosure` `() => string \| 0`. Every live answer equals the checker's, complete and warm. |
| `SDL-43` — an annotated `var` (`project_semantic_dispatch::captured_declared_type_tests::an_annotated_var_reads_its_declared_type_narrowed_by_assignment`) | Reported as degraded with `UnreducedDeclaredUnion`; re-measured on the V8 head and not reproduced: the declarator's assignment reduction and a closure's declared-type read already agree. Measured on 7.0.2 over `var w: string \| number = v` and its variants, every setting: a read after the declaration is `string \| number` (`string` for a `"s"` or `string` initializer, `boolean` for `var w: string \| number \| boolean = b`), after `w = 1` `number`, under a `typeof` guard `string \| 0`, in a closure `() => string \| number` before or after a reassignment, in an invoked closure `string \| 0`, and `var w: string \| undefined = v` is `string \| undefined` / `string` with `strictNullChecks` off. The live answers equal every row, complete; the test pins them under all four settings. Declared-union loop variables, routed here from the evolving-array work where they took the same degradation on an older base, are likewise complete here: 7.0.2 answers `string \| number` for `chainNoLoop`, `selfFor` (`v = next(v)` in a `for`), `toggleFor`, `loopForI` and `copyPrev`, `string \| number \| true` for `chainFor` (a three-variable rotation) and `"a" \| "b" \| "c"` for `litFor`, every setting, and the live answers equal them (`captured_declared_type_tests::a_declared_union_reduces_every_assignment_through_a_loop`). |
| `SDL-44` — an unannotated `var` captured after a reassignment (`project_semantic_dispatch::captured_declared_type_tests::an_unannotated_var_capture_reads_its_initializers_widened_type`, `closure_narrowing_tests::a_capture_outside_its_container_reads_its_declared_type`) | Not previously recorded; found by the class-gaps work, where the closure over `var w = "a" as string \| number; w = 1` took `FlowGap(ClosureCapture)` (and earlier read the assigned `number`, clean). The declared type of an unannotated `var` is its initializer's widened type (`getWidenedTypeForVariableLikeDeclaration`, `any` for an auto-typed one), and a closure — never an extended container for a `var` — reads that declared type whatever is assigned before or after the creation; an invoked closure shares the enclosing flow. Measured on 7.0.2, every setting unless noted: `varWrittenBefore` is `string \| number`, `varParamWrittenBefore` / `…After` / `…Both`, `varRedeclared`, `varWrittenInClosure` `() => string \| number`, `varLitWrittenBefore` `() => string`, `varNumWrittenBefore` `() => number`, `varObjInit` `() => { a: number; }`, `varArrInit` `() => number[]`, `varConstAssertInit` `() => "a"`, `varIifeWrittenBefore` `number`, `varWrittenBeforeObjMethod` `{ m(): string \| number; }`, `hopVarBefore` `() => () => string \| number`, `letNoInitAfter` `() => any`; `var w = null; w = 1; return () => w` (and `undefined`, and `let x = null`) is `() => any`, `() => null` / `() => undefined` with `strictNullChecks` on and `noImplicitAny` off. Every live answer equals the checker's, complete. |
| `SDL-45` — a conditional over an indexed-access check (`project_semantic_dispatch::conditional_indexed_check_tests`) | Not previously recorded; found by the class-gaps work. The check `T[K]` of `D<Rec, "a">` over `a: any` took the relation's true branch (`1`), and a tuple or array wrapping such a read (`[T[K]] extends [string]`) stayed an undecided conditional. The checker instantiates the check type where the conditional is written, so an indexed access over a type that is not generic IS the property type it reads: `any` takes both branches whether or not the conditional distributes (the check `T[K]` is never a naked type parameter), and the relation reads a wrapped element through the canonical frame's identity unwrap. Measured on 7.0.2, every setting: `D<Rec, "a">`, `Rec["a"] extends string ? 1 : 2`, `DK<{ a: any }>`, `DK<{ a: any; b: number }>`, `D<Rec, "a" \| "b">`, `DX<{ x: any }>`, `E<Rec["a"]>` are `1 \| 2`, `DKey<Rec, "a">` (`T[K] extends string ? T[K] : K`) `any`, `D<Rec, "b">` and `D<Rec, "n">` `1`, `D<Rec, "u">` `2`; `ND<Rec, "a">`, `ND<Rec, "n">`, `ND<Rec, "a" \| "b">`, `EW<Rec["a"]>`, `[Rec["a"]] extends [string] ? 1 : 2`, `[Rec["b"]] extends [string] ? 1 : 2` and `Rec["b"][] extends string[] ? 1 : 2` `1`. Every live answer equals the checker's. |
| `SDL-46` — `keyof` over a named application (`project_semantic_dispatch::keyof_application_tests`) | Not previously recorded; found by the class-gaps work. A binding declared `keyof Box<string>` took the declared type verbatim (`const k: keyof Box<string> = "value"; return k` was `keyof Box<string>`), and `Exclude<keyof Box<string>, "size">` was an unresolved miss. The checker's `keyof` over a type whose keys settle IS the union of those keys — its print alone keeps the `keyof` origin — so the declarator's and an assignment's `getAssignmentReducedType` filter its keys, and `Extract` / `Exclude` distribute over them; a filter that keeps every key is the declared type itself and keeps its print. Measured on 7.0.2, every setting: `kAssign`, `kLet`, `kExclude` are `"value"`, `kPair` (`keyof Pair<number>`) `"first"`, `kPlain` (`keyof Plain`) `"a"`, `kReassign` `"size"`, `kGuard` `"size" \| "value"`, `kSwitch` `"value" \| 1`, `kExtract` (`Extract<keyof Pair<number>, "second" \| "third">`) `"second"`, `keyof Box<string> extends "value" \| "size" ? 1 : 2` `1`, `Box<string>[keyof Box<string>]` `string \| number`. Every live answer equals the checker's, complete. |
| `SDL-47` — `unique symbol` widening (`project_semantic_dispatch::unique_symbol_widening_tests`) | Not previously recorded; found by the class-gaps work, where `const l = u` over `declare const u: unique symbol` kept `typeof u`. An unannotated declaration widens a `unique symbol` another declaration created (`widenTypeForVariableLikeDeclaration`; a union holding one keeps it); a return type that is one `unique symbol` — a unit type — widens (`getWidenedLiteralLikeTypeForContextualReturnTypeIfNeeded`); a mutable location with no contextual type — an object-literal member, an array element, a value assigned to an auto-typed local — widens every one, union arms included (`getWidenedUniqueESSymbolType`); an annotated return, `as const` and a union return keep it. Measured on 7.0.2, every setting unless noted: `return u`, `const l = u; return l` (and `let`, `var`, `const l: typeof u = u`), `if (c) return u; return u` and a nested arrow's call are `symbol`; `(): typeof u => u` `typeof u`; `const l = u; return c ? l : 1` (and `let`, `var`, and `let l = u; l = u`) `symbol \| 1`; `{ u }` `{ u: symbol; }`; `[u]` `symbol[]`; `c ? u : 1` and `const x = c ? u : 1` `1 \| typeof u`; `if (c) return u; return "s"` `"s" \| typeof u`; `() => u` `() => symbol`; `let x = c ? u : 1` `number \| typeof u`; `{ k: c ? u : 1 }` `{ k: number \| symbol; }`; `[u, 1]` `(number \| symbol)[]`; `{ u } as const` `{ readonly u: typeof u; }`; `if (c) return u;` `typeof u \| undefined`, `symbol` with `strictNullChecks` off; `let x; x = u; return x` `symbol` and `let x; x = u; return c ? x : 1` `1 \| typeof u` (an auto-typed variable evolves to the assigned type itself), both `any` with `noImplicitAny` off. Every live answer equals the checker's, complete. |
| `SDL-48` — a capture's extended flow and an auto-typed `let` in a closure (`project_semantic_dispatch::captured_declared_type_tests::a_capture_reads_the_checkers_extended_flow_or_declared_type`) | Not previously recorded; found by the flow-lane work, wrong and clean on the V8 base: `capLet` (`let x; if (c) { x = 1; } const f = () => x; return f()`) and `declaredTypeProbe` published `number \| undefined`, `capLetArm` (`let x; if (c) { x = 1; const f = () => x; return f(); } return "z"`) `"z" \| number`, and `capAnnArm` (the same over `let x: string \| number = "s"`) `number \| true`. Three checker rules: an assignment's position extends to the end of the outermost statement holding it below the declaration (`extendAssignmentPosition`), so a function created inside that statement is not past the last assignment and reads the declared type; a capture is assumed initialized, so where the flow reaching an extended capture's creation includes a path on which an auto-typed `let` (no annotation, no initializer, `noImplicitAny` on) was never assigned, that path reads the declared auto type, which becomes `any` and absorbs the rest — unless the variable is never assigned (`undefined`); and a captured read of a variable holding the widening `null` / `undefined` widens as a frame read does under `strictNullChecks` off. Measured on 7.0.2 (`ReturnType<typeof f>`): every setting, `capAnnAfterIf` and `capAnnLater` `string \| number`, `capAnnArm`, `capAnnNestedIf`, `capAnnIfBefore`, `capParamArm` `string \| number \| true`, `capAnnDef`, `capAnnBlock` `number`, `capLet`, `capLetArm`, `capLetLoop`, `capLetSwitch`, `capLetUninitArmExt` `any`; with `noImplicitAny` on / off: `capLetAssigned` `string` / `any`, `capLetDefinite` `string \| number` / `any`, `capLetFnExpr` `number` / `any`, `capLetNever` `undefined` / `any`, `capLetNull` and `capLetUndef` `number` / `null` and `undefined` (`any` with both off), `capLetNullNever` `null[]`, `any[]` with `strictNullChecks` off. Every live answer equals the checker's, complete. `capLetObj` (a getter of an object literal read by member access) keeps its typed `FlowGap(UnmodeledExpression)`, which is the member-read lane's. |
| `SDL-49` — template literal types and string mappings over a settled key union (`project_semantic_dispatch::string_mapping_template_tests`) | Not previously recorded; found by the class-gaps work. `` `get${Capitalize<keyof Box<string>>}` `` and `Uppercase<keyof Plain>` stayed symbolic, `Uppercase<K>` over an alias and `Uppercase<keyof Plain>` answered `string`, and `` `k${keyof Rec}` `` answered `never`. The checker reads each operand where it is written — an alias, a `keyof` whose keys settle, an intersection (`keyof T & string`) — then distributes a template over each union (`getTemplateLiteralType`) and maps each constituent (`getStringMappingType`): a string literal maps its text; `string`, `number`, `bigint` and `any` stay holes (a lone `string` or mapping hole with no text is itself); a mapping keeps its application over `string` / `any` / another mapping and applies to the pattern template of `number` / `bigint`; a mapping over a template literal type maps its texts and holes (`Capitalize` / `Uncapitalize` the first only). Measured on 7.0.2, every setting: `"getSize" \| "getValue"`, `"A" \| "B"` (both spellings), `"ona" \| "onb"`, `"onsize" \| "onvalue"`, `` `k${keyof Mixed}` `` `"k0" \| "ka"`, `` `k${keyof Sym & string}` `` `"ka"`, ``Lowercase<`A${keyof Plain}`>`` `"aa" \| "ab"`, the four-way cross product, `G<Plain>` (`` `get${Capitalize<keyof T & string>}` ``) `"getA" \| "getB"`, `` `k${boolean}` `` `"kfalse" \| "ktrue"`, `` `k${null}${undefined}` `` `"knullundefined"`; `` `k${keyof Rec}` `` `` `k${string}` \| `k${number}` ``, `Uppercase<keyof Rec>` ``Uppercase<string> \| Uppercase<`${number}`>``, `` `${string}` `` `string`, ``Uppercase<`k${string}`>`` `` `K${Uppercase<string>}` ``, ``Capitalize<`${string}x`>`` `` `${Capitalize<string>}x` ``, ``Capitalize<`ab${string}`>`` `` `Ab${string}` ``, `` `a${`b${string}`}c` `` `` `ab${string}c` ``, `Uppercase<Uppercase<string>>` `Uppercase<string>`, `` `k${any}` `` `` `k${any}` ``; a generic `` `get${Capitalize<keyof T & string>}` `` keeps its application (`ReturnType` over it, instantiated at `unknown`, is `never` on both sides). `keyof` over an index signature reads its key type (a `string` index `string \| number`), printed as the plain union when the key list is one `string` index signature and with its `keyof` origin otherwise. Every live answer equals the checker's; the checker-syntax parser reads template literal prints. The `string_intrinsics_keep_the_application_over_string` unit test replaces the one that pinned `Uppercase<string>` as `string`. |
| `SDL-50` — a control or statement call through a module-level callee (`project_semantic_dispatch::callee_signature_effect_tests`, corpus rows `N56`, `N61`, `N62`) | Routed from the evolving-array work. A control test calling a declared constant (`while (more())` over `declare const more: () => boolean`), a declared `let`, a declared overload group, or a `const` holding a predicate arrow took `FlowGap(GuardNarrowing)`: only one closed same-file function declaration was certified. The checker reads a control call's narrowing from the callee's declared signatures (a type predicate narrows; nothing else does) and a statement call's effect only through an explicitly typed callee (an assertion narrows, a declared `never` return ends the path). A callee whose declaration set the file closes — a variable of any kind, a module's unexported function or overload group — now reads its signatures through `SignaturesOfType` at evaluation: none carrying a predicate is inert, one non-generic `x is T` narrows its argument (the negated reading included), one non-generic `asserts x is T` through an annotated callee narrows at statement position, an unannotated variable's call has no effect, and any other set keeps the typed gap. Measured on 7.0.2, every setting: `w1`, `w2` (`while (more())` writing a declared union), `w8`, `w9`, `w12` (`declare let lcheck`), `a2` (unannotated `const` assertion, TS2775), `a3` are `string \| number`; `w5` (`declare const pred: (v: unknown) => v is string`), `s6` (`!pred(x)`), `s8` (`lcheck() && typeof x === "string"`) `string \| 0`; `w13` (`do … while (more())`) `number`; `w14` (declared overloads) `unknown`; `a1` (annotated `const` assertion) `string`; corpus `N56` `{ v: string \| A; }`, `N61` `{ v: string \| number; }`, `N62` `{ v: string; }`. Every live answer equals the checker's, complete and warm; the three corpus rows move from `KnownOwed` to `MatchesChecker`. A control call through an imported function is resolved by the call executor over the module binding's checker-visible signature set: `i3` (`if (check())`, no reference handed over) is `string \| number` and `i4` (`if (isStr(x))`) `string \| 0`, both complete; a statement assertion through one (`i5`, `string` on the checker) keeps the typed guard-narrowing gap, as a `declare module` augmentation can extend its signatures. |
| `SDL-51` — object-literal accessors (`project_semantic_dispatch::object_literal_accessor_tests`) | Not previously recorded. A literal holding an accessor or a method was read with its unselected siblings as unmodelled positions (`{ n: 1, get v() { … } }.v` read `number`, degraded `UnmodeledPosition`; a get / set pair read `Opaque(Miss)`); an unannotated getter paired with an annotated setter read the getter's return; and a relation compared an accessor member as its function (`{ get v() { return 1 } }` did not extend `{ readonly v: number }`). The checker evaluates such a literal whole (its members read each other through `this`), types a get / set pair as the getter's annotation, else the setter's parameter annotation, else the getter's inferred return (`getTypeOfAccessors`), and relates the property. Measured on 7.0.2, every setting: `g3`, `g6` `string`; `g4`, `g8` (`get v() { return this.n }`), `h6`, `h7` (`o.m()`), `h8`, `p4` `number`; `g5`, `p1`, `p2` (unannotated getter, `set v(x: string \| number)`) `string \| number`; `g12` `1`; `g13` `"s" \| 1`; `p3` `"a"`; `p5` (`set v(x: any)`) `any`; the indexed accesses, `keyof` and both assignability directions of `g1`, `g7`, `g9`, `g14` against their printed objects. Every live answer equals the checker's, complete. The print of an accessor member is `SDL-52`. |
| `SDL-53` — a statement call through a `never`-returning callee (`project_semantic_dispatch::never_callee_tests::a_statement_call_through_a_never_returning_callee_ends_the_path`) | Not previously recorded. `cfail();` over `declare const cfail: () => never`, `afail();` over an annotated `const`, and `lf();` over `declare let lf: () => never` did not end the path (the fall-through `undefined` joined, degraded `GuardNarrowing`), and a file holding a `declare namespace` of any name refused to close every top-level callee. The checker ends the path at a call whose explicitly typed callee (`getEffectsSignature`: a function declaration, or a variable annotated with a function type) declares only `never`-returning signatures; an unannotated variable gives its call no effect. The lowering now reads the callee's authored declarations and emits the path end; a namespace of another name no longer blocks a callee. Measured on 7.0.2 (`strictNullChecks` on / off, `noImplicitAny` changes none): `n1`, `n2`, `n4`, `n5`, `n9`, `n12` (generic), `n15` (`(fail)()`) are `string`; `n3` (unannotated `const` throwing arrow) and `n16` (`void fail()`) `string \| undefined` / `string`; `n13` (`fail(); return x`) `string \| number`. Every live answer equals the checker's, complete. |
| `SDL-54` — the template-literal relation (`project_semantic_dispatch::template_pattern_relation_tests`, `template_relation::tests`) | Not previously recorded; the value check of `keyof` over a `` `x${string}` `` index signature was left out in the previous round. A string literal below a pattern with a non-`string` hole, a template below another pattern, and a union beside a template stayed undecided conditionals (a template was a deferred shell to the relation). The relation now reads a template as the type the reducer builds (holes settled, unions distributed), treats a settled template as a terminal string type, and matches a literal or template source against a pattern as the checker does: each target text matched leftmost, each hole's slice checked against the hole (a JavaScript-finite number spelling for `number`, one bigint token for `bigint`, the unchanged text for a string mapping, a lone-hole template by its hole, anything else by assignability). Measured on 7.0.2, every setting: the 35 rows of `a_template_pattern_accepts_the_slices_its_holes_admit` (`"x1"` / `"y1"` below `` `x${string}` `` `1` / `2`, `" 1"` below `` `${number}` `` `1`, `"10n"` below `` `${bigint}` `` `2`, `` `x${string}` `` below `` `x${number}` `` `2`, `"a1bb2"` below `` `a${number}b${number}` `` `2`, `"1"` below `` `${number}${number}` `` `2`, …), the 17 number and 10 bigint spellings of the unit tests, and `[keyof T5] extends [number \| `x${string}`]` both ways `1`. Every live answer equals the checker's. |
| `SDL-55` — a call inside another call's argument (`flow_return_coverage_tests::schedule::a_call_in_another_calls_argument_evaluates_in_its_own_frame`, `a_200_level_nested_argument_chain_answers_on_the_default_stack`, `a_nested_argument_chain_costs_the_same_work_per_level`) | Newly recorded; the live answer was a semantic miss. A call written as another call's argument was typed in the file's owner scope, where the frame's parameters and locals are unbound: `cN<T>(x: T) { return id(c(N-1)(x)); }` answered `{ v: <semantic miss>; tag: "c" }` from two levels, each level re-instantiated every level beneath it (53,757 units of connected work at 11 levels), and the depth guard refused the chain from 12 levels. The checker checks an argument expression in the scope it is written in. The flow lane now lowers each argument that is itself a call through the frame's own call carrier (`SliceCallArguments`) and the call executor evaluates it there; the callee schedule evaluates such an argument's callee bottom-up whenever every call around it resolves through the executor. Measured on 7.0.2, identical under all four `strictNullChecks` × `noImplicitAny` settings: the chain's `witness(v: number \| string)` is `{ v: string \| number; tag: "c"; }` at 4 and at 200 levels, `id(id(id(c0(v))))` the same, `id(g())` over `g()` returning `"a"` is `string`, over `"a" as const` `"a"`, `id(h(n))` over a `"a"` / `"b"` return `"a" \| "b"`, `pair(num(n), id(n))` `{ a: number; b: number; }`, `id(id(id(s)))` `string`, `id(num(n ?? 1))` `number` — each the live answer, clean and admitted. A member read written as an argument had the same defect and takes the same fix: `id(o.a)` over `o: { a: string }` is `string`, `id(box.a)` over `const box = { a: 1 }` is `number` and `id(o.a.b)` over `o: { a: { b: "k" } }` is `"k"` on 7.0.2 (all four settings) and live; each was a semantic miss. The 200-level chain answers on the default test stack under the query-depth cap a three-level chain needs, at 48 units of connected work per level at 16, 64 and 200 levels. |
| `SDL-56` — a chain through type positions (`flow_return_coverage_tests::schedule::a_200_level_type_position_chain_answers_on_the_default_stack`, `a_type_position_chain_dispatches_the_same_queries_per_level`) | Newly recorded; the live answer was the typed depth refusal. `tN(x: number) { let r!: ReturnType<typeof t(N-1)>; return r; }` reached each level's return through type lowering, two nested queries per level, and the depth guard refused the chain from 13 levels. The function index now records every bare `typeof name` in a frame's type positions (`FunctionTypeQuery`), and the callee schedule evaluates the function one names bottom-up when the evaluation reads that position — the frame's parameter list, the annotation of a binding whose value the slice selects, or a type a selected expression carries. Measured on 7.0.2, identical under all four settings: `witness(v: number)` is `{ v: number; tag: "c"; }` over the 200-level chain; the live answer at the checker's print altitude is the same, clean and admitted, on the default test stack under the three-level chain's depth cap, with one `TypeOf` and one `LowerLocator` per level. A body's signature-utility application over a function or object type now resolves where the body builds it, as the checker resolves a conditional type whose check type is not generic, so no level publishes a `ReturnType<…>` carrier over the level below and the work is linear (12 units per level at 16, 64 and 200 levels; 1,000 levels answer). Measured on 7.0.2, identical under all four settings: `t1` and `t2` of the chain print `{ v: number; tag: "c"; }`, `ReturnType<typeof g0>` over a generic `g0<T>` `{ v: unknown; tag: "c"; }`, `ReturnType<() => T>` `T`, and `ReturnType<T>` over `T extends (...a: any) => any` stays `ReturnType<T>` — each the live answer (`a_return_type_in_a_body_resolves_unless_its_check_type_is_generic`, `a_type_position_chain_costs_the_same_work_per_level`, `a_1000_level_type_position_chain_answers`). |
| `SDL-57` — a new instantiation of a warm chain through local arrows (`flow_return_coverage_tests::schedule::a_new_instantiation_of_a_warm_200_level_local_arrow_chain_answers_on_the_default_stack`, `a_new_instantiation_of_a_warm_local_arrow_chain_costs_the_same_work_per_level`) | Newly recorded; the live answer was the typed depth refusal. After a first request warms `lN<T>(x: T) { const f = (y: T) => l(N-1)(y); return f(x); }`, a second call site (`second(v: boolean)`) instantiated each level inside the one above, one native evaluation per level, and the nesting bound refused it from 24 levels. The schedule reads a forwarded instantiation through a function value the frame composes as it does through the frame's own call: an argument that is a bare read of the value's parameter annotated with the frame's binder, where no clause of the value and no type the frame declares around it takes that name (`FunctionParamRecord::annotation_reference`). Measured on 7.0.2, identical under all four settings, over the 200-level chain: `witness` is `{ v: string \| number; tag: "c"; }` and `second` `{ v: boolean; tag: "c"; }` — the live answers, clean and admitted, on the default test stack, at the same connected work per level at 16, 64 and 200 levels. A callee return no discovery predicts is found where it is demanded — a probe of a scheduled evaluation records it and the entry is evaluated again once it is answered — so an instantiation read from any argument form is scheduled too. Measured on 7.0.2, identical under all four settings, over 200-level chains: a level passing the next a parameter member read (`q(N-1)(o.a, o)`), a local member read (`k(N-1)(box.a)`) or a call on its parameter (`s(N-1)(id(x))`) gives `witness` `{ v: string \| number; tag: "c"; }` and `second(v: boolean, …)` `{ v: boolean; tag: "c"; }`; one passing a literal beside its parameter (`r(N-1)(x, "lit")`) `{ v: string \| number; u: string; tag: "c"; }` and `{ v: boolean; u: string; tag: "c"; }`; and a call nested in a generic call's argument inside a local arrow (`const f = (y: T) => id(a(N-1)(y))`) `{ v: string \| number; tag: "c"; }` — each the live answer on the default test stack, at the same work per level (`a_warm_chain_passing_…_instantiates_stacklessly`, `a_200_level_nested_argument_chain_in_local_arrows_answers_on_the_default_stack`). |
| `SDL-58` — generic call chain across modules (`flow_return_coverage_tests::schedule::a_generic_chain_across_201_modules_answers_without_a_refusal`, `…::a_generic_chain_across_modules_costs_the_same_work_per_module`, `…::an_edit_in_the_middle_of_a_module_chain_reaches_the_top`) | New row. Over 201 modules, module `k` importing `c(k-1)` and exporting `c(k)<T>(x: T) { return c(k-1)(x); }` over `c0<T>(x: T) { return { v: x, tag: "c" as const }; }`, the witness `(v: number \| string) => c200(v)` is `{ v: string \| number; tag: "c"; }` on 7.0.2 under all four `strictNullChecks` × `noImplicitAny` settings (every `c(k)` is `<T>(x: T) => { v: T; tag: "c"; }`); with `m100` edited to return `{ inner: c99(x), depth: 100 as const }` it is `{ inner: { v: string \| number; tag: "c"; }; depth: 100; }`. The live answer was the work rail's typed refusal from 201 modules: each level re-evaluated its callee's body under an instantiation over its OWN module's binder, re-instantiating the whole chain beneath it (262,097 units at 200 modules). An instantiated return is now read off the uninstantiated return under the instantiation, as the checker instantiates a signature's return type (`getReturnTypeOfSignature` of an instantiated signature instantiates its target's return type), so every function's body is evaluated once and each module costs the same work (3,213 units at 201). After an edit, the callee schedule counts only a still-valid warm answer as answered, so the invalidated functions above the edit re-evaluate bottom-up; both the edited and the unedited answer now agree with 7.0.2. |
| `SDL-59` — a nested generic re-declaring a binder name (`flow_return_coverage_tests::schedule::a_256_level_chain_returning_a_same_name_generic_answers`, `…::an_instantiated_return_keeps_a_nested_clause_of_the_same_name`, `…::an_instantiation_swapping_same_file_binders_binds_them_at_once`) | New row. Over a 256-level chain `lN<T>(x: T) { const f = (y: T) => l(N-1)(y); return f(x); }` whose head returns `{ v: x, tag: "c" as const, id: <T,>(z: T) => z }`, 7.0.2 answers `witness(v: number \| string)` as `{ v: string \| number; tag: "c"; id: <T>(z: T) => T; }` and `second(v: boolean)` as `{ v: boolean; tag: "c"; id: <T>(z: T) => T; }`; with the head's member `K: class<T> { own!: T; }` the member is `K: { new <T>(): { own: T; }; }` — all four `strictNullChecks` × `noImplicitAny` settings alike. The live answer over the function head was the typed `CONNECTED_QUERY_DEPTH_LIMIT` refusal for both: the flow lane interned a nested clause's `T` as the same node as the enclosing function's `T`, so an instantiation could not be read off the uninstantiated return and every level was evaluated again, nested. Each checker type parameter is its own symbol; a nested function value's or class expression's clause now interns its own binders, qualified by the declaration's offset, so substituting the enclosing `T` leaves the nested one alone and the chain is read off its uninstantiated returns. The frame's clause is bound simultaneously, so a same-file `g(b, a)` inside `f<A, B>` gives `f` the checker's `{ a: B; b: A; }` and `w(n: number, s: string)` its `{ a: string; b: number; }`. |
| `SDL-60` — declaration-file namespaces (`project_semantic_dispatch::module_value_surface_tests`) | Measured and resolved in one change; the live answers were typed gaps. A declaration file is ambient: 7.0.2 exports every member of `export namespace EN { function f(): 1; const c: 2; namespace Inner { function g(): 3 } }` written `export` or not (`EN.f()` `1`, `EN.c` `2`, `EN.Inner.g()` `3`, and `readEvV` / `readEvL` through a namespace import `9` / `10`), a global `declare namespace GD` alike (`12`, `13`), and a module body without an export declaration every top-level declaration (`L.hidden()` over `declare function hidden(): 8` is `8`); beside `export {}` only the members written `export` are (`EX.h()` `5`, `typeof EX.f` TS2339). Types reach through it the same way: `EN.T`'s member `"en-t"`, `EN.A` `"en-a"`, `EN.Inner.U` `"en-u"` through a named import, `M.EN.Inner.U` through a namespace import, merged with a function (`MF()` `"mf-call"`, `MF.c`, `MF.T`), a class (`MC.c`) and a second block (`EN.second`). Outside a declaration file `P.In.U` and `P.H` over unexported members are TS2694. The live lane answered `Opaque(Miss)` / `Opaque(UnmodeledPosition)` for every exported row and resolved nothing through a named import of a namespace; it now matches every row, the TS2339 / TS2694 rows as the typed miss, on the four `strictNullChecks` × `noImplicitAny` settings. |
| `SDL-61` — a script's block-scoped `var` (`module_value_surface_tests::a_var_in_a_script_block_is_global`, `a_script_var_is_a_global_read_every_way`, `a_var_in_a_module_block_is_module_scoped`) | Measured and resolved in one change. 7.0.2 hoists a `var` declared in any nested block of a script to the global scope: over `if (cond) { var bvIf = 1; } else { var bvElse = "e"; }` and a `var` in a bare block, every loop and its head, `try` / `catch` / `finally`, a `switch` case and a labeled statement, the reads are `number`, `string`, `boolean`, `number`, `string`, `string` (a `for…in` key), `number` (a `for…of` element), `bigint`, `string \| null` (`string` without `strictNullChecks`), `number`, `string`, `boolean`, `number`, `string`, `number`, and `globalThis.bvIf` `number`; a script holding only a nested `var` declares it (`"n"` read as `string`); `var noInit;` is the implicit `any`; `var` in a function body and a block `let` are TS2304. The live answers were typed gaps for every hoisted `var` and for `noInit`; they now match every row on the four settings. |
| `SDL-62` — values assigned with `export =` (`module_value_surface_tests::an_import_assignment_binds_the_assigned_value`, `a_default_or_named_import_reads_the_assigned_value`, `a_namespace_import_reads_the_assigned_value_members`, `an_assigned_value_is_the_surface_of_its_import`) | Measured and resolved in one change (`--module commonjs`). 7.0.2: `import N = require('./ns')` over `declare namespace N { … } export = N` reads `N.f()` `"n-f"` and `N.c` `"n-c"`; over a function `F()` `"f-call"`, a class `new C().m()` `"c-m"` and `C.s` `"c-s"`, a function merged with a namespace `M()`, `M.c`, `M.g()`, a namespace's type `NXR.T` and nested `NXR.Inner.g()`; a default import reads X itself, a named import one of X's namespace members (value or type), a namespace import X's members with `default` naming a function or class X (`F.default()`, `new C.default()`, `M.default.c`; `typeof N.default` over a namespace and `typeof VS.default.a` over a `const` X are TS2339); `typeof V` over `declare const V: { a: "v-a"; b(): "v-b" }` is that object, `typeof F` `() => "f-call"`, `InstanceType<typeof C>['m']` `() => "c-m"`. The live answers were typed gaps (no route for an import assignment, no export for the assigned value); they now match every row on the four settings. |
| `SDL-63` — widening reads of a module `const` (`project_semantic_dispatch::const_literal_widening_tests`) | Measured and resolved in one change. 7.0.2 declares an unannotated `const` with a literal initializer at the FRESH literal type, so a read widens wherever a bare literal would: `const mc = 1; return mc` is `number` (likewise a string, `true`, a signed number, a template, a bigint, a copy `const mcCopy = mc`, a parenthesized or `satisfies` initializer, an imported `const`, a namespace member, a namespace import's member, a declaration file's `declare const dd = 1`, a global `declare const gd = 2`, a script's `const sc = 3`, a module `const` over a same-name global `var`), `{ v: mc }` `{ v: number; }`, `[mc]` `number[]`, `let x = mc` and `const x = mc` `number`, a generic call's argument `number`; an annotated, asserted or declared-literal `const` stays `1`, a union keeps `1 \| 2`, `b ? mc : mcStr` keeps `"s" \| 1`, `if (b) return c; return undefined` is `1 \| undefined` with `strictNullChecks` and `number` without it, and `typeof mc` is `1`. The live answers kept the literal at every widening position; they now match every row on both `strictNullChecks` settings (the rows answer alike under `noImplicitAny`). |
| `SDL-64` — globals a library declares (`project_semantic_dispatch::lib_global_tests`) | Measured and resolved in one change (`tsc --noLib` over a library declaring `interface String { readonly length: number; readonly [index: number]: string; charAt(pos: number): string; }` and `declare var String: StringConstructor`). 7.0.2: `String['length']` is `number`, `ReturnType<String['charAt']>` `string`, `(typeof String)['prototype']['length']` `number`, `ReturnType<typeof String.fromCharCode>` `string`, `ReturnType<typeof String>` `string`, `ReturnType<typeof globalThis.String.fromCharCode>` `string`, a program's `declare global { interface String { extra: "x" } }` merges behind the library (`String['extra']` `"x"`, `String['length']` still `number`). The live answers were typed gaps: an authored global reference never reached the registered library. It now resolves through the one global lookup, the library first, and matches every row on the four settings. |
| `SDL-65` — module objects (`project_semantic_dispatch::module_object_tests`) | Measured and resolved in one change (`--module commonjs`, each row an exact type equality). 7.0.2 reads a module as one object of its value exports: over `import * as D from './dep'` (a `const`, a `let`, a function, a class, an enum, a namespace, `export default 5`, an interface and a type alias) `keyof typeof D` is `"E" \| "K" \| "NN" \| "a" \| "b" \| "default" \| "f"`, the same through `typeof import('./dep')` and `import DR = require('./dep')`; `export * from './dep'` beside `export const extra` and `export { a as renamed }` gives `"E" \| "K" \| "NN" \| "a" \| "b" \| "extra" \| "f" \| "renamed"`; `(typeof D)['a']` `1`, `['b']` `string`, `['default']` `5`, `['NN']['q']` `2`, `InstanceType<(typeof D)['K']>['m']` `() => "km"`; `typeof D` relates to `{ a: number }` and not to `{ zz: number }`; `retD` returns `typeof D`, `obj` `{ d: typeof D; }`. A module assigned with `export =` is its value (`keyof typeof N` `"c" \| "f"`, `keyof typeof V` `"a" \| "b"`, `keyof typeof import('./fn')` `never`), wrapped by the interop for a namespace import of a callable or constructable one (`keyof typeof F` `"default"`, `keyof typeof M` `"c" \| "default" \| "g"`, `InstanceType<(typeof C)['default']>['m']` `() => "c-m"`). A namespace declaration is the object of its exported values and value-declaring namespaces (`keyof typeof LN` `"Inner" \| "c" \| "f"`), and an ambient module the object of its blocks (`keyof typeof A` `"AN" \| "Cls" \| "default" \| "hidden" \| "k" \| "l" \| "x"`, `keyof typeof Q` over a block's `export = Q` `"default" \| "w"`). The live answers were typed gaps for every whole-module read; they now match every row on the four settings. |
| `SDL-66` — values of ambient modules (`project_semantic_dispatch::ambient_module_value_tests`) | Measured and resolved in one change. 7.0.2 resolves a value imported from a module only `declare module "amb" { … }` declares as the block's declaration, as it resolves the block's types: `x()` / `x(1)` over two overloads `"x"` / `"xn"`, `k` `"k"`, `l` `number`, `new Cls().m()` `"cm"`, `Cls.s` `"cs"`, `AN.f()` `"an-f"`, `AN.c` `"an-c"`, the default import `"d"`, `A.x()`, `A.k`, `A.AN.f()` through a namespace import, a second block's `y()` / `z` (a block without an export declaration exports every declaration, `hidden` `"h"` among them), and a block's `export = Q` through `import Q = require('eqmod')` (`Q()` `"q"`, `Q.w` `"w"`) and a default import (`"q"`). The live answers were typed gaps for every value row; they now match every row on the four settings. |
| `SDL-67` — functions declared in a namespace (`namespace_member_value_tests::a_namespace_function_returns_its_inferred_type`) | Measured and resolved in one change. 7.0.2 infers the return of a function declared in a namespace as any function's: over `export namespace N { … }` `N.lit` returns `number`, `N.obj` `{ a: number; b: string; }`, `N.decl` `2`, `N.arr` `number[]`, `N.callsLit` (a sibling read unqualified) `number`, `N.usesPriv` (a private function) `boolean`, `N.readK` (an exported `const`) `number`, `N.Inner.deep` `"d"`, `N.cond` `"x" \| 1`, `N.arrow` `number`; calls through a namespace in another file (`L.lit()`, `LL.L.str()`) `number` / `"s"`; a `bigint` literal return (`7n`, `-7n`) `bigint`. The live answers were typed gaps for every function of an exported namespace and for an unqualified read of a namespace member, and kept `7n`; they now match every row on the four settings. |
| `SDL-68` — `static readonly` literals and nullish declarations without `strictNullChecks` (`const_literal_widening_tests::a_readonly_static_and_a_nullish_declaration_widen_as_the_checker_declares`) | Measured and resolved in one change. 7.0.2 declares an unannotated `static readonly s = 1` at the fresh literal type: `KW.s` returns `number` (in another file and in the same file, `-1` and `"s"` alike), `{ v: KW.s }` `{ v: number; }`, `let v = KW.s` `number`, while `static readonly sAnn: 1 = 1` stays `1`, `2 as const` `2` and `typeof KW.s` is `1`. With `strictNullChecks` off an unannotated declaration initialized by `null`, `undefined` or `void 0` declares `any` (`rmn`, `rmu`, `rmv`, an imported `rdn` / `rdu`, `rmnLet` `any`, `rmnObj` `{ v: any; }`, `rmnArr` `any[]`, `typeof mn` `any`), and `const dnAnn: null` stays `null`; with it they are `null` / `undefined`. The live answers kept `1` for every static read and `null` / `undefined` without `strictNullChecks`; they now match every row under both settings (alike under `noImplicitAny`). |
| `SDL-69` — the wrapper a project declares (`lib_global_tests::a_primitive_call_reads_the_wrapper_the_project_declares`, `a_global_declared_after_a_miss_misses_the_warm_read`, `a_primitive_wrapper_read_is_the_demanding_projects`) | Measured and resolved in one change (`--noLib`). 7.0.2 reads a primitive's apparent call signatures from the global `String` the program declares: with `declare global { interface String { (n: number): "called"; } }` beside a library's `String`, or beside a program script declaring it, `s(1)` over `s: string` and `"lit"(2)` are `"called"`; with `strictBindCallApply` off, `g.extra` over `function g() {}` is `"x"` once a script declares `interface Function { extra: "x" }`; two programs whose `String` declares `(): "a"` and `(): "b"` call a `string` to `"a"` and `"b"`. The live answers were typed gaps with no library, a warm miss survived the declaration's arrival, and the second project was served the first project's signatures; each read now goes through the one global lookup (whose miss depends on the program's global contributors), and a `SignaturesOfType` read of a primitive is rewritten before admission to the same read over the wrapper the demanding project declares (to `never`, which has no signature, when it declares none), so the admitted key names that project; a primitive reached inside another subject stays out of the shared memo. |
| `SDL-70` — a member call on a shadowing binding (`flow_return_lexical_tests::a_member_call_on_a_shadowing_binding_reads_the_binding`) | Measured and resolved in one change. 7.0.2 resolves `c.m()` over a local `const c = new DK()`, a `let`, an object literal or a parameter `c: DK` to that binding even when the file declares `function c()`: `"km"`, `"om"`, `"km"`, `"km"`, while `c()` outside is the file's `number`. The live answers were typed gaps: the frame-shadowed call failed closed whenever the owner scope also answered the root name. A member call whose callee references only a frame binding resolves its root through the frame, as a member read does; it now matches every row on the four settings. |
| `SDL-71` — global namespaces, uninstantiated namespaces and callable `export =` values (`module_object_tests::a_global_namespace_is_its_object`, `a_namespace_declaring_a_value_is_a_value`, `a_callable_value_assigned_with_export_equals_has_a_default`) | Measured and resolved in one change. 7.0.2: a script's `namespace GN { … }` read from another file is its object (`keyof typeof GN` `"Inner" \| "c" \| "f"`, `keyof typeof GD` `"d" \| "g"`, `(typeof GN)['Inner']['i']` `3`); a namespace declaring only unexported values is a value with no member (`keyof typeof OnlyPriv` `never`, `ReturnType<typeof rOP>` `{}`) while one holding only types is not (TS2708, so `keyof typeof Outer` is `"PrivInner"`); a module assigned with `export =` a callable `const` gives a namespace import `default` and keeps its properties (`EQ.default()` `"ci"`, `keyof typeof EQ` `"default" \| "p"`, `EQC.default()` `"cf"`). The live answers were typed gaps or dropped `"p"`; the namespace object covers global scripts and instantiated namespaces, and `default` is decided by `SignaturesOfType`. They now match every row on the four settings. |
| `SDL-72` — merged function declarations (`namespace_member_value_tests::a_module_function_merges_its_augmentations`, `a_global_function_merges_every_scripts_overloads`) | Measured and resolved in one change. 7.0.2 merges a module's `export function f(x: number): "m-num"` with a `declare module './m' { function f(x: string): "aug-str"; function f(x: boolean): "aug-bool"; }` augmentation after its own: `ReturnType<typeof f>` `"aug-bool"`, `Parameters<typeof f>[0]` `boolean`, `f("a")` / `M.f("a")` `"aug-str"`, `f(true)` `"aug-bool"`, `f(1)` `"m-num"`; a global function declared in two scripts reads every script's overloads in program order (`ReturnType<typeof gf>` `"s2-bool"`, `gf(1)` `"s1-num"`). The live answers kept the module's own signature alone; `typeof f` merges every augmenting block's declaration through the module-augmentation index. They now match every row on the four settings. |
| `SDL-73` — enum member literal types and their print (`project_semantic_dispatch::enum_literal_tests::an_enum_member_is_its_own_literal_type_and_prints_as_the_checker_prints_it`, `typeinfo_tests::enums`) | Measured and resolved in one change; the lane lowered `E.A` to its value (`0`), so `E.A` printed `0`, `E.A \| E.B` `0 \| 1` and `NS.Inner.X` stayed an unresolved reference. 7.0.2 gives every enum member its own literal type and prints `E.A` `E.A`, `E.A \| E.B` over a two-member `E` `E`, `E3.A \| E3.B` over a three-member `E3` `E3.A \| E3.B`, `E3.C \| E3.A \| E3.B \| 7` `7 \| E3`, `Single` and `Single.Only` over a one-member enum `Single`, `NS.Inner.X` `NS.Inner.X` and `NS.Inner.X \| NS.Inner.Y` `NS.Inner`, `typeof E.A` `E.A`, `` `${E.A}` `` `"0"`, `{ a: E.A \| E.B }` `{ a: E; }`, `E.A \| 0` `0 \| E.A`. The lane now matches every row on the four settings. |
| `SDL-74` — relations of enum member literals (`enum_literal_tests::an_enum_member_relates_nominally_and_through_its_value`) | Measured and resolved in one change; the lane related a member as its value, so `F.A extends G.A`, `F extends G` and `"p" extends S.P` were `"y"` and `number extends E.A` `"n"`. 7.0.2 (48 rows, the test's doc comment): a member relates to a non-enum type as its value does; `number` is assignable to a numeric member and enum; a number literal only to a member of its value, or to any member whose value is not a constant (`5 extends Comp.A`, `7 extends DE.A` over a member of a `declare enum` without an initializer); no string literal and no `string` to a string member; a member of one enum never to another enum's member. The lane now matches every row. |
| `SDL-75` — widening of a fresh enum member (`enum_literal_tests::a_fresh_enum_member_widens_to_its_enum`) | Measured and resolved in one change; the lane read `E.A` as the literal `0`, so `function eA() { return E.A }` was `0` and `{ e: E.A }` `{ e: 0 }`. 7.0.2 declares each member at its FRESH literal type: `eA` is `E`, `eObj` `{ e: E; }`, `eArr` `E[]`, `eConst` `E`, `eSingle` `Single`, `sP` `S`, a member of a `const`, merged, ambient or namespace enum its enum, `b ? E.A : E.B` `E`, `b ? E3.A : E3.B` `E3.A \| E3.B`; a `let` initialized with a member is typed by the enum and holds the member (`eLet` `E.A`, `letAssign` `E3.B`); a read through `const o: typeof E` widens (`E`). The lane now matches every row. |
| `SDL-76` — equality and `switch` narrowing by an enum member (`enum_literal_tests::an_enum_member_narrows_equality_and_switch`; the flow corpus row `N65_enum_member_discriminant`, re-pinned `MatchesChecker`) | Measured and resolved in one change; an equality or `case` operand spelled `E3.A` was not a narrowing operand, so the lane published the unnarrowed union behind `FlowGap::GuardNarrowing`. 7.0.2: `x === E3.A` narrows `E3` to `E3.A` and its other edge to `E3.B \| E3.C`; `x === 0` narrows it to `E3.A`; `x === S.P`'s other edge is `S.Q`; a `switch` over every member of `E` is exhaustive (its default edge is `never`, no `undefined` contributor), one over two of three members keeps `undefined` under `strictNullChecks`; `s.shape === Shape.Circle` discriminates `C \| S` (`{ v: string \| number; }`). The lane now matches every row. |
| `SDL-77` — enum member values (`enum_literal_tests::an_enum_member_value_is_the_checkers_constant_evaluation`) | Measured and resolved in one change; the lane folded only literal initializers, so `B = A << 1`, `C = A \| B`, a reference to a `const`, another enum's member or another declaration of a merged enum were computed (`number`), and a `declare enum`'s bare members were numbered. 7.0.2 evaluates initializers as constants (29 rows: operators, templates, references within the enum, across a merged enum, to other enums in the file, a namespace and another module, to unannotated `const` variables), computes a member initialized by an annotated or mutable variable, and computes a bare member of an ambient non-`const` enum. The lane now matches every row. |
| `SDL-81` — a static member reading its own class (`project_semantic_dispatch::class_self_reference_tests::a_static_reading_its_own_class_reads_the_member_alone`) | New row; the live answer was `Opaque(Miss)` for `ReturnType<typeof S.s>` over `static s() { return this; }`, and for `return S` and `S.k` inside a static method: `typeof S.s` built the whole static surface, whose `s` return named `typeof S` and re-entered it. The checker resolves a value's members one at a time: a `typeof C.m` path now lowers only the member position the class (or a variable's object literal) declares, an inherited static only the base surfaces that declare it; a `typeof C` named inside `C`'s own declared body stays the reference; and a static's `C` / `this` reads resolve per member (an annotated, inherited or method static through the constructor). Measured on 7.0.2 in all four `strictNullChecks` × `noImplicitAny` projects: `ReturnType<typeof S.s>`, `…S.e`, `…S.viaS` are `typeof S` (and relate to it both ways), `ReturnType<typeof S.s>['k']`, `…S.e>['k']`, `…S.kk` `number`, over `S2 extends B0s` `…S2.r`, `…S2.w`, `…S2.y` `number` and `…S2.q`, `…S2.x` `string`, and the same through `(typeof S)['s']`. |
| `SDL-82` — self-reference through a parameter or the declaring variable (`class_self_reference_tests::a_method_reading_its_class_through_a_parameter_reads_the_member`, `…::a_literal_method_reading_its_variable_reads_the_member`) | New row; the live answer was a partial demand for `ReturnType<H2['m']>` over `m(h: H2) { return h.v; }` and `Opaque(Miss)` for `ReturnType<typeof obj.m>` over `const obj = { v: 1, m() { return obj.v; } }`: only reads through `this` took the member-by-member path. A member read off a class reference (a `DeclRef`, an application, or the body's self-reference sentinel) reads the member where the class declares it, binding its polymorphic `this` to the reference; the relation no longer builds a class body to test it as a spread program; and a variable's literal method reading the variable reads it as its receiver. Measured on 7.0.2 in all four projects: `ReturnType<H2['m']>`, `…H2['n']` (`h.m(h)`), `…H3['m']` (through `const o: H3 = h`), `…H3['k']`, `ReturnType<typeof obj.m>`, `…obj.n`, `…o2.m` (`o2.a.b`), `…o2.n` and `ReturnType<typeof o2.self>['a']['b']` are `number`. |
| `SDL-84` — abstract construct signatures (`project_semantic_dispatch::abstract_construct_tests`) | New row; `abstract new () => T` lowered as a plain construct signature and an abstract class's construct signatures were not marked, so `AC extends NC` over `AC = abstract new () => { x: number }`, `NC = new () => { x: number }` was `1` live and `typeof AB extends new () => any` over `abstract class AB` `1`. A constructor type and a construct signature now carry the checker's `SignatureFlags.Abstract` (an abstract class's own signatures, and the default signatures a constructor-less class inherits, are abstract exactly when that class is), it prints `abstract new`, and an abstract construct signature is not assignable to a non-abstract one. Measured on 7.0.2 in all four projects: `typeof AB extends new () => any` `0`, `… abstract new () => any` `1`, `typeof CC extends new () => AB` (a concrete subclass) `1`, `typeof AD …` (an abstract subclass of it) `0`, `typeof CE …` `1`, `NC extends AC` `1`, `AC extends NC` `0`, `{ new (): { x: number } } extends AC` `1`, `AC extends { new (): … }` `0`, `AC extends Function` `1`, `typeof AD extends typeof AB` `1`, `typeof AB extends typeof CC` `0`, `typeof CC extends typeof AB` `1`, `InstanceType<AC>` `{ x: number; }`, `InstanceType<typeof AB>` `AB`, `ConstructorParameters<typeof AB>` `[]`. |
| `SDL-85` — local function declarations (`project_semantic_dispatch::local_declaration_tests`, `flow_return_tests::flow_return_nested_value_self_name_shadowed_by_inner_declaration_reads_it`, `flow_return_lexical_tests::flow_return_nested_function_declaration_read_is_its_value`, `flow_return_frame_seal_tests::a_local_function_declaration_is_called_as_its_value`, `flow_return_root_gate_tests`) | New row; the live answer for a function declared in a function body and called, read, or called before its declaration was the typed shadow marker (`return g()` over `function g() { return 1; }` inside the body), and a call of a function-expression's own name shadowed by a body declaration kept the marker. The checker reads a function declaration as its value from its frame's entry: the flow lane lowers a read to the declaration's function value and a call to its signature (a generic one inferring through the call executor), with every capture at its declared type (the checker's control-flow container stops at a function declaration); a `return` of a bare call of the function itself (parentheses, and `await` in an async function, peeled) contributes nothing (`never`, dropped from the join beside other returns, so `if (n <= 0) return 0; return rec(n - 1)` widens to `number`); a return read again while it is being resolved (mutual recursion) is `any`; the skeleton gives each site that reads a declaration the declaration's captures, and a declaration owes no slot product. Measured on 7.0.2 in all four `strictNullChecks` × `noImplicitAny` projects: `ReturnType<typeof …>` of a called, hoisted, sibling-calling, arrow-captured or aliased declaration `number` / `string` / `boolean`, read as a value `(x: number) => "p" \| 0` and `(x: string) => string`, generic `number[]`, bare self-return `number`, bare self-only `never`, async `Promise<number>` / `Promise<never>`, mutual recursion `any`, a narrowed capture `string \| number`. |

## Class 2 — presentation-only

| ID | Subject | Both sides | Effects verified |
|---|---|---|---|
| SDL-25 | A written intersection read through a declared member, and `Exclude` over a mapped application | 7.0.2 prints `T1[0]` over `type T1 = [1 & (1 \| 2)]` and `O1['a']` over `type O1 = { a: 1 & (1 \| 2) }` as `1`, and `Exclude<Partial<Face \| string>, string>` as `Partial<Face>`; the checker-probe lane prints the written intersection `1 & (1 \| 2)` for the two reads (a written intersection is lowered as its display shell and a derived composite alone routes canonical; the flow-return join reduces the probe's own written intersection, not one a declared member holds) and the object the surviving mapped constituent evaluates to for `Exclude`. | Measured on 7.0.2 and live alike: `T1[0] extends 1` and `1 extends O1['a']` are `"y"`, `2 extends O1['a']` is `"n"`; `Exclude<Partial<Face \| string>, string> extends { a?: 1 }` and `{ a?: 1 } extends Exclude<Partial<Face \| string>, string>` are `"y"`. |
| SDL-78 | A class expression's constructor type as published (`flow_return_coverage_tests::class_expression_return_is_its_constructor_type`) | 7.0.2's declaration emit writes `ReturnType<typeof leafClassExpr>` over `return class {}` as `{ new (): {}; }`, omitting the `prototype` property (the emitter skips a class's prototype symbol when it writes a class expression as a type literal); the published surface carries it: `{ prototype: {}; new (): {}; }`. | Measured on 7.0.2 and live alike under each `strictNullChecks` × `noImplicitAny` setting: `keyof ReturnType<typeof leafClassExpr>` is `"prototype"`, `keyof ReturnType<typeof mk>` over `class { ce = 1 as const; static z = 2 }` is `"prototype" \| "z"` and `ReturnType<typeof mk>['prototype']['ce']` is `1` (`class_prototype_property_tests`). |
| SDL-28 | Structural-twin object join (`flow_return_null_policy_tests::an_object_join_of_structural_twins_differs_only_in_presentation`) | `if (c) return { a: null }; return { a: undefined }` with `strictNullChecks` off: 7.0.2 prints `{ a: any; } \| { a: any; }` — two fresh object identities of one structure, kept apart by the discriminant shortcut before widening. The live answer is the one object `{ a: any }`: the canonical union collapses the arms (`T \| T = T`; derived-composite constituent identity excludes source spans, and the discarded arm's member spans stay recoverable through the normalization origin edge). | Measured on 7.0.2 over the checker's answer `R`: `keyof R` is `"a"`, `R["a"]` is `any`, `R` is neither callable nor constructable, `R` and `{ a: any }` are assignable each to the other, and a consumer joining the value again reduces the twins to one (`c ? twin(c) : { a: 1 }` is `{ a: any; }`). The live answer: one member `a` of type `any`, no call, construct or index signature, assignable each way with the lane's own `{ a: any }`, and the same rejoined answer — asserted by the test. |
| SDL-29 | `NonNullable` over a union that stays a union (`flow_return_null_policy_tests::conditional_applications_decide_under_the_functions_own_algebra`) | `NonNullable<string \| number \| null>` prints `NonNullable<string \| number \| null>` on 7.0.2 (`NonNullable<string \| number>` with `strictNullChecks` off): the checker names the union its alias instantiation produced. The live answer at the checker's print altitude is `number \| string`. A result that is not a union prints the reduced type on both sides (`NonNullable<string \| null>` is `string`, over an interface or class reference that reference). | The alias names the very union the lane prints: the same two members, no call, construct or index signature on either, and no binding the alias adds. |
| SDL-40 | Object rest of a parameter with a method member (`flow_return_null_policy_tests::object_rest_keeps_a_method_member_a_method`) | 7.0.2 prints the rest as `{ m(x: string): void; }`; the lane's spelling prints it `{ m: (x: string) => void }`, as it prints every method member as a property. | The rest's member keeps its method kind and its signature, as the whole parameter publishes, asserted by the test. |
| SDL-52 | Object-literal accessor print (`project_semantic_dispatch::object_literal_accessor_tests::an_accessor_surface_reads_and_relates_as_its_property`) | 7.0.2 prints a literal's accessor as the property it is: `{ readonly v: number; }` for `{ get v() { return 1 } }`, `{ v: number; }` for an untyped get / set pair, `{ n: "a"; readonly v: "a"; }` for a getter returning `this.n`, `{ v: string; }` for `set v(x: string)` beside `get v() { return "a" }`. The lane keeps the get / set members (`{ v: () => number }`, `{ v: () => number, v: (any) => void }`), which every reader resolves to the property. | Measured on 7.0.2 and asserted live: each surface's `["v"]` read is the property type (`number`, `number`, `"a"`, `string`), its `keyof` is `"v"`, and it and the printed object each extend the other (`1`), while `{ v: string }` is not above the `number` pair (`2`). |
| SDL-83 | An object literal whose method or getter returns its own `this` (`class_self_reference_tests::a_literal_returning_its_this_is_one_recursive_type`, `…::a_literal_getter_returning_its_this_reads_the_literal`, `this_receiver_tests::a_receiver_returned_whole_is_the_declared_value`) | 7.0.2 prints the recursive anonymous type `{ v: number; me(): ...; }` (declaration emit: `me(): { v: number; me(): /*elided*/ any; }`), for a variable's literal and a returned one alike. The lane names a variable's literal `typeof own`, and gives a returned literal its own identity (the literal's offset, with the literal's `this` bound to that identity), printed as its surface with `this` at the recursion. Previously the returned literal's method took the typed marker. | Measured on 7.0.2 in all four projects and equal live: member reads (`ReturnType<typeof own.me>['v']`, `ReturnType<ReturnType<typeof own.me>['me']>['v']`, over `selfFn` the `v` of the return, of `me`'s return and of `me`'s return's `me`'s return, `number`; over `viaLocal` `w`, `string`), `ReturnType` of `me` (the literal again), and assignability both ways (`typeof own` to `me`'s return, the return to `me`'s return and back, the return to `{ v: number }`: `1`; `{ v: number; me(): { v: number } }` to the return: `0`). A getter returning `this` reads the same way: `typeof og.self.n`, `ReturnType<typeof useOg>` (`og.self.n`) and `…useOgDeep` (`og.self.self.n`) `number`, `typeof og` to `typeof og.self` `1`, over a returned literal the `self`'s `n` and `self`'s `self`'s `n` `string` and `self` to the return `1`. |
| SDL-86 | A returned enum object (`project_semantic_dispatch::differential_literal_tests::a_returned_enum_object_reads_as_typeof_the_enum`) | 7.0.2 prints `return Color` over `enum Color { Red, Green = 5, Blue }` by the value's name, `typeof Color`; the lane prints the object it is, `{ readonly [x: number]: string; readonly Red: Color.Red; readonly Green: Color.Green; readonly Blue: Color.Blue; }`, the reverse mapping included. | Measured on 7.0.2 and live alike under each `strictNullChecks` × `noImplicitAny` setting: `ReturnType<typeof eObj>[number]` and `eObj()[5]` are `string`, `keyof ReturnType<typeof eObj>` and `keyof typeof Color` are `"Blue" \| "Green" \| "Red"` (the reverse mapping is no key), `ReturnType<typeof eObj>['Green']` is `Color.Green`, and the value and `typeof Color` are assignable each way. |
| SDL-87 | A returned class value (`project_semantic_dispatch::differential_class_tests::a_returned_class_value_reads_as_typeof_the_class`) | 7.0.2 prints `return K` over `class K { v = 1; static s = "s"; m() { return this.v; } }` by the value's name, `typeof K`; the lane prints the constructor type it is, `{ new (): K; prototype: K; s: string; }`. | Measured on 7.0.2 and live alike under each `strictNullChecks` × `noImplicitAny` setting: `ReturnType<typeof ctorValue>['s']` is `string`, `InstanceType<…>` and `…['prototype']` are `K`, `keyof ReturnType<typeof ctorValue>` is `"prototype" \| "s"`, `new (ctorValue())().m()` is `number`, and the value and `typeof K` are assignable each way. |

## Class 3 — `VerterStableV1` order-induced (causal proof required)

An admission needs the §5.8 counterfactual: only semantic union traversal
order changed, in an isolated cache namespace (parents included), and
restoring the stable order recovers the recorded observation. The corpus
carries such a row as `Verdict::StableOrderDifference`, which pins both
halves — the members match the checker unordered, and the order differs.

| ID | Subject | Both sides | Counterfactual |
|---|---|---|---|
| SDL-12 | Deduplicated generic union order (`SV25_generic_union_dedup`) | `ReturnType<typeof witness>` over `A<T> \| B<T> \| A<T>` instantiates `T` at its constraint: 7.0.2 answers `A<unknown> \| B<unknown>`, ordered by its own type identities (it does not preserve authored first occurrence either — `B<T> \| A<T> \| B<T>` prints `A<T> \| B<T>`). The live answer holds the same two members, deduplicated, ordered by the `VerterStableV1` stable key: `B<unknown> \| A<unknown>`; a reversed authored input gives the byte-identical union. | `union_order_is_the_only_difference_from_the_checker`: a fresh host whose store reverses the one union order (`sort_union_members_by_stable_key` and the `ReduceUnion` member canonicalization; union views are store-scoped, so the namespace is isolated with its parents) answers `A<unknown> \| B<unknown>` in order; a fresh host under the stable order answers `B<unknown> \| A<unknown>` again. The contract (§1.1) mandates `VerterStableV1` and forbids cloning the checker's order. |
| SDL-30 | Survivor of two structurally identical interfaces (`flow_return_null_policy_tests::a_structural_twin_survivor_follows_the_stable_union_order`) | `if (c) return a; return b` and `c ? a : b` over `a: A1`, `b: B1`, both `{ x: string }`: each is a strict subtype of the other, and 7.0.2's `removeSubtypes` keeps the one its type order visits first, printing `A1`. The lane visits the operands in the union's `VerterStableV1` order and keeps the one that order puts first, which the stable key decides per project: `B1` in the `strictNullChecks`-off project at `/off/p.ts`, `A1` in others. | The test's fresh store with ONLY its union order reversed (`reverse_union_order_for_tests`, set before the store builds any union) answers `A1`, the checker's survivor; a fresh store under the stable order answers `B1` again. |

`SV26_literal_generic_union_order` is exact agreement: its probe is
`unknown` on both sides. The witness's own declared return orders
`"a" | "b" | T` on 7.0.2 and by the stable key live; no row observes that
order, and it would be admitted here on the same counterfactual.

## Class 4 — independent semantic difference / incompleteness

No entry: every difference recorded here has been classified.

## Checker-version history

Not live differences: both sides answer `never` on 7.0.2, and the rows
stay as a record of the contract's §6.2 recheck.

- `SDL-2` — intersection grouping witness `L` (`type L<T extends string> =
  (number & T) & { x: 1 }`): `L<"a">` is `never` on TypeScript 5.8.3 (the
  contract's historical probe) and on 7.0.2; corpus row
  `SV12_grouping_witness_L` is `MatchesChecker`.
- `SDL-3` — intersection grouping witness `R` (`type R<T extends string> =
  number & (T & { x: 1 })`): `R<"a">` stayed an unreduced intersection on
  5.8.3 and is `never` on 7.0.2; corpus row `SV13_grouping_witness_R` is
  `MatchesChecker`, and the pair is pinned by
  `signature_corpus_records_the_7_0_2_grouping_witness_as_a_pair`.

## Unrecovered evidence

The reported 9,300-union / 56,548-observation corpus (measured on
`tsgo 7.0.0-dev.20260526.1`) was not recovered. No reproduction claim
is made; the new signature corpus carries its own identity
(`verter-signature-corpus-v0@typescript-7.0.2`). Recorded in
`manifest.json` (`unrecovered_report`).

The 7.1.0-dev nightlies are out of scope for this train (moving
target; their content-mapper work is unrelated).
