# VSW0 evidence index

Selected case IDs: `VSW0-AC1`, `VSW0-AC2`, `VSW0-AC-OWNER`, `VSW0-ratification`.

Commands (run on the candidate; do not treat this file as an execution transcript):

- `node --test tests/vscode-web/VSW0/vsw0.test.mjs`
- `node --test tests/playground/PG0/pg0.test.mjs` (predecessor-receiver harness stays green after the DX0 ownership-map amendment)
- the remaining `docs-domain` final profile commands (see the gate profile catalog)

Deletion population this node: empty (pinned by the WebProductOwnershipMap and discriminated by the VSW0-AC-OWNER premature-deletion twin). VSW0 ratifies contracts and inventories only; no route is displaced or retired, and the charter's placement surfaces (`packages/vue-vscode/src/browser`, `src/shared`, `e2e/web`) are characterized as absent-today — the grounding case fails if VSW0 itself creates them.

VSW0.1 grounding: the startup-adaptation inventory (activation spawn of the verter-lsp child, TypeScript plugin/engine-tier provisioning, the verter-mcp child, tsdk/fs staging, activation gating) was read from the live `packages/vue-vscode` sources named in the feature-contract product; the harness re-reads the manifest and build config live so drift fails rather than stales. VSW0.2: every planned browser route cites BWH0 platform-host services and keeps VSC0 owners/tiers (no desktop fork, no child process, no second engine); NativeOnly rows are labelled gaps under the dx0ClassPortLaw until a DX0 receiving-owner amendment exists, semantic rows additionally under the semanticHostLaw until BWH1..BWH6B deliver the semantic-host proof, and every row citing a live desktop-tier DesktopWebSharedBoundary module names that module and its VSW1 conversion (or desktop-by-design route) instead of claiming host-neutral readiness. VSW0.3: the HostStatusVocabulary separates vscode.dev/github.dev local web workers (browser-local) from Codespaces/SSH/WSL/tunnel remote extension hosts (remote-native) and the desktop host, with the non-parity claim law bound to the DX0 hostIdentity receipt field.

VSW0-AC1's admission facts are live-derived and fail-closed: the desktop and browser bundle platforms are parsed from the esbuild config text (a missing platform value is an error, never an implicit browser verdict), and the browser-entry Node-import fact walks the real entry module graph (the twin uses the live `src/extension.ts` graph whose `vscode-languageclient/node` import is derived, not hand-set). The predicate admits the dual-entry product the policy requires (main kept on its node build, browser entry declared, browser-targeted browser bundle, node-free entry graph) and rejects main-only, browser-only, node-platform browser bundles and Node-importing browser entries alike.

AC-BASIS is bound to VSW1 (browser entrypoint build admission and shared-code module-load runtime checks) and VSW5 (packaging, offline and browser-matrix qualification) as downstream runtime-test owners; AC-RESOURCE is not applicable (no hot paths, no UI, 0 production LOC) and binds to VSW5 for future real-client claims; AC-EXPOSURE registration is owned by DX1.

This file does not claim the cases executed. Copying the charter here is not evidence.
