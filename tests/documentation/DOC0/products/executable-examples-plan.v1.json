{
  "id": "executable-examples-plan.v1",
  "harnessOwner": "DOC1",
  "home": "examples/reference",
  "status": "unbuilt",
  "registration": "the public executable-example home is registered by contracts/web-product-expansion-v1.md §7 and built by DOC1",
  "currentEstates": [
    {
      "path": "examples",
      "kind": "sandbox-app",
      "audience": "contributor",
      "evidence": ["examples/package.json", "examples/src"],
      "rule": "sandbox application exercised by compiler and conformance work; not a published user example"
    },
    {
      "path": "packages/example",
      "kind": "compiler-fixture",
      "audience": "contributor",
      "evidence": ["packages/example/package.json"],
      "rule": "fixture components for compiler tests; not a published user example"
    }
  ],
  "requirements": [
    "an example is publishable only when it runs under a release-selected documentation check with pinned engine and package versions",
    "example output is reproducible from the product receipt basis of contracts/product-experience.md §4",
    "screenshots and roadmap claims never stand in for working examples",
    "until DOC1 delivers the harness, no documentation page may present an example as executed evidence"
  ],
  "doc1Acceptance": [
    "the harness executes every published example in the release lane",
    "reference pages generated by DOC3 are validated against their sources by the same harness",
    "a broken or drifted example fails the release-selected documentation checks before promotion"
  ]
}
