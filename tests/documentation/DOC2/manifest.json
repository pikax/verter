{
  "node": "DOC2",
  "kind": "implementation",
  "products": [
    "docs/contributing/architecture-contracts.md (owner boundaries, layer rules, hotspot surfaces, capability evidence basis)",
    "docs/contributing/source-identity.md (canonical file identity, span types, position encodings, stable hashes, source maps)",
    "docs/contributing/query-lifetimes.md (lifetime classes, memo identity, scheduler generations, determinism)",
    "docs/contributing/first-contribution.md (end-to-end lint-rule walkthrough on the owning interfaces)",
    "docs/scripts/reference-harness.mjs contributorDocs section (sole owning interface for contributor-docs checks)",
    "contributor-docs-model.v1 machine product"
  ],
  "cases": [
    {
      "id": "DOC2-AC1",
      "disposition": "reject",
      "twins": [
        "clean contributor docs pass and cover every contract hotspot",
        "every contributor page is registered in the DOC0 docs inventory",
        "a page unlisted by the contributing index fails",
        "a missing contract source fails"
      ]
    },
    {
      "id": "DOC2-AC2",
      "disposition": "reject",
      "twins": [
        "a tutorial that teaches a local resolver fails",
        "a tutorial that teaches a duplicate cache fails",
        "a same-crate duplicate memo cache fails on authority",
        "substituting the memo authority fails on authority",
        "a tutorial that teaches a test-only scheduler hook fails",
        "a page-only test-only hook recommendation fails without a model edit",
        "a taught symbol dropped from its documentation page fails",
        "a taught interface whose symbol left its source fails",
        "a cited source path that no longer exists fails",
        "the walkthrough teaches the canonical owners, not local substitutes"
      ]
    },
    {
      "id": "DOC2-AC3",
      "disposition": "reject",
      "twins": [
        "perturbed model order yields an identical canonical receipt",
        "incremental receipt with the same digest matches a fresh run",
        "a taught-source edit changes receipt identity",
        "a catalog-only edit changes receipt identity",
        "edit fails and revert restores a complete receipt",
        "pre-aborted signal yields cancelled and never complete",
        "mid-page cancellation yields cancelled with a source digest",
        "a missing contributor page is a partial result"
      ]
    },
    {
      "id": "DOC2-AC4",
      "disposition": "accept",
      "twins": [
        "clean contributor docs pass and cover every contract hotspot",
        "every contributor page is registered in the DOC0 docs inventory",
        "the walkthrough cites the current contracts and real code",
        "an undocumented retained scheduler surface fails",
        "an unmapped retained surface fails"
      ]
    },
    {
      "id": "DOC2-AC5",
      "disposition": "accept",
      "twins": [
        "clean contributor docs pass and cover every contract hotspot"
      ]
    }
  ],
  "runtimeCorrectness": false,
  "notes": "Implementation node (documentation delivery): four contributor pages under docs/contributing plus the contributorDocs validation section of the existing docs reference harness. Static checks only (proofClass static-proof): no example execution, no runtime spawn. Production change is the harness extension (1 file); pages, model, tests and inventory rows are docs/test artifacts inventoried separately. AC3 N/A rationale: no production state, query, map or scheduling boundary is touched at this node; their fresh/incremental, edit/revert, cancellation, stale/partial and determinism proofs remain owned by their untouched owners (rev11.scheduler-runtime admission and cancellation, rev11.flow dispatch, B4R0 source lineage, LSO0 snapshot publication), and the equivalent docs-boundary properties are proven on the harness receipt itself including capabilityCatalog digest identity. ARH1 ac4 obligation discharged: per-hotspot import direction, constructor capabilities, state lifetimes and the complete retained public surfaces are documented with the contract rows cited as source of truth; each retained surface maps to DOC0 catalog ids (no second matrix); host/profile evidence names the contract candidate SHA, records the original measurement host as unrecorded in ARH1 products, and names the live architecture-health ubuntu-latest / .nvmrc re-derivation (not `--provenance`)."
}
