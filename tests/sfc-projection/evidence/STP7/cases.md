# STP7 evidence index

Input snapshot: `da71a1dfb44208ee8a21af2b4c3c929a288a103d` (captured STP7 candidate) on `tama_dag/0.1.0-tama/STP7`.

Engine pins: `tests/sfc-projection/STP1/products/engine-matrix.json` — ts-js `typescript@6.0.3` (`packages/playground/node_modules/typescript`), ts-native `typescript@7.0.2` (`typescript/unstable/sync`). Svelte adapter owner: CCA1I `SvelteProjectionBackend`.

Selected case IDs: `STP7-svelte-shape`, `STP7-holes`, `STP7-realm`, `STP7-reuse`, `STP7-scope-claim`.

Commands (run on the candidate; do not treat this file as an execution transcript):

- `node scripts/sfc-projection/verify-node.mjs --node STP7 --engine all --require-all --json`
- `node --test tests/sfc-projection/STP7/framework-boundary.spec.ts`
- `node --test scripts/sfc-projection/verify-node.test.mjs`

Products: `SecondFrameworkBoundaryEvidence`, `ExecutionContextBoundaryContract`.

Deletion population this node: empty. Later Vue removal: STP58. Later Svelte removal: STS15. ABI ratification: STP8. Full Svelte typing/IDE: STS15. Astro/MDX/Lit retain their own roadmap claims.

STP7-AC3/AC4 production projection state remains with the untouched owners named in `tests/sfc-projection/STP7/products/second-framework-boundary-evidence.json`. Harness-owned probe runs are `incremental=fresh` and close the native API after use.

Negative twins discriminate: a Vue constructor/ref/directive/event record required by Svelte, a one-way hole map that drops the authored expression, a same-program server/client pair claimed as ambient isolation, and an architecture proof advertising full Astro/MDX/Lit support.

AC2 edit application remains with the existing provider/LSO snapshot authority. This proof records hover, definition, references and edit participation on the Svelte-shaped fixture; it does not add a production edit applicator.

This file does not claim the cases executed. Copying the charter here is not evidence.
