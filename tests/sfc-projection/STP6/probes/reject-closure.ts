import type { Hidden } from "@stp6/lib/__virtual_sfc";

/** Emitted declaration / consumer path into a private virtual unit. */
export const captured: Hidden = null as never;
export const stp6HoverTarget = captured;
export const stp6DefinitionTarget = captured;
