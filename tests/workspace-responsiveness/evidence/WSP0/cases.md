# WSP0 evidence index

Selected case IDs: `WSP0-AC1`, `WSP0-AC2`, `WSP0-AC3`, `WSP0-AC-OWNER`, `WSP0-AC-BASIS`, `WSP0-AC-RESOURCE`, `WSP0-AC-EXPOSURE`, `WSP0-ratification`.

Commands (run on the candidate; do not treat this file as an execution transcript):

- `node tests/workspace-responsiveness/WSP0/verify.mjs`
- `node --test tests/workspace-responsiveness/WSP0/wsp0.test.mjs`
- `node roadmap/0.1.0-tama/tools/validate-program-dag.mjs --strict`
- the remaining `docs-domain` final profile commands (see `catalogs/gate-profiles.toml`)

Deletion population this node: empty. The boundary is additive; no route is displaced or retired by WSP0.

AC1 grounding: the server-only-responsiveness, server-dependent-ui-metric and server-certified-interactivity twins are planted negatives proving a client-domain budget with input-to-paint semantics is mandatory; no runtime is executed (WSP1 owns instrumentation).

AC2 grounding: the reduced-feature-set, diagnostic-truncation, unequal-work-denominator and forbidden-improvement twins pin the correctness denominator (all RequiredCurrent features, full publication, full membership, no truncation) as part of every budget.

AC3 grounding: the original issue facts (id 93, opened 2026-07-25, suspected excess LSP information, no proved root cause) and the wholly unrecorded version-capture checklist are pinned; the unverified-issue-claim and fabricated-version-capture twins prove the label cannot silently become fixed.

AC-BASIS is bound to WSP1 (registered in-train successor) as the downstream runtime-test owner — a docs-only node adds no meaningless runtime test; PER0, WPF0 and JBT1 bind through their own charters. AC-RESOURCE grounds itself in the live MEM0 workload corpus (`catalogs/semantic-memory-workload/`, 9 fixtures at ratification) and the empty reference-machine population; every not-yet-instrumented metric is labelled. AC-EXPOSURE records zero introduced operations; later exposures register through DX1.

This file does not claim the cases executed. Copying the charter here is not evidence.
