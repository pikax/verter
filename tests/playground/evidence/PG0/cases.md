# PG0 evidence index

Selected case IDs: `PG0-AC1`, `PG0-AC2`, `PG0-AC-OWNER`, `PG0-grounding`.

Commands (run on the candidate; do not treat this file as an execution transcript):

- `node --test tests/playground/PG0/pg0.test.mjs`
- `pnpm docs:build` (the surviving `docs-domain` final-profile command; the profile's roadmap validators went with the repository roadmap in b940abc1e — the DAG is database-owned)

Deletion population this node: empty. No route is displaced or retired by PG0; the manually curated compiler-demo tab list (`packages/playground/src/output/Output.vue` `allTabs`) and the sibling App.vue `Output|Why?` nav (the Why? control mounts `packages/playground/src/components/AuditTree.vue`) are characterized and preserved, and PG2 owns their cutover to the generated matrix.

PG0 grounding: the matrix was generated by joining every one of the 23 DX0 `feature-exposure-contract.v1.json` operation ids (hostExecutionClass, answerRail, browserExecutable, playground status, promotionBlocked, exposure.replayCase, the blockedUntil route and replay case, and the unlabelledAnalysisMerge pins read from the shipped descriptors, never restated independently) and mapping the twelve PG0.2 feature families plus the audit-provenance descriptor-gap family (which owns the preserved App.vue Why?/AuditTree.vue surface; DX0 has no why/audit operation id, and the missing metadata family's gap evidence does not cover it) to their route owners across PG1–PG17; a missing family cites only the DX0 gap evidence for its own operations, and `displacedRoute.tabs` maps every live Output.vue tab mode plus the ssr insert while `displacedRoute.appNav` maps every live App.vue nav control to its owning family and panel, so no user-visible surface loses its surviving producer at the PG2 cutover. The WorkbenchSession model binds the five DX0 `product-receipt-basis.v1.json` fields verbatim, and its project obligations ride the BWH0 `platform-host-services.v1.json` io service rather than a new VFS authority. Family states are derived (exposed/partial/missing/future), and the performance family is the AC2 future case: named producer PG14, no shipping operation, no success presentation.

AC-BASIS is bound to PG2, PG12 and PG16 as downstream runtime-test owners; AC-RESOURCE is not applicable (no hot paths, no UI, 0 production LOC) and binds to PG14 for future real-client claims; AC-EXPOSURE registration is owned by DX1, with schema and replay cases registered in the matrix until DX1 ships.

This file does not claim the cases executed. Copying the charter here is not evidence.
