{
  "name": "@verter/wasm",
  "version": "0.0.1-beta.1",
  "description": "WASM bindings for Verter Vue compiler",
  "keywords": [
    "compiler",
    "vue",
    "wasm",
    "webassembly"
  ],
  "homepage": "https://verterjs.dev/",
  "license": "MIT",
  "author": "pikax",
  "repository": {
    "type": "git",
    "url": "https://github.com/pikax/verter"
  },
  "files": [
    "dist",
    "wasm"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.mts",
  "exports": {
    ".": {
      "types": {
        "import": "./dist/index.d.mts",
        "require": "./dist/index.d.cts"
      },
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs"
    }
  },
  "scripts": {
    "clean:wasm": "npx shx rm -rf wasm/snippets && npx shx rm -f wasm/verter_wasm.js wasm/verter_wasm.d.ts wasm/verter_wasm_bg.wasm wasm/verter_wasm_bg.wasm.d.ts wasm/package.json wasm/README.md wasm/LICENSE",
    "build:wasm": "pnpm run clean:wasm && cargo build --manifest-path ../../Cargo.toml --release -p verter_wasm --target wasm32-unknown-unknown && wasm-bindgen --target web --out-dir wasm --out-name verter_wasm ../../target/wasm32-unknown-unknown/release/verter_wasm.wasm && pnpm run opt:wasm",
    "opt:wasm": "wasm-opt -Os wasm/verter_wasm_bg.wasm -o wasm/verter_wasm_bg.wasm",
    "build:ts": "tsdown",
    "build:copy-playground": "npx shx cp wasm/verter_wasm_bg.wasm ../playground/public/verter_wasm_bg.wasm",
    "build": "pnpm run build:wasm && pnpm run build:ts && pnpm run build:copy-playground",
    "test": "vitest run"
  },
  "devDependencies": {
    "@verter/native": "workspace:^",
    "@verter/types": "workspace:^",
    "tsdown": "^0.22.0",
    "typescript": "^6.0.3",
    "binaryen": "^130.0.0",
    "vitest": "^4.1.7"
  },
  "engines": {
    "node": ">=18"
  }
}
