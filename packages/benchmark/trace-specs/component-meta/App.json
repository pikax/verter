{
  "component": "App",
  "componentPath": "src/runtime/components/App.vue",
  "maxTotalDurationMs": 1500,
  "required": [
    {
      "namePattern": "compute_component_meta_state",
      "detailPattern": "mode=Expanded",
      "note": "session path must run compute phase in Expanded mode"
    },
    {
      "namePattern": "extract_component_meta",
      "detailPattern": "has_evaluated_types=true",
      "note": "extract_component_meta must report has_evaluated_types=true on session path"
    },
    {
      "namePattern": "extract_component_meta",
      "detailPattern": "macros=3",
      "note": "App has 3 macros"
    },
    {
      "namePattern": "rematerialize_public_component_meta_types",
      "note": "session path materializes public component meta types after extract"
    }
  ],
  "forbidden": [
    {
      "namePattern": "/^resolve_component_meta$/",
      "note": "host-path resolve_component_meta span must not appear on session path"
    },
    {
      "namePattern": "/^resolve_component_meta_result$/",
      "note": "host-path resolve_component_meta_result point event is retired on session path"
    },
    {
      "namePattern": "compute_evaluated_types_expand_macros",
      "detailPattern": "store_view=true",
      "note": "store_view is a retired concept — macro expansion runs without store_view on session path"
    },
    {
      "namePattern": "seed_imported_dependency_base_in_view",
      "note": "legacy imported-dependency seeding path must not execute"
    },
    {
      "namePattern": "legacy_resolved_type_cache",
      "note": "legacy resolved type cache must not be populated"
    },
    {
      "namePattern": "cached_dependency_resolution_in_view_result",
      "note": "retired with store_view — must not appear in session path"
    }
  ],
  "maxCounts": [
    {
      "namePattern": "/^resolve_imported_type_root$/",
      "maxCount": 90,
      "note": "App resolves ~30 imported type roots (start+end+result events ~60); allow 50% headroom"
    },
    {
      "namePattern": "current_eval_state",
      "maxCount": 90,
      "note": "current_eval_state baseline ~58 calls; allow 50% headroom for variance"
    }
  ],
  "maxDurations": [
    {
      "namePattern": "compute_component_meta_state",
      "maxDurationMs": 1200,
      "note": "compute phase baseline ~582ms; allow 2x headroom"
    },
    {
      "namePattern": "rematerialize_public_component_meta_types",
      "maxDurationMs": 100,
      "note": "rematerialize phase baseline ~35ms; allow generous headroom"
    },
    {
      "namePattern": "compute_evaluated_types_expand_macros",
      "maxDurationMs": 20,
      "note": "macro expansion baseline ~3ms; allow generous headroom"
    },
    {
      "namePattern": "/^resolve_imported_type_root$/",
      "maxDurationMs": 60,
      "note": "any single imported type root resolution should take under 60ms (baseline max ~28ms)"
    }
  ],
  "expectedResult": {
    "minProps": 7,
    "minSlots": 1,
    "requireEvaluatedTypes": true,
    "note": "App must return at least 7 props, 1 slot with evaluated types"
  }
}
