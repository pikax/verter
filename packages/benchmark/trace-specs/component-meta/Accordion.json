{
  "component": "Accordion",
  "componentPath": "src/runtime/components/Accordion.vue",
  "maxTotalDurationMs": 5000,
  "required": [
    {
      "namePattern": "resolve_component_meta",
      "detailPattern": "mode=Expanded",
      "note": "must resolve component meta in `ResolverMode::Expanded`"
    },
    {
      "namePattern": "compute_evaluated_types_expand_macros",
      "detailPattern": "macros=4",
      "note": "Accordion has 4 macros (defineProps, defineEmits, defineSlots, withDefaults)"
    },
    {
      "namePattern": "resolve_component_meta_result",
      "detailPattern": "has_evaluated_types=true",
      "note": "must produce evaluated types"
    },
    {
      "namePattern": "compute_evaluated_types_expand_macros",
      "detailPattern": "store_view=true",
      "note": "macro expansion must run with a valid store view"
    }
  ],
  "forbidden": [
    {
      "namePattern": "seed_imported_dependency_base_in_view",
      "note": "legacy imported-dependency seeding path must not execute — semantic DB owns all dependency resolution"
    },
    {
      "namePattern": "legacy_resolved_type_cache",
      "note": "legacy resolved type cache must not be populated — all type resolution goes through the semantic DB path"
    },
    {
      "namePattern": "current_eval_state",
      "detailPattern": "/types/index\\.ts.*store_view=false/",
      "note": "types/index.ts barrel must not be accessed without a store view — indicates fallback to permissive reopening path"
    }
  ],
  "maxCounts": [
    {
      "namePattern": "resolve_imported_type_root",
      "maxCount": 12,
      "note": "should resolve at most 6 imported type roots (start+end = 12 events); currently 4 roots"
    },
    {
      "namePattern": "current_eval_state",
      "maxCount": 70,
      "note": "current_eval_state calls should be bounded; baseline is 38-53 across runs, allow headroom for variance"
    }
  ],
  "maxDurations": [
    {
      "namePattern": "resolve_component_meta",
      "maxDurationMs": 5000,
      "note": "total component meta resolution should complete within 5s"
    },
    {
      "namePattern": "compute_evaluated_types_expand_macros",
      "maxDurationMs": 4000,
      "note": "macro expansion should complete within 4s"
    },
    {
      "namePattern": "resolve_imported_type_root",
      "maxDurationMs": 500,
      "note": "any single imported type root resolution should take under 500ms"
    }
  ],
  "expectedResult": {
    "minProps": 13,
    "minEvents": 1,
    "minSlots": 5,
    "requireEvaluatedTypes": true,
    "note": "Accordion must return 13 props, 1 event, 5 slots with evaluated types"
  }
}
