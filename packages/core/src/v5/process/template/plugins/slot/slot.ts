import { TemplatePlugin } from "../../template";


export const SlotPlugin = {
    name: "VerterSlot",

    transformSlotDeclaration(slot, s, ctx) {
    },
    transformSlotRender(slot, s, ctx) {

    }
} as TemplatePlugin