import { VerterASTNode } from "../../ast";
import { ScriptItem, ScriptTypes } from "../types";

export function handleOptionsNode(node: VerterASTNode): ScriptItem[] {
  switch (node.type) {
    
  
    default:
      return [];
  }
}
