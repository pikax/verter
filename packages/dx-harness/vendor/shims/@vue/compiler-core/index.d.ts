// Version-pinned `@vue/compiler-core` marker. The generated baseline TSX never
// imports it; it is vendored so the workspace's `@vue/*` line is represented in
// the committed shim set and validated by the bridge's vendored-Vue version sync.

export {};
