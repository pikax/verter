/* This file is auto-generated by packages/types/scripts/generate-tsx-attributes.js */

/**
 * Maps HTML element types to their corresponding Vue HTML attribute types.
 *
 * This type provides a direct mapping from DOM element interfaces (e.g., `HTMLAudioElement`)
 * to Vue's corresponding attribute types (e.g., `AudioHTMLAttributes`). This enables
 * type-safe extraction of element-specific attributes.
 *
 * **Important**: The conditional type ordering is critical due to TypeScript's structural typing!
 * More specific element types must be checked before their parent types:
 * - `HTMLVideoElement` and `HTMLAudioElement` must come before `HTMLMediaElement`
 * - All specific element types must come before the generic `HTMLElement` fallback
 *
 * Note: Some HTML elements like `<fieldset>`, `<keygen>`, and `<webview>` don't have
 * dedicated DOM element interfaces, so they fall through to the final `HTMLAttributes` case.
 * Similarly, `<colgroup>` and `<th>` share DOM types with `<col>` and `<td>` respectively.
 *
 * @example
 * ```ts
 * // Specific element types get their corresponding attributes
 * type AudioAttrs = ExtractFromHTMLElement<HTMLAudioElement>;
 * // => AudioHTMLAttributes
 *
 * type InputAttrs = ExtractFromHTMLElement<HTMLInputElement>;
 * // => InputHTMLAttributes
 *
 * type VideoAttrs = ExtractFromHTMLElement<HTMLVideoElement>;
 * // => VideoHTMLAttributes
 *
 * // Generic HTMLElement falls back to base HTMLAttributes
 * type GenericAttrs = ExtractFromHTMLElement<HTMLElement>;
 * // => HTMLAttributes
 * ```
 *
 * @typeParam T - An HTML element type (must extend HTMLElement)
 */
export type ExtractFromHTMLElement<T> =
  T extends HTMLAnchorElement
  ? import("vue").AnchorHTMLAttributes
  : T extends HTMLAreaElement
  ? import("vue").AreaHTMLAttributes
  : T extends HTMLBaseElement
  ? import("vue").BaseHTMLAttributes
  : T extends HTMLQuoteElement
  ? import("vue").BlockquoteHTMLAttributes
  : T extends HTMLButtonElement
  ? import("vue").ButtonHTMLAttributes
  : T extends HTMLCanvasElement
  ? import("vue").CanvasHTMLAttributes
  : T extends HTMLTableColElement
  ? import("vue").ColHTMLAttributes
  : T extends HTMLDetailsElement
  ? import("vue").DetailsHTMLAttributes
  : T extends HTMLModElement
  ? import("vue").DelHTMLAttributes
  : T extends HTMLDialogElement
  ? import("vue").DialogHTMLAttributes
  : T extends HTMLEmbedElement
  ? import("vue").EmbedHTMLAttributes
  : T extends HTMLFormElement
  ? import("vue").FormHTMLAttributes
  : T extends HTMLHtmlElement
  ? import("vue").HtmlHTMLAttributes
  : T extends HTMLIFrameElement
  ? import("vue").IframeHTMLAttributes
  : T extends HTMLImageElement
  ? import("vue").ImgHTMLAttributes
  : T extends HTMLInputElement
  ? import("vue").InputHTMLAttributes
  : T extends HTMLLabelElement
  ? import("vue").LabelHTMLAttributes
  : T extends HTMLLIElement
  ? import("vue").LiHTMLAttributes
  : T extends HTMLLinkElement
  ? import("vue").LinkHTMLAttributes
  : T extends HTMLMapElement
  ? import("vue").MapHTMLAttributes
  : T extends HTMLMenuElement
  ? import("vue").MenuHTMLAttributes
  : T extends HTMLMetaElement
  ? import("vue").MetaHTMLAttributes
  : T extends HTMLMeterElement
  ? import("vue").MeterHTMLAttributes
  : T extends HTMLObjectElement
  ? import("vue").ObjectHTMLAttributes
  : T extends HTMLOListElement
  ? import("vue").OlHTMLAttributes
  : T extends HTMLOptGroupElement
  ? import("vue").OptgroupHTMLAttributes
  : T extends HTMLOptionElement
  ? import("vue").OptionHTMLAttributes
  : T extends HTMLOutputElement
  ? import("vue").OutputHTMLAttributes
  : T extends HTMLParamElement
  ? import("vue").ParamHTMLAttributes
  : T extends HTMLProgressElement
  ? import("vue").ProgressHTMLAttributes
  : T extends HTMLScriptElement
  ? import("vue").ScriptHTMLAttributes
  : T extends HTMLSelectElement
  ? import("vue").SelectHTMLAttributes
  : T extends HTMLSourceElement
  ? import("vue").SourceHTMLAttributes
  : T extends HTMLStyleElement
  ? import("vue").StyleHTMLAttributes
  : T extends HTMLTableElement
  ? import("vue").TableHTMLAttributes
  : T extends HTMLTextAreaElement
  ? import("vue").TextareaHTMLAttributes
  : T extends HTMLTableCellElement
  ? import("vue").TdHTMLAttributes
  : T extends HTMLTimeElement
  ? import("vue").TimeHTMLAttributes
  : T extends HTMLTrackElement
  ? import("vue").TrackHTMLAttributes
  : T extends HTMLDataElement
  ? import("vue").DataHTMLAttributes
  : T extends HTMLVideoElement
  ? import("vue").VideoHTMLAttributes
  : T extends HTMLAudioElement
  ? import("vue").AudioHTMLAttributes
  : T extends HTMLMediaElement
  ? import("vue").MediaHTMLAttributes
  : import("vue").HTMLAttributes;
