# Changelog

All notable changes to this project will be documented in this file.

### Component-meta cold-path long-tail track

Plan §1 / §10 / §11 / §12 — sixteen-commit refactor track on
`refactor/semantic-db-overhaul`. Introduced the session-layer
structural materialiser (`materialize_component_meta_structure`,
`MaterializeStructureDb`, per-shape Object handler with
`MaterializeOutcome` cacheability matrix) and atomically cut the
legacy `walk_component_meta_member_surface_expr` shim over to
the new entry (plan §11.2 / §11.4). Added Phase 1 audit-surface
observability (six new `RustStoreAudit` u64 counters —
`materialize_structure_calls`, `materialize_structure_cache_hits`,
`node_arena_lock_acquisitions`, `family_map_lock_acquisitions`,
`dep_signature_merges`, `dep_signature_intern_hits` — plus five
new `StructuredComponentMetaEvent` variants and the
`CacheOutcomeKind::Tainted` / `MaterializeSkipReason` enums per
plan §3.2 / §3.3). Hardened the cache cluster with the Γ.A
NodeArena per-canonical shard (plan §11.5 Γ.A), Γ.B family-map
reverse-index (Γ.B), Γ.C `DepSignatureInterner` with sweep (Γ.C),
and the Fix-D substitute change-tracking. Deleted the dead
`MaterializedMemberSurfaceDb` family with zero callers
post-cutover (plan §11.2). Stabilised
`cooperative_admission_panic_wakes_waiters` via
`mpsc::sync_channel(0)` rendezvous and refreshed the audit-
footprint docs to reflect the post-cutover surface (plan §11.6).

### Bug Fixes

- V-for sort resolve keys (db36584)
- Panic handling on some expressions (47f3d4f)
- Better parity with vue compiler and also support custom blocks (ef11445)
- Fix gen bugs (e16f5e7)
- Strip inter-block HTML comments and fix slot fallback codegen (24a6999)
- Template-only exports, TS declare stripping, dynamic :is expressions (063e7ae)
- DefineOptions stripping, empty event handlers, mixed slot children (ebac434)
- Use pipeline events for SFC block ranges instead of naive byte scan (b40da82)
- V-for conditional (1a98cf4)
- Hyphenated slot names, v-once+v-if, ref comma, shorthand expansion (770b964)
- Exclude v-show/directive from mergeProps prop counter (3fd2bc5)
- Recursive component self-reference, cross-env for Windows tests (9cdde7e)
- DefineProps/withDefaults macro replacement matching Vue output (0562df5)
- Whitespace condensation, v-for destructuring, slot text wrapping (a11eae5)
- Whitespace condense removes all-ws text at first/last child position (0e71652)
- Merge dual script blocks for inheritAttrs and component options (7963255)
- Emit \_withDirectives for components with children (0dd092a)
- Quote hyphenated slot prop keys in renderSlot output (4779636)
- V-show on components needs \_withDirectives prefix (8998b97)
- Strip HTML comments between v-if/v-else-if branches (f5a365f)
- Escape newlines in multi-line HTML comment codegen (af732c2)
- Fixes (6610eec)
- Fix benchmark (5e06352)
- Merge duplicate event handler keys and fix destructured props binding (cd91a60)
- Emit \_\_scopeId for template-only components with scoped styles (5c20def)
- Replace removed compile() with VerterHost API (147ccef)
- Use bracket notation for reserved word bindings in template codegen (ddbc41b)
- Detect top-level await inside TS expression wrappers (3f7ffe0)
- Scope selectors inside @media/@supports blocks and add fast CSS scoper (a0daab8)
- Set explicit body color/background in preview iframe (55fbd45)
- Wrap single-element children in array and expand HTML entity decoder (5ed7d18)
- Remove dark mode color-scheme from preview iframe (76ac349)
- Fix \_withAsyncContext parens, $event prefixing, and built-in components (7a0694d)
- Fix Analysis tab crash from snake_case serde serialization (dd44ba3)
- Fix unrecoverable restart failure and TSGO orphan prevention (49f6ffb)
- Fix build (bcffa76)
- Fix (96b74e7)
- Fix playground build (b501c41)
- Fix text lint rule (1580ca6)
- Fix benchmarks CI (2c12470)
- Partial AST recovery and parameter hover source maps (3ab34d0)
- Route all codegen output through CodeTransform operations (dba876e)
- Handle trailing commas in tsconfig.json and improve vite config eval (f03a8ac)
- Fully async initialized() to prevent startup freeze (cf174e5)
- Resolve @/ path aliases and .vue.ts imports in TypeScript plugin (c7ee171)
- Bundle TypeScript fallback, scan monorepo tsconfigs, refresh stale diagnostics (73a28d7)
- Correct off-by-1 source map position for v-bind shorthand prop names (450ad38)
- Use bracket notation for hyphenated slot names in IDE codegen (68e6334)
- Preserve component types through barrel re-exports (77c00ee)
- Go-to-definition cross-file navigation and prop resolution (7f3a1ce)
- Slot outlet hover source maps and class/style merge redirect (72335fd)
- Use TypeScript grammar for template expression syntax highlighting (62c02b0)
- Add Verter-level hover for slot outlet elements (c11a451)
- Prevent crash on .d.ts files and filter completion triggers (3ede339)
- Deduplicate hover kind prefix and add Vue reactivity labels (b8b7e0a)
- Send workspace/diagnosticRefresh after debounced sync (7a1adff)
- Prevent TS type assertion syntax from breaking template highlighting (7c99601)
- Add emits-to-props in .vue.ts public API $props type (951d0fe)
- Prioritize local variables over globals in script completions (b6712aa)
- Use single cursor context pass for template completion transforms (dc10952)
- Improve cursor (00f74ed)
- Switch to push diagnostics to eliminate flickering during typing (5d677c3)
- Improve template completion quality (kind mapping + global filtering) (d30c8ca)
- Recompute verter diagnostics on typing + refresh after background init (4308bf6)
- Filter type provider globals in Unknown expression context (1b6639e)
- Resolve 8 E2E test failures across definition, hover, diagnostics, and TS plugin (92d250b)
- Follow barrel re-exports to actual .vue definition (f1ce920)
- Fix 4 MCP output quality bugs in summary, props check, dead code, and scoring (16e1a6c)
- Emit both kebab and camelCase $props keys for hyphenated events (d2e955c)
- Use \_\_EmitToProps for type-based emits to preserve handler types (fe5f7f6)
- Fix 6 false positive diagnostic bugs + analysis gap (a6f5231)
- Fix 10 IDE template codegen bugs and verter-tsc pipeline issues (9c86746)
- Fix template-first SFC ordering and global component TDZ in IDE codegen (6569b60)
- Fix 4 IDE codegen bugs causing false TSX errors (69aca8c)
- Fix slot outlet inside v-for producing object literal ambiguity (14b77ef)
- Filter Vue JSX children errors and temp tsconfig diagnostics (cf25d1f)
- Expand Vue JSX type gap filter to cover textContent and innerHTML (bb193a3)
- Merge v-show with existing :style binding to avoid duplicate JSX attributes (3181f5a)
- Handle pseudo-relative absolute paths in verter-tsc import rewriting (e3310bc)
- Prevent duplicate JSX attributes from v-model and repeated event handlers (ebad545)
- Strip trailing commas from tsconfig.json (JSONC compatibility) (246ee71)
- Escape newlines in static style/class values merged with normalizeStyle (9f3b535)
- Emit closing fragment for self-closing <template> elements in JSX (6dff65d)
- Suppress JSX comments before <component :is> with v-if IIFE (efd6822)
- Use PascalCase for kebab-case component names in Comp functions (f51c31a)
- Skip Comp function for dynamic <component :is> elements (4bf348e)
- Declare ***VERTER***props for destructured defineProps (ba34251)
- Emit unknown-typed Comp function for <component :is> elements (1bd7a6e)
- Fix 4 IDE TSX codegen issues for template-only and prop-scoped SFCs (6deee4d)
- Rewrite closing tag name when case differs from opening tag (edbaba1)
- Auto-append tsconfig.json when --project is a directory (5483a74)
- Add 5-minute timeout to verter-tsc type checker subprocess (437c863)
- Suppress TS6133 for generated \_\_props and auto-var declarations (e6048d7)
- Use actual line count instead of hardcoded 1_000_000 in tsserver commands (55a2359)
- Prevent index-out-of-bounds panic in SSR merge_duplicate_event_handlers (e25f84c)
- Skip PROPS patch flag for literal bind expressions (97d5c6b)
- Eliminate patch flag count diffs in VDOM codegen (e610bee)
- Simplify TSC constructor to avoid excessive type depth (6cf326f)
- Drop ComponentPublicInstance from TSC constructor return type (d71857e)
- Typed emit args + preserve kebab-case events in IDE TSX (c0cb0a5)
- Await client.start() and guard TsgoStarted notification (a05d5e9)
- Slot cache codegen fixes + hoisted patchFlag for cached VNodes (c29509e)
- Emit void() for script/style-referenced bindings in IDE codegen (a85bd20)
- Release NAPI VerterHost ref in closeBundle to allow process exit (0119749)
- Avoid double preprocessing style virtual modules (cd7c55c)
- Enforce absolute internal offsets (04ba0bf)
- Use UTF-16 encoding for TSGO position conversions (4107471)
- Hydrate imported type dependencies (e8c2def)
- Resolve unrelated failing suites (124ffcc)
- Prevent imported macro prop regressions (379d6d6)
- Resolve component event definitions (88eb9a0)
- Resolve go-to-definition for directly imported .vue components (ea81dd4)
- Prefer child component hover context (f3660a3)
- Correct completion item kinds for template bindings (32a75f2)
- Stale TSC extract cache and silent lightweight sync failures (4afc090)
- Skip vite analysis for roots with tsconfig, quiet vite logs (d322c9a)
- Avoid \_\_props prefixing for object property keys and sanitize dotted component names (5c8d2e8)
- Resolve macro types through barrel chains ending at .vue SFCs (9498ede)
- Isolate style preprocessor in child process to prevent exit hang (65871c0)
- Stabilize preprocessor worker lifecycle (a1ce19d)
- A private component library pipeline fixes — slots, bind shorthand, CSS scoping, tsc declarations (a142c79)
- Fix integration test runner infrastructure issues (96aecec)
- Eliminate .verter/ide/ directory and fix TypeProvider regressions (0429e0d)
- Use analysis.imports for Vue priority IDs and add host-backed collision guard (0c1e029)
- Add TYPE_PROVIDER guards to E2E tests and fix @provider parsing (4dd9130)
- Add remaining TYPE_PROVIDER guards and fix completion kind assertions (dc21258)
- Fix three CI check failures on PR (9c1fe06)
- Add null guard for comp.props in ComponentTreeProvider tooltip (2b038f0)
- Suppress component unknown-prop/model diagnostics when TypeProvider active (c78195d)
- Handle non-serializable vite config and widen unplugin dep (7efb56a)
- Tier 2 integration test runner fixes (0af37b0)
- Normalize dot-slash segments in rewritten import paths (ebfac08)
- Strip .vue.ts suffix in generated TSX for cross-component imports (bd2d9ed)
- Downgrade XInvalidExpression to warning and add missing defineProps code action (9c388ca)
- Options API type checking across TS core, IDE, and TSC paths (e4f292d)
- Accept kebab-case components when import exists (#34) (53913fe)
- Add reactive dependency so CssMatch and Outline panels update on typing (4cc2874)

### Documentation

- Add agent feedback capture system (36516b6)
- Add mandatory VS Code extension automated testing requirement (ffbd572)
- Extend VS Code testing requirement to cover verter_lsp changes (36d782b)
- Add agent-neutral guide (8f74e54)
- Update architecture skill with CssModuleInfo reference (d47afc3)
- Add @verter/component-meta to architecture docs (27d5aee)

### Features

- Support for script setup exports (ae0ac00)
- Add Vue playground URL compatibility with fflate encoding (91708cf)
- Add preCompile option for build-start cache warming (096a256)
- Elide SetupImport bindings from **returned** unless used in template (dff7ab6)
- AST-based import elision and companion script import fix (017954f)
- Wrap await in \_withAsyncContext for async script setup (fe2d7f9)
- Add TSX code generation module for IDE type checking (60da706)
- Add Types tab to display TSX output (222d3b5)
- Integrate analsysis and LSP (9347ac9)
- Make utf16 support on js (e0ea40a)
- Add cross-component CSS variable analysis (4c731e1)
- Per-project configuration for multi-root monorepo workspaces (7b47121)
- Add VS Code extension E2E testing framework (060cada)
- Auto-detect composite tsconfigs and prefer tsserver over TSGO (ce85433)
- Extract defineProps prop fields and support go-to-definition for individual props (5389cae)
- Type-resolved defineSlots code actions with cursor filtering (fa0bde8)
- Native cross-file go-to-definition with precise export spans (deaf3c3)
- Surface rich analysis data in playground and VS Code sidebar (e854ac1)
- Per-binding type inlay hints and span-based decorations (8d970c9)
- Experimental prop-based conditional root generic narrowing (9d64c79)
- Add instantiateComponent helper and attrs-fallthrough E2E fixtures (7f76e2e)
- Context-aware template completions and JSX→Vue transformation (c01e955)
- Replace placeholder inlay hints with type provider resolved types (dfbd7fc)
- Infer $attrs and useAttrs() type from root element fallthrough (7002e6f)
- Comprehensive slot hover for all slot syntax variants (2d83d97)
- Attrs parameter support and generic/attrs grammar fixes (6220d93)
- Scope-aware Comp functions for v-slot/v-for template refs (ba29c81)
- Auto-register MCP server with VS Code + dynamic port (15645ed)
- Implement 8 MCP feedback improvements (8bb1f9e)
- Implement block-tree optimization in VDOM codegen (2caec04)
- Emit normalizeProps/guardReactiveProps for v-bind spreads and dynamic keys (1d4b603)
- Implement toHandlers for v-on object spreading (457ed35)
- Implement static hoisting and cache wrapping in VDOM codegen (c01a420)
- Implement VDOM codegen edge cases (withDirectives, dynamic slots, resolveComponent hoisting, static attr hoisting) (1131cd1)
- Report unresolved compile blockers (bb942ef)
- Add host-driven provider sync (2a6fd21)
- Land native resolver core (e7459ea)
- Arc snapshots, batch API, script binding usages, ModuleReferenceCollector refactor (f22c6b3)
- Finish module reference consumer migration (7c88e95)
- Finish shared module reference migration (cab8f9a)
- Hydrate vue compile blockers before sync (f6d3af2)
- Add test-aware vue import resolution (c90ba4f)
- Component opening-tag completions from macro prop/emit fields (b110bf4)
- Typed v-slot params and checkJs for JS SFCs (20b788a)
- TSC extract cache and did_open race fix (1176290)
- Replace lossy dep sets with exact per-specifier import resolutions (3517435)
- Tsconfig-first alias resolution with static vite fallback (cfb39c7)
- Resolve declarationDir/outDir from tsconfig extends chain (0a36fcb)
- Hydrate package-backed macro type dependencies before compile (0c351b7)
- First-class barrel export support — FFI, NAPI, host API, unplugin (c695063)
- Custom directive type checking in IDE codegen (143a28b)
- Implement textDocument/typeDefinition handler (2e677ba)
- Add defineExpose bindings to TSC codegen (6c39f0b)
- Full virtual file system for type providers (0b8a0b4)
- Register file watcher for non-Vue source file changes (b95d486)
- Register file watcher for .vue and config file changes (6b48ea8)
- Add -b/--build flag for tsc build-mode compat (237a79d)
- Implement remaining deferred template error codes (32a7b52)
- Options API instance types + README verification E2E tests (8ce0f4b)
- Infer CSS class names from string literal union types (64b9ae4)
- Add Phase 0 public API stubs for cross-component type resolution (669402b)
- Enhance extract-bare-text with props accessor and scope analysis (f9d5b4d)
- Support @ts-expect-error/@ts-ignore in Vue templates for all element types (6ee1cca)
- Add route analysis for Vue Router, Nuxt, and unplugin-vue-router (e5c583e)
- Extract Options API fields into structured analysis types (020076c)
- Add store/state management tracking and diagnostics (3e7173c)
- Add route analysis LSP handler and VS Code tree view (cfb855e)
- Emit self-referencing component declaration in IDE codegen (#28) (db0da9c)
- Add no-invalid-html-nesting lint rule (#29) (029ecff)
- Add CSS module class name extraction for IDE codegen (#76) (70379a6)
- Add strict slot children type checking (experimental) (f2f23b0)
- Comprehensive SSR integration across all layers (47b047a)
- Emit getCurrentInstance type override in IDE codegen (#11) (afe1a9e)
- Add CSSProperties satisfies annotation for :style objects (#31) (76327ef)
- Add @verter/component-meta package (f22791a)

### Miscellaneous

- Add integration local test (f2925fc)
- Fix integration test infra, add VitePress + E2E support (4b63bfe)
- Only build vuetify docs (694fda1)
- Rename verter-logo.svg to logo.svg (cc3686d)
- Remove 63 dead code warnings from verter_core (05c9f41)
- Update all dependencies to latest versions (6cbfef4)
- Cargo fmt macro_actions (186c948)
- Add launch without volar (7f082b1)
- Apply clippy and fmt auto-fixes (afcd9c0)
- Prefer verter-native binary name and clean stale dist artifacts (ac45e97)
- Commit remaining workspace changes (3543ea4)
- Bump all dependencies and track Cargo.lock (2cff038)
- Gitignore .claude/worktrees/ (6f5e016)
- Apply rustfmt formatting fixes (7f0455f)
- Feedback cleanup — docs, dead code, diagnostics, tracking (081af0e)

### Performance

- Async priority-based workspace scanner for initialization (4a3d1a9)
- Cache parsed SFC to avoid double tokenize+parse (808e40d)
- Replace blind sleeps with adaptive waits in E2E tests (bbfe6bf)
- Benchmark and defer vscode startup (cf8eb41)
- Trim vue cold-start critical path (5d433e2)
- Benchmark provider-backed vue cold start (7bce23c)
- Migrate verter_host HashMap to FxHashMap (b261309)

### Refactoring

- Track SFC metadata in Syntax pipeline, remove event scanning (b007b32)
- Use ast based compiler instead of events (ed3ff34)
- Merge TsxScript/TsxTemplate into single Tsx virtual node (60f6602)
- Make TSX a distinct API separate from virtual files (cc62c4b)
- Replace text-scanning completion context with AST-based detection (839ff7c)
- Refactor lsp resilience and diagnostics test helpers (74acacb)
- Dedup casing helpers, unify whitespace expansion, extract directive checks (9ec7896)
- P2 debt cleanup — AST prop resolver, dead code, dedup, host close, E2E guards (bfe5b60)

### Testing

- Add unplugin-vue-router and npmx.dev to integration tests (ed9fdc7)
- Add barrel export type preservation tests (a076495)
- Add regression tests and ignore known v-else+v-for edge case (cc301e9)
- Add template intellisense regression coverage (fe09f33)
- Add FFI, NAPI, and TypeScript tests for barrel export signatures (63f8184)
- Expand E2E suite to all 9 fixtures with CI matrix (61ebff6)
- Add E2E suites for script-block, style-block, provider-parity, barrel-type-integrity, and rename coverage (7afa604)
- Comprehensive TypeResolver diagnostic tests (62ab375)
- Add dual-script JS SFC codegen tests for IDE and TSC paths (6404a6d)
- Remove TSGO barrel re-export canary guards in E2E tests (a3dbb5f)
- Add comprehensive template compilation error test suite (714ca55)
- Add diagnostic E2E test suite for verter-tsc (a71dada)
- Resolve final 2 deferred error codes (XVBindNoExpression, XVOnNoExpression) (5ff4adb)
- Add DuplicateAttribute regression coverage (888bdc2)
- Add nested macro call detection tests (402a42d)

### Ci

- Fix slidev and add timeout (64c93ce)
- Trying to fix some failing integrations (8113d87)
- Replace unplugin-router with vue/router (2fa2f74)
- Add native-test job and wasm vitest to CI (6c49787)

### Style

- Format core and host sources (9bdb4cc)

### Miscellaneous

- Improve working on benchmark (ecd5dd9)
- Add packages repo (2893ae0)

### Features

- Remove oxc_transform and remove typescript as plugin (474eeba)
- Improve code quality and handling (3fe41aa)

### Refactoring

- Improve template generation, improved vapor generation (#86) (3c73730)

### Ci

- Update workflow to contain changelog changing (bb355e5)
- Performance and benchmark (#85) (270f228)
- Fix benchmark and integration tests (fa95e72)
- Update benchmark and fix build (a2593ad)
- Remove pnpm version from benchmark (b4957f7)
- Update integration (538044d)
- More (efbb99c)
- Integration (041a48c)
- Update integration projects and update benchmark (d368d90)
- Improvements (32efbce)
- Integration (05c62fc)
- Fix (4140784)
- Fix integration (723f64c)
- Grep integration (c9dca0c)
- Another go (de80f02)
- Tarbal approach (a001016)
- Again (c0ce892)
- Only hoist verter (ca277ff)
- Add agentic workflow update-docs (#87) (d5748c2)
- Benchmark results (3a0bceb)
- Fix integration (843d0e3)
- Fix release (d97d194)

### Bug Fixes

- Fix generics (4c19877)
- Fix appending ctx when the name ends in a `.` (69bc5e2)
- Fix binding on multiline (319900b)
- Fix build (cf9b975)
- Fix script-block moving to 0 when is 0 (d5514eb)
- Fix(plugin): handle multi-line and improve block cleanup
  fixes #38 (fd9b2b8)
- Handle when imports end in just spaces or new lines (d93e8e1)
- Literals should be ignored in bindings (220997c)
- Comment empty blocks (#54) (6571902)
- On slot declaretion create unique variable name (#70) (989fa05)
- Improve `component :is` and fix optional props (#73) (6503ea7)
- Invalid error when the return is `any[]` (00f2f99)
- Shallow unwrap defineExpose (d8f9ef5)
- Resolve the model better also making optional boolean default (46debe7)
- $event should always be ignored (4f12bbc)
- Prevent slot from narrowing too many times (9aeae42)
- Handle generics better (350a30b)
- Omitting passed props to rootElement (1c3fc53)
- Do not camelize props in elements (f30850a)
- Offset (875003f)

### Documentation

- Use github alerts for better formatting (#62) (a188ab2)

### Features

- Improve document handling (#8) (e2ab78e)
- Expose function to the binding and support typescript `declare` (7b2d1db)
- Improve bundle type (#55) (e774c15)
- Merge options with PublicInstance (#57) (0ec17a2)
- Support for Component type inferrence (#63) (5bf94ba)
- Support function type inference from template (#40) (2d7af6d)
- Use Comp to retrieve the actual component for a ref (#65) (2de35bc)
- Infer attributes from the root element (#68) (9608e98)
- Improve handling of broken expressions (#69) (afe14eb)
- Add DiagnosticsManager (85b4d76)
- ExtractComponents components can only be from capitalized (5d1f266)
- Improve slots resolution type (541c24a)
- Use shallow unwrap for the template binding (86b3e6d)
- Ignore class and style for fallthrough components (389d333)
- For element use Vue prop type to accurately infer the correct prop (545e3fd)
- Improve directive handling and modifiers (#81) (fbd33ba)
- Single bundle support (#82) (955140e)
- Add vue tokenizer in rust (#83) (a1b5f0e)
- Use Buffer instead of string when communicating between JS and Rust (00c83aa)
- Add native time to wasm (980b8d3)

### Miscellaneous

- Bump oxc (#43) (39b9baa)
- Update all (#53) (7f82f64)
- Added many examples (#60) (240b2f8)
- Run workflow in every PR (#67) (a86c9f6)
- Improve diagnostics speed (2553cfd)
- Add simple statistics gathering (38e8ece)
- Fix tests (e1cd934)
- Bump dependencies (6d9b6d1)
- Fix tests (9be189d)
- Fix tests (943124f)
- Netlify build headers (23b38e7)
- Format with oxfmt (62863aa)

### Refactoring

- Improve type helpers & components (#39) (1a102c2)

### Ci

- Remove netlify config and nightly play deploy (742068c)
- Change release targets (9312822)
