# Generated TypeScript projection of the native host compile-request
# schema — source of truth is the Rust declarations that decode it
# (`crates/verter_napi/src/host_compile_request_ts.rs` renders them).
# Formatting this file would break the `host_compile_request_ts_freshness`
# byte-pin.
packages/native/host-compile-request.generated.ts

# Generated TypeScript bindings from ts-rs — source of truth is the
# Rust derive annotations in verter_session's component_meta_audit
# module. Formatting this file would diverge from ts-rs output and
# break the `audit_ts_bindings_are_in_sync` test.
packages/types/audit.generated.ts

# Vendored official Vue RC compiler output (conformance goldens) — the
# committed bytes ARE the oracle: metadata records SHA-256 over them and
# `gen:vue-goldens:check` byte-compares against a fresh generator run.
# Formatting them would diverge from the official compiler output.
crates/verter_vue_conformance/corpus/goldens/**

# Provider-published JavaScript declaration fixture — its bytes are pinned by
# the host integration test, so formatting it would disconnect the probe from
# actual compiler output.
tests/sfc-projection/STP12/probes/generated/JSDocGeneric.vue.verter.js.d.ts

# Public constructor declaration fixtures — each ends with the compiler's
# rendered declaration byte for byte, pinned by the public-constructor Rust
# tests, so formatting them would disconnect the probes from actual compiler
# output.
tests/sfc-projection/STP16/probes/components/
