# Pre-Rev11 orchestration skill backup

Historical, non-active copies retained for audit only. This directory is outside `.claude/skills/` and must not participate in skill discovery or live routing.

- `mom-cto-orchestration/` and `agent-prompts/` are the exact active-tree versions immediately before removal.
- `multi-agent-orchestration/` is the exact pre-rewrite version from commit `7f75a42eebb59ae09aff636326f4bb5e5b9c95d0`.

The live orchestration and prompt authority is `.claude/skills/multi-agent-orchestration/`.
